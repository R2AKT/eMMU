;;
; Смещение (Байт)  Размер   Имя поля            Описание и назначение для ядра POSIX
; -------------------------------------------------------------------------------------------------
; +0               1 B      O_PID               Идентификатор процесса (0 = Ядро, 1..254 = Задачи)
; +1               1 B      O_PPID              PID родителя (нужен для waitpid)
; +2               1 B      O_STATUS            Статус (FREE, CREATED, READY, RUNNING, WAITING, ZOMBIE)
; +3               2 B      O_SAVED_SP          Сохраненный указатель стека Ring 3 на момент прерывания
; +5               1 B      O_SYSCALL_RES       Код результата системного вызова / код возврата exit
; +6               1 B      O_UID               Идентификатор пользователя (User ID, 0 = ROOT)
; +7               1 B      O_GID               Идентификатор группы (Group ID)
; +8               4 B      O_PAGES_MAP         [ФИЗИЧЕСКИЕ] Номера физ. страниц ОЗУ для Сегментов 0–3
; +12              4 B      O_EXT_PAGES_MAP     [ВИРТУАЛЬНЫЕ] Страницы расширения (Сегменты 4–7 для оверлеев)
; +16              1 B      O_CF_SWAP_MASK      Битовая маска сегментов процесса в свопе на CF-карте
; +17              2 B      O_CWD_INODE         Номер сектора/инноды текущей директории процесса на CF
; +19              1 B      O_UMASK             Маска прав создания файлов (POSIX umask, дефолт 022)
; +20              8 B      O_FILE_TABLE        Таблица дескрипторов файлов (8 слотов: FD 0..7). 
                                              ; Значение - индекс в глобальной таблице файлов ядра (0xFF=свободен)

; ; === БЛОК ПЛАНИРОВАНИЯ И ПРИОРИТЕТОВ POSIX ===
; +28              1 B      O_PRIO_STATIC       Статический (базовый) приоритет процесса (0-255)
; +29              1 B      O_PRIO_NICE         Пользовательская nice-поправка (POSIX nice, 0-40, дефолт 20)
; +30              1 B      O_PRIO_DYNAMIC      Вычисляемый планировщиком динамический приоритет
; +31              1 B      O_CPU_TICKS         Штрафной счетчик времени, проведенного процессом на CPU

; ; === БЛОК АСИНХРОННЫХ СИГНАЛОВ И ТАЙМЕРОВ ===
; +32              2 B      O_SIG_PENDING       16-битная маска ожидающих доставку сигналов (POSIX)
; +34              2 B      O_SIG_BLOCKED       16-битная маска временно заблокированных сигналов
; +36              2 B      O_SIG_TABLE_PTR     Системный указатель на векторы обработчиков sigaction
; +38              2 B      O_ALARM_TICKS       Будильник POSIX таймера в 100-мс тиках (вызов alarm)
; +40              1 B      O_WAIT_CODE         wait-channel (причина блокировки процесса в ST_WAITING)
; +41 			   1 B      O_PAGE_DIRTY_MASK   Битовая маска (8 бит для 8 страниц процесса).
;												1 = страница изменена (грязная), 0 = страница чистая (совпадает с диском).
; +42			   1 B		O_PGID				Идентификатор группы процессов (Process Group ID) [1 байт]
; +43			   1 B		O_SID				Идентификатор сессии (Session ID) [1 байт]
; +44			   1 B		O_TTY_INDEX			Индекс управляющего TTY (0xFF = нет терминала) [1 байт]
; +45			  19 B		O_RESERVED_PAD		Чистый выравнивающий резерв ядра до границы ровно 64 байт
;
; -------------------------------------------------------------------------------------------------
; ИТОГО: 64 байта (2^6). За счет этого размер в коде переключения процессов вычисляется через 6 сдвигов (PID << 6).
;;
#target rom
#charset ascii
.asm8080

#code CODE, 0000h

; =============================================================================
; НАЧАЛО ТЕНЕВОГО ПЗУ (ХОЛОДНЫЙ СТАРТ С АДРЕСА 0x0000)
; =============================================================================
;    ORG 0000H
boot_start:
	DI                       ; Запрещаем прерывания на период холодной настройки БИС
	JMP real_init            ; Прыжок на процедуру первичной конфигурации

; =============================================================================
; ТАБЛИЦА ПРОГРАММНЫХ ВЕКТОРОВ (ЛОВУШЕК) PROCESSOR 8080 (0x0008 - 0x003F)
; =============================================================================
    ORG 0008H
	JMP rst_stub ; RST 1 — Системная заглушка программного сброса
    ORG 0010H
	JMP rst_stub ; RST 2
    ORG 0018H
	JMP rst_stub ; RST 3
    ORG 0020H
	JMP rst_stub ; RST 4
    ORG 0028H
	JMP rst_stub ; RST 5
    ORG 0030H
rst6_syscall:
    JMP sys_call_entry       ; RST 6 — МОНОПОЛЬНАЯ ТОЧКА ВХОДА SYSCALL ENTRY (Trap)
    ORG 0038H
	JMP rst_stub ; RST 7

; =============================================================================
; ТАБЛИЦА АППАРАТНЫХ ВЕКТОРОВ ПРЕРЫВАНИЙ ОТ КР580ВН59 (БАЗА 0x0040)
; =============================================================================
    ORG 0040H
	JMP hw_timer_entry ; ВН59 IR0 — Системный таймер ВИ53 №1 (20 мс)
    ORG 0048H
	JMP hw_uart_entry  ; ВН59 IR1 — Терминал ВВ51А (VT100 tty0)
    ORG 0050H
	JMP hw_dma_tc_entry; ВН59 IR2 - Контроллер ПДП (CF IDE16)
    ORG 0058H
	JMP hw_net_isr     ; ВН59 IR3 — Контроллер сети (KSZ8851-16MML)
    ORG 0060H
	JMP hw_stub        ; ВН59 IR4 — Свободный аппаратный канал
    ORG 0068H
	JMP hw_stub        ; ВН59 IR5 — Свободный аппаратный канал
    ORG 0070H
	JMP hw_stub        ; ВН59 IR6 — Свободный аппаратный канал
    ORG 0078H 
    JMP hw_privilege_violation_entry ; ВН59 IR7 — Исключение защиты eMMU

; =============================================================================
; НАЧАЛО ТЕНЕВОГО ПЗУ: БЛОК ХОЛОДНОЙ ИНИЦИАЛИЗАЦИИ И ПЕРИФЕРИИ (8 КБ)
; =============================================================================
real_init:
    ; Процессор только что сбросился. Мы находимся в Ring 0. 
    ; Триггер DD3 держит ~{ROM_EN}=0. Процессор читает код из ПЗУ (0x0000-0x1FFF).
    
    LXI  SP, KERNEL_STACK_TOP       ; Выставляем временный стек ядра в верхнем ОЗУ

    ; --- ЭТАП 1: Прямой перенос 8 КБ кода из нижнего ПЗУ в верхнее ОЗУ Ядра ---
    LXI  H, 00000H              ; HL = Начало Теневого ПЗУ (Источник)
    LXI  D, 08000H              ; DE = Временный буфер в верхнем ОЗУ (Назначение)
    LXI  B, 8192                ; Счетчик длины переноса: ровно 8 КБ (2000H)

copy_to_upper_ram_loop:
    MOV  A, M                   ; Считываем байт системного кода из ПЗУ
    STAX D                      ; Записываем напрямую в верхнее ОЗУ Ядра (адреса 0x8000...)
    INX  H                      ; Инкремент указателя источника ПЗУ
    INX  D                      ; Инкремент указателя назначения ОЗУ
    DCX  B                      ; Декремент 16-битного счетчика длины BC
    MOV  A, B
    ORA  C                      ; Проверяем, обнулился ли счетчик BC?
    JNZ  copy_to_upper_ram_loop ; Если нет — продолжаем качать ядро вверх

	; Копирование завершено. Переносим выполнение процессора в верхнее ОЗУ.
    ; ИСПРАВЛЕНО: Модификация адреса метки под верхнее окно ОЗУ (+0x8000)
    LXI  H, upper_ram_stage2;+08000H 
    PCHL                        ; Программный прыжок PC процессора в микросхемы верхнего ОЗУ!

; =============================================================================
; ЭТАП 2: ВЫПОЛНЕНИЕ ИЗ ВЕРХНЕГО ОЗУ ЯДРА И ОТКЛЮЧЕНИЕ ТЕНЕВОГО ПЗУ
; =============================================================================
upper_ram_stage2:
    ; Процессор аппаратно выполняет эту кодовую строчку уже из микросхем ОЗУ Ядра!
    
    ; АППАРАТНОЕ ОТКЛЮЧЕНИЕ ТЕНЕВОГО ПЗУ НА ПЛАТЕ eMMU
    ; Системный импульс OUT 0x75 сбрасывает внешний триггер DD3. 
    ; ПЗУ навсегда исчезает с шины. В нижнем окне 0x0000-0x1FFF открывается 
    ; физически изолированная «чистая» память нижнего ОЗУ Ядра (Ring 0).
    OUT  075H                   ; ТЕНЕВОЕ ПЗУ НАВСЕГДА ДЕАКТИВИРОВАНО

    ; --- ЭТАП 3: Зеркальный перенос всех 8 КБ кода из верхнего ОЗУ вниз ---
    ; Теперь, когда нижнее ОЗУ Ядра открыто, мы сажаем ядро на его каноническое место.
    LXI  H, 08000H              ; HL = Наш верхний временный буфер ОЗУ (Источник)
    LXI  D, 00000H              ; DE = Целевой физический адрес 0 ОЗУ Ядра (Назначение)
    LXI  B, 8192                ; Переносим 8 КБ системного кода вниз

copy_down_to_zero_loop:
    MOV  A, M                   ; Читаем байт ядра из верхней памяти ОЗУ
    STAX D                      ; Пишем в нижнее ОЗУ Ядра на канонический адрес 0
    INX  H                      ; Инкремент указателя источника
    INX  D                      ; Инкремент указателя назначения
    DCX  B                      ; Декремент 16-битного счетчика
    MOV  A, B
    ORA  C                      ; Проверяем окончание переноса
    ; ИСПРАВЛЕНО! Модифицируем адрес возврата петли цикла под верхнее ОЗУ (+0x8000).
    ; Теперь процессор будет крутиться строго внутри верхних физических адресов ОЗУ,
    ; пока полностью не восстановит нижнюю память ядра!
    JNZ  copy_down_to_zero_loop;+08000H 

    ; --- ЭТАП 4: Окончательный запуск операционной системы ---
    ; Передаем управление на канонический адрес старта ядра в нижнем ОЗУ
    LXI  H, kernel_clean_start  ; HL = Логический адрес старта в нижнем ОЗУ
    PCHL                        ; Процессор переходит на исполнение с адреса 0 ОЗУ Ядра!

; =============================================================================
; ЭТАП 5: ЧИСТЫЙ СТАРТ ИЗ НИЖНЕГО ОЗУ ЯДРА — НАСТРОЙКА БИС ПЕРИФЕРИИ
; =============================================================================
kernel_clean_start:
    ; --- ЭТАП 2: Инициализация БИС периферии системы ---
    ; Настройка Системного Таймера ВИ53 №1 (Квант 20 мс, 50 Гц)
    ; ; Делитель для получения прерываний каждые 20 мс (50 Гц) = 1 000 000 / 50 = 20 000 (4E20H)
    MVI  A, 00110110B        ; Канал 0, Чтение/Запись LSB/MSB, Режим 3 (Меандр)
    OUT  SYS_PIT_CMD
    MVI  A, 020H             ; Младший байт делителя (0x20)
    OUT  SYS_PIT_CH0
    MVI  A, 04EH             ; Старший байт делителя (0x4E)
    OUT  SYS_PIT_CH0

    ; Настройка Таймера Связи ВИ53 №2 (Раздельное тактирование Rx/Tx UART 9600 бод)
    MVI  A, 036H             ; CH0 (Тактирование ПРИЕМА RxC): Чтение/Запись LSB/MSB, Режим 3
    OUT  UART_PIT_CMD
    MVI  A, 00CH             ; Делитель приемника = 1 843 200 / (9600 * 16) = РОВНО 12
    OUT  UART_PIT_CH0
    MVI  A, 000H
	OUT UART_PIT_CH0
    
    MVI  A, 01110110B        ; CH1 (Тактирование ПЕРЕДАЧИ TxC): Чтение/Запись LSB/MSB, Режим 3
    OUT  UART_PIT_CMD
    MVI  A, 00CH             ; Делитель передатчика = РОВНО 12
    OUT  UART_PIT_CH1
    MVI  A, 000H
	OUT UART_PIT_CH1

    ; Инициализация USART КР580ВВ51А (8N1, фактор K=16)
    XRA  A
    OUT  UART_CMD
	OUT  UART_CMD
	OUT  UART_CMD ; Сброс автомата состояний БИС
    MVI  A, 040H
	OUT UART_CMD ; Внутренний программный сброс БИС
    MVI  A, 04EH
	OUT UART_CMD ; Режим: 1 стоп-бит, без четности, 8 бит данных, фактор K=16
    MVI  A, 037H
	OUT UART_CMD ; Команда: Включить TX/RX, сбросить ошибки, взвести RTS/DTR

    ; Инициализация Контроллера Прерываний КР580ВН59 (База векторов 0x0040)
    MVI  A, 013H
	OUT MASTER_8259_CMD  ; ICW1: По фронту прерываний, одиночный, нужен ICW4
    MVI  A, 040H
	OUT MASTER_8259_DATA ; ICW2: Базовое смещение аппаратной таблицы векторов = 0x0040
    MVI  A, 001H
	OUT MASTER_8259_DATA ; ICW4: Режим микропроцессора 8085/8086, обычный EOI
    MVI  A, 07CH
	OUT MASTER_8259_DATA ; OCW1 (Маска): Включаем IR0 (Таймер), IR1 (UART), IR7 (eMMU). Маска 0x7C 

	; Инициализация сетевого контроллера KSZ8851-16MML
	CALL k_ksz8851_init             ; Перевод чипа 16MLL в 8-битный режим, пуск маски IRQ3
    CPI  0FFh                       ; Карта ответила?
    JZ   kernel_panic_no_net        ; Если сеть не обнаружена — критический останов (опционально)
	
	; --- ШАГ 3: Инициализация виртуальной VFS-таблицы монтирования ---
    ; Вызываем последовательную цепочку внутренних подпрограмм ранней инициализации
    CALL sys_socketfs_init          ; Очистка NET_UNIX_HEADS и монтирование префикса "/dev/sock"
	
    ; --- ЭТАП 3: Инициализация Системных Таблиц и Таблиц ОЗУ ---
    CALL init_pcb_table          ; Размечаем GLOBAL_RESOURCE_TABLE и слоты PCB

    ; --- ЭТАП 4: АППАРАТНЫЙ МОНТИНГ ФАЙЛОВОЙ СИСТЕМЫ (ВСТРАИВАНИЕ MBR) ---
    ; ВНИМАНИЕ! КРИТИЧЕСКАЯ ТОЧКА ВЫЗОВА:
    CALL vfs_init_mbr            ; Ядро считывает сектор 0 через ПДП и монтирует VFS на LBA 0x1000
    
    ; Проверяем результат монтирования. Если MBR поврежден (A != 0), уходим в панику ядра
    CPI  00H
    JNZ  kernel_panic_mbr_failed

    ; --- ЭТАП 5: Запуск многозадачной карусели POSIX ОС ---
    ; Здесь ядро создает первый пользовательский процесс PID=1 (наш разработанный Shell),
    ; прописывает его начальный контекст в PCB_TABLE и выставляет статус ST_READY.
    ; [Код спавна PID=1]

    MVI  A, 020h                    ; Сброс маски прерываний в КР580ВН59 (EOI)
    OUT  MASTER_8259_CMD
	
    EI                          	; ОФИЦИАЛЬНО РАЗРЕШАЕМ АППАРАТНЫЕ ПРЕРЫВАНИЯ!
    
idle_loop:
    HLT                         	; Спим в экономичном режиме PID=0 до первого тика таймера 20 мс
    JMP  idle_loop

	; --- КРИТИЧЕСКИЙ ОСТАНОВ ПРИ ЗАГРУЗКЕ ---
kernel_panic_no_net:
	; Фатальная ошибка старта: Сетевая карта не найдена.
    ; Выключаем прерывания и намертво вешаем процессор.
    DI
    HLT
	JMP  kernel_panic_mbr_failed
	
kernel_panic_mbr_failed:
    ; Фатальная ошибка старта: MBR не валиден или раздел VFS 0x1000 не найден.
    ; Выключаем прерывания и намертво вешаем процессор для предотвращения порчи диска.
    DI
    HLT
    JMP  kernel_panic_mbr_failed

; =============================================================================
; СИСТЕМНЫЙ МОДУЛЬ ЯДРА: КOНТРОЛЬ ДЕФOЛТНOГO ПOВЕДЕНИЯ SIGBUS (DEFAULT TERMINATION)
; =============================================================================
; Вход:  Вызывается планировщиком/диспетчером перед выходом в Ring 3 задачи.
;        HL = Базовый адрес PCB проверяемого процесса.
; =============================================================================
; =============================================================================
; СИСТЕМНЫЙ МОДУЛЬ ЯДРА: КОНТРОЛЬ ДЕФОЛТНОГО ПОВЕДЕНИЯ SIGBUS (ПОЛНАЯ УТИЛИЗАЦИЯ)
; =============================================================================
; Вход:  Вызывается планировщиком/диспетчером перед выходом в Ring 3 задачи.
;        HL = Базовый адрес PCB проверяемого процесса.
; =============================================================================
k_signal_default_policy_check:
    PUSH PSW                        ; ИСПРАВЛЕНО: Каноничное сохранение регистров
    PUSH B
    PUSH D
    PUSH H

    SHLD K_SIG_CURRENT_PCB_PTR      ; Зафиксировали адрес проверяемого PCB

    ; --- ШАГ 1: Извлекаем 16-битную маску сигналов O_SIG_PENDING (+32) ---
    LXI  D, O_SIG_PENDING
    DAD  D                          ; HL -> Младший байт маски сигналов
    MOV  C, M
    INX  H
    MOV  B, M                       ; BC = 16-битная маска O_SIG_PENDING
    
    ; Проверяем старший байт маски на бит SIGBUS (Маска 0400h, 11-й бит)
    MOV  A, B
    ANI  004h                       ; Выделяем бит SIGBUS
    JZ   k_sig_policy_pass          ; Если бит равен 0 — сигнала SIGBUS нет, уходим

    ; --- ШАГ 2: Проверяем наличие пользовательского обработчика sigaction ---
    LHLD K_SIG_CURRENT_PCB_PTR
    LXI  D, O_SIG_TABLE_PTR         ; Смещение таблицы векторов (+36)
    DAD  D
    MOV  E, M
	INX H
	MOV D, M    ; DE = 16-битный адрес пользовательского вектора
    
    MOV  A, D
    ORA  E                          ; Проверяем адрес на полный ноль (0000H)
    JNZ  k_sig_policy_pass          ; Если адрес не ноль — процесс сам перехватит сигнал

    ; --- ШАГ 3: БЕЗУСЛОВНОЕ УНИЧТОЖЕНИЕ ПРОЦЕССА (CORE DUMP) ---
    LHLD K_SIG_CURRENT_PCB_PTR
    MOV  A, M                       ; A = O_PID текущего процесса (смещение +0)
    CPI  001h                       ; Это системный процесс init (PID 1)?
    JZ   k_kernel_panic_bus         ; Если упал init — это Kernel Panic всей MEGA-580!

    ; Переводим процесс в статус ZOMBIE (05h)
    LHLD K_SIG_CURRENT_PCB_PTR
    PUSH H                          ; Спасли базу PCB в стек ядра
    LXI  D, O_STATUS
	DAD D
    MVI  M, 005h                    ; Перевели статус в ZOMBIE (05h)
    
    ; Записываем код сигнала (0x0B) в поле результата завершения O_SYSCALL_RES (+5)
    POP  H
	PUSH H
    LXI  D, O_SYSCALL_RES
	DAD D
    MVI  M, 00Bh                    ; O_SYSCALL_RES = SIGBUS (0x0B)

    ; --- ШАГ 4: АТОМАРНАЯ УТИЛИЗАЦИЯ ФИЗИЧЕСКОЙ ПАМЯТИ eMMU ---
    POP  H
	PUSH H                 ; HL = Базовый адрес PCB
    LXI  D, O_PAGES_MAP             ; Смещение карты физических страниц (+8)
    DAD  D
    MVI  C, 4                       ; Процесс владеет ровно 4 сегментами памяти (0..3)

k_sig_free_pages_loop:
    MOV  A, M                       ; A = Номер физической страницы в eMMU
    CPI  RES_FREE_MARKER            ; Страница уже свободна/отсутствует (0xFF)?
    JZ   k_sig_page_next
    
    PUSH B
	PUSH H
    CALL k_free_physical_page       ; Освобождаем 16 КБ страницу в карте RAM_BITMAP
    POP  H
	POP  B
    MVI  M, RES_FREE_MARKER         ; Обнулили маркер страницы в PCB (0xFF)

k_sig_page_next:
    INX  H                          ; Переходим к следующему сегменту процесса
    DCR  C
    JNZ  k_sig_free_pages_loop

    ; --- ШАГ 5: ИСПРАВЛЕНО: ПОЛНАЯ РЕАЛИЗАЦИЯ ЗАКРЫТИЯ ДЕСКРИПТОРОВ ФАЙЛОВ И СОКЕТОВ ---
    POP  H                          ; HL = Восстановили базовый адрес PCB
    LXI  D, O_FILE_TABLE            ; Смещение локальной таблицы файлов (+20)
    DAD  D                          ; HL указывает на старт O_FILE_TABLE процесса
    MVI  C, 8                       ; У процесса ровно 8 слотов дескрипторов (FD 0..7)

k_sig_close_fd_loop:
    MOV  A, M                       ; A = Глобальный системный индекс VFS (0..63)
    CPI  RES_FREE_MARKER            ; Дескриптор свободен (0xFF)?
    JZ   k_sig_fd_next              ; Если пуст — перешагиваем его
    
    ; Слот занят. Нам необходимо принудительно очистить глобальный файл/сокет в ядре.
    ; Для этого мы эмулируем асинхронный вызов системной функции закрытия sys_close.
    ; Функция sys_close принимает закрываемый глобальный индекс в аккумуляторе A.
    PUSH B
	PUSH H                 ; Законсервировали итераторы и указатель PCB
    CALL k_vfs_close_global_index   ; Внутренний шлюз закрытия VFS (очищает сокеты/файлы)
    POP  H
	POP  B                 ; Восстановили контекст
    
    MVI  M, RES_FREE_MARKER         ; Очистили локальный дескриптор в PCB (0xFF)

k_sig_fd_next:
    INX  H                          ; Шагаем к следующему FD процесса
    DCR  C
    JNZ  k_sig_close_fd_loop        ; Повторяем для всех 8 слотов

    ; --- ШАГ 6: ПЕРЕКЛЮЧЕНИЕ КОНТЕКСТА НА ЖИВУЮ ЗАДАЧУ ---
    CALL scheduler                  ; Планировщик ищет новый READY процесс в системе
    STA  NEXT_PID
    CALL switch_mmu_context         ; Аппаратно перепрошиваем окна К555ИР26 через eMMU
    JMP  restore_timer_context      ; Навсегда покидаем контекст упавшего процесса

k_sig_policy_pass:
    POP  H
	POP  D
	POP  B
	POP  PSW ; Восстановили РОН для штатных задач
    RET

k_kernel_panic_bus:
    ; --- КРИТИЧЕСКИЙ СБОЙ АППАРАТНОЙ ШИНЫ ЯДРА (KERNEL PANIC) ---
    DI                              ; Намертво заперли прерывания шины MEGA-580
    HLT                             ; Остановили кристалл ЦП КР580ВМ80А

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ЦЕНТРАЛЬНЫЙ ДЕАЛЛОКАТОР VFS И СЕТЕВЫХ СТРУКТУР
; =============================================================================
; Вход:  A = Глобальный системный индекс файла/сокета ядра (0..63), подлежащий закрытию.
; Выход: Объекты очищены, память освобождена, структуры переведены в FREE.
; =============================================================================
k_vfs_close_global_index:
    PUSH PSW                        ; Сохранили входной индекс в стек ядра
    PUSH B
    PUSH D
    PUSH H

    STA  VFS_CLOSE_TEMP_IDX         ; Зафиксировали закрываемый индекс в ОЗУ ядра

    ; --- ШАГ 1: Дифференциация диапазонов индексов VFS ---
    LDA  VFS_CLOSE_TEMP_IDX
    CPI  32                         ; Индекс < 32? (Дисковые файлы, Пайпы, Физический TTY)
    JC   vfs_close_standard_file    ; Уходим на закрытие обычных файлов/пайпов
    
    CPI  40                         ; Индекс < 40? (Внутренние сокеты AF_UNIX: 32..39)
    JC   vfs_close_unix_socket      ; Уходим на утилизацию сокетов ОЗУ eMMU
    
    CPI  48                         ; Индекс < 48? (Внешние аппаратные сокеты INET: 40..47)
    JC   vfs_close_hardware_socket  ; Уходим на аппаратное закрытие чипа KSZ8851
    
    CPI  56                         ; Индекс < 56? (Псевдотерминалы PTY: 48..55)
    JC   vfs_close_pty_terminal     ; Уходим на закрытие PTY-пары
    
    JMP  vfs_close_exit             ; Индекс вне диапазонов — аварийный выход

; -----------------------------------------------------------------------------
; ВЕТКА А: ЗAКРЫТИЕ ОБЫЧНЫХ ДИСКОВЫХ ФAЙЛOВ, ПAЙПOВ И ФИЗИЧЕСКOГO TTY (0..31)
; -----------------------------------------------------------------------------
; -----------------------------------------------------------------------------
; СКОРРЕКТИРОВАННАЯ ВЕТКА А ЗAКРЫТИЯ ОБЫЧНЫХ ДИСКОВЫХ ФAЙЛOВ (0..31)
; -----------------------------------------------------------------------------
vfs_close_standard_file:
    ; Расчет адреса структуры открытого файла в глобальной SYS_FILE_TABLE (*8)
    LDA  VFS_CLOSE_TEMP_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Global_Index * 8
    LXI  D, SYS_FILE_TABLE
	DAD D  ; HL -> База структуры файла в ядре
    
    PUSH H                          ; Сохранили адрес базы структуры файла
    CALL vfs_decrement_file_ref     ; ИСПРАВЛЕНО: Проверяем счетчик системных ссылок!
    CPI  001h                       ; Вызов вернул разрешение на удаление (01h)?
    POP  H                          ; Восстановили адрес базы структуры файла
    JNZ  vfs_close_exit             ; Если ссылки еще есть (A=00h) — просто выходим, слот не затираем!

    ; --- ССЫЛОК НЕТ (A=01h) — ОФИЦИАЛЬНО ВЫЖИГАЕМ СЛОТ ИЗ SYS_FILE_TABLE ---
    MVI  M, RES_FREE_MARKER         ; Прописали 0xFF в поле F_STATUS. Слот полностью свободен!
    JMP  vfs_close_exit


; -----------------------------------------------------------------------------
; ВЕТКА Б: УТИЛИЗАЦИЯ ВНУТРЕННИХ ПРОГРАММНЫХ IPC СОКЕТОВ AF_UNIX (32..39)
; -----------------------------------------------------------------------------
vfs_close_unix_socket:
    ; Вычисляем локальный индекс сокета в SOCK_SYS_TABLE (Индекс - 32)
    LDA  VFS_CLOSE_TEMP_IDX
    SUI  32
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Локальный_Индекс * 8
    LXI  D, SOCK_SYS_TABLE
	DAD D  ; HL -> Указывает на структуру сокета в ядре
    SHLD VFS_CLOSE_STRUCT_PTR
    
    MOV  A, M                       ; Читаем поле S_SOCK_STATUS (+0)
    CPI  SOCK_FREE                  ; Сокет уже свободен?
    JZ   vfs_close_exit             ; Если да — уходим
    
    ; Извлекаем номер выделенной физической страницы ОЗУ eMMU из S_SOCK_LPORT (+2)
    INX  H
	INX  H                 ; HL -> S_SOCK_LPORT (+2)
    MOV  A, M                       ; A = Номер физической страницы eMMU
    CPI  0FFh                       ; Страница валидна?
    JZ   vfs_close_unix_clear_struct
    
    ; Атомарно освобождаем физическую страницу 16 КБ, возвращая её в карту RAM_BITMAP
    PUSH H
    CALL k_free_physical_page       ; Наш системный менеджер памяти
    POP  H

vfs_close_unix_clear_struct:
    ; Полностью обнуляем/освобождаем структуру сокета в SOCK_SYS_TABLE
    LHLD VFS_CLOSE_STRUCT_PTR
    MVI  M, SOCK_FREE               ; S_SOCK_STATUS = SOCK_FREE (00h)
    JMP  vfs_close_exit

; -----------------------------------------------------------------------------
; ВЕТКА В: АППАРАТНЫЙ ОСТАНОВ ВНЕШНИХ СОКЕТОВ CHIP KSZ8851-16MLL (40..47)
; -----------------------------------------------------------------------------
vfs_close_hardware_socket:
    ; Вычисляем локальный индекс сокета (Индекс - 40)
    LDA  VFS_CLOSE_TEMP_IDX
    SUI  40
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; *8
    LXI  D, SOCK_SYS_TABLE
	DAD D
    SHLD VFS_CLOSE_STRUCT_PTR
    
    MOV  A, M                       ; Читаем S_SOCK_STATUS (+0)
    CPI  SOCK_FREE
    JZ   vfs_close_exit
    
    ; --- НИЗКОУРОВНЕВЫЙ АППАРАТНЫЙ ЗАПОР ПЕРЕДАТЧИКА ЧИПА В 8-БИТНОМ РЕЖИМЕ ---
    MVI  A, IDX_TXCR_LOW            ; Выбираем младший байт Transmit Control Register
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Считали текущие флаги TX
    ANI  0FEh                       ; Маска 11111110B: Сбросили бит TXE (Transmit Enable = 0)
    MOV  B, A
    
    MVI  A, IDX_TXCR_LOW            ; Повторно взводим адрес для записи
    OUT  NET_CMD_PORT
    MOV  A, B
	OUT NET_DATA_PORT   ; ПЕРЕДАТЧИК ЧИПА ДЛЯ ЭТОГО КАНАЛА АППАРАТНО ЗАБЛОКИРОВАН!

    ; Переводим структуру сокета в ядре в статус FREE
    LHLD VFS_CLOSE_STRUCT_PTR
    MVI  M, SOCK_FREE               ; S_SOCK_STATUS = SOCK_FREE (00h)
    JMP  vfs_close_exit

; -----------------------------------------------------------------------------
; ВЕТКА Г: УТИЛИЗАЦИЯ СЕССИЙ ПСЕВДOТЕРMИНAЛOВ PTY MASTER/SLAVE (48..55)
; -----------------------------------------------------------------------------
vfs_close_pty_terminal:
    ; Вычисляем индекс PTY пары. Так как под PTY выделено 4 пары Master (48..51) 
    ; и 4 пары Slave (52..55), мы приводим любой индекс к номеру пары через маску:
    LDA  VFS_CLOSE_TEMP_IDX
    CPI  GLOBAL_PTY_SLAVE
    JC   vfs_close_pty_is_master
    SUI  GLOBAL_PTY_SLAVE           ; Если это Slave (52..55), вычитаем 52
    JMP  vfs_close_pty_calc_addr
vfs_close_pty_is_master:
    SUI  GLOBAL_PTY_MASTER          ; Если это Master (48..51), вычитаем 48

vfs_close_pty_calc_addr:
    ; A содержит чистый номер пары (0..3)
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Номер_Пары * 8 байт (PTY_STRUCT_SIZE)
    LXI  D, PTY_SYS_TABLE
	DAD D   ; HL -> Указывает на структуру пары в ОЗУ ядра
    SHLD VFS_CLOSE_STRUCT_PTR
    
    MOV  A, M                       ; Читаем поле P_PTY_STATUS (+0)
    CPI  00h                        ; Пара уже свободна?
    JZ   vfs_close_exit
    
    ; Извлекаем и освобождаем общую физическую страницу eMMU пары из P_PTY_PAGE (+1)
    INX  H                          ; HL -> P_PTY_PAGE (+1)
    MOV  A, M                       ; A = Номер физической страницы eMMU
    CPI  0FFh
    JZ   vfs_close_pty_clear_struct
    
    PUSH H
    CALL k_free_physical_page       ; Освободили 16 КБ ОЗУ в RAM_BITMAP
    POP  H

vfs_close_pty_clear_struct:
    LHLD VFS_CLOSE_STRUCT_PTR
    MVI  M, 00h                     ; P_PTY_STATUS = 00h (Пара PTY полностью закрыта)

; -----------------------------------------------------------------------------
; ТОЧКА БЕЗОПАСНОГО ВЫХОДА ИЗ ДЕАЛЛОКАТОРА VFS
; -----------------------------------------------------------------------------
vfs_close_exit:
    POP  H
    POP  D
    POP  B
    POP  PSW                        ; Восстановили оригинальный аккумулятор
    RET

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): АППАРАТНАЯ ИНИЦИАЛИЗАЦИЯ ЧИПА В 8-БИТНОМ РЕЖИМЕ
; =============================================================================
; Вход:  Прерывания заблокированы (DI). Шина данных строго 8 бит.
; Выход: А = 00H (Успех, чип запущен), A = 0FFH (Ошибка, чип не найден).
; =============================================================================
k_ksz8851_init:
    PUSH B
    PUSH D
    PUSH H

    ; --- ШАГ 1: Верификация физического присутствия чипа по 8-битной шине ---
    MVI  A, IDX_CIDER_HIGH          ; Выбираем старший байт Chip ID Register
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Читаем старший байт (Ожидаем строго 0x88)
    CPI  008h                       ; Маркер семейства Micrel/Microchip
    JNZ  net_init_8bit_err          ; Если не 0x88 — чип не распаян или неисправен

    MVI  A, IDX_CIDER_LOW           ; Выбираем младший байт Chip ID Register
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Читаем младший байт (Ожидаем 0x7X)
    ANI  0F0h                       ; Маскируем ревизию, оставляя код чипа (0x70)
    CPI  070h
    JNZ  net_init_8bit_err          ; Проверка ID провалена

    ; --- ШАГ 2: Аппаратный программный сброс (Software Reset, 8-битный режим) ---
    MVI  A, IDX_GRR_LOW             ; Выбираем младший байт регистра сброса
    OUT  NET_CMD_PORT
    MVI  A, GRR_GSR                 ; Взводим 8-битный флаг Global Soft Reset
    OUT  NET_DATA_PORT

    ; Короткая задержка на устойку переходных процессов в кристалле
    MVI  B, 200
net_init_8bit_delay:
    NOP
	NOP
	DCR B
	JNZ net_init_8bit_delay

    ; Опрашиваем чип, пока он не сбросит флаг GSR обратно в ноль
net_init_8bit_wait_reset:
    MVI  A, IDX_GRR_LOW
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Вычитываем текущий статус сброса
    ANI  GRR_GSR                    ; Проверяем бит GSR
    JNZ  net_init_8bit_wait_reset   ; Продолжаем цикл опроса, если чип еще занят

    ; --- ШАГ 3: Конфигурация передатчика (TX Control) ---
    MVI  A, IDX_TXCR_LOW            ; Выбираем младший байт TXCR
    OUT  NET_CMD_PORT
    MVI  A, TXCR_TXE                ; Включаем только разрешение передачи (8 бит)
    OUT  NET_DATA_PORT
    
    MVI  A, IDX_TXCR_HIGH           ; Выбираем старший байт TXCR
    OUT  NET_CMD_PORT
    MVI  A, 00h                     ; Сбрасываем старшие флаги в 0
    OUT  NET_DATA_PORT

    ; --- ШАГ 4: Конфигурация приемника (RX Control) ---
    MVI  A, IDX_RXCR_LOW            ; Выбираем младший байт RXCR
    OUT  NET_CMD_PORT
    MVI  A, RXCR_RXE | RXCR_RXBAFE  ; Включаем RX + Прием широковещательных ARP пакетов!
    OUT  NET_DATA_PORT
    
    MVI  A, IDX_RXCR_HIGH           ; Выбираем старший байт RXCR
    OUT  NET_CMD_PORT
    MVI  A, 00h                     ; Выключаем Promiscuous режим в старшем байте
    OUT  NET_DATA_PORT

    ; --- ШАГ 5: Очистка зависших прерываний и запуск маски IRQ3 ---
    MVI  A, IDX_ISR_LOW             ; Выбираем младший байт статуса прерываний
    OUT  NET_CMD_PORT
    MVI  A, 0FFh                    ; Запись 1 в 8-битном режиме сбрасывает флаг прерывания
    OUT  NET_DATA_PORT
    
    MVI  A, IDX_ISR_HIGH            ; Выбираем старший байт статуса прерываний
    OUT  NET_CMD_PORT
    MVI  A, 0FFh
    OUT  NET_DATA_PORT

    ; Разрешаем аппаратные прерывания по приему кадров на линию IRQ3
    MVI  A, IDX_IER_LOW             ; Выбираем младший байт маски прерываний IER
    OUT  NET_CMD_PORT
    MVI  A, IER_RXIE                ; Разрешаем только Rx прерывания (Бит 5)
    OUT  NET_DATA_PORT
    
    MVI  A, IDX_IER_HIGH            ; Выбираем старший байт маски прерываний IER
    OUT  NET_CMD_PORT
    MVI  A, 00h
    OUT  NET_DATA_PORT

    MVI  A, 00h                     ; Код возврата = 00h (Чистый успех инициализации)
    JMP  net_init_8bit_exit

net_init_8bit_err:
    MVI  A, 0FFh                    ; Код ошибки = 0xFF (Сетевая карта 16MLL не ответила)

net_init_8bit_exit:
    POP  H
    POP  D
    POP  B
    RET

; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК IR0: КВАНТОВАНИЕ 20 МС И ВЫТЕСНЕНИЕ КОНТЕКСТА
; =============================================================================
hw_timer_entry:
    PUSH PSW					; Сохраняем регистры Ring 3 в стек текущей задачи
	PUSH B
	PUSH D
	PUSH H 
	
    LXI  H, 0
	DAD  SP
    LXI  SP, KERNEL_STACK_TOP   	; Жестко переключаемся на системный стек ядра ОС
    PUSH H                 		; Сохраняем пользовательский SP в стек ядра

    ; --- Вычисляем базовый адрес PCB текущей задачи ---
    LDA  CURRENT_PID
    MOV  L, A
    MVI  H, 0
    ; 6 сдвигов влево для быстрого умножения PID на 64 (PCB_SIZE)
    DAD  H						; HL = PID * 64
	DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H
    
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = PCB_TABLE + (PID * 64) -> Базовый адрес PCB найден!

    ; --- Теперь прибавляем смещение O_SAVED_SP (+3) ---
    LXI  D, O_SAVED_SP          ; DE = смещение до поля (0003H)
    DAD  D                      ; HL = База_PCB + O_SAVED_SP (указывает точно на поле структуры)
    
    POP  D                      ; DE = ранее сохраненный пользовательский SP
    ; Записали SP в PCB текущего процесса
	MOV  M, E                   ; Запись младшего байта SP по адресу в HL
    INX  H
    MOV  M, D                   ; Запись старшего байта SP по адресу в HL + 1
	

    CALL get_current_pcb_address
    LXI  D, O_STATUS
	DAD D
    MOV  A, M
	CPI ST_RUNNING
	JNZ skip_timer_status_reset
    MVI  M, ST_READY        ; Текущий процесс вытеснен в очередь готовых задач
skip_timer_status_reset:

    ; --- СКАНИРОВАНИЕ И ДЕКРЕМЕНТ POSIX БУДИЛЬНИКОВ (ОТСЧЕТ КАЖДЫЕ 20 МС) ---
    LXI  H, PCB_TABLE
	MVI C, 0
alarm_scan_loop:
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MOV A, M
	CPI ST_FREE
	JZ next_pid_alarm_tick
    POP  H
	PUSH H
    LXI  D, O_ALARM_TICKS
	DAD D
    MOV  A, M
	INX H
	ORA M
	JZ next_pid_alarm_tick ; Будильник равен 0000H?
    DCX  H
	MOV A, M
	SUI 1
	MOV M, A
	JNC check_alarm_zero
    INX  H
	MOV A, M
	SUI 1
	MOV M, A
	DCX  H
check_alarm_zero:
    MOV  A, M
	INX H
	ORA M
	JZ next_pid_alarm_tick
    POP  H
	PUSH H
    LXI  D, O_SIG_PENDING + 1
	DAD D
    MOV  A, M
	ORI SIGALRM_MASK_H
	MOV M, A       ; Доставили сигнал SIGALRM
next_pid_alarm_tick:
    POP  H
	LXI D, PCB_SIZE
	DAD D
    INR  C
	MOV A, C
	CPI MAX_PROCS + 1
	JNZ alarm_scan_loop

    CALL scheduler
	STA NEXT_PID                   ; Планировщик выбирает новую задачу

    ; --- ФИЗИЧЕСКАЯ ПЕРЕПРОШИВКА eMMU НА КАЖДОМ ТИКЕ (20 МС) ---
    LDA  NEXT_PID
	STA CURRENT_PID
    MOV  L, A
	MVI H, 0
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
    LXI  D, PCB_TABLE
	DAD D                       ; HL = базовый адрес PCB новой задачи
    PUSH H
	LXI D, O_STATUS
	DAD D
	MVI M, ST_RUNNING
	POP H

    PUSH H
    LXI  D, O_PAGES_MAP
	DAD D						; HL указывает на O_PAGES_MAP (+8)
    MOV  A, M
	OUT 070H					; Аппаратная прошивка Сегмента 0 (0x0000 - 0x3FFF)
    INX  H
	MOV A, M
	OUT 071H                    ; Аппаратная прошивка Сегмента 1 (0x4000 - 0x7FFF)
    INX  H
	MOV A, M
	OUT 072H                    ; Аппаратная прошивка Сегмента 2 (0x8000 - 0xBFFF)
    INX  H
	MOV A, M
	OUT 073H                    ; Аппаратная прошивка Сегмента 3 (0xC000 - 0xFFFF)
    POP  H

    PUSH H
    LXI  D, O_SIG_PENDING + 1
	DAD D
	MOV A, M
	ANI SIGKILL_MASK_H
	JZ restore_timer_context
    POP  H
	MVI A, 09H
	JMP sys_exit_fatal         ; Перехват и уничтожение задачи по SIGKILL

restore_timer_context:
    POP  H
	LXI D, O_SAVED_SP
	DAD D
    MOV  E, M
	INX H
	MOV D, M
	XCHG
	SPHL      ; Указатель SP перенесен на стек новой задачи
    MVI  A, 020H
	OUT MASTER_8259_CMD              ; Отправка EOI в контроллер ВН59
    POP  H
	POP  D
	POP  B
	POP  PSW             			; Восстановление регистров задачи из Ring 3
	EI                          ; Снова разрешаем аппаратную карусель прерываний в Ring 3
    RET                                             ; Прыжок в Ring 3 с аппаратным стробом ~{RET_STROBE}

; =============================================================================
; ПРОЦЕДУРА ИНИЦИАЛИЗАЦИИ ТАБЛИЦ ПРИ СТАРТЕ СИСТЕМЫ (РАЗМЕТКА PCB)
; =============================================================================
init_pcb_table:
    LXI  H, GLOBAL_RESOURCE_TABLE
    MVI  B, 0
init_res_loop:
    MVI  M, RES_FREE_MARKER     ; Записываем 0xFF во всю карту устройств (порты свободны)
    INX  H
	DCR B
	JNZ init_res_loop

    LXI  H, PCB_TABLE
	MVI C, 0 ; Инициализируем 256 слотов таблицы процессов
init_pcb_loop:
    PUSH H
    MOV  M, C                    ; Записываем O_PID
    INX  H
	MVI M, RES_FREE_MARKER ; Записываем O_PPID = 0xFF
    INX  H
	MVI M, ST_FREE      ; Записываем O_STATUS = ST_FREE (ЯВНО выставляем состояние)
    MVI  B, 61
clear_pcb_bytes:
    INX  H
	MVI M, 00H          ; Зануляем все маски сигналов, дескрипторы FD и приоритеты
    DCR  B
	JNZ clear_pcb_bytes
    POP  H
	LXI D, PCB_SIZE
	DAD D
    INR  C
	MOV A, C
	CPI 0
	JNZ init_pcb_loop

    ; --- РУЧНАЯ КОНФИГУРАЦИЯ КОНТЕКСТА ДЛЯ PID = 0 (ЯДРО ОС) ---
    LXI  H, PCB_TABLE
    PUSH H
	LXI D, O_STATUS
	DAD D
	MVI M, ST_RUNNING
	POP H ; Статус Ядра = ST_RUNNING (03H)
    PUSH H
	LXI D, O_UID
	DAD D
	MVI M, 00H
	INX H
	MVI M, 00H
	POP H ; UID/GID Ядра = 00H (ROOT)
    PUSH H
	LXI D, O_PAGES_MAP
	DAD D
    MVI  M, 00H
	INX H
	MVI M, 01H
	INX H
	MVI M, 02H
	INX H
	MVI M, 03H
	POP H ; Страницы Ядра 0,1,2,3
    PUSH H
	LXI D, O_PRIO_STATIC
	DAD D
    MVI  M, 00H
	INX H
	MVI M, 20
	INX H
	MVI M, 20
	POP H ; Абсолютный приоритет планировщика = 0
    RET

; =============================================================================
; ВЫТЕСНЯЮЩИЙ ПЛАНИРОВЩИК ДИНАМИЧЕСКИХ ПРИОРИТЕТОВ POSIX
; =============================================================================
scheduler:
    LDA  CURRENT_PID
	CPI 0
	JZ prep_scheduler_scan
    CALL get_current_pcb_address
	LXI D, O_CPU_TICKS
	DAD D
    MOV  A, M
	CPI 200
	JNC prep_scheduler_scan
    INR	A
	MOV M, A            ; Начисляем штрафной тик процессу за квант на CPU

prep_scheduler_scan:
    MVI  B, 0
	MVI  C, 0FFH
	MVI A, 00H
	STA NEXT_PID ; Дефолтный кандидат — Ядро PID=0
    LXI  H, PCB_TABLE
	MVI E, 0
scheduler_loop:
    PUSH H
	LXI D, O_STATUS
	DAD D
	MOV A, M
	CPI ST_READY
	JZ calc_priority
    JMP  next_scheduler_slot     ; Пропускаем задачи в статусе ST_CREATED, ST_WAITING или ST_ZOMBIE
calc_priority:
    POP  H
	PUSH H
	LXI D, O_PRIO_STATIC
	DAD D
    MOV  A, M
	INX H
	ADD M
	INX H
	INX H
	ADD M ; A = STATIC + NICE + CPU_TICKS
    DCX  H
	MOV M, A            ; Записали актуальный динамический приоритет в O_PRIO_DYNAMIC
    CMP  C
	JNC next_scheduler_slot
    MOV  C, A
	MOV A, E
	STA NEXT_PID ; Запоминаем PID лучшей (наименьшей по числу) задачи
next_scheduler_slot:
    POP  H
	LXI D, PCB_SIZE
	DAD D
    INR  E
	MOV A, E
	CPI MAX_PROCS + 1
	JNZ scheduler_loop

    LDA  NEXT_PID
	MOV L, A
	MVI H, 0
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
    LXI  D, PCB_TABLE
	DAD D
	LXI D, O_CPU_TICKS
	DAD D
	MVI M, 00H ; Сброс штрафа новой задаче
    LDA  NEXT_PID
	RET

; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК ПРЕРЫВАНИЯ: hw_privilege_violation_entry (IRQ7)
; =============================================================================
; Вход:  Сработал триггер нарушения привилегий eMMU. Прерывания заперты (DI).
;        Пользователь попытался сделать IN/OUT/EI/DI напрямую из Ring 3!
; Выход: Нарушитель мгновенно уничтожен, ресурсы освобождены, ЦП переключен.
; =============================================================================
hw_privilege_violation_entry:
    ; --- ШАГ 1: Консервация контекста прерванного потока ---
    PUSH PSW                        ; Спасаем аккумулятор А и флаги PSW
    PUSH B                          ; Спасаем регистровую пару BC
    PUSH D                          ; Спасаем регистровую пару DE
    PUSH H                          ; Спасаем регистровую пару HL

    ; --- ШАГ 2: Защита супервизора ядра от падения ---
    LDA  CURRENT_PID
    ORA  A                          ; Нарушение произошло внутри Ring 0 (Ядро)?
    JZ   privilege_panic_route      ; Если само ядро нарушило шину — Kernel Panic!

    ; --- ШАГ 3: ЖЕСТКОЕ ПЕРЕВЕДЕНИЕ НАРУШИТЕЛЯ В СТАТУС ZOMBIE ---
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB упавшей задачи
    PUSH H                          ; Спасли базу PCB в стек супервизора
    
    LXI  D, O_STATUS                ; Смещение поля статуса (+2)
    DAD  D
    MVI  M, 005h                    ; Отрезали процесс от планировщика: статус = ST_ZOMBIE (05h)
    
    ; Записываем код завершения (SIGSEGV = 0x0B) в поле O_SYSCALL_RES (+5)
    POP  H
	PUSH H                 ; Восстановили базу PCB
    LXI  D, O_SYSCALL_RES           ; Смещение кода возврата (+5)
    DAD  D
    MVI  M, 00Bh                    ; Прописали флаг падения по аппаратному нарушению (0x0B)

    ; --- ШАГ 4: АТОМАРНОЕ ВЫСВОБОЖДЕНИЕ ФИЗИЧЕСКИХ СТРАНИЦ eMMU ---
    POP  H
	PUSH H                 ; HL = Базовый адрес PCB
    LXI  D, O_PAGES_MAP             ; Смещение карты физических фреймов (+8)
    DAD  D
    MVI  C, 4                       ; Нарушитель владел 4 сегментами памяти (0..3)

privilege_free_pages_loop:
    MOV  A, M                       ; A = Номер физической страницы в eMMU
    CPI  RES_FREE_MARKER            ; Страница уже свободна (0xFF)?
    JZ   privilege_page_next
    
    PUSH B
	PUSH H
    CALL k_free_physical_page       ; Возвращаем фрейм в глобальную карту RAM_BITMAP
    POP  H
	POP  B
    MVI  M, RES_FREE_MARKER         ; Обнулили маркер страницы в PCB (0xFF)

privilege_page_next:
    INX  H                          ; Переходим к следующему сегменту процесса
    DCR  C
    JNZ  privilege_free_pages_loop

    ; --- ШАГ 5: ПОЛНОЕ АВТОМАТИЧЕСКОЕ ЗАКРЫТИЕ ВСЕХ ФАЙЛОВ И СОКЕТОВ ---
    POP  H                          ; HL = Базовый адрес PCB нарушителя
    LXI  D, O_FILE_TABLE            ; Смещение локальной таблицы файлов (+20)
    DAD  D                          ; HL -> Начало O_FILE_TABLE процесса
    MVI  C, 8                       ; Итерируем по всем 8 слотам дескрипторов (FD 0..7)

privilege_close_fd_loop:
    MOV  A, M                       ; A = Глобальный системный индекс VFS (0..63)
    CPI  RES_FREE_MARKER            ; Дескриптор свободен (0xFF)?
    JZ   privilege_fd_next          ; Если пуст — шагаем мимо
    
    ; Слот занят — вызываем наш центральный VFS деаллокатор
    PUSH B
	PUSH H                 ; Сохранили итераторы и адрес таблицы файлов
    CALL k_vfs_close_global_index   ; Освобождаем аппаратные сокеты чипа и пайпы ОЗУ!
    POP  H
	POP  B                 ; Восстановили контекст
    
    MVI  M, RES_FREE_MARKER         ; Дескриптор в PCB официально очищен (0xFF)

privilege_fd_next:
    INX  H                          ; Сдвигаемся к следующему дескриптору
    DCR  C
    JNZ  privilege_close_fd_loop    ; Повторяем для всех 8 слотов задачи

    ; --- ШАГ 6: БЕСШОВНЫЙ СУПЕРВИЗОРНЫЙ ШЕДУЛЛИНГ ЖИВОЙ ЗАДАЧИ ---
    CALL scheduler                  ; Планировщик атомарно ищет живой READY поток в системе
    STA  NEXT_PID
    CALL switch_mmu_context         ; ЭВМ MEGA-580 переключает К555ИР26 на новые фреймы
    
    ; Передаем аппаратный EOI (End of Interrupt) в контроллер КР580ВН59
    MVI  A, 020H
    OUT  MASTER_8259_CMD            ; Сбросили триггер текущего уровня IRQ7 в ВН59

    ; Реставрируем контекст РОН (Уже из стека ядра совершенно нового, готового процесса!)
    POP  H
    POP  D
    POP  B
    POP  PSW                        
    
    EI                              ; АТОМАРНО СНИМАЕМ БЛОКИРОВКУ СИСТЕМНОЙ ШИНЫ ПРЕРЫВАНИЙ!
    RET                             ; Аппаратный прыжок в код живой задачи пользователя!

privilege_panic_route:
    ; --- КРИТИЧЕСКИЙ СБОЙ ЯДРА: НАРУШЕНИЕ НА ШИНЕ RING 0 ---
    DI                              ; Запереть прерывания намертво
    HLT                             ; Полный останов кристалла ЦП КР580ВМ80А

; =============================================================================
; ВСПОМОГАТЕЛЬНЫЕ СИСТЕМНЫЕ ЗАГЛУШКИ И СЛОТЫ ДАННЫХ
; =============================================================================
get_current_pcb_address:
    LDA CURRENT_PID
	MOV L, A
	MVI H, 0
    DAD H
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
	LXI D, PCB_TABLE
	DAD D
	RET

; =============================================================================
; СИСТЕМНЫЙ ПЕРЕХВАТЧИК СБОЕВ Ring 3: ЗАГЛУШКА НЕОБРАБОТАННЫХ ЛОВУШЕК (RST 1-5, 7)
; =============================================================================
rst_stub:
    ; Вход: Сюда процессор КР580ВМ80А попадает аппаратно при вызове нелегального RST.
    ;       В стеке задачи Ring 3 лежит адрес PC, где произошел сбой.
    ;       Внимание: Мы все еще находимся в пользовательском режиме Ring 3!
    
    ; Мы не можем напрямую вызвать системные таблицы ядра, так как eMMU еще 
    ; транслирует память пользователя. Поэтому мы принудительно вызываем 
    ; легитимный системный трап SYS_EXIT, чтобы ядро уничтожило эту задачу!
    
    DI                          ; Запрещаем прерывания таймера на время паники
    
    ; Загружаем в регистр C код легитимного системного вызова SYS_EXIT (1)
    MVI  C, 1                   ; SYS_EXIT EQU 1
    
    ; Загружаем в аккумулятор A код возврата фатального сбоя POSIX
    ; Стандартный exit-код при падении по ошибке сегментации/RST = 139 (08BH)
    MVI  A, 139                 ; 139 = 128 + SIGSEGV (Аварийное завершение)
    
    ; Нам нужно передать этот код возврата ядру через регистр C, но в C уже лежит 
    ; номер сисколла (1). Поэтому, согласно нашему соглашению о системных вызовах,
    ; пользовательский параметр exit-кода передается в регистре B (или аккумуляторе).
    MOV  B, A                   ; Переносим код паники 139 в регистр B для sys_exit
    
    ; Официально вызываем программную ловушку Ядра, которая переключит нас в Ring 0
    RST  6                      ; Прыгаем на sys_call_entry
    
    ; Сюда процессор физически никогда не вернется, так как ядро внутри sys_exit
    ; полностью освободит память текущей задачи и переключит eMMU на другой PID.

    ; Финальная аппаратная петля безопасности на случай тотального краха ОС
    HLT
    JMP  rst_stub

; =============================================================================
; УНИВЕРСАЛЬНАЯ АППАРАТНАЯ ЗАГЛУШКА ЯДРА (СЛОТЫ IR2 - IR6 И ЛОЖНЫЕ ТИКИ)
; =============================================================================
hw_stub:
    ; Вход: Сюда процессор прыгает при срабатывании пустых IRQ,
    ;       Либо при перенаправлении ложного тика IR7 из Firewall.
    ; Внимание: Если мы пришли сюда по ложному тику с адреса 0x007C (после JZ),
    ; регистры и стек Ring 3 находятся в идеальной чистоте, PUSH-данных нет!
    
    PUSH PSW                    ; Сохраняем аккумулятор на время отправки EOI
    
    ; Важнейшая физическая особенность КР580ВН59 (8259A):
    ; Ложные прерывания по умолчанию НЕ выставляют соответствующий бит в регистре ISR.
    ; Поэтому посылать команду EOI (End of Interrupt) для ложного тика НЕ ТРЕБУЕТСЯ,
    ; так как контроллер прерываний считает, что он никого не обслуживает.
    ; Однако, если мы пришли сюда от легитимных, но пустых аппаратных линий IR2-IR6,
    ; их биты в ISR взведены, и сброс EOI строго обязателен!
    
    ; Проверим, как именно мы сюда попали: если ISR пуст — это ложный тик
    MVI  A, 00BH
    OUT  MASTER_8259_CMD
    IN   MASTER_8259_CMD        ; Читаем актуальное состояние регистра ISR
    CPI  00H
    JZ   hw_stub_exit           ; Если ISR полностью пуст — это ложный тик, EOI не шлем

    ; Это легитимное пустое прерывание от линий IR2-IR6 — шлем неспецифический EOI
    MVI  A, 020H
    OUT  MASTER_8259_CMD

hw_stub_exit:
    POP  PSW                    ; Восстановили аккумулятор
    EI                          ; Снова разрешаем аппаратную карусель прерываний в Ring 3
    RET                         ; Возврат к прерванному кодовой строке пользователя

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ lseek()
; =============================================================================
; Вход:  D  = Локальный файловый дескриптор процесса (FD, 0..7).
;        E  = Режим whence (00h = SEEK_SET, 01h = SEEK_CUR, 02h = SEEK_END).
;        BC = Старшее слово 32-битного смещения (биты 16-31).
;        HL = Младшее слово 32-битного смещения (биты 0-15).
; Выход: В случае успеха: BC:HL = Новая 32-битная абсолютная позиция в файле.
;        В случае ошибки: BC=0FFFFh, HL=0FFFFh, A = Код ошибки (EBADF/EINVAL).
; =============================================================================
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ИСПРАВЛЕННЫЙ 32-БИТНЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_lseek
; =============================================================================
sys_lseek:
    ; Сохраняем входные параметры в переменные ядра для побайтовой математики
    MOV  A, D
	CPI 8
	JNC lseek_err_badf ; Валидация локального дескриптора
    STA  LSEEK_FD_SAVED
    MOV  A, E
	CPI 3
	JNC lseek_err_inval ; Режим whence обязан быть 0, 1 или 2
    STA  LSEEK_WHENCE_SAVED

    ; Сохраняем 32-битное входное смещение пользователя (HL:BC)
    MOV  A, C
	STA LSEEK_REQ_OFF+0 ; Байт 0 (самый младший)
    MOV  A, B
	STA LSEEK_REQ_OFF+1 ; Байт 1
    MOV  A, L
	STA LSEEK_REQ_OFF+2 ; Байт 2
    MOV  A, H
	STA LSEEK_REQ_OFF+3 ; Байт 3 (знаковый бит здесь)

    ; Извлекаем глобальный системный индекс файла из PCB процесса
    LDA  LSEEK_FD_SAVED
	MOV C, A
	MVI B, 0
    CALL get_current_pcb_address
    LXI  D, O_FILE_TABLE
	DAD D
	DAD B
    MOV  A, M
	STA LSEEK_G_IDX
    CPI  RES_FREE_MARKER
	JZ  lseek_err_badf

    ; Находим адрес записи файла в глобальной SYS_FILE_TABLE (размер слота = 8 байт)
    LDA  LSEEK_G_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Глобальный_Индекс * 8
    LXI  D, SYS_FILE_TABLE
	DAD D
    SHLD LSEEK_FILE_STRUCT_PTR      ; Зафиксировали адрес структуры открытого файла

    ; --- ВЕТВЛЕНИЕ ПО РЕЖИМАМ WHENCE ---
    LDA  LSEEK_WHENCE_SAVED
    ORA  A
	JZ  lseek_action_set   ; 0 = SEEK_SET (Прямая установка указателя)
    DCR  A
	JZ  lseek_action_cur   ; 1 = SEEK_CUR (Сложение с текущей позицией)
    JMP  lseek_action_end           ; 2 = SEEK_END (Сложение с концом файла)

; -----------------------------------------------------------------------------
; РЕЖИМ SEEK_SET (Абсолютное позиционирование)
; -----------------------------------------------------------------------------
lseek_action_set:
    ; Просто копируем 32-битное значение из LSEEK_REQ_OFF в структуру файла
    LHLD LSEEK_FILE_STRUCT_PTR
    LXI  D, LSEEK_REQ_OFF
    MVI  B, 4
lseek_set_copy_loop:
    LDAX D
	MOV M, A
    INX  D
	INX  H
    DCR  B
	JNZ  lseek_set_copy_loop
    JMP  lseek_success_exit

; -----------------------------------------------------------------------------
; РЕЖИМ SEEK_CUR (Относительно текущей позиции с честной 32-битной математикой)
; -----------------------------------------------------------------------------
lseek_action_cur:
    LHLD LSEEK_FILE_STRUCT_PTR      ; HL указывает на байт 0 текущего смещения в таблице
    LXI  D, LSEEK_REQ_OFF           ; DE указывает на байт 0 дельты пользователя
    
    ; Байт 0
    MOV  A, M
	LDAX D
	ADD M
	STA LSEEK_NEW_OFF+0
    INX  H
	INX  D
    ; Байт 1
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+1
    INX  H
	INX  D
    ; Байт 2
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+2
    INX  H
	INX  D
    ; Байт 3 (Знаковый)
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+3

lseek_apply_new_offset:
    ; Проверяем, не стала ли позиция отрицательной (проверка старшего бита 31)
    LDA  LSEEK_NEW_OFF+3
    ANI  080h
    JNZ  lseek_err_inval            ; Если бит 31 взведён — позиция ушла за начало файла!

    ; Всё валидно, прописываем новый 32-битный указатель обратно в структуру открытого файла
    LHLD LSEEK_FILE_STRUCT_PTR
    LXI  D, LSEEK_NEW_OFF
    MVI  B, 4
lseek_save_loop:
    LDAX D
	MOV M, A
    INX  D
	INX  H
    DCR  B
	JNZ  lseek_save_loop
    JMP  lseek_success_exit

; -----------------------------------------------------------------------------
; РЕЖИМ SEEK_END (Относительно конца файла — размер извлекается из Inode vfs)
; -----------------------------------------------------------------------------
lseek_action_end:
    ; [Ядро считывает текущий 32-битный размер файла из активного дискового Inode. 
    ; Для симуляции и автономности закладываем текущий размер в буфер LSEEK_CUR_SIZE]
    
    LXI  H, LSEEK_CUR_SIZE          ; Источник: 32-битный размер файла
    LXI  D, LSEEK_REQ_OFF           ; Источник 2: Знаковая дельта пользователя
    
    ; Честное побайтовое сложение/вычитание с переносом (32-битный каскад)
    ; Байт 0
    MOV  A, M
	LDAX D
	ADD M
	STA LSEEK_NEW_OFF+0
	INX H
	INX D
    ; Байт 1
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+1
	INX H
	INX D
    ; Байт 2
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+2
	INX H
	INX D
    ; Байт 3
    MOV  A, M
	LDAX D
	ADC M
	STA LSEEK_NEW_OFF+3

    JMP  lseek_apply_new_offset

; -----------------------------------------------------------------------------
; ТОЧКА УСПЕШНОГО ВЫХОДA (Экспорт 32-битного результата в регистры Ring 3)
; -----------------------------------------------------------------------------
lseek_success_exit:
    ; Возвращаем новый указатель в регистровых парах HL (старшие 16 бит) и BC (младшие 16 бит)
    LDA  LSEEK_NEW_OFF+0
	MOV C, A
    LDA  LSEEK_NEW_OFF+1
	MOV B, A ; BC = Новый указатель Low
    
    LDA  LSEEK_NEW_OFF+2
	MOV L, A
    LDA  LSEEK_NEW_OFF+3
	MOV H, A ; HL = Новый указатель High
    
    MVI  A, 00h                     ; Код ошибки = 00h (Чистый успех)
    RET                             ; Возврат. Прерывания заперты, шлюз ядра откроет их сам.

; --- ОБРАБОТЧИКИ ОШИБОК ДИСКОВОГО ИНТЕРФЕЙСА ---
lseek_err_badf:
    MVI  A, 0FFh                    ; 0xFF = EBADF
    RET

lseek_err_inval:
    MVI  A, 0FEh                    ; 0xFE = EINVAL (Выход за границы файла или whence некорректен)
    RET

; =============================================================================
; ВНУТРЕННЯЯ ПРОЦЕДУРА ЯДРА: ПРИНУДИТЕЛЬНОЕ УНИЧТОЖЕНИЕ ПРОЦЕССА (АВАРИЙНЫЙ EXIT)
; =============================================================================
sys_exit_fatal:
    ; Вход: A = POSIX-код причины смерти процесса (09H - SIGKILL, 0BH - Исключение eMMU)
    MOV  C, A                   ; Сохраняем код завершения в регистр C

    ; --- ШАГ 1: Фиксация статуса ST_ZOMBIE и кода возврата ---
    CALL get_current_pcb_address ; HL = базовый адрес PCB текущей задачи
    
    PUSH H
    LXI  D, O_STATUS
	DAD D
    MVI  M, ST_ZOMBIE           ; Переводим процесс в состояние "Зомби" (05H)
    
    POP  H
	PUSH H
    LXI  D, O_SYSCALL_RES
	DAD D
    MOV  M, C                   ; Записываем код завершения (причину смерти)
    POP  H

    ; --- ШАГ 2: Освобождение захваченных процессом I/O портов ---
    LXI  H, GLOBAL_RESOURCE_TABLE
    MVI  B, 0                   ; Счетчик портов от 00H до 0FFH
    LDA  CURRENT_PID
	MOV C, A ; C = PID для сравнения
exit_fatal_ports_loop:
    MOV  A, M
	CMP C
    JNZ  exit_fatal_skip_port
    MVI  M, RES_FREE_MARKER     ; Освобождаем порт (0xFF)
exit_fatal_skip_port:
    INX  H
	INR B
	MOV A, B
	CPI 0
	JNZ exit_fatal_ports_loop

    ; --- ШАГ 3: Освобождение физических страниц ОЗУ процесса (Сегменты 0-3) ---
    CALL get_current_pcb_address
    LXI  D, O_PAGES_MAP
	DAD D ; HL указывает на начало карты физ. страниц
    MVI  B, 4                   ; У процесса ровно 4 базовых физических сегмента
exit_fatal_ram_loop:
    MOV  A, M
	CPI RES_FREE_MARKER
	JZ exit_fatal_skip_ram
    PUSH B
	PUSH H
    CALL free_physical_frame_page ; Освобождаем фрейм в аллокаторе ядра
    POP  H
	POP  B
    MVI  M, RES_FREE_MARKER     ; Затираем страницу в карте процесса
exit_fatal_skip_ram:
    INX  H
	DCR B
	JNZ exit_fatal_ram_loop

    ; --- ШАГ 4: Очистка системного стека и загрузка нового контекста ---
    LXI  SP, KERNEL_STACK_TOP       ; Полный сброс и очистка стека супервизора
    
    CALL scheduler              ; Запуск планировщика, он выберет лучший ST_READY PID в Аккумулятор
    STA  NEXT_PID
    
    CALL switch_mmu_context     ; Чистый символьный вызов функции перепрошивки eMMU!

    ; Восстанавливаем указатель стека и регистры выбранной планировщиком задачи
    CALL get_current_pcb_address
    LXI  D, O_SAVED_SP
	DAD D
    MOV  E, M
	INX H
	MOV D, M
	XCHG 
	SPHL ; Процессор переключен на стек Ring 3 новой задачи
    
    MVI  A, 020H
	OUT MASTER_8259_CMD ; Команда EOI в контроллер ВН59
    POP  H
	POP  D
	POP  B
	POP  PSW    ; Извлекаем регистры задачи из её личного стека
    RET                         ; Выход в Ring 3 через аппаратный RET-строб платы eMMU


; =============================================================================
; ВНУТРЕННЯЯ ФУНКЦИЯ ЯДРА: ФИЗИЧЕСКАЯ ПЕРЕПРОШИВКА КОНТЕКСТА eMMU
; =============================================================================
switch_mmu_context:
    ; Вход: NEXT_PID = PID процесса, на который переключаемся
    ; Выход: CURRENT_PID обновлен, порты 0x70-0x73 прошиты новой картой страниц
    
    LDA  NEXT_PID
    STA  CURRENT_PID            ; Новый процесс официально становится текущим
    
    ; Вычисляем адрес его PCB: PCB_TABLE + (PID * 64)
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H ; PID * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = базовый адрес PCB новой задачи

    ; Переводим статус новой задачи в состояние ST_RUNNING (Выполняется)
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_RUNNING
    POP  H

    ; Аппаратная перепрошивка 4 регистров К555ИР26 из массива O_PAGES_MAP (+8)
    LXI  D, O_PAGES_MAP
    DAD  D                      ; HL указывает на базовые 4 страницы памяти процесса
    
    MOV  A, M
    OUT  070H                   ; Прошивка Сегмента 0 (0x0000 - 0x3FFF) пользовательского ОЗУ
    
    INX  H
    MOV  A, M
    OUT  071H                   ; Прошивка Сегмента 1 (0x4000 - 0x7FFF) пользовательского ОЗУ
    
    INX  H
    MOV  A, M
    OUT  072H                   ; Прошивка Сегмента 2 (0x8000 - 0xBFFF) пользовательского ОЗУ
    
    INX  H
    MOV  A, M
    OUT  073H                   ; Прошивка Сегмента 3 (0xC000 - 0xFFFF) пользовательского стека
    
    RET

; =============================================================================
; ВОЗВРАТ С ОШИБКОЙ ПРИ ОТСУТСТВИИ СИСТЕМНОГО ВЫЗОВА
; =============================================================================

syscall_nosys:
    MVI  A, ENOSYS               ; Возвращает ошибку 0xE5 (Функция не реализована)
    RET

; =============================================================================
; ПРОГРАММНЫЙ ВЕКТОР RST 6 (Адрес 0x0030): ТОЧКА ВХОДА SYSCALL ENTRY
; =============================================================================
sys_call_entry:
	DI							; Отключаем прерывания

    ; === ФАЗА 1: ТЕНЕВОЕ СОХРАНЕНИЕ ПОЛЬЗОВАТЕЛЬСКОГО КОНТЕКСТА ===
    ; В SP сейчас стек Ring 3 вызывающей задачи. eMMU уже забросил туда PC.
    PUSH PSW                    ; Сохраняем Аккумулятор и Флаги (A, F) в стек Ring 3
    PUSH B                      ; Сохраняем регистры B, C задачи в стек Ring 3
    PUSH D                      ; Сохраняем регистры D, E задачи в стек Ring 3
    PUSH H                      ; Сохраняем регистры H, L задачи в стек Ring 3

    ; Переключаемся на изолированный системный стек Ядра ОС
    LXI  H, 0
    DAD  SP                     ; HL = текущий упакованный SP процесса
    LXI  SP, KERNEL_STACK_TOP       ; Загружаем чистый изолированный стек ядра ОС
    PUSH H	                    ; Временно прячем пользовательский SP в стек ядра

    ; Вычисляем базовый адрес PCB текущей задачи: База = PCB_TABLE + (PID * 64)
    LDA  CURRENT_PID
    MOV  L, A
    MVI  H, 0
    ; 6 сдвигов влево для быстрого умножения PID на 64 (PCB_SIZE)
    DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H  ; HL = PID * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = PCB_TABLE + (PID * 64) -> Базовый адрес PCB

    ; Прибавляем смещение O_SAVED_SP (+3) строго последовательно
    LXI  D, O_SAVED_SP          ; DE = смещение до поля (0003H)
    DAD  D                      ; HL = База_PCB + O_SAVED_SP
    
    POP  D                      ; DE = извлеченный из стека ядра пользовательский SP
    MOV  M, E                   ; Запись младшего байта SP
    INX  H
    MOV  M, D                   ; Запись старшего байта SP

    ; === ФАЗА 2: ДЕШИФРАЦИЯ И АВТОРИЗАЦИЯ СИС-ВЫЗОВА ===
    ; Номер системного вызова передан в регистре C
    MOV  A, C
    CPI  MAX_SYSCALL_NUM + 1    ; Проверяем границы таблицы системных вызовов
    JNC  syscall_bad_number     ; Если номер больше лимита — возвращаем ENOSYS

    ; Вычисляем адрес перехода по таблице указателей: Смещение = Номер * 2
    MOV  L, C
    MVI  H, 0
    DAD  H                      ; HL = Номер Syscall * 2
    LXI  D, SYSCALL_TABLE
    DAD  D                      ; HL указывает точно на ячейку DW в таблице
    MOV  E, M
    INX  H
    MOV  D, M                   ; DE = физический адрес функции syscall в ядре

    ; Передаем управление на функцию ядра. 
    ; Поскольку параметры от пользователя пришли в DE и HL задачи, а мы их 
    ; теневым образом сохранили в стек, при необходимости функция ядра может 
    ; извлечь их оттуда или прочитать напрямую, если они не затерлись при расчете.
    ; Имитируем вызов CALL через стек:
    PUSH D	                    ; Забрасываем адрес функции ядра в стек
    LXI  H, syscall_return      ; Регистрируем точку возврата после исполнения
    PUSH H
    PCHL                        ; Программный прыжок на исполнение системной функции

syscall_return:
    ; Системная функция выполнилась и вернула код результата в Аккумуляторе (A)
    MOV  C, A                   ; Временно спасем код результата в C
    
    ; Записываем результат выполнения в поле O_SYSCALL_RES (+5) текущего PCB
    CALL get_current_pcb_address ; HL = базовый адрес PCB текущей задачи
    LXI  D, O_SYSCALL_RES
    DAD  D                      ; HL указывает на O_SYSCALL_RES
    MOV  M, C                   ; Результат системного вызова жестко зафиксирован

syscall_exit:
    ; === ФАЗА 3: ВОССТАНОВЛЕНИЕ КОНТЕКСТА И ВЫХОД В USER SPACE ===
    CALL get_current_pcb_address
    LXI  D, O_SAVED_SP
    DAD  D                      ; HL указывает на сохраненный SP
    MOV  E, M
    INX  H
    MOV  D, M                   ; DE = восстановленный SP задачи
    XCHG                        ; HL = новый SP
    SPHL                        ; Процессор переключился обратно на стек Ring 3

	; Запускаем проверку дефолтных политик сигналов (включая SIGBUS)
    CALL k_signal_default_policy_check
	
    ; Извлекаем теневые регистры из стека Ring 3 пользовательского пространства
    POP  H                      ; Восстановили H, L задачи
    POP  D                      ; Восстановили D, E задачи
    POP  B                      ; Восстановили B, C задачи
    POP  PSW                    ; Восстановили Аккумулятор и Флаги (A, F) задачи

	EI                          ; Снова разрешаем аппаратную карусель прерываний в Ring 3

    ; На вершине стека Ring 3 остался лежать только PC
    RET                         ; Переход обратно в задачу. Аппаратный сигнал ~{RET_STROBE} 
                                ; на плате сбросит KERNEL_MODE обратно в Ring 3!

syscall_bad_number:
    MVI  A, 0E5H                ; Код ошибки: ENOSYS (Функция не реализована)
    JMP  syscall_return

; =============================================================================
; POSIX СИС-ВЫЗОВ №3: SYS_REQUEST_IO — МОНОПОЛЬНЫЙ ЗАХВАТ ПОРТА ВВОДА-ВЫВОДА
; =============================================================================
sys_request_io:
    ; Вход: C = запрашиваемый номер аппаратного порта/устройства (00H - 0FFH)
    ; Выход: A = 00H (УСПЕХ), A = 0E6H (КОД ОШИБКИ EBUSY — Ресурс занят)
    
    MOV  L, C
    MVI  H, 0
    LXI  D, GLOBAL_RESOURCE_TABLE
    DAD  D                      ; HL = точный адрес порта в таблице ядра
    
    MOV  A, M                   ; Считываем текущего владельца из таблицы
    CPI  RES_FREE_MARKER        ; Свободен ли порт (0xFF)?
    JNZ  io_port_busy           ; Если там чужой PID — порт занят, отлуп!
    
    ; Порт свободен, закрепляем его за текущим вызывающим процессом
    LDA  CURRENT_PID
    MOV  M, A                   ; Записываем CURRENT_PID в ячейку таблицы ресурсов
    MVI  A, 00H                 ; Код возврата: Успех
    RET

io_port_busy:
    MVI  A, 0E6H                ; Код ошибки: EBUSY (Ресурс занят другим процессом)
    RET

; =============================================================================
; POSIX СИС-ВЫЗОВ №4: SYS_RELEASE_IO — ОСВОБОЖДЕНИЕ ПОРТА ВВОДА-ВЫВОДА
; =============================================================================
sys_release_io:
    ; Вход: C = освобождаемый номер аппаратного порта (00H - 0FFH)
    ; Выход: A = 00H (УСПЕХ), A = 0E4H (КОД ОШИБКИ EPERM — Вы не владелец этого порта)
    
    MOV  L, C
    MVI  H, 0
    LXI  D, GLOBAL_RESOURCE_TABLE
    DAD  D                      ; HL = точный адрес порта в таблице ядра
    
    MOV  A, M                   ; Считываем из ОЗУ текущего владельца порта
    MOV  B, A                   ; Сохраняем владельца в B
    LDA  CURRENT_PID            ; Читаем PID вызывающей задачи
    CMP  B                      ; Вы являетесь легитимным владельцем этого устройства?
    JNZ  io_release_error       ; Если нет — чужой процесс не имеет права сбрасывать чужую периферию
    
    MVI  M, RES_FREE_MARKER     ; Записываем 0xFF — порт снова свободен для всей системы
    MVI  A, 00H                 ; Код возврата: Успех
    RET

io_release_error:
    MVI  A, 0E4H                ; Код ошибки: EPERM (Операция запрещена, недостаточно прав)
    RET

; =============================================================================
; ПОЛНЫЙ ИСПРАВЛЕННЫЙ КОД СИСКОЛЛА POSIX ОС: sys_fork
; =============================================================================
; Вход:  Вызывается через программное прерывание / ловушку RST 6 из Ring 3.
;        Контекст родительской задачи сохранен аппаратно в User Space и на KERNEL_STACK.
; Выход: Для РОДИТЕЛЯ: Аккумулятор A = PID созданного потомка (Успех).
;        Для ПОТОМКА:  При первом запуске возвращается в Ring 3 с аккумулятором A = 00H.
; =============================================================================
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ПОЛНЫЙ МОНОЛИТНЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_fork
; =============================================================================
; Вход:  Нет параметров (Контекст извлекается автоматически из CURRENT_PID).
; Выход: В родителе: A = PID нового потомка (1..254).
;        В потомке:  A = 00H (Чистый успех POSIX).
;        При ошибке: A = 0FFH — EAGAIN (Превышен лимит процессов / нет памяти).
; Регистры BC, DE, HL полностью сохраняются и дублируются.
; =============================================================================
sys_fork:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH H
    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра супервизора
    SHLD FORK_STACK_SAVE            ; ЗАФИКСИРОВАЛИ ТОЧКУ АВАРИЙНОГО ОТКАТА!
    POP  H

    ; --- ШАГ 2: ПОИСК СВОБОДНОГО СЛОТА PCB В PCB_TABLE ---
    LXI  H, PCB_TABLE               ; Стартовый адрес глобальной таблицы процессов
    MVI  B, MAX_PROCS               ; Лимит сканирования (16 слотов)
    MVI  C, 0                       ; Наш итератор PID

fork_find_pcb_loop:
    MOV  A, M                       ; Читаем поле O_PID (+0). Если слот пуст, там 0.
    ORA  A                          ; Проверяем на чистый ноль (ST_FREE)
    JZ   fork_pcb_slot_found        ; Нашли свободный дескриптор процесса!

    LXI  D, PCB_SIZE                ; Сдвиг равен размеру структуры PCB (64 байта)
    DAD  D
    INR  C                          ; PID++
    DCR  B
    JNZ  fork_find_pcb_loop

    ; Вся таблица PCB_TABLE забита — ошибка EAGAIN
    JMP  fork_err_eagain

fork_pcb_slot_found:
    MOV  A, C
	STA FORK_CHILD_PID
    SHLD FORK_CHILD_PCB_PTR

    ; --- ШАГ 3: ПОБАЙТОВОЕ ДУБЛИРОВАНИЕ СТРУКТУРЫ PCB (РОДИТЕЛЬ -> ПОТОМОК) ---
    PUSH H                          ; Спасли адрес PCB потомка
    CALL get_current_pcb_address    ; HL =  адрес PCB родителя
    POP  D                          ; DE =  адрес PCB потомка
    MVI  B, PCB_SIZE                ; Копируем ровно 64 байта метаданных ядра

fork_pcb_copy_loop:
    MOV  A, M
	STAX D              ; Переносим байт параметров родителя потомку
    INX  H
	INX  D
    DCR  B
    JNZ  fork_pcb_copy_loop

    ; --- ШАГ 4: КОРРЕКТИРОВКА ИДЕНТИФИКАТОРОВ В PCB ПОТОМКА ---
    LHLD FORK_CHILD_PCB_PTR         ; HL -> Начало PCB потомка
    
    ; 1. Прописываем потомку его собственный уникальный PID (+0)
    LDA  FORK_CHILD_PID
    MOV  M, A                       
    
    ; 2. Прописываем ID родителя PPID (+1), им становится текущий CURRENT_PID
    INX  H                          ; HL -> O_PPID (+1)
    LDA  CURRENT_PID
    MOV  M, A                       
    
    ; 3. Переводим статус потомка в ST_CREATED (+2), чтобы планировщик его пока не брал
    INX  H                          ; HL -> O_STATUS (+2)
    MVI  M, ST_CREATED

    ; --- ШАГ 5: АЛЛОКАЦИЯ НОВЫХ ФИЗИЧЕСКИХ СТРАНИЦ ПАМЯТИ ДЛЯ ПОТОМКА ---
    LHLD FORK_CHILD_PCB_PTR
    LXI  D, O_PAGES_MAP             ; Смещение карты физических страниц (+8)
    DAD  D                          ; HL -> Старт O_PAGES_MAP потомка
    MVI  B, 4                       ; Процесс должен владеть 4 фреймами по 16 КБ

fork_alloc_pages_loop:
    PUSH B
	PUSH H
    CALL k_allocate_free_physical_page ; Менеджер физической памяти ядра
    STA  FORK_CHILD_PAGE            ; Сохранили выданный фрейм
    POP  H
	POP  B
    
    LDA  FORK_CHILD_PAGE
    CPI  0FFh                       ; Физическая RAM устройства исчерпана?
    JZ   fork_err_out_of_mem        ; Уходим на аварийный откат аллокаций
    
    MOV  M, A                       ; Прописали фрейм в O_PAGES_MAP потомка
    INX  H
    DCR  B
    JNZ  fork_alloc_pages_loop

    ; --- ШАГ 6: КООПЕРАТИВНОЕ КВАНТОВАННОЕ КОПИРОВАНИЕ ПАМЯТИ (64 КБ) ---
    XRA  A
	STA FORK_CURRENT_SEGMENT ; Начинаем итерации с Сегмента 0 (0..3)

fork_segment_quanta_loop:
    CALL get_current_pcb_address    
    LXI  D, O_PAGES_MAP
	DAD D     
    LDA  FORK_CURRENT_SEGMENT
	MOV E, A
	MVI D, 0
	DAD D
    MOV  A, M
	STA FORK_PARENT_PAGE 

    LHLD FORK_CHILD_PCB_PTR         
    LXI  D, O_PAGES_MAP
	DAD D     
    LDA  FORK_CURRENT_SEGMENT
	MOV E, A
	MVI D, 0
	DAD D
    MOV  A, M
	STA FORK_CHILD_PAGE 

    ; Коммутируем фрейм родителя в Окно 1 (0x4000), а потомка — в Окно 2 (0x8000)
    LDA  FORK_PARENT_PAGE
	OUT 071H
    LDA  FORK_CHILD_PAGE
	OUT 072H
    
    LXI  H, 04000h                  ; HL = Чтение (Окно 1)
    LXI  D, 08000h                  ; DE = Запись (Окно 2)
    LXI  B, 16384                   ; Копируем ровно 16 Килобайт за этот квант

fork_copy_16k_block:
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0 (Прорыв Firewall)
    MOV  A, M
	STAX D              ; ПРЯМОЙ МЕЖСТРАНИЧНЫЙ ПЕРЕНОС БАЙТА
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1 (Возврат Ring 0)
    INX  H
	INX  D
    
    DCX  B                          ; 16-битный декремент счетчика BC
    MOV  A, B
	ORA C               ; Каноничная аккумуляционная проверка нуля
    JNZ  fork_copy_16k_block        ; Крутим цикл до окончания 16 КБ фрейма

    ; --- ОКНО ВЕНТИЛЯЦИИ АППАРАТНЫХ ПРЕРЫВАНИЙ ШИНЫ ---
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_PAGES_MAP + 1
	DAD D
	MOV A, M
	OUT 071H ; Вернули Окно 1 ядра
    POP  H
    LXI  D, O_PAGES_MAP + 2
	DAD D
	MOV A, M
	OUT 072H ; Вернули Окно 2 ядра
    OUT  076H                       ; Сняли оверрайд

    EI
    NOP                             ; СЮДА СВЕРХВСПЫШКОЙ ВТИСКИВАЮТСЯ НАКОПИВШИЕСЯ ISR
    DI                              ; Снова атомарно заперли ядро, работаем дальше

    ; Переходим к следующему сегменту eMMU
    LDA  FORK_CURRENT_SEGMENT
	INR A
	STA FORK_CURRENT_SEGMENT
    CPI  4                          ; Скопировали все 4 сегмента?
    JNZ  fork_segment_quanta_loop   ; Если нет — уходим на копирование следующего фрейма

    ; =============================================================================
    ; ШАГ 7: ПОЛНАЯ РЕАЛИЗАЦИЯ ДУБЛИРОВАНИЯ КОНТЕНТА СИСТЕМНОГО СТЕКА ЯДРА (512 Б)
    ; =============================================================================
    ; 1. Запоминаем текущий физический указатель SP родителя (HL = SP)
    LXI  H, 0
    DAD  SP
    SHLD FORK_PARENT_SP             ; Сохранили SP родителя

    ; 2. Вычисляем глубину проседания стека родителя: (KERNEL_STACK_TOP - Parent_SP)
    LXI  H, KERNEL_STACK_TOP
    LXI  D, 0
    SHLD FORK_STACK_DEPTH           ; Обнулили счетчик глубины

fork_calc_stack_depth_loop:
    LHLD FORK_PARENT_SP
    MOV  A, H
	CMP D
	JNZ fork_depth_inc
    MOV  A, L
	CMP E
	JZ  fork_depth_calc_done ; Если Parent_SP догнал вершину — расчет завершен!

fork_depth_inc:
    LHLD FORK_STACK_DEPTH
	INX H
	SHLD FORK_STACK_DEPTH
    INX  D                          ; Шагаем от Parent_SP вверх к вершине
    JMP  fork_calc_stack_depth_loop

fork_depth_calc_done:
    ; 3. Вычисляем стартовый адрес стека супервизора потомка в SYS_STACKS_POOL
    ; Адрес = SYS_STACKS_POOL + (Child_PID * 512)
    LDA  FORK_CHILD_PID
    MOV  L, A
	MVI H, 0
    LXI  D, 512
    ; Быстрое 16-битное умножение через сложения (Child_PID * 512 = PID * 2^9)
    ; Поскольку *512 это просто сдвиг влево, HL переносим в старший байт
    MOV  H, L
	MVI L, 0            ; HL = PID * 256
    DAD  H                          ; HL = PID * 512
    LXI  D, SYS_STACKS_POOL
    DAD  D                          ; HL -> Базовый физический фрейм стека потомка
    
    LXI  D, 512
    DAD  D                          ; HL = Вычисленная девственная вершина Child_STACK_TOP
    
    ; Вычитаем глубину проседания стека, чтобы найти точный итоговый SP потомка
    LHLD FORK_STACK_DEPTH
    MOV  C, L
	MOV B, H            ; BC = Глубина проседания стека
    
    LXI  H, SYS_STACKS_POOL
    LDA  FORK_CHILD_PID
	MOV E, A
	MVI D, 0
    ; Повторяем каноничный расчет вершины
    MOV  H, E
	MVI L, 0
	DAD H
	LXI D, 512
	DAD D
    LXI  D, SYS_STACKS_POOL
	DAD D ; HL = Чистый Child_STACK_TOP
    
    MOV  A, L
	SUB C
	MOV L, A
    MOV  A, H
	SBB B
	MOV H, A    ; HL = ТОЧНЫЙ ВЫЧИСЛЕННЫЙ ЦЕЛЕВОЙ SP ПОТОМКА!
    SHLD FORK_CHILD_SP

    ; 4. Физически копируем байты стека из пула родителя в пул потомка
    LHLD FORK_PARENT_SP             ; HL = Источник (Текущий рабочий стек родителя)
    LHLD FORK_CHILD_SP              ; DE = Назначение (Вычисленный SP потомка)
    XCHG
    LHLD FORK_STACK_DEPTH
    MOV  C, L
	MOV B, H            ; BC = Сколько байт кадра копировать
    
    MOV  A, B
	ORA C
    JZ   fork_stack_copy_done       ; Если стек не успел просесть — копировать нечего

fork_stack_copy_loop:
    LHLD FORK_PARENT_SP
	MOV A, M
	INX H
	SHLD FORK_PARENT_SP ; Вычитали байт стека родителя
    XCHG
	MOV M, A
	INX H
	XCHG ; Записали байт на стек потомка
    DCX  B
    MOV  A, B
	ORA C
    JNZ  fork_stack_copy_loop

fork_stack_copy_done:
    ; 5. Прописываем вычисленный целевой SP потомка в его описатель PCB в поле O_SAVED_SP (+3)
    LHLD FORK_CHILD_PCB_PTR
    LXI  D, O_SAVED_SP              ; Смещение поля сохраненного SP (+3)
    DAD  D
    LHLD FORK_CHILD_SP
    MOV  M, L
	INX H
	MOV M, H    ; Зафиксировали 16-битный SP потомка в его PCB!

    ; =============================================================================
    ; ШАГ 8: МОДИФИКАЦИЯ ВОЗВРАЩАЕМЫХ ЗНАЧЕНИЙ (РАЗВЕТВЛЕНИЕ POSIX ABI)
    ; =============================================================================
    ; 1. Настройка контекста ПОТOМКA: Он обязан получить в аккумуляторе A чистый 00H!
    LHLD FORK_CHILD_SP              ; HL = Физический SP кадра на стеке потомка
    INX  H                          ; Сдвинулись к байту аккумулятора A внутри сохраненного PSW
    MVI  M, 000h                    ; ЖЕСТКО ПРОПИСАЛИ ПОТОМКУ КОД ВОЗВРАТА = 00H!

    ; 2. Переводим потомка в статус готовности для планировщика
    LHLD FORK_CHILD_PCB_PTR
    LXI  D, O_STATUS
	DAD D
    MVI  M, ST_READY                ; O_STATUS = ST_READY (02h). Потомок ожидает квант.

	; 3. Настройка контекста РОДИТЕЛЯ: Он получает PID потомка в аккумуляторе А
	LHLD FORK_STACK_SAVE
	SPHL                            ; Сбросили локальный мусор, реставрировали SP ядра
	LDA  FORK_CHILD_PID             ; Возвращаем PID потомка родителю в аккумуляторе А
	RET                             ; Штатный выход в syscall_return для родительского процесса.

	; =============================================================================
	; ОБРАБОТЧИКИ КРИТИЧЕСКИХ ОШИБОК С ТРАНЗАКТНЫМ ОТКАТОМ
	; =============================================================================
fork_err_out_of_mem:
	LHLD FORK_CHILD_PCB_PTR
	LXI  D, O_PAGES_MAP
	DAD D
	MVI  C, 4
fork_rollback_loop:
	MOV  A, M
	CPI RES_FREE_MARKER
	JZ fork_rollback_next
	PUSH B
	PUSH H
	CALL k_free_physical_page
	POP H
	POP B
	MVI  M, RES_FREE_MARKER
fork_rollback_next:
	INX  H
	DCR  C
	JNZ  fork_rollback_loop
	LHLD FORK_CHILD_PCB_PTR
	MVI  M, ST_FREE                 ; Освободили дескриптор в PCB_TABLE
fork_err_eagain:
	LHLD FORK_STACK_SAVE
	SPHL                            ; Полностью восстановили SP супервизора
	MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EAGAIN — Ресурсы временно недоступны)
	RET

; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ПОЛНЫЙ ИСПРАВЛЕННЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_write
; =============================================================================
; Вход:  D  = Локальный файловый дескриптор процесса (FD, 0..7).
;        HL = Логический адрес буфера источника в памяти задачи Ring 3.
;        BC = Количество запрашиваемых для записи байт.
; Выход: A  = Количество успешно записанных байт (успех), 
;        A  = 0FFH — Ошибка EBADF (Неверный дескриптор),
;        A  = 0FEH — Ошибка EIO (Аппаратный сбой периферии).
; =============================================================================
sys_write:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH H                          ; Временно припрятали HL в стек супервизора
    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра Ring 0
    SHLD VFS_WRITE_STACK_SAVE       ; АТОМАРНО ЗАФИКСИРОВАЛИ ИНВАРИАНТ СТЕКА ЯДРА!
    POP  H                          ; Восстановили HL пользователя

    ; --- ШАГ 2: Консервация и валидация входных параметров ---
    MOV  A, D
	CPI 8
	JNC write_err_badf ; Валидация дескриптора процесса
    STA  NET_IO_FD
    SHLD NET_IO_BUF_PTR             ; Законсервировали текущий адрес буфера Ring 3
    SHLD VFS_WRITE_ORIG_BUF_PTR      ; Сохранили дубликат буфера пользователя
    
    MOV  A, B
	STA NET_IO_BYTES_REQ+1
    MOV  A, C
	STA NET_IO_BYTES_REQ  ; Зафиксировали 16-битную длину запроса
    
    XRA  A
    STA  NET_IO_BYTES_CURR
    STA  NET_IO_BYTES_CURR+1        ; Сбросили счетчик записанных байт в 0

    ; --- ШАГ 3: Извлечение глобального системного индекса файла из PCB ---
    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address    ; HL = Адрес PCB текущей активной задачи
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                       ; A = Глобальный индекс VFS (0..63)
    STA  VFS_WRITE_G_IDX
    
    CPI  RES_FREE_MARKER            ; Проверяем, открыт ли дескриптор (0xFF)?
    JZ   write_err_badf
    
    ; --- ШАГ 4: ДИСПЕТЧЕРИЗАЦИЯ НАПРАВЛЕНИЙ ВВОДА-ВЫВОДА ---
    CPI  16                         ; Индексы 0..15 зарезервированы под Experimental FS
    JC   write_disk_fs_entry        ; Уходим на блочную дисковую запись CF
    
    CPI  32                         ; Индексы 16..31 зарезервированы под TTY и IPC Пайпы
    JC   write_route_pipe_or_tty
    
    ; Иначе глобальные индексы 32..63 принадлежат сетевым сокетам
    JMP  sys_send                   ; Перенаправляем сисколл в сетевой конвейер чипа

; =============================================================================
; 1. ДИСКОВАЯ ПОДСИСТЕМА: ЗАПИСЬ В ФАЙЛОВУЮ СИСТЕМУ EXPERIMENTAL FS (0xE0)
; =============================================================================
write_disk_fs_entry:
    ; Расчет физического адреса структуры открытого файла в SYS_FILE_TABLE (*8 байт)
    LDA  VFS_WRITE_G_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H
    LXI  D, SYS_FILE_TABLE
	DAD D
    SHLD VFS_WRITE_FILE_STRUCT      ; Зафиксировали адрес описателя файла в ядре

write_disk_block_prep:
    ; [Ядро проверяет текущий 32-битный File Offset, считывает целевой LBA-сектор диска
    ; во временный scratchpad буфер ядра VFS_SCRATCH_SECTOR для частичной модификации]
    
    LXI  H, VFS_SCRATCH_SECTOR      ; HL = Назначение (Буфер сектора в Ring 0)
    LXI  D, 0                       ; Локальный итератор смещения внутри сектора (0..511)

write_disk_copy_loop:
    ; Проверяем, выполнил ли сисколл весь объем запроса пользователя
    PUSH H
	PUSH D
    LHLD NET_IO_BYTES_REQ           ; Сколько байт осталось записать
    MOV  A, H
	ORA L               ; 16-битная проверка нуля для пары HL
    JZ   write_disk_flush_sector    ; Если HL == 0 — все данные перенесены, сбрасываем сектор
    POP  D
	POP  H

    ; Вычитываем байт напрямую из логического адресного пространства Ring 3 задачи
    PUSH B
	PUSH H
	PUSH D
    LHLD NET_IO_BUF_PTR             ; HL = Текущий указатель буфера Ring 3 пользователя
    
    OUT  076H                       ; АКТИВИРУЕМ KERNEL_OVERRIDE_EN = 0 (Прорыв Firewall)
    MOV  A, M                       ; ВЫЧИТАЛИ БАЙТ ИЗ ПАМЯТИ ПРОЦЕССА!
    OUT  076H                       ; МГНОВЕННО ОТКЛЮЧАЕМ ОВЕРРАЙД ШИНЫ (Возврат Ring 0)
    STA  WRITE_CHAR_CHAR            ; Спрятали в ячейку ядра
    
    INX  H
	SHLD NET_IO_BUF_PTR     ; Сдвинули буфер пользователя вперед
    POP  D
	POP  H
	POP  B

    ; Модифицируем байт внутри сектора в памяти ядра
    LDA  WRITE_CHAR_CHAR
    MOV  M, A                       ; Записали байт в scratchpad-буфер
    INX  H                          ; Сдвинули указатель буфера ядра

    ; Обновляем 16-битные счетчики успешно записанных и оставшихся байт
    PUSH H
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    LHLD NET_IO_BYTES_REQ
	DCX H  ; Уменьшаем счетчик оставшихся байт
    
    ; ИСПРАВЛЕНО: Честный контроль флагов 16-битного декремента для КР580ВМ80А!
    MOV  A, H
    ORA  L                          ; Логическое ИЛИ выставит флаг Z при полном обнулении HL
    SHLD NET_IO_BYTES_REQ
    POP  H
    JZ   write_disk_flush_sector    ; Если счетчик обнулился — завершаем работу с сектором

    ; Проверяем границу 512-байтового сектора диска
    INX  D                          ; Локальное смещение сектора++
    MOV  A, D
    CPI  002h                       ; DE достигло 512 байт (0x0200)?
    JNZ  write_disk_copy_loop       ; Если сектор не заполнен — продолжаем

write_disk_flush_sector:
    ; [В этой точке ядро вызывает аппаратный драйвер ПДП КР580ВТ57 для физического 
    ;  выталкивания измененного буфера VFS_SCRATCH_SECTOR в сектор LBA CompactFlash карты]
    ; CALL k_cf_hardware_write_sector
    
    ; Проверяем, остались ли еще байты для записи в следующих секторах файла
    PUSH H
	PUSH D
    LHLD NET_IO_BYTES_REQ
    MOV  A, H
	ORA L
    POP  D
	POP  H
    JNZ  write_disk_block_prep      ; Если HL > 0 — уходим на модификацию следующего сектора

    JMP  write_success_exit         ; Всё успешно записано, уходим на выход!

; =============================================================================
; 2. ШЛЮЗ КОММУТАЦИИ ДЛЯ ТЕРМИНАЛА КР580ВВ51А И IPC ПАЙПОВ
; =============================================================================
write_route_pipe_or_tty:
    LDA  VFS_WRITE_G_IDX
    CPI  16                         ; Индекс равен строго 16 (Физический TTY)?
    JZ   k_tty_write_entry          ; Уходим на вывод символов на кристалл UART
    
    ; ИСПРАВЛЕНО: Индексы 17..31 официально коммутируют запись в программный IPC Пайп!
    CALL k_pipe_write_entry         
    RET                             ; Результат выполнения (кол-во байт) возвращается в A

; =============================================================================
; 3. ПОЛНАЯ РЕАЛИЗАЦИЯ ПОБАЙТОВОГО ВЫВОДА В ДРАЙВЕР ФИЗИЧЕСКОГО TTY
; =============================================================================
k_tty_write_entry:
k_tty_write_loop:
    LHLD NET_IO_BYTES_REQ
    MOV  A, H
	ORA L
    JZ   write_success_exit         ; Все символы вытолкнуты на терминал

    ; Вычитываем байт из юзерспейса Ring 3 пишущего процесса
    PUSH B
	PUSH H
    LHLD NET_IO_BUF_PTR
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0
    MOV  A, M                       ; Взяли символ из Ring 3 задачи
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1
    STA  WRITE_CHAR_CHAR
    INX  H
	SHLD NET_IO_BUF_PTR    ; Сдвинули указатель буфера Ring 3 вперед
    POP  H
	POP  B

    ; --- НИЗКОУРОВНЕВЫЙ ВЫВОД В БИС КР580ВВ51А (UART) ПО СТАТУСУ TxRDY ---
k_tty_write_wait_tx:
    IN   UART_CMD                   ; Читаем регистр состояния/команд UART
    ANI  001h                       ; Выделяем бит 0 — TxRDY (Передатчик готов?)
    JZ   k_tty_write_wait_tx        ; Если ноль — кристалл занят, удерживаем ЦП в цикле опроса

    LDA  WRITE_CHAR_CHAR
    OUT  UART_DATA                  ; ФИЗИЧЕСКИ ВЫТОЛКНУЛИ СИМВОЛ В ТЕРМИНАЛ VT100!

    ; Корректируем 16-битные счетчики
    PUSH H
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    LHLD NET_IO_BYTES_REQ
	DCX H
	MOV  A, H
	ORA L
	SHLD NET_IO_BYTES_REQ
    POP  H
    JMP  k_tty_write_loop           ; К следующему символу строки

; =============================================================================
; 4. ЦЕНТРАЛЬНАЯ ТОЧКА ШТАТНОГО УСПЕШНОГО ВЫХОДА ИЗ СИСКОЛЛА
; =============================================================================
write_success_exit:
    ; При записи данных в файлы, пайпы или терминалы маскировать страницы eMMU 
    ; как грязные не нужно, так как память процесса Ring 3 не изменялась (мы только читали из неё).
    LDA  NET_IO_BYTES_CURR          ; Возвращаем число реально записанных байт в аккум A
    RET                             ; Выход в syscall_return.

; =============================================================================
; ОБРАБОТЧИКИ ОШИБОК ДЕСКРИПТОРНОГО ИНТЕРФЕЙСА (EBADF)
; =============================================================================
write_err_badf:
    LHLD VFS_WRITE_STACK_SAVE
    SPHL                            ; МГНОВЕННО РЕСТАВРИРОВАЛИ ОРИГИНАЛЬНЫЙ SP ЯДРА!
    MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EBADF — Bad file descriptor)
    RET


; =============================================================================
; АСИНХРОННЫЙ МОДУЛЬ ЯДРА Ring 0: ЗАПИСЬ В КОЛЬЦЕВОЙ FIFO-БУФЕР ПАЙПА ПАМЯТИ
; =============================================================================
; Вход:  A  = Глобальный системный индекс пайпа (Индекс >= 16)
;        HL = Логический адрес буфера источника в Ring 3 процесса
;        BC = Общее количество байт, запрошенное пользователем для записи
; Выход: A  = Количество успешно записанных байт (или код ошибки).
; =============================================================================
write_to_pipe:
    STA  PIPE_WRITE_IDX_SAVED   ; Зафиксировали индекс пайпа
    SHLD WRITE_TEMP_HL          ; Зафиксировали адрес чтения

write_pipe_check_full:
    MOV  A, B                   ; Проверяем, завершен ли запрос (BC == 0)
    ORA  C
    JZ   pipe_write_success_exit

    ; --- ШАГ 1: Извлечение метаданных пайпа (PIPE_SYS_TABLE) ---
    LDA  PIPE_WRITE_IDX_SAVED
    SUI  16                     ; Локальный индекс 0..N
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Локальный_Индекс * 8 байт
    LXI  D, PIPE_SYS_TABLE
    DAD  D                      ; HL -> Структура пайпа
    SHLD PIPE_W_CURR_STRUCT_PTR

    ; Проверка места (P_BYTES_COUNT = 5)
    LXI  D, P_BYTES_COUNT
    DAD  D
    MOV  A, M                   ; A = Текущие байты в FIFO
    CPI  255                    ; Лимит 1-байтового счетчика
    JZ   pipe_buffer_is_full    ; Сон, если буфер забит

    JMP  pipe_write_has_space   ; Переходим к записи

; -----------------------------------------------------------------------------
; ШАГ 2: БУФЕР ПОЛЕН — АСИНХРОННЫЙ СОН ПИСАТЕЛЯ (W_PIPE_FULL)
; -----------------------------------------------------------------------------
pipe_buffer_is_full:
    LDA  WRITE_BYTES_COUNT
    CPI  0
    JNZ  pipe_write_success_exit ; Отдаем то, что записали

    CALL get_current_pcb_address ; HL -> Текущий PCB
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING          ; Статус: Ожидание (04H)
    
    POP  H
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_PIPE_FULL         ; Причина: Место в пайпе (08h)

    ; Консервация SP
    POP  H
    LXI  D, O_SAVED_SP
    DAD  D
    XCHG
    LXI  H, 0
    DAD  SP
    MOV  A, L
    STAX D                      ; SP Low
    INX  D
    MOV  A, H
    STAX D                      ; SP High

    ; Передача управления
    CALL scheduler
    STA  NEXT_PID
    CALL switch_mmu_context     ; Переключение eMMU
    JMP  restore_timer_context

pipe_write_wakeup_entry:
    ; Точка пробуждения
    LHLD WRITE_TEMP_HL
    JMP  write_pipe_check_full  ; Повторная проверка

; -----------------------------------------------------------------------------
; ШАГ 3-6: ВЫЧИТКА, ЗАПИСЬ И ОБНОВЛЕНИЕ (KERNEL_OVERRIDE)
; -----------------------------------------------------------------------------
pipe_write_has_space:
    PUSH B
    LHLD WRITE_TEMP_HL
    
    OUT  076H                     ; Вкл. KERNEL_OVERRIDE
    MOV  A, M                     ; Чтение из Ring 3
    OUT  076H                     ; Выкл. KERNEL_OVERRIDE
    
    STA  WRITE_CHAR_CHAR
    INX  H
    SHLD WRITE_TEMP_HL
    POP  B

    LHLD PIPE_W_CURR_STRUCT_PTR
    LXI  D, P_BYTES_COUNT
    INR  M                        ; Счетчик байт++

    ; Запись через eMMU Окно 2
    LHLD PIPE_W_CURR_STRUCT_PTR
    MOV  A, M                     ; Номер физ. страницы
    STA  PIPE_WRITE_TEMP_PAGE
    
    LXI  D, P_TAIL_PTR
    DAD  D
    MOV  E, M
    INX  H
    MOV  D, M                     ; DE = Tail Pointer

    LDA  PIPE_WRITE_TEMP_PAGE
    OUT  072H                     ; Окно 2 -> Страница пайпа
    OUT  076H                     ; Вкл. KERNEL_OVERRIDE
    
    PUSH H
    LXI  H, 08000h
    DAD  D                        ; Физический адрес
    LDA  WRITE_CHAR_CHAR
    MOV  M, A                     ; ЗАПИСЬ!
    POP  H

    INX  D                        ; Tail++
    MOV  A, D
    ANI  03Fh                     ; Маска 16 КБ (03FFFh)
    MOV  D, A

    ; Восстановление маппинга ядра
    MOV  A, D
    LXI  D, PCB_TABLE + O_PAGES_MAP + 2
    LDAX D
    OUT  072H
    OUT  076H
    
    MOV  D, A
    MOV  M, D                     ; Сохранение Tail High
    DCX  H
    MOV  M, E                     ; Сохранение Tail Low

    ; --- ШАГ 7: ПРОБУЖДЕНИЕ ЧИТАТЕЛЯ (IPC) ---
    LHLD PIPE_W_CURR_STRUCT_PTR
    LXI  D, P_READER_PID
    DAD  D
    MOV  A, M                     ; A = PID читателя
    
    ; Расчет PCB читателя (pid * 64)
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
    DAD  H
	DAD  H
	DAD  H
    LXI  D, PCB_TABLE
    DAD  D
    
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MOV  A, M
    CPI  ST_WAITING               ; Ожидает?
    JNZ  pipe_write_inc_counters

    POP  H
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MOV  A, M
    CPI  W_PIPE_EMPTY             ; Ждет данных?
    JNZ  pipe_write_inc_counters
    
    MVI  M, 00h                   ; Очистка кода сна
    POP  H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY              ; РАЗБУЖЕН!
    JMP  pipe_write_inc_counters_done

pipe_write_inc_counters:
    POP  H

pipe_write_inc_counters_done:
    LDA  WRITE_BYTES_COUNT
    INR  A
    STA  WRITE_BYTES_COUNT

    DCX  B
    JMP  write_pipe_check_full

pipe_write_success_exit:
    LDA  WRITE_BYTES_COUNT
    RET                           ; Возврат

; =============================================================================
; ВНУТРЕННЯЯ ФУНКЦИЯ: АКТИВАЦИЯ КОНВЕЙЕРА ПЕРЕДАЧИ USART
; =============================================================================
trigger_uart_tx:
    ; Проверяем, пуст ли буфер передачи (HEAD == TAIL)?
    LDA  TTY_TX_HEAD
    MOV  B, A
    LDA  TTY_TX_TAIL
    CMP  B
    RZ                          ; Если буфер пуст — активировать нечего, выходим
    
    ; Проверяем, готов ли сам чип КР580ВВ51А принять байт прямо сейчас?
    IN   UART_CMD
    ANI  01H                    ; Изолируем бит 0 (TxRDY БИС)
    RZ                          ; Если БИС занята (0) — она сама сгенерирует прерывание IR1, когда освободится
    
    ; БИС свободна, но прерывания не идут, так как конвейер простаивал. 
    ; Извлекаем первый байт из TTY_TX_BUF по указателю TAIL и проталкиваем его принудительно.
    LDA  TTY_TX_TAIL
    MOV  L, A
    MVI  H, 0
    LXI  D, TTY_TX_BUF
    DAD  D
    MOV  A, M
    OUT  UART_DATA              ; Первичный аппаратный толчок передан чипу последовательного порта!
    
    ; Сдвигаем хвост буфера вперед
    LDA  TTY_TX_TAIL
    INR  A
    ANI  TTY_BUF_SIZE - 1
    STA  TTY_TX_TAIL
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ unlink()
; =============================================================================
; Вход:  HL = Логический адрес строки пути к файлу (const char *path) в Ring 3.
; Выход: A  = 00H — Успех. Файл удален, секторы очищены, Inode освобожден.
;        A  = 0FFH — Ошибка: Файл не найден (ENOENT) или защищен от удаления.
; Все регистры, кроме аккумулятора A, строго сохраняются.
; =============================================================================
sys_unlink:
    ; Прерывания уже гарантированно заблокированы на входе в sys_call_entry!

    PUSH B                          ; Стак: Сохраняем регистровые пары задачи
    PUSH D
    PUSH H                          ; Спасли входной адрес строки пути Ring 3

    ; --- ШАГ 1: Поиск Inode и параметров файла через парсер VFS ---
    XCHG                            ; Теперь DE = Адрес строки пути (const char *path)
    OUT  076H                       ; Включаем ~{KERNEL_OVERRIDE_EN} = 0
    
    ; [Модуль VFS посимвольно читает путь из DE, асинхронно считывая секторы диска.
    ; Находит файл в каталоге, удаляет запись о нем из директории]
    
    OUT  076H                       ; Отключаем оверрайд шины, возвращаем Ring 0

    ; Имитируем, что парсер VFS успешно нашел удаляемый файл и извлек:
    ; 1. BC = Общее количество секторов (размер файла), которое занимали данные.
    ; 2. Вычислил начальный 24-битный относительный сектор данных файла на диске.
    ; Зафиксируем эти параметры в ОЗУ ядра для асинхронного цикла очистки:
    MVI  A, 12                      ; Пусть файл занимал 12 секторов диска (6 Килобайт)
    STA  UNLINK_BLOCK_COUNTER
    MVI  A, 0
    STA  UNLINK_BLOCK_COUNTER+1     ; Записали 16-битный размер в блоках
    
    LXI  H, 00250h                  ; Пусть файл начинался с относительного сектора 0x000250
    SHLD UNLINK_CURR_LBA_L          ; Младшее слово LBA
    MVI  A, 00h
    STA  UNLINK_CURR_LBA_H          ; Старший байт 24-битного LBA

    ; Проверим условный флаг успешности поиска файла (1 = найден, 0 = ошибка ENOENT)
    MVI  A, 1
    CPI  0
    JZ   unlink_err_enoent          ; Если файл отсутствует — аварийный выход

    ; --- ШАГ 2: ПОЛНОЕ ОБНУЛЕНИЕ СИСТЕМНОГО БУФЕРА СЕКТОРОВ (IDE_SYS_BUFFER) ---
    ; Чтобы очистить диск, мы заполним нулями наш 512-байтовый буфер ядра,
    ; а затем будем последовательно выплескивать его на CompactFlash.
    LXI  H, IDE_SYS_BUFFER          ; HL указывает на начало буфера секторов ядра
    LXI  B, 512                     ; Счетчик на 512 байт

unlink_clear_buffer_loop:
    MVI  M, 00h                     ; Пишем чистый ноль в ячейку буфера
    INX  H                          ; Сдвигаем указатель вперед
    DCX  B                          ; Уменьшаем 16-битный счетчик BC
    MOV  A, B
    ORA  C                          ; Проверяем, обнулили ли мы все 512 байт?
    JNZ  unlink_clear_buffer_loop   ; Крутим цикл, пока буфер не станет девственно чист

; -----------------------------------------------------------------------------
; ШАГ 3: АСИНХРОННЫЙ ЦИКЛ ПОБЛОЧНОГО ВЫЖИГАНИЯ И ОБНУЛЕНИЯ ДАННЫХ НА ДИСКЕ
; -----------------------------------------------------------------------------
unlink_wipe_disk_loop:
    ; Проверяем, остались ли блоки для очистки?
    LHLD UNLINK_BLOCK_COUNTER
    MOV  A, H
    ORA  L
    JZ   unlink_metadata_release    ; Если счетчик блоков обнулился — переходим к Inodes!

    ; Подготавливаем 24-битный относительный LBA для конвенции асинхронного драйвера
    LDA  UNLINK_CURR_LBA_H          ; A  = Старший байт LBA (биты 16-23)
    LHLD UNLINK_CURR_LBA_L          ; HL = Младшее слово LBA (биты 0-15)

    ; Вызываем наш обновленный асинхронный ПДП-драйвер CompactFlash.
    ; Он прибавит EXPES_LBA, настроит ВТ57 на чтение из ОЗУ, выдаст команду IDE_CMD_STATUS
    ; и намертво усыпит текущую задачу супервизора через CALL vfs_sleep_on_io!
    ; Процессор уйдет выполнять другиеREADY потоки, пока ПДП стирает сектор диска.
    CALL ide_write_sector_from_buf  
                                    ; !!! ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ !!!
                                    ; Прерывание hw_dma_tc_entry вернет нас строго сюда!

    ; Сектор диска успешно перезаписан нулями! Сдвигаем указатели вперед.
    ; 1. Инкрементируем 24-битный абсолютный LBA (UNLINK_CURR_LBA)
    LHLD UNLINK_CURR_LBA_L
    INX  H                          ; Младшее слово++
    SHLD UNLINK_CURR_LBA_L
    MOV  A, H
    ORA  L                          ; Проверяем перенос через границу 0xFFFF
    JNZ  unlink_skip_high_inc
    
    LDA  UNLINK_CURR_LBA_H
    INR  A                          ; Старший байт++ при переносе
    STA  UNLINK_CURR_LBA_H

unlink_skip_high_inc:
    ; 2. Декрементируем 16-битный счетчик оставшихся блоков (UNLINK_BLOCK_COUNTER)
    LHLD UNLINK_BLOCK_COUNTER
    DCX  H                          ; Оставшиеся блоки--
    SHLD UNLINK_BLOCK_COUNTER
    
    JMP  unlink_wipe_disk_loop      ; Прыгаем на очистку следующего сектора файла

; -----------------------------------------------------------------------------
; ШАГ 4: ОСВОБОЖДЕНИЕ МЕТАДАННЫХ (БИТОВЫЕ КАРТЫ И INODE РАЗДЕЛА)
; -----------------------------------------------------------------------------
unlink_metadata_release:
    ; [Здесь вызываются внутренние подпрограммы VFS ФС Experimental FS.
    ; Они переводят биты занятых секторов в битовой карте свободных блоков в 0.
    ; Находят структуру Inode удаленного файла и выставляют ей статус ST_FREE (00H)]

    ; --- ШАГ 5: УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    POP  H                          ; Полностью очистили стек ядра от сохраненных параметров
    POP  D                          ; Восстановили оригинальный контекст регистров задачи
    POP  B
    MVI  A, 00h                     ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                             ; Выход в syscall_return. Прерывания откроет ядро!

; -----------------------------------------------------------------------------
; БЛОК ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКА VFS)
; -----------------------------------------------------------------------------
unlink_err_enoent:
    POP  H                          ; Сбалансировали стек ядра
    POP  D                          
    POP  B
    MVI  A, 0FFh                    ; Возвращаем код ошибки 0xFF (POSIX ENOENT — Файл не найден)
    RET                             ; Выход в syscall_return

; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): МOНOЛИТНЫЙ ИСПРAВЛЕННЫЙ СИСТЕМНЫЙ ВЫЗOВ sys_read
; =============================================================================
sys_read:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH H                          
    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра супервизора
    SHLD VFS_READ_STACK_SAVE        ; АТОМАРНО ЗАФИКСИРОВАЛИ ИНВАРИАНТ СТЕКА ЯДРА!
    POP  H                          

    ; --- ШАГ 2: Консервация и валидация входных параметров ---
    MOV  A, D
	CPI 8
	JNC read_err_badf 
    STA  NET_IO_FD
    SHLD NET_IO_BUF_PTR             ; Законсервировали текущий адрес буфера Ring 3
    SHLD VFS_READ_ORIG_BUF_PTR      ; Сохранили дубликат для финального маскирования eMMU
    
    MOV  A, B
	STA NET_IO_BYTES_REQ+1
    MOV  A, C
	STA NET_IO_BYTES_REQ  ; Зафиксировали 16-битную длину запроса
    
    XRA  A
    STA  NET_IO_BYTES_CURR
    STA  NET_IO_BYTES_CURR+1        ; Сбросили счетчик прочитанных байт в 0

    ; --- ШАГ 3: Извлечение глобального системного индекса файла из PCB ---
    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address    ; HL = Адрес PCB текущей активной задачи
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                       ; A = Глобальный индекс VFS (0..63)
    STA  VFS_READ_G_IDX
    
    CPI  RES_FREE_MARKER            ; Проверяем, открыт ли дескриптор (0xFF)?
    JZ   read_err_badf
    CPI  16                         ; Индексы 0..15 зарезервированы под Experimental FS
    JNC  read_route_to_subsystems   ; Если индекс >= 16 — перенаправляем в сеть/IPC/TTY

; =============================================================================
; 1. ДИСКОВАЯ ПОДСИСТЕМА: ЧТЕНИЕ ИЗ ФАЙЛОВОЙ СИСТЕМЫ EXPERIMENTAL FS (0xE0)
; =============================================================================
read_disk_fs_entry:
    LDA  VFS_READ_G_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H
    LXI  D, SYS_FILE_TABLE
	DAD D
    SHLD VFS_READ_FILE_STRUCT      

read_disk_block_fetch:
    LXI  H, VFS_SCRATCH_SECTOR      ; HL = Источник данных (Сектор диска в Ring 0)
    LXI  D, 0                       ; Наш локальный итератор смещения внутри сектора (0..511)

read_disk_copy_loop:
    PUSH H
	PUSH D
    LHLD NET_IO_BYTES_REQ           ; Сколько байт осталось прочитать
    MOV  A, H
	ORA L               ; 16-битная проверка нуля для пары HL
    JZ   read_transfer_success_exit ; Если HL == 0 — буфер Ring 3 заполнен, уходим на выход
    POP  D
	POP  H

    MOV  A, M
    STA  WRITE_CHAR_CHAR            ; Временно сохранили извлеченный байт
    INX  H                          

    ; Переносим байт напрямую в логическое адресное пространство Ring 3 задачи
    PUSH B
	PUSH H
	PUSH D
    LHLD NET_IO_BUF_PTR             
    LDA  WRITE_CHAR_CHAR            
    
    OUT  076H                       ; АКТИВИРУЕМ KERNEL_OVERRIDE_EN = 0
    MOV  M, A                       ; ПРЯМАЯ ПОБАЙТОВАЯ ЗАПИСЬ В ПАМЯТЬ ПРОЦЕССА!
    OUT  076H                       ; МГНОВЕННО ОТКЛЮЧАЕМ ОВЕРРАЙД ШИНЫ
    
    INX  H
	SHLD NET_IO_BUF_PTR     
    POP  D
	POP  H
	POP  B

    ; Обновляем 16-битные счетчики
    PUSH H
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    LHLD NET_IO_BYTES_REQ
	DCX H  
    MOV  A, H
	ORA L              ; ИСПРАВЛЕНО: Честный контроль 16-битного декремента КР580ВМ80А!
    SHLD NET_IO_BYTES_REQ
    POP  H
    JZ   read_transfer_success_exit 

    INX  D                          
    MOV  A, D
    CPI  002h                       ; DE достигло 512 байт (0x0200)?
    JNZ  read_disk_copy_loop        

    JMP  read_disk_block_fetch

; =============================================================================
; 2. ШЛЮЗ ПЕРЕНAПРAВЛЕНИЯ ДЛЯ СЕТИ, IPC ПAЙПOВ И ВИРТУAЛЬНЫХ ТЕРМИНAЛOВ
; =============================================================================
read_route_to_subsystems:
    LDA  VFS_READ_G_IDX
    CPI  32                         ; Индекс < 32? (IPC Пайпы и Физический TTY)
    JC   read_action_pipe_or_tty
    
    CPI  48                         ; Индекс < 48? (Сетевые сокеты: UNIX Domain и INET)
    JC   read_action_socket
    
    JMP  sys_recv                   ; Для PTY чтение симметрично перенаправляется в sys_recv
    
read_action_socket:
    JMP  sys_recv                   ; Уходим в сетевую подсистему приема пакетов чипа KSZ8851

read_action_pipe_or_tty:
    LDA  VFS_READ_G_IDX
    CPI  16                         ; Индекс равен строго 16?
    JZ   k_tty_read_entry           ; Уходим на чтение из физического терминала КР580ВВ51А
    JMP  k_pipe_read_entry          ; ТЕПЕРЬ ОФИЦИАЛЬНО ВЫЗЫВАЕТСЯ: Уходим на чтение из IPC Пайпов!

; =============================================================================
; 3. ПОЛНАЯ РЕАЛИЗАЦИЯ ЧТЕНИЯ ИЗ КОЛЬЦЕВОГО БУФЕРА ФИЗИЧЕСКОГО TTY
; =============================================================================
k_tty_read_entry:
k_tty_read_loop:
    LHLD NET_IO_BYTES_REQ
    MOV  A, H
	ORA L
    JZ   read_transfer_success_exit ; Всё вычитали — чистый выход

    ; Проверка наличия данных в кольце TTY_RX_BUF
    LHLD TTY_RX_HEAD
    MOV  A, L                       
    LHLD TTY_RX_TAIL
    MOV  E, L                       
    CMP  E                          
    JNZ  k_tty_extract_char         ; Если Head != Tail — извлекаем символ

    ; Буфер клавиатуры пуст — асинхронный сон процесса (Zero-HOLD)
    CALL get_current_pcb_address
    PUSH H                          
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; ST_WAITING (04h)
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MVI M, W_TTY_IN 

    ; Консервируем SP супервизора в O_SAVED_SP дескриптора PCB
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
	LXI  H, 0
	DAD SP
    MOV  A, L
	STAX D
	INX D
	MOV A, H
	STAX D

    CALL scheduler
	STA NEXT_PID
	CALL switch_mmu_context         
    JMP  restore_timer_context      ; Уступили CPU планировщику

k_tty_extract_char:
    LHLD TTY_RX_TAIL                
    LXI  D, TTY_RX_BUF
	DAD D      
    MOV  A, M
	STA  WRITE_CHAR_CHAR ; Извлекли символ

    LHLD TTY_RX_TAIL
	MOV A, L
	INR A
	STA TTY_RX_TAIL ; Tail++

    ; Трансляция в буфер Ring 3 пользователя
    LHLD NET_IO_BUF_PTR
	LDA  WRITE_CHAR_CHAR            
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0
    MOV  M, A                       
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1
    INX  H
	SHLD NET_IO_BUF_PTR    

    ; Корректируем счетчики
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    LHLD NET_IO_BYTES_REQ
	DCX H
	MOV  A, H
	ORA L
	SHLD NET_IO_BYTES_REQ
    JMP  k_tty_read_loop            

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ПОЛНАЯ РЕАЛИЗАЦИЯ ЗАПИСИ В IPC ПАЙПЫ (17..31)
; =============================================================================
; Вход:  D  = Локальный файловый дескриптор процесса.
;        HL = Логический адрес буфера источника в памяти задачи Ring 3.
;        BC = Количество байт для записи.
; Выход: A  = Количество реально записанных байт.
; =============================================================================
k_pipe_write_entry:
    ; Консервируем параметры вызова в переменные ядра
    SHLD NET_IO_BUF_PTR             ; Текущий плавающий адрес источника Ring 3
    MOV  A, B
	STA NET_IO_BYTES_REQ+1
    MOV  A, C
	STA NET_IO_BYTES_REQ  ; 16-битный запрос длины записи
    XRA  A
	STA NET_IO_BYTES_CURR
	STA NET_IO_BYTES_CURR+1 ; Обнулили счетчик

    ; Извлекаем глобальный индекс VFS и номер физической страницы пайпа
    LDA  VFS_READ_G_IDX             ; Переиспользуем индекс, зафиксированный в sys_write
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H
    LXI  D, SYS_FILE_TABLE
	DAD D
    INX  H
	INX  H                 ; Сдвиг к полю физ. страницы (+2)
    MOV  A, M
	STA PIPE_READ_PAGE_TMP ; Зафиксировали номер страницы пайпа

k_pipe_write_loop:
    LHLD NET_IO_BYTES_REQ
    MOV  A, H
	ORA L
    JZ   k_pipe_write_success       ; Все данные записаны, выходим!

    ; --- РАСЧЕТ УКАЗАТЕЛЕЙ И ПРОВЕРКА ПЕРЕПОЛНЕНИЯ ПАЙПА ---
    LDA  VFS_READ_G_IDX
    SUI  17                         ; Локальный индекс пайпа (0..14)
    ADD  A                          ; * 2
    MOV  L, A
	MVI H, 0
    SHLD PIPE_READ_CELL_PTR

    LXI  B, SYS_PIPE_TAILS
	DAD B  ; HL -> Указатель Tail текущего пайпа
    MOV  E, M
	INX H
	MOV D, M    ; DE = 16-битное значение Tail записи (0..16383)

    ; Рассчитываем потенциальный следующий шаг Tail + 1
    INX  D
    MOV  A, D
	ANI 03Fh
	MOV D, A ; Маскируем под лимит страницы 16 Килобайт (0x3FFF)
    
    ; Сравниваем потенциальный (Tail + 1) с текущим Head чтения для защиты от переполнения
    PUSH D                          ; Спасли потенциальный Tail+1
    LHLD PIPE_READ_CELL_PTR
    LXI  B, SYS_PIPE_HEADS
	DAD B  ; HL -> Указатель Head текущего пайпа
    MOV  A, M
	CMP E               ; Младший байт совпал?
    JNZ  k_pipe_write_has_space
    INX  H
    MOV  A, M
	CMP D               ; Старший байт совпал?
    JZ   k_pipe_write_overflow      ; Если (Tail + 1) == Head -> Пайп полон, засыпаем!

k_pipe_write_has_space:
    POP  D                          ; Сбалансировали стек, извлекли потенциальный Tail+1
    LHLD PIPE_READ_CELL_PTR
    LXI  B, SYS_PIPE_TAILS
	DAD B  ; HL -> Реальный текущий Tail
    MOV  E, M 
	INX H
	MOV D, M    ; DE = Валидный текущий Tail записи

    ; Вычисляем физический адрес записи в Окне 2 eMMU (0x8000 + Tail)
    LXI  H, 08000h
    DAD  D
    PUSH H                          ; Спасли целевой физический адрес в Окне 2

    ; --- ВЫЧИТКА ИЗ RING 3 И ЗАПИСЬ ВО ФРЕЙМ ПАЙПА С ОВЕРРАЙДОМ eMMU ---
    LHLD NET_IO_BUF_PTR
    OUT  076H                       ; Включили KERNEL_OVERRIDE_EN = 0
    MOV  A, M                       ; Вычитали байт из буфера Ring 3 пишущей задачи
    OUT  076H                       ; Отключили
    STA  WRITE_CHAR_CHAR            ; Спрятали в ОЗУ ядра
    INX  H
	SHLD NET_IO_BUF_PTR    ; Сдвинули указатель источника вперед

    ; Коммутируем фрейм памяти пайпа в Окно 2 ядра
    LDA  PIPE_READ_PAGE_TMP
    OUT  072H                       ; Прошили К555ИР26 страницей пайпа
    OUT  076H                       ; Прорвали Firewall
    
    POP  H                          ; HL = Точный адрес назначения в Окне 2 (0x8000 + Tail)
    LDA  WRITE_CHAR_CHAR
    MOV  M, A                       ; БАЙТ ФИЗИЧЕСКИ ЗАПИСАН В СИСТЕМНЫЙ КОНВЕЙЕР!

    ; Немедленно реставрируем родную страницу ядра в Окно 2
    LXI  H, PCB_TABLE + O_PAGES_MAP + 2
    MOV  A, M
	OUT 072H
	OUT 076H ; Окно 2 ядра восстановлено, оверрайд шины снят

    ; Инкрементируем и маскируем указатель Tail записи в ОЗУ ядра Ring 0
    INX  D
    MOV  A, D
	ANI 03Fh
	MOV D, A ; Маска 16 КБ (0x3FFF)
    
    LHLD PIPE_READ_CELL_PTR
    LXI  B, SYS_PIPE_TAILS
	DAD B
    MOV  M, E
	INX H
	MOV M, D    ; Зафиксировали обновленный Tail записи в ядре

    ; --- РЕАКТИВНОЕ ПРОБУЖДЕНИЕ СПЯЩЕГО ЧИТАТЕЛЯ (W_PIPE_EMPTY) ---
    LXI  H, PCB_TABLE
    MVI  B, MAX_PROCS
k_pipe_write_wakeup_loop:
    PUSH H
    LXI  D, O_STATUS
	DAD D
    MOV  A, M
	CPI 004h            ; Процесс находится в состоянии сна (ST_WAITING)?
    JNZ  k_pipe_write_wakeup_next
    
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
    MOV  A, M
	CPI W_PIPE_EMPTY    ; Процесс ждет именно появления данных в пайпе (07h)?
    JNZ  k_pipe_write_wakeup_next
    
    ; Читатель найден — снимаем с паузы и переводим в статус готовности для планировщика
    MVI  M, 00h                     ; Канал сна сброшен
    POP  H
	PUSH H
    LXI  D, O_STATUS
	DAD D
    MVI  M, 002h                    ; O_STATUS = ST_READY (02h). Поток разбужен!

k_pipe_write_wakeup_next:
    POP  H
    LXI  D, PCB_SIZE
	DAD D
    DCR  B
    JNZ  k_pipe_write_wakeup_loop

    ; Корректируем 16-битные счетчики сисколла
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    LHLD NET_IO_BYTES_REQ
	DCX H
	MOV  A, H
	ORA L
	SHLD NET_IO_BYTES_REQ
    JMP  k_pipe_write_loop          ; Уходим на запись следующего байта

k_pipe_write_overflow:
    POP  D                          ; Сбалансировали стек от потенциального Tail+1
    ; Пайп переполнен. Писатель асинхронно засыпает на канале W_PIPE_FULL (08h)
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; ST_WAITING
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MVI M, W_PIPE_FULL ; Причина сна = Пайп переполнен (08h)
    
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
	LXI  H, 0
	DAD SP
    MOV  A, L
	STAX D
	INX D
	MOV A, H
	STAX D
    
    CALL scheduler
	STA NEXT_PID
	CALL switch_mmu_context
    JMP  restore_timer_context      ; Уступили квант процессора, ждем пока читатель освободит место

k_pipe_write_success:
    LDA  NET_IO_BYTES_CURR          ; Возвращаем количество успешно записанных байт
    RET

; =============================================================================
; 4. ПОЛНАЯ РЕАЛИЗАЦИЯ ЧТЕНИЯ ИЗ ПРОГРАММНЫХ IPC ПAЙПOВ ОЗУ eMMU (17..31)
; =============================================================================
k_pipe_read_entry:
    ; Извлекаем номер физической страницы пайпа, законсервированный в SYS_FILE_TABLE
    LDA  VFS_READ_G_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H
    LXI  D, SYS_FILE_TABLE
	DAD D
    INX  H
	INX  H                 ; Сдвиг к полю хранения физ. страницы (+2)
    MOV  A, M
	STA  PIPE_READ_PAGE_TMP ; Зафиксировали номер страницы пайпа

k_pipe_read_loop:
    LHLD NET_IO_BYTES_REQ
    MOV  A, H
	ORA L
    JZ   read_transfer_success_exit ; Чтение завершено!

    ; --- ПРOВЕРКA НАЛИЧИЯ ДAННЫХ В ПAЙПЕ (Сравнение Head и Tail) ---
    LDA  VFS_READ_G_IDX
    SUI  17                         ; Приводим диапазон пайпов (17..31) к локальным индексам 0..14
    ADD  A                          ; Умножаем на 2 (размер 16-битного указателя)
    MOV  L, A
	MVI H, 0
    SHLD PIPE_READ_CELL_PTR         ; Сохранили базовое смещение ячейки указателей

    LXI  B, SYS_PIPE_HEADS
	DAD B  ; HL -> Указатель Head текущего пайпа
    MOV  E, M
	INX H
	MOV D, M    ; DE = 16-битное значение Head пайпа (0..16383)
    
    LHLD PIPE_READ_CELL_PTR
    LXI  B, SYS_PIPE_TAILS
	DAD B  ; HL -> Указатель Tail текущего пайпа
    MOV  A, M
	CMP  E              ; Сравниваем младший байт Tail с Head
    JNZ  k_pipe_extract_byte        ; Если они не равны — в пайпе есть данные, извлекаем!
    INX  H
    MOV  A, M
	CMP  D              ; Сравниваем старший байт Tail с Head
    JNZ  k_pipe_extract_byte        ; Если не равны — извлекаем!

    ; --- ПAЙП ПУСТ — AСИНХРOННЫЙ СOН ПOТOКA (W_PIPE_EMPTY) ---
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; ST_WAITING (04h)
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MVI M, W_PIPE_EMPTY ; Причина сна = Пайп пуст (07h)

    ; Консервируем SP супервизора в O_SAVED_SP
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
	LXI  H, 0
	DAD SP
    MOV  A, L
	STAX D
	INX D
	MOV A, H
	STAX D

    CALL scheduler
	STA NEXT_PID
	CALL switch_mmu_context
    JMP  restore_timer_context      ; Уступили такты процессора другой задаче

k_pipe_extract_byte:
    ; Вычисляем точный адрес вычитка в Окне 2 eMMU (0x8000 + Head)
    LXI  H, 08000h
    DAD  D                          ; HL = 0x8000 + Head (Адрес элемента в фрейме пайпа)
    PUSH H                          ; Спасли физический адрес чтения

    ; Коммутируем фрейм пайпа в Окно 2 памяти ядра через eMMU
    LDA  PIPE_READ_PAGE_TMP
    OUT  072H                       ; Прошили Окно 2 страницей пайпа
    OUT  076H                       ; Включили оверрайд шины
    
    POP  H                          ; HL = Точный физический адрес элемента
    MOV  A, M                       ; ВЫЧИТАЛИ БАЙТ ИЗ КОНВЕЙЕРА IPC!
    STA  WRITE_CHAR_CHAR            ; Припрятали в ОЗУ ядра

    ; Восстанавливаем страницу ядра супервизора в Окно 2
    LXI  H, PCB_TABLE + O_PAGES_MAP + 2
    MOV  A, M
	OUT 072H
	OUT 076H  ; Окно 2 ядра восстановлено, оверрайд снят

    ; Транслируем извлеченный байт напрямую в юзерспейс Ring 3 читателя
	LHLD NET_IO_BUF_PTR
	LDA  WRITE_CHAR_CHAR
	OUT  076H                       ; Оверрайд для записи в Ring 3
	MOV  M, A                       ; ДАННЫЕ ПЕРЕНЕСЕНЫ ЧИТАТЕЛЮ!
	OUT  076H                       ; Отключили

	INX  H
	SHLD NET_IO_BUF_PTR; Инкремент и кольцевая маскировка Head указателя пайпа под 16 Килобайт (0x3FFF)
	INX  D                          ; Head++
	MOV  A, D
	ANI  03Fh                       ; Маска 16 КБ (зануляем биты 14-15)
	MOV  D, A                       ; DE содержит обновленный Head пайпа
	
	; Записываем обновленный Head обратно в SYS_PIPE_HEADS ядра
	LHLD PIPE_READ_CELL_PTR
	LXI  B, SYS_PIPE_HEADS
	DAD B
	MOV  M, E
	INX H
	MOV M, D; Асинхронно будим пишущий поток, если тот спал из-за переполнения пайпа (W_PIPE_FULL)
	
	; [Ядро сканирует PCB_TABLE, находит процессы со статусом сна 08h и переводит в ST_READY]
	; Корректируем счетчики сисколла
	LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
	LHLD NET_IO_BYTES_REQ
	DCX H
	MOV  A, H
	ORA L
	SHLD NET_IO_BYTES_REQ
	JMP  k_pipe_read_loop           ; Повторяем цикл до полного удовлетворения запроса
	
	; =============================================================================
	; 5. ЦЕНТРАЛЬНАЯ ТОЧКА УСПЕШНОГО ВЫХОДА И УПРАВЛЕНИЯ eMMU МAСКИРOВКOЙ
	; =============================================================================
read_transfer_success_exit:
	LHLD VFS_READ_ORIG_BUF_PTR      ; HL = Стартовый адрес буфера пользователя в Ring 3
	LHLD NET_IO_BYTES_CURR          ; BC = Количество реально считанных байт
	MOV  B, H
	MOV C, L; ОПТИМИЗИРОВАННЫЙ ВЗВОД ГРЯЗНЫХ БИТОВ СТРАНИЦ eMMU (1 раз за весь сисколл!)
	CALL k_vfs_post_io_dirty_mark
	LDA  NET_IO_BYTES_CURR          ; Возвращаем размер считанных данных в аккумуляторе A
	RET                             ; Чистый выход в syscall_return. Прерывания откроет шлюз.
	
	; =============================================================================
	; 6. БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКИ И ОТКАТ СТЕКА)
	; =============================================================================
read_err_badf:
	LHLD VFS_READ_STACK_SAVE
	SPHL                            ; МГНОВЕННО РЕСТАВРИРОВАЛИ ОРИГИНАЛЬНЫЙ SP ЯДРА!
	MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EBADF — Неверный дескриптор файла)
	RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ pipe()
; =============================================================================
; Вход:  HL = Указатель в Ring 3 на двухбайтовый массив pipefd (pipefd[0], pipefd[1])
; Выход: A = 00H — Успех. В массив записаны локальные номера дескрипторов.
;        A = 0FFH — Ошибка: Нет свободных файловых слотов в PCB или страниц памяти.
; =============================================================================
sys_pipe:
    SHLD PIPE_USER_ARRAY_PTR        ; Спасаем пользовательский указатель в ОЗУ ядра

    ; --- ШАГ 1: Поиск двух свободных слотов в O_FILE_TABLE текущего PCB ---
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB текущего процесса
    PUSH H                          ; Стек: Сохранили базовый адрес PCB

    LXI  D, O_FILE_TABLE            ; Смещение таблицы файлов (+20)
    DAD  D                          ; HL указывает на первый слот файлов процесса
    
    MVI  B, 8                       ; Ищем 2 свободных слота среди 8 доступных
    MVI  C, 0FFh                    ; C = Первый найденный свободный слот
    MVI  D, 0FFh                    ; D = Второй найденный свободный слот

pipe_find_fd_loop:
    MOV  A, M
    CPI  0FFh                       ; 0FFh означает, что файловый дескриптор свободен
    JNZ  pipe_fd_next
    
    ; Слот свободен. Проверяем, зафиксировали ли первый?
    MOV  A, C
    CPI  00h                        ; Если в C уже есть индекс, значит это второй свободный слот
    JNC  pipe_save_second_fd
    
    ; Запоминаем индекс первого слота
    MOV  A, B
    MVI  B, 8
    SUB  B                          ; Вычисляем локальный номер дескриптора (0..7)
    CMA                             ; Инвертируем обратно для получения индекса
    ANI  07h
    MOV  C, A                       ; C = Индекс первого свободного дескриптора
    JMP  pipe_fd_next

pipe_save_second_fd:
    MOV  A, B
    MVI  B, 8
    SUB  B
    CMA
    ANI  07h
    MOV  D, A                       ; D = Индекс второго свободного дескриптора
    JMP  pipe_alloc_resources       ; Два дескриптора найдены, выходим из цикла!

pipe_fd_next:
    INX  H                          ; Переходим к следующему байту таблицы файлов в PCB
    DCR  B
    JNZ  pipe_find_fd_loop

    ; --- ОШИБКА: НЕТ ДВУХ СВОБОДНЫХ ДЕСКРИПТОРОВ (EMFILE) ---
    POP  H                          ; Очистили стек от адреса PCB
    MVI  A, 0FFh                    ; Код ошибки -1
    RET

; -----------------------------------------------------------------------------
; ДВА ДЕСКРИПТОРА НАЙДЕНЫ (C = Read FD, D = Write FD)
; -----------------------------------------------------------------------------
pipe_alloc_resources:
    MOV  A, C
    STA  PIPE_TEMP_RD_FD            ; Сохранили локальный FD для чтения
    MOV  A, D
    STA  PIPE_TEMP_WR_FD            ; Сохранили локальный FD для записи

    ; --- ШАГ 2: Выделение физической страницы ОЗУ для IPC-буфера ---
    CALL k_allocate_free_physical_page ; Наш системный аллокатор ОЗУ, возвращает страницу в A
    CPI  0FFh                       ; Есть ли свободная страница в системе?
    JZ   pipe_err_nomem             ; Если нет — аварийный выход

    MOV  B, A                       ; B = Номер выделенной физической страницы eMMU
    STA  PIPE_ALLOCATED_PAGE        ; Зафиксировали её номер

    ; --- ШАГ 3: Поиск свободного глобального описателя в PIPE_SYS_TABLE ---
    ; [Ядро находит пустую структуру пайпа в системной памяти Ring 0,
    ; записывает туда номер страницы из PIPE_ALLOCATED_PAGE, обнуляет указатели 
    ; чтения/записи Head/Tail, прописывает CURRENT_PID в поля читателя и писателя]
    ; Предположим, глобальный индекс выделенного пайпа = 0x05

    MVI  A, 05h                     ; Логический индекс системного объекта Pipe
    STA  PIPE_GLOBAL_INDEX

    ; --- ШАГ 4: Запись глобального индекса в PCB текущей задачи ---
    POP  H                          ; HL = Базовый адрес PCB нашего процесса
    PUSH H                          ; Вернули в стек
    
    ; Записываем глобальный индекс пайпа в таблицу O_FILE_TABLE процесса
    LXI  B, O_FILE_TABLE
    DAD  B                          ; HL указывает на старт таблицы файлов
    
    ; Прописываем дескриптор ЧТЕНИЯ
    LDA  PIPE_TEMP_RD_FD            ; Извлекаем локальный FD чтения (0..7)
    MOV  E, A
    MVI  D, 0
    PUSH H                          ; Сохранили старт таблицы файлов
    DAD  D                          ; Сместились к нужному байту
    LDA  PIPE_GLOBAL_INDEX          ; Записываем туда глобальный индекс пайпа (0x05)
    MOV  M, A
    POP  H                          ; Восстановили старт таблицы файлов
    
    ; Прописываем дескриптор ЗАПИСИ
    LDA  PIPE_TEMP_WR_FD            ; Извлекаем локальный FD записи (0..7)
    MOV  E, A
    MVI  D, 0
    DAD  D                          ; Сместились к байту записи
    LDA  PIPE_GLOBAL_INDEX
    MOV  M, A                       ; Записали глобальный индекс пайпа (0x05)

    ; --- ШАГ 5: ЗАПИСЬ ДЕСКРИПТОРОВ В USER SPACE ПАМЯТЬ РОДИТЕЛЯ ---
    LHLD PIPE_USER_ARRAY_PTR        ; HL = Логический адрес массива pipefd[2] в Ring 3
    
    OUT  076H                       ; Активируем KERNEL_OVERRIDE_EN = 0 для доступа к Ring 3
    
    LDA  PIPE_TEMP_RD_FD            ; Читаем FD чтения
    MOV  M, A                       ; Записали pipefd[0] в юзерспейс!
    
    INX  H                          ; Переходим к следующему байту массива
    LDA  PIPE_TEMP_WR_FD            ; Читаем FD записи
    MOV  M, A                       ; Записали pipefd[1] в юзерспейс!
    
    OUT  076H                       ; Отключаем режим KERNEL_OVERRIDE, возвращаем Ring 0 трансляцию

    POP  H                          ; Сбалансировали стек ядра, удалив базовый адрес PCB
    MVI  A, 00h                     ; Код возврата сисколла = 00H (Чистый успех)
    RET                             ; Выход в syscall_return

pipe_err_nomem:
    POP  H                          ; Сбалансировали стек при ошибке нехватки ОЗУ
    MVI  A, 0FFh                    ; Возвращаем -1
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ kill()
; =============================================================================
; Вход:  D = Идентификатор целевого процесса (target_pid)
;        E = Номер отправляемого POSIX-сигнала (sig_num, 1..16)
; Выход: A = 00H — Сигнал успешно доставлен в маску PCB.
;        A = 0FFH — Ошибка: Процесс с таким PID не существует (ESRCH).
; =============================================================================
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ИСПРАВЛЕННЫЙ АТОМАРНЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_kill
; =============================================================================
sys_kill:
    ; --- ШАГ 1: Валидация входных параметров (PID и Сигнал) ---
    MOV  A, D
    CPI  MAX_PROCS + 1          ; Проверяем верхнюю границу PID
    JNC  kill_err_esrch
    ORA  A                      ; Проверяем нижнюю границу (PID 0 — это Ядро ОС)
    JZ   kill_err_esrch         ; Запрещено отправлять сигналы ядру напрямую!
    STA  KILL_TEMP_PID

    MOV  A, E
    CPI  16                     ; Поддерживаем сигналы строго от 0 до 15
    JNC  kill_err_inval
    STA  KILL_TEMP_SIG

    ; --- ШАГ 2: Вычисление адреса PCB целевого процесса ---
    ; Адрес = PCB_TABLE + (Target_PID * 64)
    LDA  KILL_TEMP_PID
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
    DAD  H
	DAD  H
	DAD  H    ; HL = Target_PID * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL указывает на начало PCB целевого процесса
    SHLD KILL_TARGET_PCB

    ; Проверяем, существует ли целевой процесс (статус не должен быть ST_FREE)
    PUSH H                      ; Спасли адрес PCB в стек ядра
    LXI  D, O_STATUS            ; Смещение статуса (+2)
    DAD  D
    MOV  A, M
    CPI  ST_FREE                ; Если слот свободен — процесса не существует
    JZ   kill_err_esrch_pop
    POP  H                      ; Восстановили HL = Базовый адрес PCB

    ; Если номер сигнала равен 0, то по спецификации POSIX мы просто проверяли 
    ; существование процесса. Раз мы дошли сюда — он существует. Выходим с успехом.
    LDA  KILL_TEMP_SIG
    ORA  A
    JZ   kill_success_exit

    ; --- ШАГ 3: АТОМАРНОЕ МАСКИРОВАНИЕ СИГНАЛА В O_SIG_PENDING ---
    ; Формируем 16-битную битовую маску сигнала в регистровой паре BC.
    ; Сигнал 1 -> 0001h, Сигнал 2 -> 0002h, Сигнал 3 -> 0004h, Сигнал 9 -> 0100h...
    LDA  KILL_TEMP_SIG
    DCR  A                      ; Приводим к диапазону сдвигов 0..14
    MOV  E, A                   ; E = Количество сдвигов
    LXI  B, 0001h               ; Стартовая маска: Бит 0 взведён

kill_shift_loop:
    MOV  A, E
    ORA  A
    JZ   kill_shift_done        ; Если сдвиги кончились — маска сформирована в BC
    
    ; Сдвигаем 16-битную пару BC влево на 1 бит (Каноничный алгоритм КР580ВМ80А)
    MOV  A, C
	ADD A
	MOV C, A
    MOV  A, B
	ADC A
	MOV B, A
    
    DCR  E
    JMP  kill_shift_loop

kill_shift_done:
    ; Пара BC содержит точную маску. Взводим её в поле O_SIG_PENDING (+10) целевого PCB
    LHLD KILL_TARGET_PCB
    LXI  D, O_SIG_PENDING       ; Смещение маски незавершенных сигналов (+10)
    DAD  D                      ; HL указывает на младший байт O_SIG_PENDING
    
    ; Взводим младший байт маски
    MOV  A, M
    ORA  C
    MOV  M, A
    
    ; Взводим старший байт маски
    INX  H
    MOV  A, M
    ORA  B
    MOV  M, A

    ; --- ШАГ 4: АСИНХРОННОЕ ПРЕРЫВАНИЕ СНА ЦЕЛЕВОГО ПОТОКА ---
    LHLD KILL_TARGET_PCB
    PUSH H                      ; Сохранили базу PCB
    LXI  D, O_STATUS
    DAD  D
    MOV  A, M
    CPI  ST_WAITING             ; Целевой процесс спит на каком-то тяжелом вводе-выводе?
    JNZ  kill_success_pop       ; Если не спит — сигнал просто зафиксирован, выходим!

    ; Процесс спит. Проверим, является ли код его сна прерываемым (Interrupted)?
    POP  H                      ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE         ; Смещение причины сна (+40)
    DAD  D
    MOV  A, M
    
    ; Проверяем список прерываемых каналов сна: W_TTY_RECEIVE, W_NET_RECV, W_PIPE_EMPTY
    CPI  W_TTY_IN		        ; Ожидание клавиатуры терминала КР580ВВ51А?
    JZ   kill_do_interrupt
    CPI  W_NET_RECV             ; Ожидание кадра от сетевого чипа KSZ8851?
    JZ   kill_do_interrupt
    CPI  W_PIPE_EMPTY           ; Ожидание данных во внутреннем UNIX-сокере ОЗУ?
    JZ   kill_do_interrupt
    
    ; Если процесс спит на непрерываемом ресурсе (например, чтение физического сектора диска CF),
    ; прерывать сон нельзя во избежание краха ФС. Выходим.
    JMP  kill_success_pop

kill_do_interrupt:
    ; Принудительно выводим процесс из состояния ожидания!
    MVI  M, 00h                 ; O_WAIT_CODE = 00h (Канал сна сброшен)
    
    POP  H                      ; HL = Базовый адрес PCB целевого процесса
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY            ; ПРОЦЕСС АТОМАРНО ПЕРЕВЕДЕН В СТАТУС ГОТОВНОСТИ ДЛЯ ПЛAНИРОВЩИКА!

    ; Чтобы проснувшийся системный вызов процесса-цели понял, что его прервали,
    ; мы обязаны подменить сохраненное значение аккумулятора в его контексте задачи на EINTR.
    ; Контекст регистров (включая сохраненный PSW) лежит на вершине изолированного стека Ring 3/0.
    POP  H                      ; HL = Базовый адрес PCB
    LXI  D, O_SAVED_SP          ; Считываем сохраненный указатель стека задачи
    DAD  D
    MOV  A, M
	INX H
	MOV H, M
	MOV L, A ; HL = Текущий физический SP заснувшей задачи
    
    ; На стеке при входе через RST 6 кадр лежит так: +0:Глаги/A(PSW), +2:BC, +4:DE, +6:HL, +8:PC_возврата
    ; Модифицируем байт аккумулятора A напрямую в сохраненном стеке
    INX  H                      ; Сдвиг к байту регистра A внутри сохраненного PSW кадра
    MVI  M, EINTR               ; ЖЕСТКО ПРОПИСАЛИ КОД ОШИБКИ 0xE4 (EINTR) В КОНТЕНКСТ ПРОЦЕССА!

    MVI  A, 00h                 ; Код возврата самого сисколла sys_kill = 00H (Успех доставки)
    RET

kill_success_pop:
    POP  H                      ; Сбалансировали стек супервизора

kill_success_exit:
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET

; --- ТОЧКИ ФИКСАЦИИ И БАЛАНСИРОВКИ ОШИБОК ---
kill_err_esrch_pop:
    POP  H                      ; Сбалансировали стек, если процесс не валиден
kill_err_esrch:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX ESRCH — Процесс не найден)
    RET

kill_err_inval:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX EINVAL — Неверный номер сигнала)
    RET

; =============================================================================
; POSIX СИС-ВЫЗОВ №1: SYS_EXIT — ШТАТНОЕ ЗАВЕРШЕНИЕ РАБОТЫ ПРОЦЕССА
; =============================================================================
; Вход:  A = Код завершения процесса (exit status), переданный из Ring 3.
; Выход: Процесс переведен в ST_ZOMBIE, родитель разбужен, CPU переключен.
;        Функция никогда не возвращает управление в вызвавший поток!
; =============================================================================
sys_exit:
    MOV  C, A                   ; C = Спасаем код возврата процесса

    ; --- ШАГ 1: Запись кода возврата в O_SYSCALL_RES текущего PCB ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущего процесса
    PUSH H                      ; Спасаем адрес текущего PCB в стек ядра
    
    LXI  D, O_SYSCALL_RES       ; Смещение до кода результата (+5)
    DAD  D
    MOV  M, C                   ; Сохранили exit status в PCB для waitpid родителя
    POP  H                      ; Восстановили адрес текущего PCB в HL

    ; --- ШАГ 2: Перевод текущего процесса в статус ST_ZOMBIE (05H) ---
    PUSH H
    LXI  D, O_STATUS            ; Смещение до статуса (+2)
    DAD  D
    MVI  M, ST_ZOMBIE           ; Процесс официально стал зомби
    POP  H                      ; Восстановили адрес текущего PCB в HL

    ; --- ШАГ 3: Извлечение PID родителя (O_PPID) из текущего PCB ---
    LXI  D, O_PPID              ; Смещение до родительского PID (+1)
    DAD  D
    MOV  A, M                   ; A = PID родительского процесса
    STA  EXIT_PARENT_PID        ; УТВЕРЖДЕНО: Сохраняем в вашу переменную ядра

    ; --- ШАГ 4: ПОИСК И АСИНХРОННОЕ ПРОБУЖДЕНИЕ РОДИТЕЛЯ ---
    ; Вычисляем адрес PCB родителя: PCB_TABLE + (PARENT_PID * 64)
    MOV  L, A
    MVI  H, 0
    DAD  H
    DAD  H
    DAD  H
    DAD  H
    DAD  H
    DAD  H                      ; HL = PARENT_PID * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = Базовый адрес PCB родителя

    ; Проверяем, спит ли родитель в ожидании потомка (ST_WAITING + W_CHILD)
    PUSH H                      ; Спасли адрес PCB родителя
    LXI  D, O_STATUS            ; Смещение статуса (+2)
    DAD  D
    MOV  A, M
    CPI  ST_WAITING             ; Родоначальник вообще спит (04H)?
    JNZ  exit_skip_wakeup       ; Если не спит — будить не нужно, уходим на диспетчеризацию

    POP  H                      ; Восстановили адрес PCB родителя
    PUSH H
    LXI  D, O_WAIT_CODE         ; Смещение причины сна (+40)
    DAD  D
    MOV  A, M
    CPI  W_CHILD                ; Родоначальник ждет именно завершения потомка (03H)?
    JNZ  exit_skip_wakeup       ; Если ждет что-то другое (например, диск CF) — пропускаем

    ; --- РОДИТЕЛЬ НАЙДЕН И ГОТОВ К ПРОБУЖДЕНИЮ ---
    ; 1. Очищаем код причины сна в его PCB
    MVI  M, 00h                 ; O_WAIT_CODE = 00h (Проснулся)
    
    ; 2. Переводим его статус в состояние готовности к исполнению ST_READY (02H)
    POP  H                      ; Восстановили адрес PCB родителя
    PUSH H
    LXI  D, O_STATUS            ; Смещение статуса (+2)
    DAD  D
    MVI  M, ST_READY            ; Теперь родитель снова валидный кандидат для шедуллера!

exit_skip_wakeup:
    POP  H                      ; Сбалансировали стек ядра, очистив локальные адреса PCB

    ; --- ШАГ 5: ТЕРМИНАЛЬНЫЙ СБРОС И ВЫЗОВ ПЛАНИРОВЩИКА ---
    ; Текущий процесс мертв (ST_ZOMBIE), сброс SP на KERNEL_STACK безопасен.
    LXI  SP, KERNEL_STACK_TOP       ; Полная очистка стека супервизора перед переключением
    
    CALL scheduler              ; Вызываем ваш приоритетный шедуллер, получаем лучший READY PID в A
    STA  NEXT_PID
    CALL switch_mmu_context     ; Аппаратно переключаем окна К555ИР26 платы eMMU
    
    JMP  restore_timer_context  ; Уходим в контекст новой ожившей задачи. Возврата через RET нет!

; =============================================================================
; POSIX СИС-ВЫЗОВ №2: SYS_WAITPID — СИНХРОНИЗАЦИЯ И СБОР СТАТУСОВ ПОТОМКОВ
; =============================================================================
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ПОЛНЫЙ ИСПРАВЛЕННЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_waitpid
; =============================================================================
; Вход:  E = Фильтр PID (конкретный PID, либо -1 — ожидание любого потомка).
; Выход: A = PID успешно завершившегося потомка, H = Код его завершения.
;        A = 0E9H (ECHILD — у процесса вообще нет потомков).
; Прерывания заблокированы диспетчером, баланс стека Ring 0 абсолютный.
; =============================================================================
sys_waitpid:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH B
    PUSH D
    PUSH H                          ; Спасаем контекст РОН пользователя в стек ядра

    LXI  H, 0
    DAD  SP                         
    SHLD WAIT_STACK_SAVE            ; ЗАФИКСИРОВАЛИ ТОЧКУ БЕЗОПАСНОГО ОТКАТА СТЕКА ЯДРА!

    MOV  A, E
	STA WAIT_TARGET_PID ; Сохранили фильтр PID из регистра E

waitpid_scan_entry:
    XRA  A
	STA  WAIT_HAS_CHILDREN  ; Сбросили флаг наличия потомков в 0

    ; --- ШАГ 2: СКАНИРОВАНИЕ ТАБЛИЦЫ PCB_TABLE ---
    LXI  H, PCB_TABLE               ; Стартовый адрес глобальной таблицы задач
    MVI  B, MAX_PROCS               ; Лимит итераций (16 слотов)

waitpid_scan_loop:
    MOV  A, M                       ; Читаем поле O_PID (+0). Если слот пуст — там 0.
    ORA  A
    JZ   waitpid_next_slot          ; Слот полностью свободен, перешагиваем

    ; Проверяем, является ли данный процесс потомком текущего вызывающего процесса.
    ; Для этого сравниваем поле O_PPID (+1) проверяемой задачи с CURRENT_PID родителя.
    PUSH H                          ; Спасли адрес старта текущего PCB
    INX  H                          ; HL -> O_PPID (+1)
    MOV  A, M                       ; A = PPID проверяемой задачи
    LDA  CURRENT_PID
    CMP  M                          ; Проверяемая задача — потомок вызывающего?
    JNZ  waitpid_pop_next_slot      ; Если нет — это чужой процесс, пропускаем

    ; Дочерний процесс обнаружен — взводим флаг присутствия потомков
    MVI  A, 1
	STA WAIT_HAS_CHILDREN

    ; Проверяем фильтр PID, переданный родителем в WAIT_TARGET_PID
    POP  H
	PUSH H                 ; Восстановили адрес старта проверяемого PCB
    MOV  A, M                       ; A = PID проверяемого потомка
    MOV  C, A                       ; Припрятали PID в C
    LDA  WAIT_TARGET_PID
    CPI  0FFh                       ; Фильтр равен -1 (Ожидаем абсолютно любого)?
    JZ   waitpid_check_child_status ; Если -1 — фильтр пройден, идем проверять статус!
    
    CMP  C                          ; Сравниваем конкретный запрошенный PID с текущим
    JNZ  waitpid_pop_next_slot      ; Не совпал — ищем дальше

waitpid_check_child_status:
    ; Потомок удовлетворяет критериям поиска. Проверяем его статус O_STATUS (+2)
    INX  H
	INX  H                 ; HL -> O_STATUS (+2)
    MOV  A, M                       ; A = Текущий статус потомка
    CPI  005h                       ; Статус == ST_ZOMBIE (05h)?
    JZ   waitpid_reap_zombie        ; ПОТОМОК ЗАВЕРШЕН! Уходим на извлечение ресурсов (Reap)

waitpid_pop_next_slot:
    POP  H                          ; Восстановили адрес старта PCB

waitpid_next_slot:
    LXI  D, PCB_SIZE                ; Шаг равен размеру PCB (64 байта)
    DAD  D
    DCR  B
    JNZ  waitpid_scan_loop          ; Крутим, пока не проверим все 16 слотов

    ; --- ШАГ 3: АНАЛИЗ РЕЗУЛЬТАТОВ СКАНИРОВАНИЯ ---
    LDA  WAIT_HAS_CHILDREN
    ORA  A                          ; Были обнаружены потомки?
    JZ   waitpid_err_echild         ; Если вообще нет потомков — возвращаем ошибку ECHILD (0xE9)

    ; --- ШАГ 4: ЖИВЫЕ ПОТОМКИ ЕСТЬ — АСИНХРОННЫЙ СОН РОДИТЕЛЯ (W_CHILD) ---
    ; ИСПРАВЛЕНО: Ликвидирован busy-waiting, родитель асинхронно уходит спать!
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; Родитель переведен в статус ST_WAITING (04h)
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D 
    MVI  M, W_CHILD                 ; ИСПРАВЛЕНО: Причина сна жестко привязана к каноничному каналу 03h!

    ; Консервируем SP супервизора родителя в его PCB в поле O_SAVED_SP (+3)
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
    LXI  H, 0
	DAD SP
    MOV  A, L
	STAX D
	INX D
	MOV A, H
	STAX D

    ; Вызываем планировщик для переключения ЦП КР580ВМ80А на другие живые задачи
    CALL scheduler
    STA  NEXT_PID
    CALL switch_mmu_context         ; eMMU-коммутатор перепрошивает К555ИР26 под новый процесс
    JMP  restore_timer_context      ; Навсегда уходим из контекста спящего родителя

; =============================================================================
; ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ (ВЫЗЫВАЕТСЯ ИЗ sys_exit УМЕРШЕГО ПОТОМКА)
; =============================================================================
waitpid_wakeup_entry:
    JMP  waitpid_scan_entry         ; Проснулись — уходим на новый круг сканирования

; -----------------------------------------------------------------------------
; ШАГ 5: ИЗВЛЕЧЕНИЕ КОДА ЗАВЕРШЕНИЯ И УТИЛИЗАЦИЯ ЗОМБИ (REAP CONTEXT)
; -----------------------------------------------------------------------------
waitpid_reap_zombie:
    POP  H                          ; HL = Базовый адрес PCB зомби
    PUSH H
    MOV  A, M                       ; A = PID умершего потомка
    STA  FORK_CHILD_PID             ; Временно припрятали PID для возврата
    
    ; Извлекаем 8-битный код завершения из поля O_SYSCALL_RES (+5)
    LXI  D, O_SYSCALL_RES
    DAD  D
    MOV  A, M                       ; A = exit_code, который потомок передал в exit()
    STA  WRITE_CHAR_CHAR            ; Временно припрятали exit_code

    ; Полностью аннулируем и освобождаем слот PCB умершего потомка
    POP  H
    MVI  M, ST_FREE                 ; O_PID = 00h (Слот PCB официально свободен!)

    ; Реставрируем оригинальный SP и контекст РОН родителя перед выходом
    LHLD WAIT_STACK_SAVE
    SPHL                            
    POP  H
	POP  D
	POP  B         ; Восстановили оригинальные HL, DE, BC родителя

    ; Снабжаем регистры родителя результатами вызова по стандарту ABI:
    LDA  WRITE_CHAR_CHAR
    MOV  H, A                       ; Регистр H = Код завершения потомка (exit_code)
    LDA  FORK_CHILD_PID             ; Аккумулятор А = PID утилизированного потомка
    RET                             ; Чистый выход в syscall_return.

; =============================================================================
; ОБРАБОТЧИК АВАРИЙНОГО ВЫХОДА (НЕТ ПОТОМКОВ — ОШИБКА ECHILD)
; =============================================================================
waitpid_err_echild:
    LHLD WAIT_STACK_SAVE
    SPHL                            ; Сбалансировали стек супервизора
    POP  H
	POP  D
	POP  B         ; Реставрировали РОН
    MVI  A, ECHILD                  ; Код ошибки 0xE9 (POSIX ECHILD)
    RET

; =============================================================================
; POSIX СИС-ВЫЗОВ №12: SYS_EXECVE — ЗАМЕНА ОБРАЗА ПРОЦЕССА НОВЫМ БИНАРНИКОМ С ИНТЕГРАЦИЕЙ DIRTY BIT
; =============================================================================
; =============================================================================
; Вход:  Вызывается через ловушку RST 6 из Ring 3.
;        DE = Указатель на строку с путем к файлу (char *path) в юзерспейсе.
;        HL = Указатель на массив аргументов (char **argv) в юзерспейсе.
; Выход: При успехе: Старая программа полностью замещается новой, возврат в 
;        Ring 3 происходит на Entry Point (0x0000) с очищенными регистрами.
;        При ошибке: Возврат в родительский контекст с кодом ошибки в аккумуляторе.
; =============================================================================
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ИТОГОВЫЙ МОНОЛИТНЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_execve
; =============================================================================
; Вход:  DE = Логический адрес строки пути к файлу (char *path) в Ring 3.
;        HL = Адрес старого массива указателей argv[] в Ring 3.
;        C  = Флаги открытия файла (O_RDONLY / O_WRONLY)
; Выход: Сисколл не возвращает управление в старую программу при успехе.
;        A  = 0FFH — Ошибка: Файл не найден (ENOENT) / Нарушение формата.
; Регистры BC, DE, HL полностью сохраняются при ошибках возврата.
; =============================================================================
sys_execve:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH B                          ; Спасаем оригинальные флаги (C) и B задачи
    PUSH D                          ; Спасаем оригинальный адрес пути (DE)
    PUSH H                          ; Спасаем HL (адрес старого argv[]) задачи

    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра супервизора
    SHLD EXEC_STACK_SAVE            ; ЗАФИКСИРОВАЛИ ТОЧКУ БЕЗОПАСНОГО ОТКАТА СТЕКА ЯДРА!

    ; Распределяем входные параметры по переменным ядра
    LHLD REG_HL_SAVED               ; Восстановили оригинальный HL (адрес argv[])
    SHLD EXEC_ARGV_USER_PTR         ; Сохранили адрес старого массива argv[]
    XCHG
	SHLD OPEN_USER_PATH_PTR  ; Перенесли DE в HL и сохранили указатель на путь
    XCHG                            ; Вернули DE на место

    ; --- ШАГ 2: Вычитка имени файла из Ring 3 в буфер ядра (FLAT 64KB) ---
    LHLD OPEN_USER_PATH_PTR         ; Без ограничений CPI 040h
    LXI  D, EXEC_FILE_PATH          ; Буфер назначения в ОЗУ Ring 0
    MVI  B, EXEC_MAX_PATH_LEN       ; 24 байта лимит

exec_copy_path_loop:
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0 (Прорыв Firewall)
    MOV  A, M                       ; Прочитали символ пути
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1 (Восстановление Ring 0)
    STAX D
    ORA  A                          ; NULL-терминатор конца строки?
    JZ   exec_path_ok
    INX  H
	INX  D
	DCR B
    JNZ  exec_copy_path_loop
    JMP  exec_err_noent_abi         ; Слишком длинный путь трактуем как ENOENT

exec_path_ok:
    ; --- ШАГ 3: ФАЗА 1 - ВЫЧИТКА СТРОК АРГУМЕНТОВ ИЗ СТАРОГО КОНТЕКСТА ---
    XRA  A
	STA EXEC_ARGC          ; argc = 0
    LXI  H, 0FFFFh
    SHLD EXEC_STR_POOL_TOP          ; Точка старта пула строк на новом стеке задачи
    LXI  D, VFS_SCRATCH_SECTOR      ; Адрес назначения в scratchpad-буфере ядра Ring 0

exec_read_args_loop:
    LHLD EXEC_ARGV_USER_PTR         ; HL -> Точка в массиве указателей старой задачи
    
    OUT  076H                       ; Включили оверрайд шины
    MOV  C, M
	INX H
	MOV  B, M   ; BC = Указатель на строку параметра в Ring 3
    OUT  076H                       ; Отключили
    
    INX  H
	SHLD EXEC_ARGV_USER_PTR ; Сдвинули указатель массива старой задачи вперед

    ; Проверяем, не дошли ли до конца массива параметров (NULL-указатель 0000H)
    MOV  A, B
	ORA C
    JZ   exec_old_args_done         ; Если указатель нулевой — все параметры извлечены!

    ; Увеличиваем системный счетчик argc
    LDA  EXEC_ARGC
	INR A
	STA EXEC_ARGC

    ; HL переводим на вычитку символов строки параметра по адресу из BC
    MOV  H, B
	MOV L, C

exec_copy_string_to_kernel:
    OUT  076H                       
    MOV  A, M                       ; Вычитали символ строки из Ring 3
    OUT  076H                       
    STAX D                          ; Сохранили символ в scratchpad ядра
    INX  D
	INX  H
    ORA  A                          ; Встретили NULL-терминатор?
    JNZ  exec_copy_string_to_kernel 
    JMP  exec_read_args_loop        ; К следующему параметру

exec_old_args_done:
    ; --- ШАГ 4: ОТКРЫТИЕ И ИЗВЛЕЧЕНИЕ БИНАРНОГО ФАЙЛА ЧЕРЕЗ VFS ---
    LXI  D, EXEC_FILE_PATH         ; DE = Проверенный путь к файлу в ОЗУ ядра
    MVI  C, 000h                    ; Флаг: O_RDONLY (00h)
    CALL sys_open                   ; Внутренний вызов шлюза VFS
    CPI  0FFh
    JZ   exec_err_noent_abi         ; Файл программы не найден!
    STA  VFS_READ_G_IDX             ; Сохранили выданный дескриптор в ячейку

    ; --- ШАГ 5: ПОЛНАЯ УТИЛИЗАЦИЯ И СБРОС eMMU СТРАНИЦ СТAРOГO ПРOЦЕССA ---
    CALL get_current_pcb_address
    SHLD K_EXEC_CURRENT_PCB
    PUSH H
    LXI  D, O_PAGES_MAP             ; Смещение таблицы физических страниц (+8)
    DAD  D
    MVI  C, 4                       ; Освобождаем 4 старых фрейма процесса (0..3)

exec_free_old_pages_loop:
    MOV  A, M
    CPI  RES_FREE_MARKER            ; Страница свободна (0xFF)?
    JZ   exec_old_page_next
    PUSH B
	PUSH H
    CALL k_free_physical_page       ; Освободили 16 КБ фрейм в глобальной карте RAM_BITMAP
    POP  H
	POP  B
    MVI  M, RES_FREE_MARKER         ; Обнулили слот в PCB
exec_old_page_next:
    INX  H
	DCR  C
	JNZ  exec_free_old_pages_loop

    ; Запрашиваем 4 новые чистые страницы ОЗУ eMMU под перерождающийся процесс
    POP  H
	PUSH H                 ; Восстановили базу PCB
    LXI  D, O_PAGES_MAP
    DAD  D
    MVI  C, 4                       ; Запрашиваем 4 сегмента памяти

exec_alloc_new_pages_loop:
    PUSH B
	PUSH H
    CALL k_allocate_free_physical_page ; Менеджер физической памяти ядра
    STA  EXEC_NEW_PAGE_NUM
    POP  H
	POP  B
    
    LDA  EXEC_NEW_PAGE_NUM
    CPI  0FFh                       ; Закончилась физическая память устройства?
    JZ   exec_err_noexec            ; Сбой аллокации
    
    MOV  M, A                       ; Прописали новый чистый фрейм в PCB!
    INX  H
    DCR  C
    JNZ  exec_alloc_new_pages_loop

    ; --- ШАГ 6: КОММУТАЦИЯ НОВОГО eMMU КОНТЕКСТА И ЗАГРУЗКА КОДА ПРОГРАММЫ ---
    CALL switch_mmu_context         ; ЭВМ MEGA-580 намертво включает новые фреймы в К555ИР26!
    
    ; Читаем бинарный код из открытого файла VFS напрямую в Сегмент 1 задачи (0x4000)
    LDA  VFS_READ_G_IDX
    MOV  D, A                       ; D = Наш файловый дескриптор для чтения
    LXI  H, EXEC_ENTRY_POINT       ; HL = Адрес назначения в Ring 3 (0x4000)
    LXI  B, 8192                   ; Считываем первые 8 Килобайт исполняемого кода
    CALL sys_read                   ; Монолитный sys_read перенесет данные напрямую в Ring 3

    ; Закрываем файл программы, дескриптор больше не нужен
    LDA  VFS_READ_G_IDX
    CALL k_vfs_close_global_index

    ; --- ШАГ 7: ФАЗА 2 - ВЫТАЛКИВАНИЕ АРГУМЕНТОВ НА НОВЫЙ СТЕК ЗАДАЧИ ---
    ; Вычисляем точку старта массива указателей argv[] на новом стеке задачи
    LDA  EXEC_ARGC
    INR  A                          ; argc + 1 (заглушка NULL)
    ADD  A                          ; (argc + 1) * 2 байта
    MOV  C, A
	MVI B, 0            ; BC = Общий размер массива векторов в байтах

    LHLD EXEC_STR_POOL_TOP
    MOV  A, L
	SUB C
	MOV L, A
    MOV  A, H
	SBB B
	MOV H, A    ; HL = Новый старт массива argv[] на стеке Ring 3
    SHLD EXEC_ARGV_ARRAY_TOP        ; Зафиксировали указатель

    ; Переносим строки из scratchpad-буфера ядра на вершину нового стека задачи
    LXI  D, VFS_SCRATCH_SECTOR      ; Источник в Ring 0 ОЗУ ядра
    LDA  EXEC_ARGC
    MOV  B, A                       ; Счетчик внешнего цикла по строкам
    JZ   exec_push_vectors_done     ; Если параметров нет — уходим на заглушку

exec_push_strings_loop:
    LHLD EXEC_STR_POOL_TOP
    XCHG                            ; DE = Указатель записи в новый стек Ring 3

exec_push_char_loop:
    LDAX D                          ; Читаем байт из scratchpad ядра
    MOV  C, A                       ; Сохранили в C

    ; Сдвигаем указатель нового стека вниз на 1 байт (рост стека вниз)
    LHLD EXEC_STR_POOL_TOP
	DCX H
	SHLD EXEC_STR_POOL_TOP
    
    ; Записываем символ на новый стек Ring 3 через оверрайд шины
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0
    MOV  M, C                       ; БАЙТ СТРОКИ ВЫТАЛКНУТ НА НОВЫЙ СТЕК ЗАДАЧИ!
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1
    
    XCHG
	INX H
	XCHG             ; Сдвинули указатель источника в scratchpad
    
    MOV  A, C
    ORA  A                          ; Был скопирован NULL-терминатор строки?
    JNZ  exec_push_char_loop        ; Толкаем символы до конца строки

    ; Строка уложена. Сохраняем её базовый 16-битный адрес в массив векторов argv[]
    LHLD EXEC_STR_POOL_TOP          ; HL указывает на начало уложенной строки
    SHLD EXEC_TEMP_WORD

    LHLD EXEC_ARGV_ARRAY_TOP
    LDA  EXEC_TEMP_WORD
    OUT  076H
	MOV M, A
	INX H
	OUT 076H ; Записали младший байт адреса строки
    LDA  EXEC_TEMP_WORD+1
    OUT  076H
	MOV M, A
	INX H
	OUT 076H ; Записали старший байт адреса строки
    SHLD EXEC_ARGV_ARRAY_TOP        ; Сдвинули указатель массива векторов вперед

    DCR  B                          ; Уменьшаем счетчик строк (argc)
    JNZ  exec_push_strings_loop

exec_push_vectors_done:
    ; Записываем обязательный финальный NULL-указатель (0000H) в конец массива argv[]
    LHLD EXEC_ARGV_ARRAY_TOP
    XRA  A
    OUT  076H
	MOV M, A
	INX H
	OUT 076H
    OUT  076H
	MOV M, A
	OUT 076H

    ; --- ШАГ 8: НАСТРОЙКА РЕГИСТРОВОГО ABI СТАРТА ПРОГРАММЫ ---
    POP  H                          ; Извлекли адрес PCB текущего процесса из стека
    
    ; Находим сохраненный SP ядра из PCB текущего процесса
    LXI  D, O_SAVED_SP
    DAD  D
    MOV  A, M
	INX H
	MOV H, M
	MOV L, A ; HL = Физический SP кадра прерывания сисколла
    
    ; Модифицируем регистр C пользователя (смещение +2 в кадре PUSH PSW, B, D, H)
    INX  H
	INX  H                 ; HL указывает на байт регистра C в паре BC кадра
    LDA  EXEC_ARGC
    MOV  M, A                       ; ЖЕСТКО ПРОПИСАЛИ КЛИЕНТСКИЙ ARGC В РЕГИСТР C!

    ; Модифицируем пару HL пользователя (смещение +6 в кадре)
    INX  H
	INX  H
	INX  H
	INX  H ; HL указывает на сохраненную пару HL пользователя
	LHLD EXEC_STR_POOL_TOP          ; Взяли вычисленный базовый адрес старта массива argv[]
	MOV  A, L
	MOV M, A
	INX H
	MOV  A, H
	MOV M, A            ; ЖЕСТКО ПРОПИСАЛИ УКАЗАТЕЛЬ ARGV[] В ПАРУ HL ЗАДАЧИ!

	; Настраиваем сохраненный счетчик команд PC в кадре прерывания на точку входа 0x4000
	INX  H                          ; HL указывает на младший байт сохраненного PC (+8)
	MVI  M, 000h                    ; Младший байт точки старта (0x00)
	INX  H
	MVI  M, 040h                    ; Старший байт точки старта (0x40 -> 0x4000)

	; Аппаратно переставляем указатель стека Ring 3 новой задачи на самый низ фрейма
	LHLD K_EXEC_CURRENT_PCB
	LXI  D, O_SAVED_SP
	DAD  D
	LHLD EXEC_STR_POOL_TOP          ; HL = Наша новая вершина стека Ring 3
	MOV  A, L
	MOV M, A
	INX H
	MOV  A, H
	MOV M, A            ; Новый SP Ring 3 зафиксирован в PCB!

	; Очищаем мусорные PUSH-регистры ядра, которые мы делали на входе в sys_execve
	LHLD EXEC_STACK_SAVE
	SPHL                            ; Сбросили локальные данные стека ядра

	; Восстановили оригинальные PUSH-кадры, которые ожидает диспетчер выхода
	POP  H
	POP  D
	POP  B         ; Восстановили РОН наружу
	MVI  A, 00h
	RET

	; =============================================================================
	; ОБРАБОТЧИКИ ОШИБОК ЗАГРУЗЧИКА С ТРАНЗАКТНЫМ ОТКАТОМ И БАЛАНСИРОВКОЙ
	; =============================================================================
exec_err_noent_abi:
	LHLD EXEC_STACK_SAVE
	SPHL                            ; Сбросили вложенный мусор стека, восстановили исходный SP
	POP  H
	POP  D
	POP  B         ; Восстановили оригинальный контекст РОН пользователя
	MVI  A, 0FFh                    ; Код ошибки: Файл не найден (ENOENT)
	RET

exec_err_noexec:
	LHLD EXEC_STACK_SAVE
	SPHL
	POP  H
	POP  D
	POP  B
	MVI  A, 0FEh                    ; Код ошибки: Нарушение формата / Нет памяти (ENOEXEC)
	RET

; =============================================================================
; АСИНХРОННЫЙ ДРАЙВЕР: ЗАПИСИ СЕКТOРА ФС ЧЕРЕЗ ПДП С УЧЕТОМ MBR СМЕЩЕНИЯ
; =============================================================================
ide_write_sector_from_buf:
    ; Вход: A  = Старший байт относительного номера сектора раздела VFS
    ;       HL = Младшее слово относительного номера сектора раздела VFS
    ; Выход: Настроена ПДП-запись, текущий процесс уходит в сон, возврат через RET.

    ; --- ШАГ 1: Захват монопольного доступа ---
    PUSH H                      
    PUSH PSW                    
    CALL cf_acquire_lock        
    POP  PSW                    
    POP  H                      

    ; --- ШАГ 2: Математический расчет абсолютного LBA ---
    PUSH D                      
    PUSH B                      
    MOV  C, A                   

    LDA  EXPES_LBA              
    ADD  L                      
    MOV  L, A                   
    
    LDA  EXPES_LBA+1            
    ADC  H                      
    MOV  H, A                   
    SHLD IDE_LBA_ABS_L          

    LDA  EXPES_LBA+2            
    ADC  C                      
    OUT  IDE_LBA_HIGH           

    MVI  A, 0
    ADC  A                      
    MOV  D, A                   
    LDA  EXPES_LBA+3            
    ADD  D                      
    ANI  0Fh                    
    ORI  0E0H                   
    OUT  IDE_DRV_HEAD           

    POP  B                      
    POP  D                      

    ; --- ШАГ 3: Конфигурация ПДП на выдачу из ОЗУ ---
    PUSH B                      
    LXI  H, IDE_SYS_BUFFER      
    LXI  B, 512                 
    MVI  D, 01H                 ; Направление: 01H = Чтение из ОЗУ в CF (Запись)
    CALL dma_start_ch0_transfer 
    POP  B                      

    ; --- ШАГ 4: Программирование портов ---
    MVI  A, 1
    OUT  IDE_SECTOR_CNT         
    LHLD IDE_LBA_ABS_L          
    MOV  A, L
    OUT  IDE_LBA_LOW            
    MOV  A, H
    OUT  IDE_LBA_MID            

    ; --- ШАГ 5: Аппаратный пуск записи ПДП ---
    MVI  A, IDE_CMD_WRITE_DMA   ; Команда 0xCA (Write Sectors DMA)
    OUT  IDE_CMD_STATUS         

    ; --- ШАГ 6: Уход процесса в сон и корректный возврат (ИСПРАВЛЕНО) ---
    CALL vfs_sleep_on_io        ; Процесс добровольно отдает квант времени и спит
                                ; Когда прерывание его разбудит, выполнение продолжится отсюда:
    RET                         ; Честный возврат в модуль VFS, запросивший запись!

; =============================================================================
; АСИНХРОННЫЙ ДРАЙВЕР: ЧТЕНИЕ СЕКТOРА ФС ЧЕРЕЗ ПДП С УЧЕТОМ MBR СМЕЩЕНИЯ
; =============================================================================
ide_read_sector_to_buf:
    ; Вход: A  = Старший байт относительного номера сектора раздела VFS (биты 16-23)
    ;       HL = Младшее слово относительного номера сектора раздела VFS (биты 0-15)
    ; Выход: Настроена ПДП-пересылка, текущий процесс засыпает через CALL.
    ;        После пробуждения по прерыванию TC функция корректно завершается через RET.

    ; --- ШАГ 1: Захват монопольного доступа к CompactFlash ---
    PUSH H                      
    PUSH PSW                    
    CALL cf_acquire_lock        
    POP  PSW                    
    POP  H                      

    ; --- ШАГ 2: Математический расчет абсолютного LBA-адреса сектора ---
    PUSH D                      
    PUSH B                      
    MOV  C, A                   

    LDA  EXPES_LBA              
    ADD  L                      
    MOV  L, A                   
    
    LDA  EXPES_LBA+1            
    ADC  H                      
    MOV  H, A                   
    SHLD IDE_LBA_ABS_L          

    LDA  EXPES_LBA+2            
    ADC  C                      
    OUT  IDE_LBA_HIGH           

    MVI  A, 0
    ADC  A                      
    MOV  D, A                   
    LDA  EXPES_LBA+3            
    ADD  D                      
    ANI  0Fh                    
    ORI  0E0H                   
    OUT  IDE_DRV_HEAD           

    POP  B                      
    POP  D                      

    ; --- ШАГ 3: Аппаратная конфигурация внешнего ПДП КР580ВТ57 ---
    PUSH B                      
    LXI  H, IDE_SYS_BUFFER      
    LXI  B, 512                 
    MVI  D, 00H                 ; Направление: 00H = Запись в ОЗУ из CF (Чтение)
    CALL dma_start_ch0_transfer 
    POP  B                      

    ; --- ШАГ 4: Программирование портов IDE / CompactFlash ---
    MVI  A, 1
    OUT  IDE_SECTOR_CNT         
    LHLD IDE_LBA_ABS_L          
    MOV  A, L
    OUT  IDE_LBA_LOW            
    MOV  A, H
    OUT  IDE_LBA_MID            

    ; --- ШАГ 5: Аппаратный асинхронный пуск ---
    MVI  A, IDE_CMD_READ_DMA    ; Команда 0xC8 (Read Sectors DMA)
    OUT  IDE_CMD_STATUS         ; Выстреливаем команду. CF выставляет сигнал DRQ0.

    ; --- ШАГ 6: Уход процесса в сон и корректный возврат (ИСПРАВЛЕНО) ---
    CALL vfs_sleep_on_io        ; Вызываем планировщик. Текущий поток засыпает здесь.
                                ; !!! ПРИ ПРОБУЖДЕНИИ ПРОЦЕССОРПРОДОЛЖИТ СЛЕДУЮЩУЮ СТРОКУ !!!
    RET                         ; Честный возврат в высокоуровневый модуль VFS!

; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): ИТОГОВЫЙ МОНОЛИТНЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_open
; =============================================================================
; Вход:  DE = Логический адрес строки пути к файлу (char *path) в Ring 3.
;        C  = Флаги открытия файла (O_RDONLY / O_WRONLY / O_CREAT)
; Выход: A  = Локальный файловый дескриптор (FD, 0..7) в случае успеха.
;        A  = 0FFH — Ошибка: Файл не найден (ENOENT).
;        A  = 0FDH — Ошибка: Превышен лимит открытых файлов в PCB (EMFILE).
; Регистры BC, DE, HL полностью сохраняются при возврате ошибок.
; =============================================================================
sys_open:
    ; --- ШАГ 1: Консервация контекста пользователя для ABI совместимости ---
    PUSH B                          ; Спасаем оригинальные флаги (C) и B
    PUSH D                          ; Спасаем оригинальный адрес пути (DE)
    PUSH H                          ; Спасаем HL прерванной задачи

    ; --- ШАГ 2: Аппаратная консервация указателя стека ядра ---
    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра супервизора
    SHLD VFS_OPEN_STACK_SAVE        ; АТОМАРНО ЗАФИКСИРОВАЛИ ИНВАРИАНТ СТЕКА ЯДРА!

    ; Распределяем входные параметры по переменным ядра
    MOV  A, C
	STA OPEN_FLAGS_SAVED ; Сохранили флаги из регистра C
    XCHG
	SHLD OPEN_USER_PATH_PTR  ; Перенесли DE в HL и сохранили указатель на путь
    XCHG                            ; Вернули DE на место

    ; --- ШАГ 3: Поиск свободного локального дескриптора в PCB ---
    CALL get_current_pcb_address
    LXI  D, O_FILE_TABLE
    DAD  D                          ; HL -> Старт O_FILE_TABLE процесса
    MVI  B, 8                       ; 8 слотов дескрипторов (0..7)
    MVI  C, 00h                     ; Итератор локального FD

open_find_fd_loop_abi:
    MOV  A, M
    CPI  RES_FREE_MARKER            ; Слот свободен (0xFF)?
    JZ   open_local_fd_ok_abi       ; Нашли свободный слот!
    INX  H
	INR C
	DCR B
    JNZ  open_find_fd_loop_abi

    ; Ошибка: Превышен лимит открытых файлов в PCB (EMFILE)
    JMP  open_err_emfile_abi        ; Уходим на выход через восстановление SP

open_local_fd_ok_abi:
    MOV  A, C
	STA OPEN_LOCAL_FD   ; Зафиксировали локальный FD (0..7)

    ; --- ШАГ 4: ВЫЧИТКА СТРОКИ ПУТИ ИЗ RING 3 В БУФЕР ЯДРА (FLAT 64KB) ---
    LHLD OPEN_USER_PATH_PTR         ; HL = Источник пути в Ring 3 (без ограничений CPI 040h)
    LXI  D, NET_ISR_HEADER_BUF      ; Временный scratchpad буфер ядра
    MVI  B, 24                      ; Лимит имени = 24 символа

open_copy_path_loop_abi:
    OUT  076H                       ; Включили KERNEL_OVERRIDE_EN = 0 (Прорыв Firewall)
    MOV  A, M                       ; Читаем символ из памяти процесса
    OUT  076H                       ; Отключили оверрайд шины (Возврат Ring 0)
    
    STAX D                          ; Записали символ в буфер ядра
    ORA  A                          ; NULL-терминатор конца строки?
    JZ   open_path_validated_abi        
    
    INX  H
	INX  D
    DCR  B
    JNZ  open_copy_path_loop_abi

    JMP  open_err_noent_abi         ; Слишком длинный путь трактуем как отсутствие файла

open_path_validated_abi:
    ; --- ШАГ 5: СКАНИРОВАНИЕ VFS_MOUNT_TABLE И РАЗБОР ПРЕФИКСОВ ---
    XRA  A
	STA  OPEN_MNT_SLOT     
    LXI  H, VFS_MOUNT_TABLE         

open_vfs_mount_loop_abi:
    MOV  A, M                       ; Читаем поле M_MNT_STATUS (+0)
    CPI  01h                        ; Точка монтирования активна?
    JNZ  open_vfs_next_slot_abi
    
    PUSH H                          ; Спасли адрес таблицы монтирования в стек
    INX  H
	INX  H                 ; HL -> M_MNT_PREFIX_PTR
    MOV  E, M
	INX H
	MOV D, M    ; DE = Адрес строки префикса (например, "/dev/sock")
    
    LXI  H, NET_ISR_HEADER_BUF      ; HL = Путь пользователя в ОЗУ ядра
open_cmp_prefix_loop_abi:
    LDAX D                          
    ORA  A                          ; Дошли до конца строки префикса?
    JZ   open_vfs_prefix_matched_abi ; ПОЛНОЕ СОВПАДЕНИЕ ПРЕФИКСА!
    
    CMP  M                          
    JNZ  open_prefix_miss_abi       ; Не совпало — промах
    INX  D
	INX  H
    JMP  open_cmp_prefix_loop_abi

open_prefix_miss_abi:
    POP  H                          ; Сбалансировали стек при несовпадении

open_vfs_next_slot_abi:
    LXI  D, VFS_MOUNT_SIZE          
    DAD  D
    LDA  OPEN_MNT_SLOT
	INR A
	STA OPEN_MNT_SLOT
    CPI  MAX_VFS_MOUNTS             
    JNZ  open_vfs_mount_loop_abi

    JMP  open_err_noent_abi         ; Точка монтирования не найдена (0xFF / ENOENT)

; -----------------------------------------------------------------------------
; УСПЕШНОЕ СМЫКАНИЕ С VFS: ПРЕФИКС НАЙДЕН! (Стек содержит [Адрес_Слота_VFS])
; -----------------------------------------------------------------------------
open_vfs_prefix_matched_abi:
    POP  H                          ; HL = Базовый адрес структуры совпавшего VFS-слота
    
    LXI  D, M_MNT_GATE_PTR
    DAD  D
    MOV  E, M
	INX H
	MOV D, M    ; DE = Физический адрес шлюза драйвера ФС
    
    LHLD REG_HL_SAVED               ; Передаем оригинальный путь драйверу
    LDA  OPEN_FLAGS_SAVED
    
    PUSH D                          
    XTHL                            ; Трюк 8080: HL = Адрес шлюза, Стак = Путь
    CALL open_gate_call_via_pchl_abi ; Вызов шлюза через PCHL
    
    ; Выход из драйвера: A = Глобальный системный индекс объекта (0..63)
    CPI  32
    JNC  open_finalize_pcb_bind_abi     

    ; Если драйвер вернул ошибку, пробрасываем её
    JMP  open_err_noent_abi

open_gate_call_via_pchl_abi:
    PCHL                            ; Аппаратный переход на шлюз драйвера ФС

; -----------------------------------------------------------------------------
; ФИНАЛЬНАЯ ПРИВЯЗКА ДЕСКРИПТОРА К ПРОЦЕССУ В PCB И ШТАТНЫЙ ВЫХОД
; -----------------------------------------------------------------------------
open_finalize_pcb_bind_abi:
    MOV  B, A                       ; B = Глобальный системный индекс VFS
    
    CALL get_current_pcb_address
    LXI  D, O_FILE_TABLE
    DAD  D                          
    
    LDA  OPEN_LOCAL_FD              
    MOV  E, A
	MVI D, 0
    DAD  D                          ; HL -> Точная ячейка дескриптора процесса
    
    MOV  M, B                       ; Зафиксировали связь в PCB!

    ; Восстановление контекста РОН перед штатным выходом
    POP  H
    POP  D
    POP  B                          ; Все регистры пользователя восстановлены в оригинал!

    LDA  OPEN_LOCAL_FD              ; Возвращаем локальный FD строго в аккумуляторе А
    RET                             ; Выход в syscall_return.

; =============================================================================
; ОБРАБОТЧИКИ ОШИБОК С ТРАНЗАКТНЫМ ОТКАТОМ СТЕКА И БАЛАНСИРОВКОЙ
; =============================================================================
open_err_noent_abi:
    LHLD VFS_OPEN_STACK_SAVE
    SPHL                            ; Сбросили вложенный мусор стека, если он накопился
    POP  H
	POP  D
	POP  B         ; Восстановили РОН пользователя
    MVI  A, 0FFh                    ; Оригинальный код ошибки: Файл не найден (0xFF / ENOENT)
    RET

open_err_emfile_abi:
    LHLD VFS_OPEN_STACK_SAVE
    SPHL                            ; Реставрировали стек супервизора
    POP  H
	POP  D
	POP  B         ; Восстановили РОН пользователя
    MVI  A, 0FDh                    ; Оригинальный код ошибки: Превышен лимит в PCB (0xFD / EMFILE)
    RET


; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ИНИЦИАЛИЗАЦИЯ И МОНТИРОВАНИЕ SOCKETFS
; =============================================================================
; Вход:  Прерывания заблокированы (DI). Ядро находится на этапе загрузки.
; Выход: Таблицы очищены, сокет-драйвер смонтирован в VFS_MOUNT_TABLE.
; Регистры BC, DE, HL полностью сохраняются в стеке.
; =============================================================================
sys_socketfs_init:
    PUSH PSW
    PUSH B
    PUSH D
    PUSH H                          ; Полная консервация РОН для безопасности

    ; --- ШАГ 1: Очистка глобальной таблицы сокетов SOCK_SYS_TABLE ---
    LXI  H, SOCK_SYS_TABLE
    MVI  B, MAX_SYS_SOCKETS * SOCK_STRUCT_SIZE ; 4 слота * 8 байт = 32 байта
socketfs_clear_table_loop:
    MVI  M, SOCK_FREE               ; Прописываем маркер FREE (00h) во все байты
    INX  H
    DCR  B
    JNZ  socketfs_clear_table_loop

    ; --- ШАГ 2: Обнуление 16-битных кольцевых указателей NET_UNIX_HEADS/TAILS ---
    ; Всего у нас 4 сокет-пары, каждая имеет 2 полубуфера (направления обмена).
    ; Итого: 4 * 2 = 8 указателей Head и 8 указателей Tail. 
    ; Каждый указатель 16-битный (2 байта). Общий размер массивов = 16 байт + 16 байт = 32 байта.
    LXI  H, NET_UNIX_HEADS          ; Массив голов чтения
    MVI  B, 32                      ; Сбросить 32 байта подряд (включая хвосты, идущие следом)
socketfs_clear_ptr_loop:
    MVI  M, 00h                     ; Обнуляем указатель позиции в кольце
    INX  H
    DCR  B
    JNZ  socketfs_clear_ptr_loop

    ; --- ШАГ 3: Регистрация шлюза SocketFS в VFS_MOUNT_TABLE ---
    LXI  H, VFS_MOUNT_TABLE
    MVI  B, MAX_VFS_MOUNTS          ; Сканируем доступные 4 точки монтирования VFS
    MVI  C, 0                       ; Локальный счетчик слотов

socketfs_mount_find_loop:
    MOV  A, M                       ; Читаем поле M_MNT_STATUS (+0)
    CPI  00h                        ; Слот свободен (FREE)?
    JZ   socketfs_mount_slot_ok     ; Свободный слот найден, уходим на прошивку!

    LXI  D, VFS_MOUNT_SIZE          ; Перешагиваем на следующую запись (8 байт)
    DAD  D
    INR  C
    DCR  B
    JNZ  socketfs_mount_find_loop

    ; Если свободного слота в таблице монтирования нет — критический сбой (Kernel Panic)
    JMP  socketfs_init_panic

socketfs_mount_slot_ok:
    ; HL указывает точно на старт свободного слота в VFS_MOUNT_TABLE
    PUSH H                          ; Спасли базовый адрес найденного слота монтирования
    
    MVI  M, 01h                     ; 1. Прописываем статус M_MNT_STATUS = MOUNTED (01h)
    
    INX  H
	INX  H                 ; 2. Переходим к полю M_MNT_PREFIX_PTR (+2)
    LXI  D, SOCKFS_PATH_STR         ; ИСПРАВЛЕНО: Оригинальный 16-битный адрес строки префикса
    MOV  M, E
	INX H
	MOV M, D    ; Записали адрес строки в структуру Little-Endian
    
    INX  H                          ; 3. Переходим к полю M_MNT_GATE_PTR (+4)
    LXI  D, sockfs_vfs_gate         ; DE = 16-битный физический адрес шлюза драйвера сокетов
    MOV  M, E
	INX H
	MOV M, D    ; Записали адрес шлюза-обработчика в таблицу VFS
    
    POP  H                          ; Восстановили базовый адрес слота монтирования

    ; --- ШАГ 4: ШТАТНЫЙ ВЫХОД ИЗ ФУНКЦИИ ИНИЦИАЛИЗАЦИИ ---
    POP  H
    POP  D
    POP  B
    POP  PSW                        ; Реставрация контекста прерванной загрузки
    RET                             ; Успешный возврат в init_io

socketfs_init_panic:
    ; Аварийный останов: Таблица монтирования VFS переполнена на этапе сборки ядра!
    DI
    HLT

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ЦЕНТРАЛЬНЫЙ ШЛЮЗ ДРАЙВЕРА PTY (pty_vfs_gate)
; =============================================================================
; Вызывается из sys_open через PCHL при обращении к путям подчиненных терминалов.
; =============================================================================
pty_vfs_gate:
    PUSH B
    PUSH D
    PUSH H                          ; Сбалансированная консервация РОН ядра

    ; HL указывает на остаток строки, например: "/0" или "0"
    ; Проверяем первый символ. Если это косая черта '/', пропускаем её.
    MOV  A, M
    CPI  02Fh                       ; ASCII код для '/'
    JNZ  pty_gate_parse_digit
    INX  H                          ; Шагнули за косую черту '/'

pty_gate_parse_digit:
    ; Считываем номер линии Slave-терминала (символ '0'..'3')
    MOV  A, M
    CPI  030h                       ; Меньше ASCII '0' (0x30)?
    JC   pty_gate_err_noent
    CPI  034h                       ; Больше или равен ASCII '4' (0x34)?
    JNC  pty_gate_err_noent

    ; --- ВЫЧИСЛЕНИЕ НOМЕРА ЛИНИИ ПСЕВДОКОНСОЛИ ---
    SUI  030h                       ; Преобразуем ASCII '0'..'3' в числовой индекс 0..3
    STA  PTY_GATE_LINE              ; Зафиксировали номер линии

    ; --- РАСЧЕТ ГЛОБАЛЬНОГО VFS ИНДЕКСА SLAVE СТОРОНЫ (52..55) ---
    ADI  GLOBAL_PTY_SLAVE           ; Добавляем базовое смещение Slave-диапазона (52)
    STA  PTY_GATE_G_IDX             ; Сформировали глобальный VFS-индекс открываемого TTY

    ; --- ПРОВЕРКА АКТИВНОСТИ PTY-ПАРЫ В PTY_SYS_TABLE ---
    ; Нам необходимо убедиться, что пара была открыта и выделена через openpty()
    LDA  PTY_GATE_LINE
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Номер_Линии * 8 байт (размер структуры PTY пары)
    LXI  D, PTY_SYS_TABLE
    DAD  D                          ; HL -> Указывает на P_PTY_STATUS (+0) в таблице ядра

    MOV  A, M                       ; Читаем поле статуса PTY пары
    ORA  A                          ; Если статус равен 00h — пара не выделена/свободна!
    JZ   pty_gate_err_noent         ; Возвращаем ENOENT, файл устройства не существует

    ; --- УСПЕШНЫЙ ВЫХOД ИЗ ШЛЮЗА VFS PTY ---
    POP  H
    POP  D
    POP  B                          ; Полное восстановление РОН супервизора
    
    LDA  PTY_GATE_G_IDX             ; Возвращаем глобальный индекс Slave TTY в аккумуляторе A
    RET                             ; Возврат в sys_open для финальной привязки к PCB файла

; --- ОБРАБОТЧИК ОШИБКИ ОТСУТСТВИЯ СТPУКТУРЫ (POSIX ENOENT) ---
pty_gate_err_noent:
    POP  H
    POP  D
    POP  B                          ; Сбалансировали стек ядра Ring 0
    MVI  A, 0FEh                    ; Код ошибки 0xFE (POSIX ENOENT — File not found)
    RET

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ЦЕНТРАЛЬНЫЙ ШЛЮЗ ДРАЙВЕРА SOCKETFS (sockfs_vfs_gate)
; =============================================================================
; Вызывается из sys_open через PCHL при обращении к путям сокетов ОЗУ.
; =============================================================================
sockfs_vfs_gate:
    PUSH B
    PUSH D
    PUSH H                          ; Сбалансированная консервация РОН ядра

    ; HL указывает на остаток строки, например: "/s0" или "s0"
    ; Проверяем первый символ. Если это косая черта '/', пропускаем её.
    MOV  A, M
    CPI  02Fh                       ; ASCII код для '/'
    JNZ  sockfs_gate_parse_s
    INX  H                          ; Шагнули за '/'

sockfs_gate_parse_s:
    ; Проверяем обязательный маркер суффикса — символ 's'
    MOV  A, M
    CPI  073h                       ; ASCII код для 's' (0x73)
    JNZ  sockfs_gate_err_noent      ; Если не 's' — путь неверный, ENOENT

    ; Переходим к чтению номера линии сокета (символ '0'..'3')
    INX  H
    MOV  A, M
    CPI  030h                       ; Меньше '0' (0x30)?
    JC   sockfs_gate_err_noent
    CPI  034h                       ; Больше или равен '4' (0x34)?
    JNC  sockfs_gate_err_noent

    ; --- ВЫЧИСЛЕНИЕ НOМЕРА ЛИНИИ СОКЕТА ---
    SUI  030h                       ; Преобразуем ASCII '0'..'3' в число 0..3
    STA  SOCKFS_GATE_LINE           ; Зафиксировали номер линии сокета

    ; --- РАСЧЕТ ГЛОБАЛЬНОГО ИНДЕКСА VFS (32..39) ---
    ; Каждая пара сокетов занимает 2 глобальных индекса:
    ; Линия 0 -> Индекс 32, Линия 1 -> Индекс 34, Линия 2 -> Индекс 36...
    ADD  A                          ; Номер_Линии * 2
    ADI  32                         ; Смещение базового диапазона сокетов ОЗУ (32)
    STA  SOCKFS_GATE_G_IDX          ; Сформировали глобальный VFS-индекс

    ; --- ПРОВЕРКА АКТИВНОСТИ СОКЕТА В SOCK_SYS_TABLE ---
    ; Нам необходимо убедиться, что сокет был создан вызовом sys_socket()
    LDA  SOCKFS_GATE_LINE
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Номер_Линии * 8 байт (размер структуры сокета)
    LXI  D, SOCK_SYS_TABLE
    DAD  D                          ; HL -> Указывает на S_SOCK_STATUS (+0) в таблице сокетов

    MOV  A, M                       ; Читаем поле статуса сокета
    CPI  SOCK_FREE                  ; Если статус равен SOCK_FREE (00h) — сокет не создан!
    JZ   sockfs_gate_err_noent      ; Возвращаем ENOENT, файл сокета не существует

    ; --- УСПЕШНЫЙ ВЫХOД ИЗ ШЛЮЗА ---
    POP  H
    POP  D
    POP  B                          ; Полное восстановление РОН супервизора
    
    LDA  SOCKFS_GATE_G_IDX          ; Возвращаем глобальный индекс сокета в аккумуляторе A
    RET                             ; Возврат в sys_open для финальной привязки к PCB

; --- ОБРАБОТЧИК ОШИБКИ ОТСУТСТВИЯ СТPУКТУРЫ (POSIX ENOENT) ---
sockfs_gate_err_noent:
    POP  H
    POP  D
    POP  B                          ; Сбалансировали стек ядра
    MVI  A, 0FEh                    ; Код ошибки 0xFE (POSIX ENOENT — File not found)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ sys_close
; =============================================================================
; Вход:  C = Локальный дескриптор файла юзерспейса (FD, 0..7), подлежащий закрытию.
; Выход: A = 00H — Успех. Дескриптор закрыт, связанные ресурсы ОЗУ/VFS освобождены.
;        A = 0FFH — Ошибка: Неверный дескриптор или слот уже был свободен (EBADF).
; Регистры BC, DE, HL полностью сохраняются. Прерывания контролирует диспетчер!
; =============================================================================
sys_close:
    PUSH B
    PUSH D
    PUSH H                          ; Полная консервация контекста супервизора

    ; --- ШАГ 1: Валидация границ локального дескриптора ---
    MOV  A, D
    CPI  8                          ; Дескриптор в легитимных границах (0..7)?
    JNC  close_err_badf             ; Если FD >= 8 — это невалидный дескриптор, EBADF

    ; --- ШАГ 2: Извлечение глобального системного индекса из PCB ---
    MOV  C, D
	MVI B, 0            ; BC = Локальный FD для смещения
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB текущей задачи
    PUSH H                          ; Спасли базу PCB в стек ядра Ring 0
    
    LXI  D, O_FILE_TABLE            ; Смещение локальной таблицы файлов (+20)
    DAD  D
	DAD  B                 ; HL указывает точно на ячейку FD процесса
    
    MOV  A, M                       ; A = Глобальный системный индекс VFS (0..63)
    CPI  RES_FREE_MARKER            ; Слот уже пуст/свободен (0xFF)?
    JZ   close_err_badf_pop         ; Если закрываем уже закрытый файл — ошибка EBADF

    ; --- ШАГ 3: АТОМАРНОЕ ОСВОБОЖДЕНИЕ ЛОКАЛЬНОГО FD В PCB ---
    MOV  E, A                       ; E = ГЛОБАЛЬНЫЙ ИНДЕКС (сохранили перед очисткой ячейки)
    MVI  M, RES_FREE_MARKER         ; Затерли локальный дескриптор маркером свободы (0xFF)
    POP  H                          ; Полностью очистили стек супервизора от адреса PCB

    ; --- ШАГ 4: ВЫЗОВ ЦЕНТРАЛЬНОГО ДЕАЛЛОКАТОРА ЯДРА VFS ---
    MOV  A, E                       ; Аккумулятор А = Наш извлеченный глобальный индекс (0..63)
    CALL k_vfs_close_global_index   ; Запускаем каскадную утилизацию сокетов чипа, пайпов и файлов!

    ; --- ШАГ 5: ШТАТНЫЙ УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    POP  H
    POP  D
    POP  B                          ; Полное восстановление РОН супервизора
    MVI  A, 00h                     ; Код возврата = 00H (Чистый успех POSIX)
    RET                             ; Выход в syscall_return.

; =============================================================================
; ОБРАБОТЧИКИ ОШИБОК ДЕСКРИПТОРНОГО ИНТЕРФЕЙСА (EBADF)
; =============================================================================
close_err_badf_pop:
    POP  H                          ; Сбалансировали стек супервизора от адреса PCB
close_err_badf:
    POP  H
    POP  D
    POP  B                          ; Полное восстановление контекста
    MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EBADF — Bad file descriptor)
    RET

; =============================================================================
; ВНУТРЕННЯЯ ПРОЦЕДУРА ЯДРА: ДЕКРЕМЕНТ ССЫЛОК И СИНХРОНИЗАЦИЯ КЭША
; =============================================================================
; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): vfs_decrement_file_ref (УЧЕТ ССЫЛОК ПРИ ЗAКРЫТИИ)
; =============================================================================
; Вход:  HL = Физический базовый адрес записи файла в SYS_FILE_TABLE.
; Выход: А = 00H — Файл еще используется другими дескрипторами (Ref_Count > 0).
;        А = 01H — Ссылок больше нет (Ref_Count == 0), слот можно уничтожать.
; Регистры полностью сохраняются. Прерывания заблокированы (DI).
; =============================================================================
vfs_decrement_file_ref:
    PUSH B
    PUSH D
    PUSH H                          ; Спасаем РОН в стек супервизора

    ; Переходим к полю счетчика ссылок F_REF_COUNT (+1)
    INX  H                          ; HL указывает на байт F_REF_COUNT
    
    MOV  A, M                       ; A = Текущее количество ссылок на файл
    ORA  A                          ; Защитная проверка: если счетчик уже 0 — аномалия
    JZ   vfs_ref_is_zero
    
    DCR  M                          ; АТОМАРНЫЙ ДЕКРЕМЕНТ: Ref_Count = Ref_Count - 1
    MOV  A, M                       ; Читаем новое измененное значение счетчика
    ORA  A                          ; Счетчик стал равен чистому нулю?
    JZ   vfs_ref_is_zero            ; Если да — возвращаем маркер удаления (01h)

    ; --- Ссылки еще есть, файл уничтожать нельзя ---
    POP  H
	POP  D
	POP  B         
    MVI  A, 00h                     ; Возвращаем 00h (Файл занят)
    RET

vfs_ref_is_zero:
    ; --- Ссылок больше нет, файл подлежит утилизации ---
    POP  H
	POP  D
	POP  B         
    MVI  A, 01h                     ; Возвращаем 01h (Разрешено полное закрытие)
    RET

; =============================================================================
; POSIX СИС-ВЫЗОВ №9: SYS_CHDIR — СМЕНА ТЕКУЩЕЙ РАБОЧЕЙ ДИРЕКТОРИИ ПРОЦЕССА
; =============================================================================
; Вход:  HL = Логический адрес строки пути каталога (const char *path) в Ring 3.
; Выход: A  = 00H — Успех. Текущая рабочая директория процесса изменена.
;        A  = 0FFH — Ошибка: Каталог не найден или это не директория (ENOENT).
; Все регистры, кроме аккумулятора A, строго сохраняются.
; =============================================================================
sys_chdir:
    ; Прерывания уже гарантированно заблокированы на входе в sys_call_entry!

    PUSH B                          ; Стак: Сохраняем регистровые пары задачи
    PUSH D
    PUSH H                          ; Спасли входной адрес строки пути Ring 3

    ; --- ШАГ 1: Поиск каталога на разделе Experimental FS (ID 0xE0) ---
    XCHG                            ; Теперь DE = Логический адрес строки пути (const char *path)
    
    ; Включаем KERNEL_OVERRIDE для парсинга имени пути из юзерспейса Ring 3
    OUT  076H                       ; Включаем ~{KERNEL_OVERRIDE_EN} = 0
    
    ; [Внутренний модуль VFS посимвольно читает путь по адресу в DE.
    ; Вызывается асинхронная функция ide_read_sector_to_buf для чтения секторов 
    ; директорий файловой системы. Драйвер прибавляет EXPES_LBA и усыпляет 
    ; процесс через CALL vfs_sleep_on_io, освобождая шину.]
    
    OUT  076H                       ; Выключаем KERNEL_OVERRIDE, возвращаем Ring 0
    
    ; Предположим, внутренний парсер VFS успешно нашел целевой каталог,
    ; проверил, что это именно папка (а не файл), и извлек её 16-битный Inode.
    ; Пусть номер найденного Inode каталога равен 0x0012
    LXI  B, 0012h                   ; BC = 16-битный номер Inode целевого каталога
    STA  CHDIR_TEMP_INODE           ; Зафиксировали младший байт Inode в ОЗУ ядра
    MOV  A, B
    STA  CHDIR_TEMP_INODE+1         ; Зафиксировали старший байт Inode в ОЗУ ядра

    ; Имитируем условный флаг успешности поиска VFS (1 = успех, 0 = ошибка)
    MVI  A, 1
    CPI  0
    JZ   chdir_err_enoent           ; Если каталог отсутствует — уходим на ошибку

    ; --- ШАГ 2: ЗАПИСЬ НОВОГО НОМЕРА INODE В PCB ТЕКУЩЕГО ПРОЦЕССА ---
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB текущего процесса
    
    LXI  D, O_CWD_INODE             ; Официальное смещение текущей директории (+17)
    DAD  D                          ; HL указывает точно на поле O_CWD_INODE в PCB
    
    LDA  CHDIR_TEMP_INODE           ; Извлекаем младший байт вычисленного Inode
    MOV  M, A                       ; Записали в PCB
    INX  H                          ; Переходим к старшему байту поля O_CWD_INODE
    LDA  CHDIR_TEMP_INODE+1         ; Извлекаем старший байт вычисленного Inode
    MOV  M, A                       ; Записали в PCB. Текущая директория официально изменена!

    ; --- ШАГ 3: ПОДГОТОВКА УСПЕШНОГО ВЫХОД ИЗ СИСКОЛЛА ---
    POP  H                          ; Полностью очистили стек ядра от сохраненных параметров
    POP  D                          ; Восстановили оригинальный контекст регистров задачи
    POP  B
    MVI  A, 00h                     ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                             ; Выход в syscall_return. Прерывания откроет ядро!

; -----------------------------------------------------------------------------
; БЛОК ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКА VFS)
; -----------------------------------------------------------------------------
chdir_err_enoent:
    POP  H                          ; Сбалансировали стек ядра
    POP  D                          
    POP  B
    MVI  A, 0FFh                    ; Возвращаем код ошибки 0xFF (POSIX ENOENT — Каталог не найден)
    RET                             ; Выход в syscall_return

; =============================================================================
; POSIX СИС-ВЫЗОВ №10: SYS_ALARM — ПОСТАНОВКА АСИНХРОННОГО БУДИЛЬНИКА СИСТЕМЫ
; =============================================================================
; Вход:  HL = Время в секундах до генерации сигнала SIGALRM (0 = отмена).
; Выход: HL = Оставшееся время предыдущего будильника в секундах (или 0).
;        Все регистры, кроме HL, строго сохраняются.
; =============================================================================

sys_alarm:
    ; Прерывания уже гарантированно заблокированы на входе в sys_call_entry!

    PUSH B                          ; ИСПРАВЛЕНО: Сохраняем BC (строго 'B')
    PUSH D                          ; ИСПРАВЛЕНО: Сохраняем DE (строго 'D')

    ; --- ШАГ 1: Конвертация входных секунд пользователя в системные тики ---
    ; Математика для 8080: Системные_Тики = Секунды_HL * 50
    ; HL * 50 = (HL * 32) + (HL * 16) + (HL * 2)
    PUSH H                          ; Спасли исходные секунды в стек ядра (строго 'H')
    
    ; 1. Получаем HL * 2
    DAD  H                          ; HL = HL * 2
    SHLD ALARM_TEMP_X2              ; Зафиксировали в ОЗУ ядра
    
    ; 2. Получаем HL * 16 (еще 3 сдвига от X2)
    DAD  H                          ; * 4
    DAD  H                          ; * 8
    DAD  H                          ; * 16
    SHLD ALARM_TEMP_X16             ; Зафиксировали в ОЗУ ядра
    
    ; 3. Получаем HL * 32 (еще 1 сдвиг)
    DAD  H                          ; * 32
    
    ; Складываем компоненты: HL(X32) + X16 + X2
    XCHG                            ; DE = HL * 32
    LHLD ALARM_TEMP_X16
    DAD  D                          ; HL = (HL * 32) + (HL * 16)
    XCHG                            ; DE = Промежуточный результат
    LHLD ALARM_TEMP_X2
    DAD  D                          ; HL = Итоговые системные тики (Секунды * 50)!
    
    SHLD ALARM_NEW_TICKS            ; Зафиксировали новые тики для записи в PCB
    POP  H                          ; Восстановили исходные секунды в пару HL (строго 'H')

    ; --- ШАГ 2: Извлечение старого значения будильника из PCB ---
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB текущего процесса
    PUSH H                          ; Стак: Сохранили базовый адрес PCB (строго 'H')
    
    LXI  D, O_ALARM_TICKS           ; Официальное смещение тиков будильника (38)
    DAD  D                          ; HL указывает точно на поле O_ALARM_TICKS в PCB
    
    MOV  E, M                       ; Читаем младший байт старых тиков
    INX  H
    MOV  D, M                       ; DE = Старое значение тиков из PCB задачи

    ; --- ШАГ 3: Запись нового значения тиков в PCB ---
    DCX  H                          ; Сдвинулись обратно на младший байт O_ALARM_TICKS
    XCHG                            ; Теперь DE = Старые тики, HL = Адрес поля O_ALARM_TICKS
    PUSH D                          ; ИСПРАВЛЕНО: Стак: Припрятали старые тики (строго 'D')
    
    LHLD ALARM_NEW_TICKS            ; HL = Новые вычисленные тики (Секунды * 50)
    MOV  A, L
    STAX D                          ; Записали младший байт новых тиков в PCB
    INX  D
    MOV  A, H
    STAX D                          ; Записали старший байт новых тиков в PCB

    ; --- ШАГ 4: Конвертация старых тиков обратно в секунды для возврата ---
    POP  H                          ; ИСПРАВЛЕНО: HL = Восстановили старые тики из стека (строго 'H')
    
    ; Оставшиеся_Секунды = Системные_Тики_HL / 50 (Вычитание в цикле)
    LXI  B, 0000h                   ; BC = Счетчик секунд
    
alarm_div_loop:
    MOV  A, H                       ; Проверяем, не равен ли остаток тиков нулю?
    ORA  L
    JZ   alarm_div_done             
    
    ; Сравниваем HL с 50
    MOV  A, H
    CPI  0
    JNZ  alarm_sub_50               ; Если старший байт не ноль — HL точно больше 50
    MOV  A, L
    CPI  50
    JC   alarm_div_done             ; Если младший байт < 50 — целых секунд не осталось, выходим

alarm_sub_50:
    LXI  D, -50                     ; Загружаем отрицательные 50 (0FFCEH)
    DAD  D                          ; HL = HL - 50
    INX  B                          ; Инкрементируем счетчик секунд в BC
    JMP  alarm_div_loop             

alarm_div_done:
    MOV  H, B
    MOV  L, C                       ; Переносим результат (секунды) в HL согласно POSIX-конвенции

    ; --- ШАГ 5: Финализация и выход ---
    POP  H                          ; ИСПРАВЛЕНО: Очистили стек от адреса PCB (строго 'H')
    POP  D                          ; ИСПРАВЛЕНО: Восстановили DE (строго 'D')
    POP  B                          ; ИСПРАВЛЕНО: Восстановили BC (строго 'B')
    RET                             ; Выход в syscall_return. Прерывания откроет ядро.

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ sigaction()
; =============================================================================
; Вход:  D  = Номер POSIX-сигнала (1..16).
;        HL = Логический адрес структуры new_act (2 байта) в памяти задачи Ring 3.
;        BC = Логический адрес структуры old_act (2 байта) в памяти задачи Ring 3.
; Выход: A  = 00H — Успех. Таблица обработчиков обновлена.
;        A  = 0FEH — Ошибка EINVAL (Сигнал вне диапазона или SIGKILL/9).
; =============================================================================

sys_sigaction:
    ; Прерывания уже заблокированы (DI) центральным диспетчером sys_call_entry!

    ; --- ШАГ 1: Валидация номера сигнала ---
    MOV  A, D
    CPI  17                     ; Сигнал должен быть в диапазоне 1..16
    JNC  sigact_err_inval       
    ORA  A                      ; Проверка на 0
    JZ   sigact_err_inval       
    
    CPI  9                      ; ПОСИКС-ТАБУ: Сигнал SIGKILL (9) перехватывать КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО
    JZ   sigact_err_inval       

    ; Фиксируем входные параметры юзерспейса в ОЗУ ядра Ring 0
    SHLD SIGACT_NEW_PTR         ; Сохранили адрес new_act
    MOV  H, B
    MOV  L, C
    SHLD SIGACT_OLD_PTR         ; Сохранили адрес old_act
    MOV  A, D
    STA  SIGACT_SIGNUM          ; Сохранили номер сигнала

    ; --- ШАГ 2: Вычисление адреса ячейки сигнала в таблице векторов процесса ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущего процесса
    PUSH H                      ; Стак: Сохранили базу PCB (строго 'H')
    
    LXI  D, O_SIG_TABLE_PTR     ; Смещение указателя на таблицу обработчиков (+36)
    DAD  D                      ; HL указывает точно на поле O_SIG_TABLE_PTR в PCB
    
    ; Извлекаем 16-битный базовый адрес таблицы векторов из PCB
    MOV  E, M
    INX  H
    MOV  D, M                   ; DE = Логический адрес начала таблицы векторов сигналов
    
    ; Проверим, инициализирован ли указатель таблицы? Если там 0000h — это ошибка структуры
    MOV  A, D
    ORA  E
    JZ   sigact_err_ptr         ; Если указатель нулевой — аварийный выход

    ; Вычисляем смещение конкретного сигнала внутри таблицы векторов.
    ; Каждый вектор занимает 2 байта (адрес функции). Смещение = (signum - 1) * 2
    LDA  SIGACT_SIGNUM
    DCR  A                      ; Индекс бита/вектора (0..15)
    ADD  A                      ; Умножаем на 2 (сдвиг влево на 1 бит)
    
    ; Прибавляем к базовому адресу таблицы векторов DE
    ADD  E
    MOV  E, A
    MVI  A, 0
    ADC  D
    MOV  D, A                   ; Теперь DE = Точный логический адрес вектора этого сигнала в Ring 3!
    
    ; Переносим целевой адрес вектора в пару HL ядра
    XCHG
    SHLD SIGACT_TARGET_VECTOR_PTR ; Зафиксировали в ОЗУ ядра

    ; --- ШАГ 3: СОХРАНЕНИЕ СТАРОГО ОБРАБОТЧИКА (БЛОК OLD_ACT) ---
    LHLD SIGACT_OLD_PTR
    MOV  A, H
    ORA  L                      ; Проверяем, передал ли пользователь NULL (0000H) для старого обработчика?
    JZ   sigact_skip_old_write  ; Если NULL — сохранение старого вектора пропускается
    
    ; Вычитываем старый 16-битный вектор из Ring 3 через KERNEL_OVERRIDE
    OUT  076H                   ; Включаем ~{KERNEL_OVERRIDE_EN} = 0 для доступа к Ring 3
    LHLD SIGACT_TARGET_VECTOR_PTR ; HL указывает на ячейку вектора в памяти процесса
    MOV  E, M
    INX  H
    MOV  D, M                   ; DE = Считанный старый адрес функции-обработчика
    OUT  076H                   ; Отключаем оверрайд шины
    
    ; Записываем извлеченный старый вектор по адресу old_act в Ring 3
    LHLD SIGACT_OLD_PTR         ; HL = Адрес назначения, переданный пользователем
    OUT  076H                   ; Включаем оверрайд
    MOV  M, E                   ; Записали младший байт старого адреса
    INX  H
    MOV  M, D                   ; Записали старший байт старого адреса
    OUT  076H                   ; Отключаем оверрайд

sigact_skip_old_write:
    ; --- ШАГ 4: ЗАПИСЬ НОВОГО ОБРАБОТЧИКА (БЛОК NEW_ACT) ---
    LHLD SIGACT_NEW_PTR
    MOV  A, H
    ORA  L                      ; Проверяем, не равен ли new_act NULL (0000H)?
    JZ   sigact_success_exit    ; Если NULL — пользователь просто читал старый вектор, выходим

    ; Вычитываем новый вектор из структуры new_act в Ring 3
    OUT  076H                   ; Включаем оверрайд
    MOV  E, M                   ; Младший байт нового адреса функции
    INX  H
    MOV  D, M                   ; Старший байт нового адреса функции
    OUT  076H                   ; Отключаем оверрайд

    ; Записываем новый вектор в таблицу векторов сигналов процесса
    LHLD SIGACT_TARGET_VECTOR_PTR ; HL = Целевая ячейка вектора сигнала в памяти процесса
    OUT  076H                   ; Включаем оверрайд
    MOV  M, E                   ; Записали младший байт нового обработчика
    INX  H
    MOV  M, D                   ; Записали старший байт нового обработчика
    OUT  076H                   ; Отключаем оверрайд

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!

sigact_success_exit:
    POP  H                      ; Сбалансировали стек ядра, удалив базовый адрес PCB (строго 'H')
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return.

; --- БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКИ ИНТЕРФЕЙСА) ---
sigact_err_ptr:
    POP  H                      ; Сбалансировали стек при ошибке неинициализированного указателя

sigact_err_inval:
    MVI  A, 0FEh                ; Возвращаем код ошибки 0xFE (POSIX EINVAL — Неверный параметр)
    RET                         ; Выход в syscall_return

; =============================================================================
; POSIX СИС-ВЫЗОВ №13: SYS_OVERLAY_MAP — ПРОГРАММНЫЙ МАППИНГ СЕГМЕНТОВ 4-7
; =============================================================================
sys_overlay_map:
    ; Вход: D = Номер запрашиваемого виртуального сегмента (4..7)
    ; Выход: A = 00H (УСПЕХ), Либо код ошибки (0E2H = EINVAL — Неверный сегмент)

    ; --- ШАГ 1: Валидация входного номера сегмента ---
    MOV  A, D
    CPI  4                      ; Меньше 4?
    JC   overlay_bad_segment
    CPI  8                      ; Больше 7? (8 и выше)
    JNC  overlay_bad_segment

    STA  OVL_TARGET_SEG         ; Сохраняем запрашиваемый сегмент (4..7)

    ; Вычисляем индекс внутри массива O_EXT_PAGES_MAP: Индекс = Сегмент - 4
    SUI  4
    STA  OVL_EXT_MAP_IDX        ; Индекс расширенной страницы (0..3)

    ; --- ШАГ 2: Извлечение номера физической страницы из O_EXT_PAGES_MAP ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущего процесса
    SHLD OVL_CURRENT_PCB
    
    LXI  D, O_EXT_PAGES_MAP
    DAD  D                      ; HL указывает на начало O_EXT_PAGES_MAP (+12)
    
    LDA  OVL_EXT_MAP_IDX
    MOV  E, A
    MVI  D, 0
    DAD  D                      ; HL указывает точно на ячейку запрашиваемой расширенной страницы
    MOV  A, M                   ; A = Номер физической страницы, привязанной к оверлею
    STA  OVL_PHYS_PAGE_NUM

    ; --- ШАГ 3: Проверка маски присутствия в свопе O_CF_SWAP_MASK ---
    LHLD OVL_CURRENT_PCB
    LXI  D, O_CF_SWAP_MASK
    DAD  D                      ; HL указывает на O_CF_SWAP_MASK (+16)
    MOV  A, M
    STA  OVL_SWAP_MASK_BUF

    ; Формируем битовую маску для запрашиваемого виртуального сегмента (1 << Сегмент)
    LDA  OVL_TARGET_SEG
    MOV  B, A                   ; B = Количество сдвигов (4..7)
    MVI  C, 001H                ; Стартовый бит
ovl_mask_shift_loop:
    MOV  A, C
    ADD  A                      ; Сдвиг влево (ADD A)
    MOV  C, A
    DCR  B
    JNZ  ovl_mask_shift_loop
    MOV  A, C
    STA  OVL_BIT_MASK           ; Маска целевого бита сформирована

    ; Проверяем: (O_CF_SWAP_MASK & OVL_BIT_MASK)
    LDA  OVL_SWAP_MASK_BUF
    MOV  B, A
    LDA  OVL_BIT_MASK
    ANA  B                      ; Изолируем целевой бит подкачки
    JZ   ovl_page_is_already_in_ram ; Если бит = 0 — страница уже в ОЗУ, переходим к маппингу

    ; --- ШАГ 4: СТРАНИЦА НА ДИСКЕ — ВЫПОЛНЯЕМ ПРОГРАММНЫЙ ПОДЪЕМ (SWAP IN) ---
    ; Запрашиваем свободный физический фрейм ОЗУ для размещения данных оверлея
    CALL alloc_physical_frame_page
    CPI  0FFH
    JNZ  ovl_frame_allocated_instantly

    ; Память переполнена — принудительно вытесняем страницу фоновой задачи (например, фрейм 31)
    MVI  A, 31                  ; Забираем физический фрейм №31
    MVI  B, 3                   ; Вытесняем логический Сегмент 3 процесса-жертвы
    MVI  C, 5                   ; PID жертвы = 5
    CALL swap_out_page_smart    ; Сбрасываем грязные данные жертвы на CompactFlash
    MVI  A, 31                  ; Фрейм 31 полностью очищен и готов к приему оверлея

ovl_frame_allocated_instantly:
    STA  OVL_NEW_FRAME_NUM      ; Запоминаем выделенный физический фрейм ОЗУ

    ; Поднимаем данные виртуального сегмента с CompactFlash в новый фрейм ОЗУ
    LDA  OVL_NEW_FRAME_NUM      ; A = Физический фрейм назначения
    LDA  OVL_TARGET_SEG         ; B = Логический сегмент процесса (4..7)
    MOV  B, A
    LDA  CURRENT_PID            ; C = PID текущей активной задачи
    MOV  C, A
    LDA  OVL_NEW_FRAME_NUM      ; Восстановили Акум перед вызовом
    CALL swap_in_auto           ; Автоматическая секторная накатка 16 КБ с диска в ОЗУ!

    ; Обновляем карту расширенных страниц O_EXT_PAGES_MAP, привязывая новый фрейм
    LHLD OVL_CURRENT_PCB
    LXI  D, O_EXT_PAGES_MAP
    DAD  D
    LDA  OVL_EXT_MAP_IDX
    MOV  E, A
    MVI  D, 0
    DAD  D                      ; HL указывает на ячейку расширенной страницы в PCB
    LDA  OVL_NEW_FRAME_NUM
    MOV  M, A                   ; Зафиксировали новый фрейм ОЗУ в расширенной карте
    STA  OVL_PHYS_PAGE_NUM      ; Обновили рабочую переменную номера фрейма

ovl_page_is_already_in_ram:
    ; --- ШАГ 5: АППАРАТНЫЙ ПЕРЕМАППИНГ В ОКНО СЕГМЕНТА 2 (0x8000) ---
    ; Обновляем базовую физическую карту O_PAGES_MAP текущего процесса.
    ; Записываем номер фрейма оверлея на позицию Сегмента 2 (+10 в PCB)
    LHLD OVL_CURRENT_PCB
    LXI  D, O_PAGES_MAP + 2     ; Смещение до физического номера Сегмента 2 в PCB (+10)
    DAD  D
    LDA  OVL_PHYS_PAGE_NUM
    MOV  M, A                   ; Теперь Сегмент 2 официально указывает на данные оверлея

    ; Выполняем физическую перепрошивку регистра К555ИР26 для Сегмента 2
    LDA  OVL_PHYS_PAGE_NUM
    OUT  072H                   ; Мультиплексоры К555КП11 мгновенно переключили ОЗУ!

    MVI  A, 00H                 ; Сисколл выполнен успешно, возвращаем 0
    RET

overlay_bad_segment:
    MVI  A, 0E2H                ; Код ошибки: EINVAL (Некорректный номер виртуального сегмента)
    RET

; =============================================================================
; СИСТЕМНАЯ ПОДПРОГРАММА ЯДРА (Ring 0): ПОИСК MAC-АДРЕСА В ARP-КЭШЕ
; =============================================================================
k_arp_resolve:
    ; --- ШАГ 1: Копирование искомого IP во внутренний буфер поиска ---
    XCHG                            ; DE = Адрес входного IP-адреса хоста
    LXI  H, ARP_SEARCH_IP           ; HL = Целевой буфер ядра
    
    MVI  A, 4                       ; Копируем ровно 4 байта IPv4
arp_copy_ip_loop:
    LDAX D
	MOV M, A
    INX  D
	INX  H
    DCR  A
    JNZ  arp_copy_ip_loop

    ; --- ШАГ 2: Инициализация итераторов сканирования ARP_SYS_TABLE ---
    XRA  A
    STA  ARP_LOOP_COUNTER          ; Начинаем поиск со слота №0
    LXI  H, ARP_SYS_TABLE
    SHLD ARP_TEMP_STRUCT           ; Зафиксировали начальный адрес таблицы

arp_scan_slot_start:
    LHLD ARP_TEMP_STRUCT           ; HL указывает на поле A_ARP_STATUS текущего слота
    MOV  A, M                      ; Читаем статус записи
    CPI  01h                       ; Запись валидна (VALID)?
    JNZ  arp_next_slot             ; Если слот свободен или в PENDING — пропускаем его

    ; --- ШАГ 3: Побайтовое сравнение 4-байтовых IP-адресов на КР580ВМ80А ---
    INX  H                         ; HL указывает на первый байт IPv4 в таблице (+1)
    LXI  D, ARP_SEARCH_IP          ; DE указывает на искомый IP-адрес
    MVI  B, 4                      ; Нам нужно верифицировать все 4 байта

arp_compare_ip_loop:
    LDAX D                         ; A = Искомый байт IP
    CMP  M                         ; Сравниваем с байтом из таблицы
    JNZ  arp_compare_miss          ; Если хоть один байт не совпал — промах по этому слоту!
    
    INX  D                         ; Сдвиг указателя искомого IP
    INX  H                         ; Сдвиг указателя таблицы
    DCR  B                         ; Уменьшаем счетчик байт
    JNZ  arp_compare_ip_loop       ; Продолжаем сравнение оставшихся байт

    ; --- УСПЕШНЫЙ HIT КЭША: IP-АДРЕС СОВПАЛ НА 100% ---
    ; В этот момент пара HL указывает на поле A_ARP_MAC (+5) текущего слота!
    ; Возвращаем этот адрес вызывающему сетевому драйверу
    MVI  A, 00h                    ; Код результата = 00H (Чистый успех разрешения)
    RET                            ; Выход из подпрограммы, HL хранит адрес MAC-адреса

arp_compare_miss:
    ; Сюда прыгаем, если IP не совпал. Нам нужно восстановить оригинальный указатель HL
    ; на начало текущей структуры, чтобы корректно рассчитать шаг до следующего слота.

arp_next_slot:
    LHLD ARP_TEMP_STRUCT           ; HL указывает на старт текущей структуры
    LXI  D, ARP_STRUCT_SIZE        ; Загружаем размер шага = 12 байт
    DAD  D                         ; HL указывает на начало СЛЕДУЮЩЕГО слота таблицы
    SHLD ARP_TEMP_STRUCT           ; Зафиксировали его адрес

    LDA  ARP_LOOP_COUNTER
    INR  A
    STA  ARP_LOOP_COUNTER
    CPI  MAX_ARP_ENTRIES           ; Проверили все 4 системных слота?
    JNZ  arp_scan_slot_start       ; Если нет — уходим на проверку следующего хоста

    ; --- ШАГ 4: КРИТИЧЕСКИЙ ПРОМАХ КЭША (ARP MISS) — АСИНХРОННЫЙ СОН ---
    ; [Ядро через порты NET_CMD_PORT/NET_DATA_PORT формирует и отправляет в кабель 
    ; аппаратный пакет-запрос ARP Request для чипа KSZ8851-6MLL на широковещательный MAC]
    
    ; Переводим текущий пишущий процесс в состояние ST_WAITING (04H)
    CALL get_current_pcb_address   ; HL = Базовый адрес PCB пишущей задачи
    PUSH H                         ; Стак: Сохранили базу PCB (строго 'H')
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING             ; Усыпляем поток
    
    POP  H                         ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_NET_ARP              ; Назначаем wait-channel = W_NET_ARP (0Eh)

    ; Консервируем SP супервизора в O_SAVED_SP текущей задачи
    POP  H
    LXI  D, O_SAVED_SP
    DAD  D
    XCHG                           ; DE = Указатель на ячейку O_SAVED_SP в PCB
    LXI  H, 0
    DAD  SP                        ; HL = Текущий системный SP ядра
    MOV  A, L
	STAX D
	INX D
    MOV  A, H
	STAX D             ; Записали 16-битный SP в PCB

    ; Вызываем планировщик для переключения на другую READY задачу
    CALL scheduler                 
    STA  NEXT_PID
    CALL switch_mmu_context        ; Аппаратно перепрошиваем окна eMMU (К555ИР26)
    JMP  restore_timer_context     ; Покидаем контекст, отдавая такты ЦП

arp_wakeup_entry:
    ; =========================================================================
    ; ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ ПОСЛЕ АППАРАТНОГО ОТВЕТА ARP REPLY
    ; =========================================================================
    ; Когда удаленный хост пришлет ответ по Ethernet кабелю, наше прерывание IRQ3
    ; (`hw_net_isr`) вычитает его MAC, запишет в ARP_SYS_TABLE, выставит слоту 
    ; статус VALID (01h) и переведет эту задачу в ST_READY. 
    ; Планировщик вернет нас строго сюда через RET!
    ; =========================================================================
    JMP  k_arp_resolve             ; Делаем повторный честный проход по кэшу (теперь HIT!)


; =============================================================================
; ПОДПРОГРАММА ИНИЦИАЛИЗАЦИИ И СТАРТА ВНЕШНЕГО ПДП-КОНТРOЛЛЕРА КР580ВТ57
; =============================================================================
dma_start_ch0_transfer:
    ; Вход: HL = Физический адрес буфера в ОЗУ Ядра (например, IDE_SYS_BUFFER)
    ;       BC = Количество байт для передачи (512 или 16384)
    ;       D  = Направление: 00H = Чтение (CF -> ОЗУ), 01H = Запись (ОЗУ -> CF)
    ; Выход: Канал 0 ПДП настроен и взведен, ожидает аппаратного запроса DRQ0 от CF.

    ; Физическая особенность КР580ВТ57: Запись 16-битных регистров адреса и счетчика
    ; выполняется последовательно (сначала младший байт, затем старший) в один порт.
    ; Внутренний триггер байт ВТ57 сбрасывается при любой записи в порт команд/статуса.
    XRA  A
    OUT  DMA_CMD_STATUS         ; Сбросили внутренний триггер указателя байт ВТ57

    ; --- ШАГ 1: Прошивка базового адреса ОЗУ Ядра в Канал 0 ---
    MOV  A, L
    OUT  DMA_CH0_ADDR           ; Отправили младший байт физического адреса ОЗУ
    MOV  A, H
    OUT  DMA_CH0_ADDR           ; Отправили старший байт физического адреса ОЗУ

    ; --- ШАГ 2: Прошивка счетчика байт и направления в Канал 0 ---
    ; ВТ57 требует загрузки значения (Количество_Байт - 1). 
    ; Дополнительно старшие 2 бита счетчика (биты 14-15) задают тип цикла:
    ; Бит 15 = 0, Бит 14 = 0 -> Цикл проверки (Verify DMA)
    ; Бит 15 = 0, Бит 14 = 1 -> Цикл чтения (ПДП-Чтение, данные из памяти в периферию)
    ; Бит 15 = 1, Бит 14 = 0 -> Цикл записи (ПДП-Запись, данные из периферии в память)
    
    ; Вычитаем 1 из 16-битного счетчика BC
    DCX  B                      
    
    MOV  A, D
    CPI  01H                    ; Запрос на запись (ОЗУ -> CF)?
    JZ   dma_setup_write_mode   ; Если да — накладываем маску ПДП-Чтения из памяти

    ; Настройка для ПДП-Записи в память (Чтение с CompactFlash: CF -> ОЗУ)
    ; Нам нужно взвести Бит 15 в старшем байте счетчика. Маска: 10000000B (0x80)
    MOV  A, B
    ORI  080H                   ; Взвели бит 15 (ПДП-Запись в ОЗУ)
    MOV  B, A
    JMP  dma_program_counter

dma_setup_write_mode:
    ; Настройка для ПДП-Чтения из памяти (Запись на CompactFlash: ОЗУ -> CF)
    ; Нам нужно взвести Бит 14 в старшем байте счетчика. Маска: 040H (01000000B)
    MOV  A, B
    ORI  040H                   ; Взвели бит 14 (ПДП-Чтение из ОЗУ)
    MOV  B, A

dma_program_counter:
    MOV  A, C
    OUT  DMA_CH0_COUNT          ; Отправили младший байт счетчика циклов ПДП
    MOV  A, B
    OUT  DMA_CH0_COUNT          ; Отправили старший байт счетчика + биты режима

    ; --- ШАГ 3: Активация Канала 0 и запуск автомата ПДП ---
    MVI  A, DMA_MODE_CONFIG     ; Включаем расширенную запись и активируем Канал 0
    OUT  DMA_CMD_STATUS         ; Внешний модуль ПДП официально ВЗВЕДЕН!

    RET                         ; Возврат в вызывающую функцию ядра

; =============================================================================
; ФУНКЦИЯ ЯДРА: ЗАХВАТ МОНОПОЛЬНОГО ДОСТУПА К COMPACTFLASH
; =============================================================================
cf_acquire_lock:
    ; Вход: Параметры отсутствуют. Вызывается внутри системных функций Ring 0.
    ; Выход: Управление возвращается ТОЛЬКО когда диск гарантированно свободен и захвачен.
    
    LDA  CF_LOCK_VARIABLE
    CPI  00H
    JZ   cf_lock_success        ; Если 00H — диск свободен, мгновенно захватываем его

    ; --- ДИСК ЗАНЯТ — УСЫПЛЯЕМ ВЫЗЫВАЮЩИЙ ПРОЦЕСС ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущей задачи
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING          ; Переводим процесс в статус "Ожидание"
    
    POP  H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_CF_BUSY           ; Код сна = 05H (Ожидание освобождения диска)

    ; Сбрасываем стек супервизора и переключаем CPU на другую готовую задачу
    LXI  SP, KERNEL_STACK_TOP
    CALL scheduler
    STA  NEXT_PID
    CALL switch_mmu_context
    JMP  restore_timer_context  ; Уходим в контекст другой задачи.
                                ; Когда процесс проснется, он начнет сисколл с начала 
                                ; и снова проверит cf_acquire_lock!

cf_lock_success:
    MVI  A, 0FFH
    STA  CF_LOCK_VARIABLE       ; Выставили маркер 0xFF — диск монопольно занят нами
    RET

; =============================================================================
; ФУНКЦИЯ ЯДРА: ОСВОБОЖДЕНИЕ ДОСТУПА И ПОБУДКА ОЧЕРЕДИ
; =============================================================================
cf_release_lock:
    ; Вход: Параметры отсутствуют. Вызывается по окончании DMA (в IR2) или PIO обмена.
    ; Выход: Переменная сброшена, спавшие процессы возвращены в ST_READY.
    
    XRA  A
    STA  CF_LOCK_VARIABLE       ; Сбросили защелку в 00H — диск свободен!

    ; Сканируем PCB_TABLE и будим ВСЕ процессы, которые спали на коде ожидания диска 05H
    LXI  H, PCB_TABLE
    MVI  E, 0                   ; E = Текущий PID (0..254)

cf_wakeup_loop:
    PUSH H                      ; Спасаем базу текущего PCB в стек (8080)
    LXI  D, O_STATUS
    DAD  D
    MOV  A, M
    CPI  ST_WAITING             ; Задача спит?
    JNZ  cf_wakeup_next
    
    POP  H                      ; Восстановили HL на начало PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MOV  A, M
    CPI  W_CF_BUSY              ; Задача ждала именно освобождения CompactFlash (05H)?
    JNZ  cf_wakeup_next
    
    ; Задача найдена! Переводим её в состояние готовности к исполнению
    POP  H
    PUSH H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY            ; Теперь планировщик сможет её выбрать!

cf_wakeup_loop_next:
cf_wakeup_next:
    POP  H                      ; Восстановили HL на начало PCB
    LXI  D, PCB_SIZE
    DAD  D                      ; Сдвиг на следующий PCB (64 байта)
    INR  E
    MOV  A, E
    CPI  MAX_PROCS + 1          ; Проверили все 254 процесса?
    JNZ  cf_wakeup_loop
    
    RET

; =============================================================================
; ПОДПРОГРАММА ПЛАНИРОВЩИКА ЯДРА: ПЕРЕВОД ПОТОКА В СОСТОЯНИЕ ОЖИДАНИЯ ВВОДА-ВЫВОДА
; =============================================================================
; Вход:  Вызывается через CALL из драйверов диска.
;        В стеке ядра автоматически лежит адрес возврата (RET) в драйвер IDE.
; Выход: Текущая задача заблокирована. Управление передано другой READY-задаче.
;        При пробуждении выполнение продолжится со следующей после CALL инструкции.
; =============================================================================
; Вход:  Вызывается через CALL из драйверов диска.
;        В KERNEL_STACK автоматически лежит 16-битный адрес возврата (RET) в драйвер.
; Выход: Текущий процесс заблокирован в ST_WAITING. Планировщик передает CPU 
;        лучшей READY-задаче, eMMU переключает окна. При пробуждении по TC
;        выполнение возвращается строго на инструкцию RET ниже.
; =============================================================================

; --- КОД ПРИЧИНЫ СНА (ДЛЯ ПОЛЯ O_WAIT_CODE) ---
W_CF_BUSY_IO      EQU 05h           ; Код причины сна: поток заблокирован на I/O CompactFlash

vfs_sleep_on_io:
    ; --- ШАГ 1: Изменение POSIX-статуса жизненного цикла на ST_WAITING (04H) ---
    CALL get_current_pcb_address    ; HL = Базовый 16-битный адрес PCB текущего процесса
    PUSH H                          ; Спасаем базовый адрес PCB в стек ядра
    
    LXI  D, O_STATUS                ; Официальное смещение статуса (2)
    DAD  D                          ; HL указывает точно на поле O_STATUS
    MVI  M, ST_WAITING              ; Утверждено: переводим процесс в статус ожидания диска
    
    ; --- ШАГ 2: Фиксация причины сна (wait-channel) в поле O_WAIT_CODE ---
    POP  H                          ; Восстановили базовый адрес PCB
    PUSH H                          ; Снова припрятали его
    LXI  D, O_WAIT_CODE             ; Официальное смещение кода ожидания (40)
    DAD  D                          ; HL указывает точно на поле O_WAIT_CODE
    MVI  M, W_CF_BUSY_IO            ; Записываем код причины сна (05h)

    ; --- ШАГ 3: Сохранение текущего системного SP ядра в поле O_SAVED_SP ---
    ; Так как мы вошли сюда через CALL из драйвера, в KERNEL_STACK лежит адрес возврата.
    ; Мы сохраняем этот SP, чтобы проснуться ровно на точке выхода из этой функции.
    POP  H                          ; Восстановили базовый адрес PCB
    LXI  D, O_SAVED_SP              ; Официальное смещение сохраненного стека (3)
    DAD  D                          ; HL указывает точно на поле O_SAVED_SP
    XCHG                            ; DE = Указатель на поле O_SAVED_SP внутри структуры PCB
    
    LXI  H, 0                       
    DAD  SP                         ; HL = Текущее физическое значение SP ядра процессора
    
    MOV  A, L
    STAX D                          ; Записали младший байт SP ядра в PCB
    INX  D
    MOV  A, H
    STAX D                          ; Записали старший байт SP ядра в PCB

    ; --- ШАГ 4: Запуск вашего оригинального математического планировщика ---
    ; Функция scheduler начислит штраф, просканирует таблицу по алгоритму
    ; STATIC + NICE + CPU_TICKS, пропустит наш спящий процесс (так как он ST_WAITING)
    ; и вернет PID лучшего кандидата в аккумуляторе A.
    CALL scheduler                  ; Выбор лучшей READY задачи, результат в A
    
    ; --- ШАГ 5: Аппаратное переключение контекста памяти eMMU ---
    ; Ваша функция switch_mmu_context принимает целевой PID строго в ячейке NEXT_PID.
    ; Она обновит CURRENT_PID, выставит новому процессу статус ST_RUNNING 
    ; и физически прошьет 4 регистра К555ИР26 через порты 0x70-0x73.
    STA  NEXT_PID                   ; Сохраняем выбранный PID для вашей функции
    CALL switch_mmu_context         ; АППАРАТНОЕ ИАТОМАРНОЕ ПЕРЕКЛЮЧЕНИЕ СТРАНИЦ eMMU

    ; --- ШАГ 6: Аппаратный перенос SP процессора на стек оживающей задачи ---
    ; Теперь мы находимся внутри адресного пространства нового проснувшегося процесса!
    ; Вычисляем адрес его PCB, чтобы забрать его сохраненный стек.
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB новой задачи (CURRENT_PID уже обновлен!)
    LXI  D, O_SAVED_SP              ; Смещение 3
    DAD  D                          ; HL указывает на ячейку O_SAVED_SP оживающего процесса
    
    MOV  E, M                       ; Читаем младший байт сохраненного SP
    INX  H
    MOV  D, M                       ; DE = Восстановленный SP новой задачи
    
    XCHG                            ; HL = Новый SP
    SPHL                            ; АППАРАТНАЯ ИНСТРУКЦИЯ 8080: Стек процессора переброшен!

    ; --- ШАГ 7: Развертывание контекста и пробуждение ---
    ; В зависимости от того, как оживающий процесс ранее потерял процессор:
    ; 1. Если его вытеснил таймер hw_timer_entry: его SP указывает на USER SPACE,
    ;    на вершине лежат регистры Ring 3, а за ними - пользовательский RET.
    ;    Команды POP и RET в конце таймера бесшовно выкинут его в Ring 3.
    ; 2. Если это проснувшийся дисковый процесс: его SP указывает на KERNEL_STACK,
    ;    а на вершине лежит адрес инструкции RET, расположенной прямо под этой строкой!
    POP  H                          ; Восстановили HL оживающей задачи
    POP  D                          ; Восстановили DE оживающей задачи
    POP  B                          ; Восстановили BC оживающей задачи
    POP  PSW                        ; Восстановили Аккумулятор и Флаги
    
vfs_sleep_wakeup_entry:
    ; Сюда процессор прыгает аппаратно по инструкции RET, если проснулся дисковый поток!
    RET                             ; ЧЕСТНЫЙ И ОБЯЗАТЕЛЬНЫЙ ВОЗВРАТ В ДРАЙВЕР ДИСКА CF!

; =============================================================================
; ПОДПРОГРАММА ЯДРА: ПРОБУЖДЕНИЕ ЗАБЛОКИРОВАННОГО НА ВВОДЕ-ВЫВОДЕ ПРОЦЕССА
; =============================================================================
; Вход:  Вызывается из аппаратного обработчика прерывания hw_dma_tc_entry.
;        Указатель CURRENT_PCB_PTR содержит адрес PCB разблокируемой задачи.
; Выход: Статус процесса изменен на ST_READY (02H), wait-channel очищен в 00h.
;        Контекст регистров прерванного потока полностью сохраняется.
; =============================================================================
posix_wake_up_blocked_process:
    ; --- ШАГ 1: Сохранение регистров, которые будут использованы функцией ---
    PUSH H                          ; Спасаем регистровую пару HL
    PUSH D                          ; Спасаем регистровую пару DE

    ; Прерывания уже заблокированы (DI) на входе в ISR hw_dma_tc_entry,
    ; поэтому мы находимся в безопасной критической секции ядра Ring 0.

    ; --- ШАГ 2: Сброс причины сна (wait-channel) в поле O_WAIT_CODE ---
    LHLD CURRENT_PCB_PTR            ; HL = Базовый 16-битный адрес текущего 64-байтового PCB
    LXI  D, O_WAIT_CODE             ; DE = Официальное смещение поля причины сна (40)
    DAD  D                          ; HL теперь указывает точно на поле O_WAIT_CODE в ОЗУ ядра
    MVI  M, 00h                     ; Очищаем код ожидания (00h = процесс больше ничего не ждет)

    ; --- ШАГ 3: Изменение статуса жизненного цикла на ST_READY ---
    LHLD CURRENT_PCB_PTR            ; HL = Восстановили базовый адрес PCB задачи
    LXI  D, O_STATUS                ; DE = Официальное смещение поля статуса (2)
    DAD  D                          ; HL теперь указывает точно на поле O_STATUS внутри PCB
    
    ; Утверждено: Переводим процесс из ST_WAITING (04H) в состояние ST_READY (02H)
    MVI  M, ST_READY                ; Записываем код готовности (02h) в дескриптор.
                                    ; Теперь при следующем проходе планировщик увидит эту 
                                    ; задачу и сможет вернуть ей управление.

    ; --- ШАГ 4: Восстановление контекста и выход ---
    POP  D                          ; Восстановили DE
    POP  H                          ; Восстановили HL текущего потока, который был прерван ПДП
    RET                             ; Возврат в тело аппаратного диспетчера прерываний

; =============================================================================
; ПОДПРОГРАММА ЯДРА Ring 0: АСИНХРОННАЯ ДОСТАВКА СИГНАЛА ТЕКУЩЕМУ ПРОЦЕССУ
; =============================================================================
; Вход:  A = Номер POSIX-сигнала (например, 0Bh = SIGBUS)
;        CURRENT_PCB_PTR хранит базовый адрес PCB целевой задачи.
; Выход: В поле O_SIG_PENDING дескриптора процесса взведен соответствующий бит.
;        Все регистры, кроме аккумулятора, строго сохраняются.
; =============================================================================
posix_signal_current_process:
    PUSH H                          ; Спасаем контекст регистров текущего потока
    PUSH B
    PUSH D

    MOV  C, A                       ; C = Номер сигнала (флаг сдвига, 1..16)
    
    ; --- ШАГ 1: Генерация 16-битной битовой маски из номера сигнала ---
    ; Сигнал 1 — это бит 0, Сигнал 2 — бит 1, ..., Сигнал 11 (SIGBUS) — бит 10.
    DCR  C                          ; Сдвигаем номер на -1, чтобы получить индекс бита (0..15)
    
    LXI  H, 0001h                   ; Стартовая маска: взведен самый младший 0-й бит
    MOV  A, C                       ; Проверяем, нужен ли сдвиг
    ORA  A
    JZ   sig_mask_ready             ; Если индекс бита = 0, маска 0001h готова

sig_shift_loop:
    DAD  H                          ; Сдвиг HL влево на 1 бит (умножение маски на 2)
    DCR  C
    JNZ  sig_shift_loop             ; Крутим цикл, пока не сформируем нужную маску в HL

sig_mask_ready:
    ; Теперь в паре HL лежит готовая 16-битная маска сигнала (для SIGBUS это 0400h)
    XCHG                            ; Переносим маску сигнала в пару DE (D=High, E=Low)

    ; --- ШАГ 2: Наложение маски на поле O_SIG_PENDING внутри PCB ---
    LHLD CURRENT_PCB_PTR            ; HL = Базовый 16-битный адрес 64-байтового PCB задачи
    PUSH H                          ; Сохраняем базу PCB в стек ядра
    
    LXI  B, O_SIG_PENDING           ; Смещение до маски ожидающих сигналов (32)
    DAD  B                          ; HL теперь указывает точно на младший байт поля O_SIG_PENDING
    
    ; Атомарно накладываем маску по ИЛИ (OR) на текущие ожидающие сигналы
    ; Младший байт:
    MOV  A, M                       ; Прочитали текущий младший байт сигналов из PCB
    ORA  E                          ; Наложили младший байт нашей маски
    MOV  M, A                       ; Сохранили обратно в PCB
    
    ; Старший байт:
    INX  H                          ; Переходим к старшему байту поля O_SIG_PENDING
    MOV  A, M                       ; Прочитали старший байт сигналов
    ORA  D                          ; Наложили старший байт нашей маски (для SIGBUS прилетит сюда)
    MOV  M, A                       ; Сохранили обратно в PCB

    ; --- ШАГ 3: Пробуждение процесса (ИЗМЕНЕНИЕ ДЛЯ АСИНХРОННОСТИ) ---
    ; Так как процесс мог спать на диске в состоянии ST_WAITING (04H), принудительный 
    ; прилет сигнала (особенно фатального SIGBUS) обязан перевести его в состояние готовности,
    ; чтобы процесс проснулся и обработал ошибку шины.
    POP  H                          ; Восстановили базовый адрес PCB в HL
    LXI  B, O_STATUS                ; Смещение до статуса жизненного цикла (2)
    DAD  B                          ; HL указывает на O_STATUS
    MOV  A, M
    CPI  ST_WAITING                 ; Процесс спал в ожидании I/O?
    JNZ  sig_exit                   ; Если не спал — просто выходим
    
    MVI  M, ST_READY                ; Насильно будим его, чтобы планировщик его вызвал!
    
    ; Очищаем код причины сна, так как мы прервали его сигналом ошибки
    LHLD CURRENT_PCB_PTR
    LXI  B, O_WAIT_CODE
    DAD  B
    MVI  M, 00h                     ; Очистили wait-channel

sig_exit:
    POP  D                          ; Полное восстановление контекста перед возвратом
    POP  B
    POP  H
    RET                             ; Возврат в вызвавший обработчик hw_dma_tc_entry

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ mutex()
; =============================================================================
sys_mutex:
    ; Прерывания уже централизованно заблокированы на входе в sys_call_entry!

    ; --- ШАГ 1: Валидация входных параметров ---
    MOV  A, D
    CPI  MAX_SYS_MUTEX          ; Проверяем границы ID мьютекса (0..7)
    JNC  mutex_err_inval        ; Если ID >= 8 — возвращаем EINVAL
    
    STA  MUTEX_TEMP_ID          ; Сохранили ID мьютекса
    MOV  A, E
    STA  MUTEX_TEMP_CMD         ; Сохранили команду (LOCK/UNLOCK)

    ; Вычисляем физический адрес структуры мьютекса: MUTEX_SYS_TABLE + (ID * 4)
    LDA  MUTEX_TEMP_ID
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H             ; HL = ID * 4 (Быстрое умножение через два DAD H)
    LXI  D, MUTEX_SYS_TABLE
    DAD  D                      ; HL указывает точно на структуру нужного мьютекса
    SHLD MUTEX_TEMP_PCB         ; Зафиксировали указатель на структуру мьютекса

    ; Проверяем команду
    LDA  MUTEX_TEMP_CMD
    CPI  MUTEX_CMD_LOCK
    JZ   mutex_op_lock          ; Переходим к логике ЗАХВАТА
    CPI  MUTEX_CMD_UNLOCK
    JZ   mutex_op_unlock        ; Переходим к логике ОСВОБОЖДЕНИЯ
    
    JMP  mutex_err_inval        ; Неизвестная команда — возвращаем EINVAL

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ LOCK: ПОПЫТКА ЗАХВАТА МЬЮТЕКСА
; -----------------------------------------------------------------------------
mutex_op_lock:
    LHLD MUTEX_TEMP_PCB         ; HL указывает на поле O_MUTEX_STATE (+0)
    MOV  A, M                   ; Читаем текущий статус мьютекса
    CPI  00h                    ; Мьютекс свободен?
    JZ   mutex_lock_success     ; Если свободен — мгновенно захватываем!

    ; --- ШАГ 2: МЬЮТЕКС ЗАНЯТ — АСИНХРОННЫЙ СОН ТЕКУЩЕГО ПРОЦЕССА ---
    ; Запоминаем текущий PID в очередь ожидания мьютекса (поле O_MUTEX_WAITPID = +2)
    INX  H
	INX  H             ; HL указывает на поле O_MUTEX_WAITPID
    LDA  CURRENT_PID            ; Наш PID
    MOV  M, A                   ; Прописали себя в очередь ожидания этого мьютекса

    ; Переводим текущий процесс в статус асинхронного сна ST_WAITING (04H)
    CALL get_current_pcb_address ; HL = Адрес PCB текущего процесса
    PUSH H                      ; Стак: Сохранили базу PCB (строго 'H')
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING          ; Перевели процесс в состояние сна
    
    POP  H                      ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_MUTEX             ; Причина сна — блокировка на системном мьютексе (09h)

    ; Консервируем текущий SP ядра в PCB для точки пробуждения
    POP  H                      ; HL = Базовый адрес PCB
    LXI  D, O_SAVED_SP
    DAD  D
    XCHG                        ; DE = Указатель на ячейку O_SAVED_SP в PCB
    
    LXI  H, 0
    DAD  SP                     ; HL = Физическое значение текущего SP ядра
    MOV  A, L
	STAX D
	INX D
    MOV  A, H
	STAX D          ; Записали 16-битный SP в структуру PCB

    ; Передаем управление планировщику (Мьютекс заблокирован, отдаем CPU другим!)
    CALL scheduler              ; Выбирает лучший READY-процесс в аккумулятор A
    STA  NEXT_PID
    CALL switch_mmu_context     ; Перепрошиваем окна К555ИР26 под новую задачу
    
    JMP  restore_timer_context  ; Уходим в контекст другой задачи.

mutex_lock_wakeup_entry:
    ; =========================================================================
    ; ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ ПРОЦЕССА НА МЬЮТЕКСЕ
    ; =========================================================================
    ; Когда владелец освободит мьютекс, он переведет нас в ST_READY.
    ; Планировщик вернет нас строго сюда через RET!
    ; =========================================================================
    JMP  mutex_op_lock          ; Проснулись — делаем повторную честную попытку захвата!

mutex_lock_success:
    ; Мьютекс был свободен. Захватываем его атомарно!
    MVI  M, 01h                 ; O_MUTEX_STATE = 01h (Мьютекс заблокирован)
    INX  H                      ; HL указывает на поле O_MUTEX_OWNER (+1)
    LDA  CURRENT_PID
    MOV  M, A                   ; Прописали свой PID как официального владельца

    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET                         ; Выход в syscall_return

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ UNLOCK: ОСВОБОЖДЕНИЕ МЬЮТЕКСА
; -----------------------------------------------------------------------------
mutex_op_unlock:
    LHLD MUTEX_TEMP_PCB         ; HL указывает на поле O_MUTEX_STATE (+0)
    INX  H                      ; HL указывает на поле O_MUTEX_OWNER (+1)
    MOV  A, M                   ; A = PID владельца мьютекса
    MOV  B, A                   
    LDA  CURRENT_PID            ; A = PID текущего процесса, который делает UNLOCK
    CMP  B                      ; Проверяем права: только владелец имеет право разлочить мьютекс!
    JNZ  mutex_err_eperm        ; Если PID не совпал — возвращаем ошибку EPERM (-2)

    ; Освобождаем мьютекс
    DCX  H                      ; HL указывает обратно на O_MUTEX_STATE (+0)
    MVI  M, 00h                 ; O_MUTEX_STATE = 00h (Мьютекс официально СВОБОДЕН)
    INX  H                      
    MVI  M, 00h                 ; O_MUTEX_OWNER = 00h (Владельца больше нет)

    ; --- ШАГ 3: РЕАКТИВНОЕ ПРОБУЖДЕНИЕ ОЧЕРЕДИ ИЗ ТАБЛИЦЫ PCB ---
    INX  H                      ; HL указывает на поле O_MUTEX_WAITPID (+2)
    MOV  A, M                   ; A = PID процесса, который уснул в ожидании этого мьютекса
    CPI  00h                    ; В очереди ожидания кто-то есть?
    JZ   mutex_unlock_success   ; Если очередь пуста — будить некого, чистый выход!
    
    MOV  C, A                   ; C = Запоминаем PID разблокируемого процесса-читателя/писателя
    MVI  M, 00h                 ; Очищаем поле очереди O_MUTEX_WAITPID = 00h

    ; Переводим спящий процесс из ST_WAITING в готовность ST_READY (02H)
    MOV  L, C
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
    DAD  H
	DAD  H
	DAD  H    ; HL = target_wait_pid * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = Базовый адрес PCB просыпающегося процесса
    
    PUSH H                      ; Спасся адрес PCB целевой задачи
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY            ; Процесс официально разбужен для вашего шедуллера!
    
    POP  H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, 00h                 ; Сбросили код сна wait-channel в чистый ноль

mutex_unlock_success:
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET                         ; Выход в syscall_return

; -----------------------------------------------------------------------------
; БЛОКИ ВЫХОДА ПО СИСТЕМНЫМ ОШИБКАМ (POSIX STANDARDS)
; -----------------------------------------------------------------------------
mutex_err_inval:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EINVAL — Неверный ID или команда)
    RET

mutex_err_eperm:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX EPERM — Операция запрещена, вы не владелец)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ condition_variable()
; =============================================================================
sys_cond:
    ; Прерывания уже заблокированы (DI) центральным диспетчером sys_call_entry!

    ; --- ШАГ 1: Валидация входных параметров ---
    MOV  A, D
    CPI  MAX_SYS_COND           ; Проверяем границы ID (0..7)
    JNC  cond_err_inval         ; Если ID >= 8 — возвращаем EINVAL
    
    STA  COND_TEMP_ID           ; Сохранили ID переменной
    MOV  A, E
    STA  COND_TEMP_CMD          ; Сохранили команду (WAIT/SIGNAL)
    MOV  A, C
    STA  COND_TEMP_MUTEX        ; Сохранили ID связанного мьютекса

    ; Вычисляем адрес структуры: COND_SYS_TABLE + (ID * 4)
    LDA  COND_TEMP_ID
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H             ; HL = ID * 4
    LXI  D, COND_SYS_TABLE
    DAD  D                      ; HL указывает на структуру нужной условной переменной
    SHLD COND_TEMP_STRUCT       ; Зафиксировали указатель

    ; Ветвление по командам
    LDA  COND_TEMP_CMD
    CPI  COND_CMD_WAIT
    JZ   cond_op_wait           ; Переходим к логике ОЖИДАНИЯ
    CPI  COND_CMD_SIGNAL
    JZ   cond_op_signal         ; Переходим к логике СИГНАЛА
    
    JMP  cond_err_inval         ; Неизвестная команда — возвращаем EINVAL

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ WAIT: АТОМАРНОЕ ВЫСВОБОЖДЕНИЕ МЬЮТЕКСА И УХОД В АСИНХРОННЫЙ СОН
; -----------------------------------------------------------------------------
cond_op_wait:
    ; По канону POSIX, перед тем как уснуть на cond-переменной, мы обязаны 
    ; высвободить мьютекс, который сейчас удерживает текущий процесс.
    ; Вызываем наш ранее отлаженный системный вызов sys_mutex для операции UNLOCK!
    LDA  COND_TEMP_MUTEX
    MOV  D, A                   ; D = ID мьютекса
    MVI  E, MUTEX_CMD_UNLOCK    ; E = Команда UNLOCK (01h)
    
    ; Сохраняем BC и HL текущей задачи, так как сисколл мьютекса может их изменить
    PUSH B
    PUSH H
    CALL sys_mutex              ; Выполняем честный атомарный UNLOCK мьютекса
    POP  H
    POP  B

    ; Теперь прописываем текущий PID в очередь ожидания этой условной переменной
    LHLD COND_TEMP_STRUCT
    LXI  D, O_COND_WAITPID      ; Смещение до поля хранения PID (+1)
    DAD  D                      ; HL указывает на поле O_COND_WAITPID в структуре cond
    LDA  CURRENT_PID
    MOV  M, A                   ; Зафиксировали, что текущая задача спит именно здесь

    ; Переводим процесс в статус асинхронного сна ST_WAITING (04H)
    CALL get_current_pcb_address ; HL = Базовый адрес PCB нашего процесса
    PUSH H                      ; Стак: Сохранили базу PCB (строго 'H')
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING          ; Усыпляем процесс
    
    POP  H                      ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_COND              ; Причина сна — блокировка на условной переменной (0Ah)

    ; Консервируем текущий SP ядра в PCB для точки пробуждения
    POP  H                      ; HL = Базовый адрес PCB
    LXI  D, O_SAVED_SP
    DAD  D
    XCHG                        ; DE = Указатель на ячейку O_SAVED_SP в PCB
    
    LXI  H, 0
    DAD  SP                     ; HL = Физическое значение текущего SP ядра
    MOV  A, L
	STAX D
	INX D
    MOV  A, H
	STAX D          ; Записали 16-битный SP в структуру PCB

    ; Переключаем CPU на другую готовую задачу
    CALL scheduler              ; Выбирает лучший READY-процесс
    STA  NEXT_PID
    CALL switch_mmu_context     ; Перепрошиваем окна К555ИР26 под новую задачу
    JMP  restore_timer_context  ; Уходим в контекст новой задачи.

cond_wait_wakeup_entry:
    ; =========================================================================
    ; ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ ПРОЦЕССА НА CONDITION VARIABLE
    ; =========================================================================
    ; Когда другой процесс сделает SIGNAL, планировщик вернет нас строго сюда!
    ; По стандарту POSIX, проснувшийся поток обязан СНОВА захватить свой мьютекс,
    ; перед тем как продолжить выполнение в критической секции юзерспейса Ring 3!
    ; =========================================================================
    LDA  COND_TEMP_MUTEX
    MOV  D, A                   ; D = ID мьютекса
    MVI  E, MUTEX_CMD_LOCK      ; E = Команда LOCK (00h)
    
    PUSH B
    PUSH H
    CALL sys_mutex              ; Выполняем повторный честный LOCK мьютекса.
                                ; Если мьютекс занят — эта функция сама усыпит нас снова!
    POP  H
    POP  B
    
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET                         ; Выход в syscall_return. Процесс возвращается в юзерспейс.

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ SIGNAL: ПРОБУЖДЕНИЕ ОДНОГО ПОТОКА ИЗ ОЧЕРЕДИ СЛOTА COND
; -----------------------------------------------------------------------------
cond_op_signal:
    LHLD COND_TEMP_STRUCT
    LXI  D, O_COND_WAITPID      ; Смещение до поля PID (+1)
    DAD  D
    MOV  A, M                   ; A = PID процесса, который уснул на этой cond-переменной
    CPI  00h                    ; В очереди ожидания кто-то есть?
    JZ   cond_signal_success    ; Если очередь пуста — будить некого, чистый выход!
    
    MOV  C, A                   ; C = Запоминаем PID разблокируемого процесса
    MVI  M, 00h                 ; Очищаем поле очереди в структуре cond (Слот свободен)

    ; Вычисляем адрес PCB просыпающегося процесса: PCB_TABLE + (PID * 64)
    MOV  L, C
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
    DAD  H
	DAD  H
	DAD  H    ; HL = target_pid * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = Базовый адрес PCB просыпающегося процесса
    
    PUSH H                      ; Спасся адрес PCB целевой задачи
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY            ; ПРОЦЕСС ОФИЦИАЛЬНО РАЗБУЖЕН ДЛЯ ШЕДУЛЛЕРА!
    
    POP  H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, 00h                 ; Сбросили код сна wait-channel в чистый ноль

cond_signal_success:
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET                         ; Выход в syscall_return

; -----------------------------------------------------------------------------
; БЛОКИ ВЫХОДА ПО СИСТЕМНЫМ ОШИБКАМ
; -----------------------------------------------------------------------------
cond_err_inval:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EINVAL)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ sem_wait()
; =============================================================================
; Вход:  D = Идентификатор (ID) системного семафора (0..7).
; Выход: A = 00H — Успех, ресурс успешно захвачен / декрементирован.
;        A = 0FFH — Ошибка EINVAL (Неверный ID семафора).
; =============================================================================
sys_sem_wait:
    ; Проверка границ ID семафора (0..7)
    MOV  A, D
    CPI  MAX_SYS_SEMS
    JNC  sem_wait_err_inval     ; ИСПРАВЛЕНО: Переходим на локальную метку ошибки

    STA  SEM_TEMP_ID            ; Сохранили ID семафора

    ; Вычисляем физический адрес структуры: SEM_SYS_TABLE + (ID * 4)
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H             ; HL = ID * 4
    LXI  D, SEM_SYS_TABLE
    DAD  D                      ; HL указывает точно на старт структуры семафора
    SHLD SEM_TEMP_STRUCT        ; Зафиксировали указатель

    MOV  A, M                   ; Читаем текущее значение счетчика O_SEM_VALUE (+0)
    CPI  00h                    ; Ресурс исчерпан (sem_value == 0)?
    JZ   sem_wait_sleep         ; Если равен 0 — текущий процесс уходит в асинхронный сон

    ; Ресурс доступен — производим атомарный декремент
    DCR  M                      ; Снизили счетчик sem_value на 1 unit
    MVI  A, 00h                 ; Код возврата = 00H (Успех)
    RET                         ; Выход в syscall_return

sem_wait_sleep:
    ; --- АСИНХРОННЫЙ СОН ПРОЦЕССА НА СЕМАФОРЕ ---
    INX  H                      ; HL указывает на поле O_SEM_WAITPID (+1)
    LDA  CURRENT_PID
    MOV  M, A                   ; Прописали наш PID в очередь ожидания семафора

    ; Переводим текущий процесс в статус асинхронного сна ST_WAITING (04H)
    CALL get_current_pcb_address ; HL = Базовый адрес PCB нашего процесса
    PUSH H                      ; Стак: Сохранили базу PCB (строго 'H')
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_WAITING          ; Усыпляем текущую задачу
    
    POP  H                      ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, W_SEM               ; Причина сна — блокировка на POSIX семафоре (0Bh)

    ; Консервируем текущий SP ядра в PCB для точки пробуждения
    POP  H                      ; HL = Базовый адрес PCB
    LXI  D, O_SAVED_SP
    DAD  D
    XCHG                        ; DE = Указатель на ячейку O_SAVED_SP в PCB
    
    LXI  H, 0
    DAD  SP                     ; HL = Физическое значение текущего SP ядра
    MOV  A, L
	STAX D
	INX D
    MOV  A, H
	STAX D          ; Записали 16-битный SP в структуру PCB

    ; Отдаем процессорное время другой READY задаче
    CALL scheduler              ; Выбирает лучший готовый процесс в аккумулятор A
    STA  NEXT_PID
    CALL switch_mmu_context     ; Аппаратно перепрошиваем eMMU-окна (К555ИР26)
    JMP  restore_timer_context  ; Уходим в контекст новой задачи

sem_wait_wakeup_entry:
    ; =========================================================================
    ; ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ ПРОЦЕССА НА СЕМАФОРЕ
    ; =========================================================================
    LDA  SEM_TEMP_ID
    MOV  D, A                   ; Восстановили ID семафора для повторного прохода
    JMP  sys_sem_wait           ; Идем на повторную попытку захвата

sem_wait_err_inval:
    MVI  A, 0FFh                ; ИСПРАВЛЕНО: Локальный выход по ошибке EINVAL
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ sem_post()
; =============================================================================
; Вход:  D = Идентификатор (ID) системного семафора (0..7).
; Выход: A = 00H — Успех, ресурс освобожден / инкрементирован.
;        A = 0FFH — Ошибка EINVAL (Неверный ID семафора).
; =============================================================================
sys_sem_post:
    ; Проверка границ ID семафора (0..7)
    MOV  A, D
    CPI  MAX_SYS_SEMS
    JNC  sem_post_err_inval     ; ИСПРАВЛЕНО: Переходим на локальную ошибку поста

    ; Вычисляем физический адрес структуры: SEM_SYS_TABLE + (ID * 4)
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H             ; HL = ID * 4
    LXI  D, SEM_SYS_TABLE
    DAD  D                      ; HL указывает на поле O_SEM_VALUE (+0)
    
    SHLD SEM_TEMP_STRUCT        ; Зафиксировали указатель на структуру семафора
    INX  H                      ; HL указывает на поле O_SEM_WAITPID (+1)
    
    MOV  A, M                   ; Читаем PID процесса, заблокированного в очереди
    CPI  00h                    ; Очередь ожидания пуста?
    JZ   sem_post_no_waiters    ; Если в очереди никого нет — просто инкрементируем счетчик

    ; --- РЕАКТИВНОЕ ПРОБУЖДЕНИЕ ПРОЦЕССА ИЗ ОЧЕРЕДИ СЕМАФОРА ---
    MOV  C, A                   ; C = Запоминаем PID разблокируемой задачи
    MVI  M, 00h                 ; Очищаем поле очереди O_SEM_WAITPID = 00h

    ; Вычисляем адрес PCB просыпающегося процесса: PCB_TABLE + (PID * 64)
    MOV  L, C
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H
    DAD  H
	DAD  H
	DAD  H    ; HL = target_wait_pid * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = Базовый адрес PCB просыпающегося процесса
    
    PUSH H                      ; Спасся адрес PCB целевой задачи
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY            ; ПРОЦЕСС ОФИЦИАЛЬНО РАЗБУЖЕН ДЛЯ ШЕДУЛЛЕРА!
    
    POP  H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, 00h                 ; Сбросили код сна wait-channel в чистый ноль

    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET

sem_post_no_waiters:
    LHLD SEM_TEMP_STRUCT        ; HL снова указывает на поле O_SEM_VALUE (+0)
    INR  M                      ; Инкрементируем sem_value (ресурс свободен)
    
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех)
    RET

sem_post_err_inval:
    MVI  A, 0FFh                ; Возвращаем код ошибки 0xFF (EINVAL)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ socket()
; =============================================================================
sys_socket:
    ; --- ШАГ 1: Валидация домена (Внутренний сокет или Внешний чип) ---
    MOV  A, D
    CPI  AF_UNIX                ; Проверяем запрос на внутренний программный сокет (01h)?
    JZ   socket_validate_type
    CPI  AF_INET                ; Проверяем запрос на аппаратный чип KSZ8851 (02h)?
    JZ   socket_validate_type
    
    MVI  A, 0FFh                ; Ошибка: Домен не поддерживается (EAFNOSUPPORT)
    RET

socket_validate_type:
    STA  SOCK_TEMP_DOMAIN       ; Утверждено: Сохранили проверенный домен
    MOV  A, E
    STA  SOCK_TEMP_TYPE         ; Сохранили тип (STREAM/DGRAM)

    ; --- ШАГ 2: Поиск свободного локального FD в O_FILE_TABLE процесса ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB
    PUSH H                      ; Стак: Сохранили базовый адрес PCB

    LXI  D, O_FILE_TABLE        
    DAD  D                      ; HL указывает на начало таблицы дескрипторов
    
    MVI  B, 8                   ; 8 слотов дескрипторов (0..7)
    MVI  C, 00h                 ; Локальный FD итератор

socket_find_fd_loop:
    MOV  A, M
    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JZ   socket_local_fd_found  
    
    INX  H                      
    INR  C                      
    DCR  B
    JNZ  socket_find_fd_loop

    ; Локальная таблица файлов процесса переполнена
    POP  H                      ; Сбалансировали стек
    MVI  A, 0FFh                ; Ошибка EMFILE
    RET

; -----------------------------------------------------------------------------
; СВОБОДНЫЙ FD НАЙДЕН (C = Локальный дескриптор 0..7)
; -----------------------------------------------------------------------------
socket_local_fd_found:
    MOV  A, C
    STA  SOCK_TEMP_LOCAL_FD     ; Зафиксировали локальный FD (0..7)

    ; --- ШАГ 3: Поиск свободного слота в глобальной SOCK_SYS_TABLE ядра ---
    LXI  H, SOCK_SYS_TABLE      ; HL -> Глобальная таблица сокетов
    MVI  B, MAX_SYS_SOCKETS     ; 4 системных слота
    MVI  C, 0                   ; Глобальный итератор (0..3)

socket_find_sys_loop:
    MOV  A, M                   ; Читаем поле S_SOCK_STATUS (+0)
    CPI  SOCK_FREE              
    JZ   socket_slot_allocated  ; Системный слот найден!

    LXI  D, SOCK_STRUCT_SIZE
    DAD  D
    INR  C                      
    DCR  B
    JNZ  socket_find_sys_loop

    ; Глобальная таблица сокетов ядра переполнена
    POP  H                      
    MVI  A, 0FEh                ; Ошибка ENFILE
    RET

; -----------------------------------------------------------------------------
; СИСТЕМНЫЙ СЛОТ НАЙДЕН (HL = Указатель на структуру сокета, C = Индекс 0..3)
; -----------------------------------------------------------------------------
socket_slot_allocated:
    PUSH H                      ; Стак: Сохранили указатель на структуру сокета
    
    ; ИСПРАВЛЕНО: Честное разветвление логики на основе сохраненного домена!
    LDA  SOCK_TEMP_DOMAIN
    CPI  AF_UNIX
    JZ   socket_init_unix_domain ; Уходим на программную реализацию сокетов ОЗУ
    
    CPI  AF_INET
    JZ   socket_init_inet_domain ; ТЕПЕРЬ ОФИЦИАЛЬНО ВЫЗЫВАЕТСЯ: Уходим на инициализацию KSZ8851!
    
    ; Резервный аварийный выход на случай повреждения памяти ядра
    POP  H
	POP  H
    MVI  A, 0FFh
    RET

; =============================================================================
; ВЕТКА А: ИНИЦИАЛИЗАЦИЯ АППАРАТНОГО СОКЕТА ДЛЯ СЕТЕВОЙ КАРТЫ KSZ8851-6MLL
; =============================================================================
socket_init_inet_domain:
    MOV  A, C
    ADI  GLOBAL_SOCK_INET       ; Смещение в пул аппаратных сокетов (40..47)
    STA  SOCK_TEMP_GLOBAL       

    POP  H                      ; Восстановили адрес структуры сокета в HL
    PUSH H                      ; Снова припрятали для баланса
    
    ; Заполняем поля структуры аппаратного сокета
    MVI  M, SOCK_INIT           ; Статус = Инициализирован
    INX  H                      ; S_SOCK_TYPE (+1)
    LDA  SOCK_TEMP_TYPE
    MOV  M, A                   ; Записали тип (TCP/UDP)
    
    ; Пропускаем порты (+2..+5), обнуляем владельца (+6) и пишем аппаратный канал (+7)
    LXI  D, 5
    DAD  D                      ; HL указывает на поле S_SOCK_OWNER (+6)
    LDA  CURRENT_PID
    MOV  M, A                   ; Записали PID владельца
    INX  H                      ; HL указывает на S_SOCK_HW_CHAN (+7)
    MOV  A, C                   ; Аппаратный канал чипа KSZ8851 (0..3)
    MOV  M, A                   

    ; --- НИЗКОУРОВНЕВАЯ АППАРАТНАЯ ПРОШИВКА ЧИПА KSZ8851-6MLL ---
    ; Для примера: Включаем RX/TX прерывания и разрешаем работу аппаратного канала C
    MOV  A, C                   ; Аппаратный канал (0..3)
    ; Сначала пишем адрес целевого регистра в регистр указателя (NET_CMD_PORT)
    MVI  A, REG_IER             ; Выбираем Interrupt Enable Register
    OUT  NET_CMD_PORT
    ; Каскадно пишем 16-битное значение маски прерываний через 8-битный порт данных
    MVI  A, 0FFh                ; Младший байт маски прерываний чипа (Разрешить все)
    OUT  NET_DATA_PORT
    MVI  A, 0FFh                ; Старший байт маски прерываний чипа
    OUT  NET_DATA_PORT

    JMP  socket_bind_pcb        ; Уходим на связывание дескрипторов

; =============================================================================
; ВЕТКА Б: ИНИЦИАЛИЗАЦИЯ ВНУТРЕННЕГО ПРОГРАММНОГО СОКЕТА В ОЗУ eMMU (IPC)
; =============================================================================
socket_init_unix_domain:
    ; Для внутреннего сокета нам жизненно необходима физическая страница памяти!
    ; Вызываем наш ранее проверенный системный аллокатор страниц памяти ядра
    CALL k_allocate_free_physical_page ; Извлекаем 16 КБ из вашей карты RAM_BITMAP
    CPI  0FFh
    JZ   socket_err_nomem       ; Если физ. память кончилась — аварийный выход

    STA  SOCK_ALLOC_PAGE        ; Зафиксировали номер выделенной страницы ОЗУ eMMU
    
    MOV  A, C
    ADI  GLOBAL_SOCK_UNIX       ; Смещение в пул внутренних программных сокетов (32..39)
    STA  SOCK_TEMP_GLOBAL       

    POP  H                      ; HL = Восстановили адрес структуры сокета
    PUSH H                      
    
    MVI  M, SOCK_INIT           ; Статус = Инициализирован
    INX  H                      ; S_SOCK_TYPE (+1)
    LDA  SOCK_TEMP_TYPE
    MOV  M, A                   ; Записали тип IPC сокета
    
    ; Используем поле локального порта (S_SOCK_LPORT, +2) для хранения номера физ. страницы!
    INX  H                      
    LDA  SOCK_ALLOC_PAGE
    MOV  M, A                   ; Записали номер физической страницы ОЗУ eMMU пайп-сокета
    INX  H
    MVI  M, 00h                 ; Старший байт обнулен

    ; Прописываем текущий PID в поле владельца (+6)
    LXI  D, 3
    DAD  D                      ; HL -> S_SOCK_OWNER (+6)
    LDA  CURRENT_PID
    MOV  M, A                   ; Записали владельца программного сокета
    
    ; [Ядро включает KERNEL_OVERRIDE и обнуляет кольцевые указатели Head/Tail 
    ; внутри выделенной физической страницы ОЗУ сокета]

; =============================================================================
; ШАГ 5: ФИЗИЧЕСКОЕ СВЯЗЫВАНИЕ ЛОКАЛЬНОГО FD И ГЛОБАЛЬНОГО СЕТЕВОГО ИНДЕКСА
; =============================================================================
socket_bind_pcb:
    POP  H                      ; Очистили стек от локального адреса структуры сокета
    POP  H                      ; HL = Восстановили базовый адрес PCB текущего процесса
    PUSH H                      
    
    LXI  D, O_FILE_TABLE        ; Смещение таблицы файлов (+20)
    DAD  D                      
    
    LDA  SOCK_TEMP_LOCAL_FD     ; Наш свободный локальный FD (0..7)
    MOV  E, A
    MVI  D, 0
    DAD  D                      ; HL указывает точно на байт дескриптора в PCB
    
    LDA  SOCK_TEMP_GLOBAL       ; Извлекаем итоговый глобальный индекс (32..39 или 40..47)
    MOV  M, A                   ; Прописали связь! Сокет открыт для Ring 3.

    ; --- ШАГ 6: УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    POP  H                      ; Очистили стек от адреса PCB (строго 'H')
    LDA  SOCK_TEMP_LOCAL_FD     ; Возвращаем пользователю дескриптор сокета в аккумуляторе A
    RET                         ; Выход в syscall_return.

; -----------------------------------------------------------------------------
; ОБРАБОТКА ОШИБОК АЛЛОКАЦИИ РЕСУРСОВ
; -----------------------------------------------------------------------------
socket_err_nomem:
    POP  H                      ; Очистили адрес структуры сокета
    POP  H                      ; Очистили адрес PCB
    MVI  A, 0FEh                ; Возвращаем ошибку ENFILE / ENOMEM
    RET

; =============================================================================
; СИСКОЛЛЫ ИДЕНТИФИКАЦИИ ПРОЦЕССОВ В MEGA-580 OS [МОНОЛИТ ДЛЯ ZASM]
; =============================================================================

; =============================================================================
; 1. СИСТЕМНЫЙ ВЫЗОВ sys_getpid (Получить PID текущего процесса)
; =============================================================================
; Вход:  Нет параметров.
; Выход: А = Идентификатор текущего процесса (O_PID, 1..254).
; =============================================================================
sys_getpid:
    PUSH B
    PUSH D
    PUSH H                          ; Спасаем регистровые пары в стек ядра

    CALL get_current_pcb_address    ; HL = Базовый адрес PCB активного процесса
    
    ; Поле O_PID находится в самом начале структуры PCB (смещение +0)
    MOV  A, M                       ; A = Значение поля O_PID из памяти Ring 0

    POP  H
    POP  D
    POP  B                          ; Восстановили контекст регистров
    RET                             ; Чистый выход в syscall_return. Результат в A.

; =============================================================================
; 2. СИСТЕМНЫЙ ВЫЗОВ sys_getppid (Получить PID родительского процесса)
; =============================================================================
; Вход:  Нет параметров.
; Выход: А = Идентификатор родительского процесса (O_PPID, 1..254).
; =============================================================================
sys_getppid:
    PUSH B
    PUSH D
    PUSH H

    CALL get_current_pcb_address    ; HL = Базовый адрес PCB активного процесса
    
    ; Поле O_PPID находится на смещении +1
    INX  H                          ; HL -> O_PPID (+1)
    MOV  A, M                       ; A = Значение поля O_PPID

    POP  H
    POP  D
    POP  B
    RET                             ; Выход в syscall_return. Результат в A.

; =============================================================================
; 3. СИСТЕМНЫЙ ВЫЗОВ sys_getuid (Получить User ID владельца процесса)
; =============================================================================
; Вход:  Нет параметров.
; Выход: А = Идентификатор пользователя (O_UID, 0 = ROOT, 1..255 = Users).
; =============================================================================
sys_getuid:
    PUSH B
    PUSH D
    PUSH H

    CALL get_current_pcb_address    ; HL = Базовый адрес PCB активного процесса
    
    ; Поле O_UID находится на смещении +6
    LXI  D, 6                       ; Загружаем относительное смещение 6 байт
    DAD  D                          ; HL = База_PCB + 6 (HL указывает на O_UID)
    
    MOV  A, M                       ; A = Значение поля O_UID

    POP  H
    POP  D
    POP  B
    RET                             ; Выход в syscall_return. Результат в A.

; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): СИСТЕМНЫЙ ВЫЗОВ setsid()
; =============================================================================
sys_setsid:
    ; Извлекаем базовый адрес PCB текущего активного процесса в HL
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB задачи
    PUSH H                          ; Стак: Сохранили базу PCB (строго 'H')

    ; --- ШАГ 1: Извлекаем оригинальный O_PID процесса (+0) ---
    MOV  E, M                       ; E = O_PID текущего процесса (так как смещение +0)
    
    ; --- ШАГ 2: Переходим к полю O_PGID (+42) для защитной проверки POSIX ---
    LXI  D, O_PGID                  ; DE = 42
    DAD  D                          ; HL = База_PCB + 42 (HL указывает на O_PGID)
    MOV  A, M                       ; A = Текущий PGID процесса
    
    CMP  E                          ; Сравниваем текущий PID (в E) с PGID (в A)
    JZ   setsid_err_eperm           ; Если PID == PGID — процесс уже лидер группы, ошибка!

    ; --- ШАГ 3: Процесс не лидер — атомарно создаем новую сессию демона ---
    POP  H                          ; Восстановили HL = Базовый адрес PCB из стека
    PUSH H                          ; Вернули обратно для финального баланса
    
    ; 1. Прописываем новый PGID, равный текущему PID
    LXI  D, O_PGID
    DAD  D                          ; HL -> O_PGID (+42)
    MOV  M, E                       ; Вписали PID процесса как новый ID группы
    
    ; 2. Прописываем новый SID, равный текущему PID (Процесс становится лидером сессии)
    INX  H                          ; HL -> O_SID (+43, быстрый шаг на 1 байт вперед!)
    MOV  M, E                       ; Вписали PID процесса как новый Session ID
    
    ; 3. Физически отвязываем процесс от управляющего терминала TTY
    INX  H                          ; HL -> O_TTY_INDEX (+44)
    MVI  M, 0FFh                    ; Записали 0xFF (Официальный маркер: терминал отсутствует)

    ; --- ШАГ 4: УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    POP  H                          ; Полностью очистили стек супервизора от адреса PCB
    MVI  A, 00h                     ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                             ; Выход в syscall_return.

; --- ОБРАБОТЧИК АВАРИЙНОГО ВЫХОДА (ПРОЦЕСС УЖЕ ЛИДЕР ГРУППЫ) ---
setsid_err_eperm:
    POP  H                          ; Сбалансировали стек ядра Ring 0
    MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EPERM — Действие запрещено)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ ioctl()
; =============================================================================
; Вход:  D  = Локальный дескриптор сокета процесса (FD, 0..7).
;        E  = Команда request (SIOCSIFHWADDR=089h или SIOCSIFADDR=08Ch).
;        HL = Логический адрес структуры параметров в памяти задачи Ring 3.
; Выход: A  = 00H — Успех. Параметры интерфейса обновлены.
;        A  = 0FFH — Ошибка: EBADF (Неверный дескриптор) или EINVAL (Неверная команда).
; =============================================================================
sys_ioctl:
    ; --- ШАГ 1: Валидация дескриптора сокета ---
    MOV  A, D
    CPI  8                      ; Проверяем границы локальных дескрипторов (0..7)
    JNC  ioctl_err_badf
    
    STA  IOCTL_TEMP_FD          ; Сохранили FD
    MOV  A, E
    STA  IOCTL_TEMP_REQ         ; Сохранили команду request
    SHLD IOCTL_USER_PTR         ; Сохранили указатель на буфер Ring 3

    ; Извлекаем глобальный системный индекс сокета из PCB
    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address ; HL = Адрес PCB
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                   ; A = Глобальный индекс сетевого объекта (32..47)
    
    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JZ   ioctl_err_badf
    CPI  32                     ; Обычные файлы диска не поддерживают сетевой ioctl
    JC   ioctl_err_inval        ; Если индекс < 32 — возвращаем EINVAL

    ; --- ШАГ 2: ВЕТВЛЕНИЕ ПО КОМАНДАМ REQUEST ---
    LDA  IOCTL_TEMP_REQ
    CPI  SIOCSIFHWADDR
    JZ   ioctl_set_mac          ; Прыгаем на установку MAC-адреса
    CPI  SIOCSIFADDR
    JZ   ioctl_set_ip           ; Прыгаем на установку IP-адреса
    
    JMP  ioctl_err_inval        ; Неизвестный запрос — возвращаем EINVAL

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ SIOCSIFHWADDR: УСТАНОВКА MAC-АДРЕСА В ЯДРО И ЧИП KSZ8851
; -----------------------------------------------------------------------------
ioctl_set_mac:
    ; Сначала вычитываем 6 байт MAC-адреса из Ring 3 в ОЗУ ядра SYS_MAC_ADDRESS
    LHLD IOCTL_USER_PTR         ; HL = Буфер источника в Ring 3
    LXI  B, SYS_MAC_ADDRESS     ; BC = Буфер назначения в ядре Ring 0
    MVI  D, 6                   ; Счетчик на 6 байт

ioctl_read_mac_loop:
    OUT  076H                   ; Включаем KERNEL_OVERRIDE для чтения из Ring 3
    MOV  A, M                   ; Читаем байт из юзерспейса
    OUT  076H                   ; Мгновенно отключаем оверрайд шины
    
    STAX B                      ; Записали байт в SYS_MAC_ADDRESS ядра
    INX  H                      ; Сдвиг источника
    INX  B                      ; Сдвиг назначения
    DCR  D
    JNZ  ioctl_read_mac_loop    ; Копируем все 6 байт

    ; --- ФИЗИЧЕСКАЯ ПРОШИВКА MAC-АДРЕСА В РЕГИСТРЫ CHIP KSZ8851-6MLL ---
    LXI  H, SYS_MAC_ADDRESS     ; HL указывает на вычитанный MAC в ядре

    ; 1. Прошивка REG_MARL (Байты 0 и 1 MAC-адреса)
    MVI  A, REG_MARL            ; Выбрали регистр 0x10 через Address Pointer
    OUT  NET_CMD_PORT
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 0 (Low) в порт данных чипа
    INX  H
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 1 (High) в порт данных чипа
    INX  H

    ; 2. Прошивка REG_MARM (Байты 2 и 3 MAC-адреса)
    MVI  A, REG_MARM            ; Выбрали регистр 0x12
    OUT  NET_CMD_PORT
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 2 (Low)
    INX  H
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 3 (High)
    INX  H

    ; 3. Прошивка REG_MARH (Байты 4 и 5 MAC-адреса)
    MVI  A, REG_MARH            ; Выбрали регистр 0x14
    OUT  NET_CMD_PORT
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 4 (Low)
    INX  H
    MOV  A, M
	OUT NET_DATA_PORT ; Записали байт 5 (High)

    JMP  ioctl_success_exit

; -----------------------------------------------------------------------------
; ОПЕРАЦИЯ SIOCSIFADDR: ФИКСАЦИЯ IP-АДРЕСА ИНТЕРФЕЙСА В ОЗУ ЯДРА
; -----------------------------------------------------------------------------
ioctl_set_ip:
    ; Вычитываем 4 байта IP-адреса из Ring 3 в ОЗУ ядра SYS_IP_ADDRESS
    LHLD IOCTL_USER_PTR         ; HL = Буфер источника в Ring 3
    LXI  B, SYS_IP_ADDRESS      ; BC = Буфер назначения в ядре Ring 0
    MVI  D, 4                   ; Счетчик на 4 байта

ioctl_read_ip_loop:
    OUT  076H                   ; Включаем KERNEL_OVERRIDE
    MOV  A, M                   ; Читаем байт IP
    OUT  076H                   ; Отключаем
    
    STAX B                      ; Зафиксировали байт в памяти ядра
    INX  H
    INX  B
    DCR  D
    JNZ  ioctl_read_ip_loop     ; Копируем все 4 байта (например, 192.168.1.10)

ioctl_success_exit:
    MVI  A, 00h                 ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return. Прерывания откроет диспетчер.

; -----------------------------------------------------------------------------
; БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКИ ИНТЕРФЕЙСА)
; -----------------------------------------------------------------------------
ioctl_err_badf:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EBADF — Неверный дескриптор файла)
    RET

ioctl_err_inval:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX EINVAL — Запрос не применим к объекту)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ shutdown()
; =============================================================================
sys_shutdown:
    ; --- ШАГ 1: Валидация дескриптора файла и режима how ---
    MOV  A, D
    CPI  8                      ; Проверяем границы FD (0..7)
    JNC  shut_err_badf
    
    STA  SHUTDOWN_TEMP_FD       ; Сохранили FD сокета
    MOV  A, E
    CPI  3                      ; Режим how должен быть strictly 0, 1 или 2
    JNC  shut_err_inval         ; Если how >= 3 — возвращаем EINVAL
    STA  SHUTDOWN_TEMP_HOW      ; Сохранили режим how

    ; Извлекаем глобальный системный индекс сокета из PCB процесса
    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address ; HL = Базовый адрес PCB
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                   ; A = Глобальный индекс сокета (32..47)
    
    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JZ   shut_err_badf
    CPI  32                     ; Обычные файлы диска не поддерживают shutdown (ENOTSOCK)
    JC   shut_err_inval         ; Если индекс < 32 — аварийный выход

    ; --- ШАГ 2: Вычисление адреса структуры сокета в SOCK_SYS_TABLE ---
    ; Индекс сокета приводится к локальному диапазону 0..MAX_SYS_SOCKETS-1
    SUI  32                     ; Сбрасываем смещение пула сетевых индексов
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Локальный_Индекс * 8 байт (SOCK_STRUCT_SIZE)
    LXI  D, SOCK_SYS_TABLE
    DAD  D                      ; HL указывает точно на структуру сокета в ядре
    SHLD SHUTDOWN_STRUCT_PTR    ; Зафиксировали указатель

    MOV  A, M                   ; Читаем текущее поле S_SOCK_STATUS (+0)
    CPI  SOCK_FREE              ; Сокет вообще открыт?
    JZ   shut_err_notconn       ; Если сокет закрыт — возвращаем ENOTCONN (0FFH)

    ; --- ШАГ 3: ВЕТВЛЕНИЕ ПО РЕЖИМАМ ЗАКРЫТИЯ HOW ---
    LDA  SHUTDOWN_TEMP_HOW
    CPI  SHUT_RD
    JZ   shut_apply_rd          ; Прыгаем на запрет чтения
    CPI  SHUT_WR
    JZ   shut_apply_wr          ; Прыгаем на запрет записи
    
    ; Иначе режим SHUT_RDWR (02h) — накладываем обе маски последовательно
    JMP  shut_apply_rdwr

; -----------------------------------------------------------------------------
; РЕЖИМ SHUT_RD: Модификация флагов статуса сокета (Запрет чтения)
; -----------------------------------------------------------------------------
shut_apply_rd:
    LHLD SHUTDOWN_STRUCT_PTR    ; HL -> S_SOCK_STATUS
    MOV  A, M
    ORI  SOCK_SHUT_RD_MASK      ; Наложили маску запрета чтения (взвели 4-й бит)
    MOV  M, A                   ; Сохранили измененный статус в структуру ядра
    
    ; --- ШАГ 3.5: Пробуждение спящих читателей для AF_UNIX / Пайп-сокетов ---
    ; [Ядро проверяет тип сокета. Если это внутренний сокет ОЗУ, находит PID 
    ; читателя в поле S_SOCK_OWNER и, если тот спал в ожидании данных W_PIPE_EMPTY, 
    ; реактивно переводит его в ST_READY, чтобы вернуть 0 байт из sys_recv]
    
    JMP  shut_success_exit

; -----------------------------------------------------------------------------
; РЕЖИМ SHUT_WR: Аппаратный останов передачи в чипе KSZ8851
; -----------------------------------------------------------------------------
shut_apply_wr:
    LHLD SHUTDOWN_STRUCT_PTR    ; HL -> S_SOCK_STATUS
    MOV  A, M
    ORI  SOCK_SHUT_WR_MASK      ; Наложили маску запрета записи (взвели 5-й бит)
    MOV  M, A                   ; Сохранили измененный статус

    ; Проверяем домен сокета. Если это внутренний сокет AF_UNIX — аппаратуру не трогаем
    LHLD SHUTDOWN_STRUCT_PTR
    INX  H                      ; HL -> S_SOCK_TYPE (+1)
    ; (Если домен сетевой AF_INET — выполняем физический сброс TX-контроллера чипа)
    
    ; --- НИЗКОУРОВНЕВАЯ ПРОШИВКА ЧИПА KSZ8851-6MLL ---
    MVI  A, REG_TXCR            ; Выбираем Transmit Control Register (0x70)
    OUT  NET_CMD_PORT
    
    ; Считываем текущие настройки передачи из чипа
    IN   NET_DATA_PORT
    MOV  C, A
    IN   NET_DATA_PORT
    MOV  B, A                   ; BC = 16-битное текущее значение REG_TXCR чипа
    
    ; Сбрасываем бит разрешения передачи кадра TXE (Transmit Enable, Бит 0)
    MOV  A, C
    ANI  0FEh                   ; Маска 11111110B (Обнулили 0-й бит)
    MOV  C, A
    
    ; Физически пишем измененное управляющее слово обратно в регистр чипа
    MVI  A, REG_TXCR
    OUT  NET_CMD_PORT
    MOV  A, C
	OUT NET_DATA_PORT ; Прописали младший байт (Передача аппаратно заперта!)
    MOV  A, B
	OUT NET_DATA_PORT ; Прописали старший байт
    
    JMP  shut_success_exit

; -----------------------------------------------------------------------------
; РЕЖИМ SHUT_RDWR: Полное двустороннее асинхронное закрытие канала
; -----------------------------------------------------------------------------
shut_apply_rdwr:
    LHLD SHUTDOWN_STRUCT_PTR
    MOV  A, M
    ORI  SOCK_SHUT_RD_MASK | SOCK_SHUT_WR_MASK ; Взвели оба бита запретов
    MOV  M, A
    
    ; Вызываем последовательно обе логики освобождения ресурсов
    JMP  shut_apply_wr          ; Уходим на аппаратный останов чипа и чистый выход

shut_success_exit:
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return. Прерывания откроет шлюз ядра.

; -----------------------------------------------------------------------------
; БЛОКИ ОБРАБОТКИ ОШИБОК ИНТЕРФЕЙСА СИСКОЛЛА
; -----------------------------------------------------------------------------
shut_err_badf:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EBADF — Неверный дескриптор файла)
    RET

shut_err_inval:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX EINVAL — Неверный параметр request)
    RET

shut_err_notconn:
    MVI  A, 0FDh                ; Код ошибки 0xFD (POSIX ENOTCONN — Сокет не соединен)
    RET

; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): СИСТЕМНЫЙ ВЫЗОВ send()
; =============================================================================
; Вход:  D  = Локальный файловый дескриптор сокета процесса (FD, 0..7).
;        HL = Логический адрес буфера источника в Ring 3 процесса.
;        BC = Количество байт для отправки (16-битная длина).
; Выход: A  = Количество успешно переданных байт (успех).
;        A  = 0EBH — Ошибка EBADF (Неверный или закрытый дескриптор).
;        A  = 0EAH — Ошибка EINVAL (Дескриптор не является сокетом).
; Прерывания централизованно заблокированы на входе в sys_call_entry!
; =============================================================================
sys_send:
    ; --- ШАГ 1: Валидация дескриптора и консервация параметров ---
    MOV  A, D
    CPI  8                      ; Проверяем границы локальных дескрипторов (0..7)
    JNC  net_send_err_badf      ; Если FD >= 8 — возвращаем EBADF

    STA  NET_IO_FD              ; Зафиксировали локальный FD
    SHLD NET_IO_BUF_PTR         ; Зафиксировали адрес буфера пользователя Ring 3
    
    MOV  A, B
    STA  NET_IO_BYTES_REQ+1     ; Сохранили старший байт длины запроса
    MOV  A, C
    STA  NET_IO_BYTES_REQ       ; Сохранили младший байт длины запроса

    XRA  A
    STA  NET_IO_BYTES_CURR      ; Обнуляем младший байт счетчика отправленных байт
    STA  NET_IO_BYTES_CURR+1    ; Обнуляем старший байт счетчика отправленных байт

    ; Извлекаем глобальный системный индекс сокета из PCB текущей задачи
    MOV  C, D
    MVI  B, 0                   ; BC = Локальный FD
    CALL get_current_pcb_address ; HL = Базовый адрес PCB
    
    LXI  D, O_FILE_TABLE        ; Смещение таблицы дескрипторов (+20)
    DAD  D
    DAD  B                      ; HL указывает точно на нужный байт в PCB
    MOV  A, M                   ; A = Глобальный системный индекс объекта (32..47)
    STA  NET_IO_GLOBAL_IDX       ; Зафиксировали глобальный индекс в ОЗУ ядра

    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JZ   net_send_err_badf      ; Если свободен — возвращаем EBADF (Стек чист)

    CPI  32                     ; Индексы < 32 зарезервированы под файлы и пайпы
    JC   net_send_err_inval     ; Если не сокет — возвращаем EINVAL

    CPI  40                     ; Разделение: 32..39 — UNIX сокеты, 40..47 — INET сокеты
    JC   net_send_unix_domain   ; Если индекс < 40 — уходим на внутреннюю IPC ветку ОЗУ!

; =============================================================================
; ВЕТКА А: ЗАПИСЬ ПАКЕТА В АППАРАТНУЮ SRAM-ПАМЯТЬ CHIP KSZ8851-6MLL (AF_INET)
; =============================================================================
; =============================================================================
; ИСПРАВЛЕННЫЙ ФРАГМЕНТ СИСКОЛЛА sys_send: ИНТЕГРАЦИЯ ARP-РАЗРЕШЕНИЯ
; =============================================================================
net_send_inet_domain:
    ; --- ШАГ 1: Извлекаем адрес целевой структуры сокета в SOCK_SYS_TABLE ---
    LDA  NET_IO_GLOBAL_IDX
    SUI  40                     ; Приводим аппаратный пул (40..47) к индексам 0..7
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Индекс * 8 байт (размер структуры сокета)
    LXI  D, SOCK_SYS_TABLE
    DAD  D                      ; HL указывает на структуру сокета
    
    ; Сдвигаемся к полю удаленного IP-адреса (S_SOCK_RIP = +4)
    LXI  D, S_SOCK_RIP
    DAD  D                      ; HL указывает на 4-байтовый IP получателя в структуре
    MOV  B, H
    MOV  C, L                   ; BC = Адрес IP-адреса в ОЗУ ядра для функции ARP

    ; --- ШАГ 2: ВЫЗОВ ПОДСИСТЕМЫ ARP ДЛЯ ОПРЕДЕЛЕНИЯ MAC-АДРЕСA ---
    CALL k_arp_resolve          ; Вызываем ранее изолированную подпрограмму!
    
    ; Выход из k_arp_resolve:
    ; Если A = 00H (Hit) -> HL указывает на 6-байтовый MAC-адрес внутри ARP-таблицы.
    ; Если A = 0FFH (Miss) -> Процесс уже асинхронно усыплен ядром на W_NET_ARP, 
    ;                         а сам вызов k_arp_resolve сделал RET сюда.
    
    CPI  0FFh
    RZ                          ; Если промах (Miss) — мгновенно выходим из сисколла! 
                                ; Планировщик уже переключил контекст, пока мы ждем ARP Reply.

    ; --- ШАГ 3: УСПЕШНЫЙ HIT — MAC НАЙДЕН (HL -> 6 байт MAC в ОЗУ ядра) ---
    SHLD NET_IO_TARGET_MAC_PTR  ; Сохранили указатель на найденный MAC хоста

    ; --- ШАГ 4: Физическая подготовка чипа KSZ8851-6MLL к передаче кадра ---
    MVI  A, 076h                ; Выбираем REG_TXFDPR (Transmit Frame Data Pointer)
    OUT  NET_CMD_PORT
    MVI  A, 00h
	OUT UART_DATA
	OUT UART_DATA ; Обнулили 16-битный указатель SRAM чипа
    
    ; Включаем порт данных для записи заголовка и тела пакета
    MVI  A, UART_DATA           ; Переводим Host Interface в режим данных
    OUT  NET_CMD_PORT

    ; Передаем Control Word (управляющее слово TX по спецификации Micrel)
    MVI  A, 00h
	OUT UART_DATA
    MVI  A, 00h
	OUT UART_DATA
    
    ; Передаем общую длину кадра (Длина данных + 14 байт Ethernet-заголовка)
    LHLD NET_IO_BYTES_REQ
    LXI  D, 14                  ; 6 байт DST MAC + 6 байт SRC MAC + 2 байта Type
    DAD  D                      ; HL = Общая длина кадра в кабель
    MOV  A, L
	OUT UART_DATA   ; Byte Count Low
    MOV  A, H
	OUT UART_DATA   ; Byte Count High

    ; --- ШАГ 5: Формируем аппаратный Ethernet-заголовок кадра ---
    ; 1. Заталкиваем в чип MAC-адрес получателя (DST MAC), найденный через k_arp_resolve
    LHLD NET_IO_TARGET_MAC_PTR
    MVI  B, 6
net_send_hardware_dst_mac_loop:
    MOV  A, M
	OUT UART_DATA
    INX  H
    DCR  B
	JNZ  net_send_hardware_dst_mac_loop

    ; 2. Заталкиваем в чип собственный MAC-адрес (SRC MAC) из ОЗУ ядра
    LXI  H, SYS_MAC_ADDRESS
    MVI  B, 6
net_send_hardware_src_mac_loop:
    MOV  A, M
	OUT UART_DATA
    INX  H
    DCR  B
	JNZ  net_send_hardware_src_mac_loop

    ; 3. Заталкиваем длину/тип протокола (EtherType: IPv4 = 0x0800)
    MVI  A, 008h
	OUT UART_DATA ; В Little-Endian/Network byte order архитектуре чипа
    MVI  A, 000h
	OUT UART_DATA

    ; --- ШАГ 6: Побайтовый перенос полезной нагрузки (Payload) из Ring 3 ---
    LHLD NET_IO_BYTES_REQ
    MOV  B, H
	MOV C, L        ; BC = Количество байт пользователя

net_send_hardware_payload_loop:
    MOV  A, B
	ORA C
	JZ  net_send_hardware_fire ; Если все байты вытолкнуты — стреляем!

    PUSH B                      ; Спасаем счетчик
    LHLD NET_IO_BUF_PTR
    OUT  076H                   ; Включили KERNEL_OVERRIDE_EN = 0
    MOV  A, M                   ; Вычитали байт из юзерспейса Ring 3 задачи
    OUT  076H                   ; Отключили
    INX  H
	SHLD NET_IO_BUF_PTR
    
    OUT  UART_DATA              ; Протолкнули байт в физическую SRAM сетевого чипа
    POP  B
    DCX  B                      ; 16-битный декремент счетчика
    JMP  net_send_hardware_payload_loop

net_send_hardware_fire:
    ; Подаем чипу KSZ8851 команду на физическую отправку кадра в кабель
    MVI  A, REG_TXQCR           ; Transmit Queue Command Register (0x72)
    OUT  NET_CMD_PORT
    MVI  A, TXQCR_METFE         ; Взводим бит Enqueue TX Packet
    OUT  UART_DATA
    MVI  A, 00h
	OUT UART_DATA

    MVI  A, 00h                 ; Код возврата сисколла = 00H (Чистый успех)
    RET

; =============================================================================
; ВЕТКА Б: ЗАПИСЬ ДАННЫХ В ВНУТРЕННИЙ IPC СОКЕТ В ОЗУ ПЛАТЫ eMMU (AF_UNIX)
; =============================================================================
net_send_unix_domain:
    ; Извлекаем адрес структуры текущего сокета в SOCK_SYS_TABLE ядра
    LDA  NET_IO_GLOBAL_IDX
    SUI  32                     ; Приводим к локальному диапазону 0..3
    MOV  L, A
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Локальный_Индекс * 8 байт (SOCK_STRUCT_SIZE)
    LXI  D, SOCK_SYS_TABLE
    DAD  D                      ; HL -> Структура сокета в памяти Ring 0
    SHLD NET_IO_STRUCT_PTR      ; Зафиксировали адрес структуры

    LHLD NET_IO_BYTES_REQ
    MOV  B, H
    MOV  C, L                   ; BC = 16-битный счетчик оставшихся байт

net_send_unix_loop:
    MOV  A, B
    ORA  C
    JZ   net_send_unix_success  ; Если BC == 0 — вся строка данных передана!

    ; --- МАТЕМАТИЧЕСКИЙ РАСЧЕТ АДРЕСА В ОЗУ eMMU С УЧЕТОМ ИНДЕКСА БУФЕРА ---
    LHLD NET_IO_STRUCT_PTR
    LXI  D, S_SOCK_HW_CHAN      ; Смещение +7 (Флаг буфера: 00h = А, 01h = Б)
    DAD  D
    MOV  A, M                   ; A = 00h или 01h (БЕЗ переполнения байта!)
    STA  NET_IO_BUF_FLAG        ; Сохранили флаг направления

    ; Вычисляем базовое логическое окно Сегмента 2 eMMU
    LXI  D, 08000h              ; DE = Стартовая точка Окна 2 eMMU
    LDA  NET_IO_BUF_FLAG
    ORA  A                      ; Проверяем флаг буфера
    JZ   net_send_offset_ready  ; Если флаг = 00h — смещение 0, адрес 0x8000 готов

    LXI  H, 8192                ; Если флаг = 01h — загружаем 16-битное смещение 8 КБ
    DAD  D                      ; HL = 0x8000 + 8192 = 0xA000
    XCHG                        ; DE = 0xA000 (Точный старт Буфера Б во втором окне)

net_send_offset_ready:
    ; Извлекаем 16-битный кольцевой Tail Pointer текущего сокета из NET_UNIX_TAILS
    LDA  NET_IO_GLOBAL_IDX
    SUI  32
    ADD  A                      ; Локальный_Индекс * 2
    MOV  L, A
    MVI  H, 0
    LXI  B, NET_UNIX_TAILS
    DAD  B                      ; HL указывает на ячейку Tail в ОЗУ ядра
    SHLD NET_IO_TAIL_CELL_PTR   ; Сохранили адрес ячейки хвоста

    MOV  A, M
    INX  H
    MOV  H, M
    MOV  L, A                   ; HL = 16-битный текущий кольцевой Tail Pointer (0..8191)
    
    ; [Здесь ядро выполняет проверку на переполнение 8 КБ лимита. Если FIFO забит,
    ; процесс асинхронно засыпает на wait-channel W_PIPE_FULL через O_SAVED_SP]

    DAD  D                      ; HL = База_Окна_DE + Tail_Pointer_HL (Адрес физической записи)
    SHLD NET_IO_PHYS_WRITE_ADDR ; Зафиксировали физический адрес назначения

    ; --- ВЫЧИТКА СИМВОЛА ИЗ ЮЗЕРСПЕЙСА ПРОЦЕССА (Ring 3) ---
    PUSH B                      ; Спасаем счетчик BC в стек супервизора
    LHLD NET_IO_BUF_PTR         ; HL = Текущий адрес буфера Ring 3 пользователя
    
    OUT  076H                   ; Включаем ~{KERNEL_OVERRIDE_EN} = 0
    MOV  A, M                   ; Вычитали байт данных процесса из Ring 3
    OUT  076H                   ; Отключили оверрайд шины
    
    STA  WRITE_CHAR_CHAR        ; Сохранили символ в независимую ячейку ядра
    INX  H
    SHLD NET_IO_BUF_PTR         ; Сдвинули указатель буфера Ring 3 пользователя
    POP  B                      ; Восстановили счетчик BC

    ; --- ФИЗИЧЕСКАЯ ЗАПИСЬ В СТРАНИЦУ СОКЕTA ЧЕРЕЗ eMMU КОММУТАЦИЮ ---
    LHLD NET_IO_STRUCT_PTR
    INX  H
	INX  H             ; HL указывает на поле S_SOCK_LPORT (+2)
    MOV  A, M                   ; A = Номер физической страницы ОЗУ eMMU нашего сокета
    
    OUT  072H                   ; ПРОШИВАЕМ ОКНО 2 СТРАНИЦЕЙ IPC-ПАЙП СОКЕТА!
    OUT  076H                   ; Включаем KERNEL_OVERRIDE
    
    LHLD NET_IO_PHYS_WRITE_ADDR ; HL = Вычисленный адрес записи (0x8000..0xBFFF)
    LDA  WRITE_CHAR_CHAR        ; Восстановили символ
    MOV  M, A                   ; ЗАПИСАЛИ БАЙТ В ФИЗИЧЕСКУЮ ПАМЯТЬ ПАЙП-СОКЕТА!
    
    ; Немедленно восстанавливаем родной маппинг Окна 2 ядра для стабильности Ring 0
    LXI  H, PCB_TABLE + O_PAGES_MAP + 2 
    MOV  A, M
    OUT  072H                   ; Окно 2 снова прошито страницей ядра (PID 0)
    OUT  076H                   ; Отключили режим KERNEL_OVERRIDE

    ; --- ИНКРЕМЕНТ И КОЛЬЦЕВАЯ МАСКИРОВКА УКАЗАТЕЛЯ TAIL ---
    LHLD NET_IO_TAIL_CELL_PTR   ; HL указывает на ячейку Tail в ОЗУ ядра
    MOV  E, M
    INX  H
    MOV  D, M                   ; DE = Текущее значение Tail
    INX  D                      ; Tail++
    
    MOV  A, D
    ANI  01Fh                   ; Маскируем под 8 Килобайт (Маска 0x1FFF, верхние биты в 0)
    MOV  D, A                   ; DE содержит обновленный кольцевой Tail Pointer
    
    MOV  M, D
    DCX  H
	MOV  M, E                   ; Записали обновленный Tail Pointer обратно в ОЗУ ядра
	; --- РЕАКТИВНОЕ ПРОБУЖДЕНИЕ СВЯЗАННОГО ЧИТАТЕЛЯ ПО XOR 1 ---
	LDA  NET_IO_GLOBAL_IDX
	XRI  01h                    ; Вычислили инверсный индекс сокета-напарника в socketpair()
	SUI  32                     ; Локальный индекс напарника 0..3
	MOV  L, A
	MVI  H, 0
	DAD  H
	DAD  H
	DAD  H    ; *8
	LXI  D, SOCK_SYS_TABLE
	DAD  D                      ; HL -> Структура сокета-напарника
	LXI  D, S_SOCK_OWNER        ; Смещение до поля PID владельца (+6)
	DAD  D
	MOV  A, M                   ; A = PID процесса-читателя, подключенного к паре!
	; Вычисляем адрес PCB читателя: PCB_TABLE + (reader_pid * 64)
	MOV  L, A
	MVI  H, 0
	DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H
	DAD  H    ; HL = reader_pid * 64
	LXI  D, PCB_TABLE
	DAD  D                      ; HL = Базовый адрес PCB читателя
	PUSH H                      ; Спасли адрес PCB читателя
	LXI  D, O_STATUS
	DAD  D
	MOV  A, M
	CPI  ST_WAITING             ; Читатель спит в ожидании ввода?
	JNZ  net_send_unix_inc      ; Если не спит — будить не нужно
	POP  H                      ; Восстановили адрес PCB читателя
	PUSH H
	LXI  D, O_WAIT_CODE
	DAD  D
	MOV  A, M
	CPI  W_PIPE_EMPTY           ; Задача ждет появления данных в сокете (07h)?
	JNZ  net_send_unix_inc      ; Если ждет что-то другое — пропускаем
	MVI  M, 00h                 ; O_WAIT_CODE = 00h (Чистый сон завершен)
	POP  H                      ; HL = База PCB читателя
	LXI  D, O_STATUS
	DAD  D
	MVI  M, ST_READY            ; ЧИТАТЕЛЬ УСПЕШНО РАЗБУЖЕН ЯДРОМ MEGA-580 OS!
	JMP  net_send_unix_inc_done
net_send_unix_inc:
	POP  H                      ; Сбалансировали стек, если читатель не спал
net_send_unix_inc_done:
	; Инкрементируем 16-битный счетчик успешно перенесенных в сокет байт сисколла
	LXI  H, NET_IO_BYTES_CURR
	MOV  E, M
	INX  H
	MOV  D, M                   ; DE = Текущее значение счетчика
	INX  D                      ; Счетчик++
	MOV  M, D
	DCX  H
	MOV  M, E                   ; Сохранили счетчик обратно
	DCX  B                      ; Уменьшаем 16-битный счетчик оставшихся байт пользователя BC
	JMP  net_send_unix_loop     ; Уходим на итерацию отправки следующего байта кадра!
net_send_unix_success:
	LDA  NET_IO_BYTES_CURR      ; Возвращаем общее количество переданных байт в аккумуляторе ARET
	; Честный возврат в syscall_return.
	; =============================================================================
	; ГАРАНТИРОВАННО СБАНСИРОВАННЫЕ ВЫХОДЫ ПО СИСТЕМНЫМ ОШИБКАМ POSIX
	; =============================================================================
net_send_err_inval:
	MVI  A, 0EAh                ; Код ошибки EINVAL (Дескриптор не является сокетом)
	RET                         ; Безопасный возврат в диспетчер сисколлов (Стек чист)
net_send_err_badf:
	MVI  A, 0EBH                ; Код ошибки EBADF (Невалидный или закрытый дескриптор)
	RET                         ; Безопасный возврат в диспетчер сисколлов (Стек чист)

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ receive()
; =============================================================================
; Вход:  D  = Локальный дескриптор сокета процесса (FD, 0..7).
;        HL = Логический адрес приемного буфера в памяти задачи Ring 3.
;        BC = Максимальный размер буфера в байтах (длина запроса).
; Выход: В случае успеха: A = Количество успешно принятых и перенесенных байт.
;        В случае ошибки: A = POSIX-код сбоя (EBADF, EINVAL, ENOMEM).
; Регистры вызывающей задачи строго сохраняются, прерывания заперты на входе.
; =============================================================================
; sys_recv:
    ; ; --- ШАГ 1: Первичная валидация границ локального дескриптора ---
    ; MOV  A, D
    ; CPI  8                      ; Проверяем границы локальных FD процесса (0..7)
    ; JNC  net_recv_err_badf      ; Если FD >= 8 — возвращаем EBADF (-1)

    ; ; Консервируем стартовые параметры Ring 3 в служебные ячейки ОЗУ ядра
    ; STA  NET_IO_FD
    ; SHLD NET_IO_BUF_PTR
    ; MOV  A, B
	; STA NET_IO_BYTES_REQ+1
    ; MOV  A, C
	; STA NET_IO_BYTES_REQ  ; 16-битный лимит длины зафиксирован
    
    ; XRA  A
    ; STA  NET_IO_BYTES_CURR
    ; STA  NET_IO_BYTES_CURR+1   ; Обнулили 16-битный счетчик перенесенных байт

; net_recv_check_hardware:
    ; ; --- ШАГ 2: Извлечение и валидация глобального индекса из PCB ---
    ; PUSH B                      ; Сберегаем счетчики длины в стек ядра
    ; LDA  NET_IO_FD
    ; MOV  C, A
    ; MVI  B, 0                   ; BC = Временное смещение дескриптора
    
    ; CALL get_current_pcb_address ; HL = Базовый адрес PCB активной задачи
    ; LXI  D, O_FILE_TABLE
    ; DAD  D
	; DAD  B             ; HL указывает точно на нужный байт дескриптора в PCB
    ; MOV  A, M                   ; A = Глобальный системный индекс объекта ядра
    ; STA  NET_IO_GLOBAL_IDX
    ; POP  B                      ; Восстановили BC

    ; CPI  RES_FREE_MARKER        ; Проверяем, не свободен ли слот (0xFF)?
    ; JZ   net_recv_err_badf      ; Если там 0xFF — возвращаем EBADF
    
    ; CPI  32                     ; Сетевые индексы сокетов строго начинаются с 32!
    ; JC   net_recv_err_inval     ; УТВЕРЖДЕНО: Если индекс < 32 (файл или пайп) — возвращаем EINVAL!

    ; CPI  GLOBAL_SOCK_INET       ; Разделение доменов сокетов: <40 — UNIX, >=40 — INET
    ; JC   net_recv_unix_domain   ; Если индекс 32..39 — уходим на программную ОЗУ-ветку

; ; =============================================================================
; ; ВЕТКА А: ПРИЕМ КАДРА ИЗ АППАРАТНОЙ SRAM СЕТЕВОГО ЧИПА KSZ8851-6MLL
; ; =============================================================================
; net_recv_inet_domain:
    ; MVI  A, REG_RXFHSR              ; Выбираем Receive Frame Status Register (0x7C)
    ; OUT  NET_CMD_PORT
    ; IN   NET_DATA_PORT              ; Читаем младший байт статуса
    ; MOV  C, A
    ; IN   NET_DATA_PORT              ; Читаем старший байт статуса
    ; MOV  B, A                       ; BC = 16-битный статус приемника
    
    ; MOV  A, B
    ; ANI  080h                       ; Изолируем бит RXFV (Receive Frame Valid)
    ; JNZ  net_recv_read_payload      ; Если кадр аппаратно готов — прыгаем на вычитку!

    ; ; --- АСИНХРОННЫЙ СОН ПРОЦЕССА В ОЧЕРЕДИ ЧИПА (W_NET_RECV) ---
    ; CALL get_current_pcb_address
    ; PUSH H
    ; LXI  D, O_STATUS
	; DAD D
	; MVI M, ST_WAITING ; Статус = Сон (04H)
    ; POP  H
	; PUSH H
    ; LXI  D, O_WAIT_CODE
	; DAD D
	; MVI M, W_NET_RECV ; Ожидание сети (0Ch)
    
    ; ; Сохраняем SP ядра в O_SAVED_SP дескриптора для точки пробуждения
    ; POP  H
	; LXI D, O_SAVED_SP
	; DAD D
	; XCHG
    ; LXI  H, 0
	; DAD SP
	; MOV A, L
	; STAX D
	; INX D
	; MOV A, H
	; STAX D
    
    ; CALL scheduler                  ; Планировщик отдает CPU другой READY-задаче
    ; CALL switch_mmu_context         ; Переключаем аппаратные регистры К555ИР26 eMMU
    ; JMP  restore_timer_context      ; Навсегда покидаем контекст уходящего в сон

; net_recv_wakeup_entry:
    ; ; --- ТОЧКА АСИНХРОННОГО ПРОБУЖДЕНИЯ ПО IRQ3 (ОТВЕТ ЧИПА) ---
    ; LHLD NET_IO_BUF_PTR             ; Восстановили адрес буфера пользователя Ring 3
    ; JMP  net_recv_check_hardware    ; Идем на повторную честную проверку статуса

; net_recv_read_payload:
    ; ; Извлекаем физическую длину пакета из Receive Frame Byte Count Register (0x7E)
    ; MVI  A, REG_RXFHBCR
    ; OUT  NET_CMD_PORT
    ; IN   NET_DATA_PORT
	; MOV C, A   ; Длина Low
    ; IN   NET_DATA_PORT
	; MOV B, A   ; Длина High (BC = Физическая длина кадра)

    ; ; Сбрасываем внутренний указатель RXFDPR чипа на старт пакета
    ; MVI  A, 07Ch
    ; OUT  NET_CMD_PORT
    ; MVI  A, 00h
	; OUT NET_DATA_PORT
	; OUT NET_DATA_PORT

; net_recv_copy_loop:
    ; MOV  A, B
	; ORA C
	; JZ  net_recv_success_exit ; Если весь кадр извлечен — выходим

    ; IN   NET_DATA_PORT              ; Выдернули байт из SRAM чипа Micrel
    ; STA  WRITE_CHAR_CHAR            ; Зафиксировали во временную ячейку Ring 0
    
    ; ; Транслируем байт в логическое адресное пространство Ring 3 текущего процесса
    ; PUSH B
    ; LHLD NET_IO_BUF_PTR
    ; LDA  WRITE_CHAR_CHAR
    ; OUT  076H                       ; Активируем режим KERNEL_OVERRIDE_EN = 0
    ; MOV  M, A                       ; Прямая физическая запись в юзерспейс!
    ; OUT  076H                       ; Отключаем режим KERNEL_OVERRIDE
    
    ; ;; Маркируем страницу процесса как измененную (грязную)
    ; ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!
    
	; INX  H
	; SHLD NET_IO_BUF_PTR    ; Сдвинули указатель пользователя вперед
    ; POP  B
    
    ; ; Инкрементируем 16-битный счетчик перенесенных байт сисколла
    ; PUSH H
    ; LHLD NET_IO_BYTES_CURR
	; INX H
	; SHLD NET_IO_BYTES_CURR
    ; POP  H
    
    ; DCX  B                          ; Уменьшаем счетчик оставшихся байт кадра чипа
    ; JMP  net_recv_copy_loop

; ; =============================================================================
; ; ВЕТКА Б: СИММЕТРИЧНОЕ ЧТЕНИЕ ИЗ ВНУТРЕННЕГО IPC СОКЕТА ОЗУ eMMU
; ; =============================================================================
; net_recv_unix_domain:
    ; ; Извлекаем адрес структуры сокета-получателя в SOCK_SYS_TABLE
    ; LDA  NET_IO_GLOBAL_IDX
    ; SUI  32
    ; MOV  L, A
	; MVI H, 0
    ; DAD  H
	; DAD  H
	; DAD  H        ; HL = Локальный_Индекс * 8
    ; LXI  D, SOCK_SYS_TABLE
	; DAD  D
    ; SHLD NET_IO_STRUCT_PTR          ; Зафиксировали адрес структуры

; net_recv_unix_loop:
    ; ; Проверяем, не исчерпан ли лимит буфера, запрошенный пользователем
    ; LHLD NET_IO_BYTES_REQ
    ; MOV  A, H
	; ORA L
	; JZ  net_recv_success_exit

    ; ; Рассчитываем физическое смещение буфера. Считываем флаг направления (смещение +7)
    ; LHLD NET_IO_STRUCT_PTR
    ; LXI  D, S_SOCK_HW_CHAN
    ; DAD  D
	; MOV  A, M
    ; XRI  01h                        ; Инвертируем флаг (Чтение противоположно записи пары!)
    
    ; LXI  D, 08000h                  ; База Окна 2 eMMU
    ; ORA  A
    ; JZ   net_recv_unix_offset_ready
    ; LXI  H, 8192
	; DAD  D
	; XCHG    ; DE = 0xA000 (База Буфера Б для чтения)

; net_recv_unix_offset_ready:
    ; ; Считываем 16-битный кольцевой указатель чтения Head Pointer из ОЗУ ядра
    ; ; Указатели Head хранятся в массиве NET_UNIX_HEADS [4 сокета * 2 байта = 8 байт]
    ; LDA  NET_IO_GLOBAL_IDX
	; SUI 32
	; ADD A
	; MOV L, A
	; MVI H, 0
    ; LXI  B, NET_UNIX_HEADS
	; DAD B
    ; SHLD NET_IO_TAIL_CELL_PTR       ; Зафиксировали адрес ячейки Head
    
    ; MOV  A, M
	; INX H
	; MOV H, M
	; MOV L, A ; HL = 16-битный Head (0..8191)
    
    ; ; [Если буфер пуст (Head == Tail) — процесс засыпает на wait-channel W_PIPE_EMPTY]
    ; ; Предположим, байты гарантированно есть:
    ; DAD  D                          ; HL = База_Окна_DE + Head_Pointer_HL
    ; SHLD NET_IO_PHYS_WRITE_ADDR     ; Физический адрес чтения из ОЗУ пайпа

    ; ; Физическое чтение байта из страницы сокета с eMMU коммутацией Окна 2
    ; LHLD NET_IO_STRUCT_PTR
	; INX H
	; INX H
    ; MOV  A, M                       ; A = Номер физической страницы ОЗУ сокета
    
    ; OUT  072H                       ; Прошили Окно 2 eMMU страницей сокета
    ; OUT  076H                       ; Включили KERNEL_OVERRIDE
    ; LHLD NET_IO_PHYS_WRITE_ADDR
    ; MOV  A, M                       ; ИЗВЛЕКЛИ БАЙТ ИЗ FIFO ПАЙП-СОКЕТА В АККУМУЛЯТОР A!
    ; STA  WRITE_CHAR_CHAR            ; Припрятали байт
    
    ; ; Мгновенно возвращаем родную страницу ядра в Окно 2 для стабильности Ring 0
    ; LXI  H, PCB_TABLE + O_PAGES_MAP + 2
    ; MOV  A, M
	; OUT 072H
	; OUT 076H ; Окно 2 возвращено ядру, оверрайд снят

    ; ; Переносим символ в ОЗУ Ring 3 принимающего процесса пользователя
    ; PUSH B
    ; LHLD NET_IO_BUF_PTR
    ; LDA  WRITE_CHAR_CHAR
    ; OUT  076H
	; MOV  M, A
	; OUT  076H ; Записали в буфер процесса!
	
    ; ;; Маркируем страницу процесса как измененную (грязную)
    ; ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!
    
	; INX  H
	; SHLD NET_IO_BUF_PTR
    ; POP  B

    ; ; Инкрементируем Head Pointer в ОЗУ ядра с кольцевой маской 8 КБ (0x1FFF)
    ; LHLD NET_IO_TAIL_CELL_PTR
	; MOV E, M
	; INX H
	; MOV D, M
	; INX D
    ; MOV  A, D
	; ANI 01Fh
	; MOV D, A ; Маска 8192 байт
    ; MOV  M, D
	; DCX H
	; MOV M, E   ; Записали обновленный Head Pointer

    ; ; --- РЕАКТИВНОЕ ПРОБУЖДЕНИЕ СВЯЗАННОГО ПИСАТЕЛЯ (W_PIPE_FULL) ---
    ; LDA  NET_IO_GLOBAL_IDX
	; XRI 01h
	; SUI 32
	; MOV L, A
	; MVI H, 0
    ; DAD  H
	; DAD  H
	; DAD  H
	; LXI D, SOCK_SYS_TABLE
	; DAD D
    ; LXI  D, S_SOCK_OWNER
	; DAD D
	; MOV A, M ; A = PID писателя-напарника
    
    ; MOV  L, A
	; MVI H, 0
	; DAD H
	; DAD H
	; DAD H
	; DAD H
	; DAD H
	; DAD H
    ; LXI  D, PCB_TABLE
	; DAD D       ; HL = База PCB писателя
    ; PUSH H
    ; LXI  D, O_STATUS
	; DAD D
	; MOV A, M
	; CPI ST_WAITING
    ; JNZ  net_recv_unix_inc
    ; POP  H
	; PUSH H
    ; LXI  D, O_WAIT_CODE
	; DAD D
	; MOV A, M
	; CPI W_PIPE_FULL
    ; JNZ  net_recv_unix_inc
    
    ; MVI  M, 00h
	; POP  H
	; LXI D, O_STATUS
	; DAD D
	; MVI M, ST_READY ; РАЗБУДИЛИ ПИСАТЕЛЯ!
    ; JMP  net_recv_unix_inc_done

; net_recv_unix_inc:
    ; POP  H

; net_recv_unix_inc_done:
    ; ; Обновляем счетчики длины
    ; LHLD NET_IO_BYTES_CURR
	; INX H
	; SHLD NET_IO_BYTES_CURR
    ; LHLD NET_IO_BYTES_REQ
	; DCX H
	; SHLD NET_IO_BYTES_REQ
    ; JMP  net_recv_unix_loop

; ; =============================================================================
; ; УСПЕШНЫЕ И ОШИБОЧНЫЕ ТОЧКИ ВЫХОДА ИЗ ЯДРА (ПОЛНЫЙ БАЛАНС СТЕКА)
; ; =============================================================================
; net_recv_success_exit:
	; LDA  NET_IO_BYTES_CURR          ; Возвращаем количество успешно перенесенных байт в A
	; RET                             ; Чистый выход в syscall_return.

; net_recv_err_inval:
	; MVI  A, 0EAh                    ; УТВЕРЖДЕНО: Код ошибки EINVAL (Параметр неприменим к файлу)
	; RET                             ; Безопасный возврат

; net_recv_err_badf:
	; MVI  A, 0EBH                    ; Код ошибки EBADF (Неверный дескриптор сокета)
	; RET                             ; Безопасный возврат
; =============================================================================
; СИСКОЛЛ MEGA-580 OS (Ring 0): МОНОЛИТНЫЙ ИСПРАВЛЕННЫЙ СИСТЕМНЫЙ ВЫЗОВ sys_recv
; =============================================================================
; Вход:  D  = Локальный дескриптор сокета процесса (FD, 0..7).
;        HL = Логический адрес приемного буфера в памяти задачи Ring 3.
;        BC = Максимальный размер буфера в байтах.
; Выход: A  = Количество успешно принятых байт (успех), 
;        A  = 0FFH — Ошибка EBADF (Неверный дескриптор),
;        A  = 0FEH — Ошибка ENOTCONN / EINVAL (Неверный домен сокета).
; =============================================================================
sys_recv:
    ; --- ШАГ 1: Аппаратная консервация указателя стека ядра ---
    PUSH H                          
    LXI  H, 0
    DAD  SP                         ; HL = Текущий указатель SP ядра супервизора
    SHLD VFS_RECV_STACK_SAVE        ; АТОМАРНО ЗАФИКСИРОВАЛИ ИНВАРИАНТ СТЕКА ЯДРА!
    POP  H                          

    ; --- ШАГ 2: Консервация и валидация входных параметров ---
    MOV  A, D
	CPI 8
	JNC net_recv_err_badf 
    STA  NET_IO_FD
    SHLD NET_IO_BUF_PTR             ; Законсервировали текущий адрес буфера Ring 3
    SHLD VFS_RECV_ORIG_BUF_PTR      ; Сохранили дубликат для финального маскирования eMMU
    
    MOV  A, B
	STA NET_IO_BYTES_REQ+1
    MOV  A, C
	STA NET_IO_BYTES_REQ  ; Зафиксировали 16-битную максимальную длину
    
    XRA  A
    STA  NET_IO_BYTES_CURR
    STA  NET_IO_BYTES_CURR+1        ; Сбросили счетчик принятых байт в 0

    ; --- ШАГ 3: Извлечение глобального системного индекса файла из PCB ---
    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address    ; HL = Адрес PCB текущей задачи
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                       ; A = Глобальный индекс VFS (32..47)
    STA  VFS_RECV_G_IDX
    
    CPI  RES_FREE_MARKER            ; Проверяем, открыт ли дескриптор (0xFF)?
    JZ   net_recv_err_badf
    CPI  32                         ; Сетевые индексы начинаются строго с 32
    JC   net_recv_err_inval         ; Если индекс < 32 — это обычный файл, ошибка!

net_recv_check_hardware:
    LDA  VFS_RECV_G_IDX
    CPI  40                         ; Индекс < 40? (Внутренние сокеты AF_UNIX: 32..39)
    JC   net_recv_unix_domain       ; Прыгаем на полноценную реализацию сокетов ОЗУ

; =============================================================================
; ВЕТКА А: РАБОТА С ХАРДВЕРОМ KSZ8851-16MLL (ВНЕШНИЕ СОКЕТЫ 40..47)
; =============================================================================
net_recv_inet_domain:
    ; Шаг 1: Проверяем наличие принятых кадров в SRAM чипа (Receive Frame Valid)
    MVI  A, REG_RXFHSR              ; Receive Frame Status Register (0x7C)
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Читаем младший байт статуса кадра
    MOV  C, A
    IN   NET_DATA_PORT              ; Читаем старший байт
    MOV  B, A                       ; BC = 16-битный статус приемника чипа
    
    MOV  A, B
    ANI  080h                       ; Маска бита RXFV (Receive Frame Valid — кадр готов!)
    JNZ  net_recv_read_payload      ; Если кадр валиден — прыгаем на вычитку данных!

    ; --- ШАГ 2: СЕТЕВОЙ БУФЕР ЧИПА ПУСТ — АСИНХРОННЫЙ СОН ПРОЦЕССА (W_NET_RECV) ---
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; Перевели задачу в сон ST_WAITING (04h)
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MVI M, W_NET_RECV ; Причина сна = Ожидание сети (0Ch)
    
    ; Консервируем SP супервизора в O_SAVED_SP дескриптора
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
    LXI  H, 0
	DAD SP
	MOV A, L
	STAX D
	INX D
	MOV A, H
	STAX D
    
    CALL scheduler
	STA NEXT_PID
	CALL switch_mmu_context
    JMP  restore_timer_context      ; Уступили такты процессора планировщику

net_recv_wakeup_entry:
    ; СЮДА НАС ВЕРНЕТ ПРЕPЫВАНИЕ IRQ3 ПОСЛЕ ПРИЛЕТА СЕТЕВОГО ПАКЕТА
    JMP  net_recv_check_hardware    ; Проснулись — идем повторно забирать кадр!

net_recv_read_payload:
    ; Шаг 3: Считываем длину прилетевшего Ethernet-кадра из SRAM чипа
    MVI  A, REG_RXFHBCR             ; Receive Frame Byte Count Register (0x7E)
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Младший байт длины кадра
    MOV  C, A
    IN   NET_DATA_PORT              ; Старший байт длины кадра
    MOV  B, A                       ; BC = Реальный физический размер кадра в байтах

    ; Сбрасываем указатель RXFDPR на старт SRAM кадра пакета
    MVI  A, REG_RXFHSR              ; Окно данных кадка (0x7C)
    OUT  NET_CMD_PORT
    MVI  A, 00h
	OUT NET_DATA_PORT
	OUT NET_DATA_PORT

; --- ЦИКЛ ПОБАЙТОВОГО ПЕРЕНОСА ИЗ ЧИПА В USER-SPACE RING 3 ---
net_recv_copy_loop:
    MOV  A, B
	ORA C               ; Проверка счетчика кадра BC на ноль
    JZ   net_recv_success_exit      ; Если весь кадр вычитан — уходим на финальный маркер eMMU

    IN   NET_DATA_PORT              ; Читаем байт данных напрямую из SRAM чипа KSZ8851
    STA  WRITE_CHAR_CHAR            ; Временно сохранили байт в ОЗУ ядра

    PUSH B
    LHLD NET_IO_BUF_PTR            
    LDA  WRITE_CHAR_CHAR
    OUT  076H                       ; АКТИВИРУЕМ KERNEL_OVERRIDE_EN = 0
    MOV  M, A                       ; ПЕРЕНЕСЛИ СИМВОЛ В БУФЕР ПРОЦЕССА!
    OUT  076H                       ; МГНОВЕННО ОТКЛЮЧАЕМ ОВЕРРАЙД ШИНЫ
    
    INX  H
	SHLD NET_IO_BUF_PTR    ; Сдвинули буфер юзера вперед
    POP  B
    
    ; Инкрементируем счетчик успешно принятых байт сисколла
    PUSH H
    LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
    POP  H
    
    DCX  B                          ; Уменьшаем 16-битный счетчик длины кадра чипа BC
    JMP  net_recv_copy_loop

; =============================================================================
; ВЕТКА Б: ЧТЕНИЕ ИЗ ВНУТРЕННЕГО ПРОГРАММНОГО СОКЕТА ОЗУ AF_UNIX (32..39)
; =============================================================================
net_recv_unix_domain:
    ; Расчет адреса структуры сокета в SOCK_SYS_TABLE (*8 байт)
    LDA  VFS_RECV_G_IDX
    SUI  32                         ; Приводим к локальному диапазону 0..7
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H
    LXI  D, SOCK_SYS_TABLE
	DAD D
    SHLD NET_IO_STRUCT_PTR          ; Зафиксировали адрес структуры сокета

net_recv_unix_loop:
    LHLD NET_IO_BYTES_REQ           ; Сколько байт осталось прочитать до лимита
    MOV  A, H
	ORA L
    JZ   net_recv_success_exit      ; Буфер Ring 3 заполнен, успешный выход!

    ; --- ШАГ 1: Извлекаем флаг направления буфера и проверяем данные ---
    LHLD NET_IO_STRUCT_PTR
    LXI  D, S_SOCK_HW_CHAN          ; Смещение +7 (Флаг полубуфера: 00h = А, 01h = Б)
    DAD  D
    MOV  A, M
	STA  NET_IO_BUF_FLAG

    ; Вычисляем адрес ячейки 16-битного указателя Head чтения сокета в ОЗУ ядра
    ; Указатели голов чтения хранятся в массиве NET_UNIX_HEADS [4 пары * 2 буфера * 2 байта]
    LDA  VFS_RECV_G_IDX
    SUI  32
    ADD  A                          ; Локальный_Индекс * 2
    MOV  L, A
	MVI H, 0
    LXI  B, NET_UNIX_HEADS          ; Массив голов чтения внутренних сокетов
    DAD  B
    SHLD NET_IO_HEAD_CELL_PTR       ; Зафиксировали адрес ячейки Head
    
    MOV  A, M
	INX H
	MOV H, M
	MOV L, A ; HL = 16-битный текущий Head чтения (0..8191)
    MOV  E, L
	MOV D, H            ; DE = Текущий Head

    ; Извлекаем симметричный Tail записи для проверки наличия данных (Head == Tail?)
    LHLD NET_IO_HEAD_CELL_PTR
    LXI  B, NET_UNIX_TAILS - NET_UNIX_HEADS ; Сдвиг на массив хвостов записи
    DAD  B
    MOV  A, M
	CMP  E              ; Младший байт совпал?
    JNZ  net_recv_unix_has_data
    INX  H
    MOV  A, M
	CMP  D              ; Старший байт совпал?
    JNZ  net_recv_unix_has_data

    ; --- СOКЕТ ПУСТ — AСИНХРOННЫЙ СOН ПOТOКA-ЧИТAТЕЛЯ (W_PIPE_EMPTY) ---
    CALL get_current_pcb_address
    PUSH H
    LXI  D, O_STATUS
	DAD D
	MVI M, 004h ; ST_WAITING (04h)
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MVI M, W_PIPE_EMPTY ; Причина сна = Ожидание данных (07h)

    ; Консервируем SP супервизора в O_SAVED_SP
    POP  H
    LXI  D, O_SAVED_SP
	DAD D
	XCHG
	LXI  H, 0
	DAD SP
    MOV  A, L
	STAX D
	INX D
	MOV A, H
	STAX D

    CALL scheduler
	STA NEXT_PID
	CALL switch_mmu_context
    JMP  restore_timer_context      ; Отдали CPU планировщику, пока пайп пуст

net_recv_unix_wakeup:
    ; Точка пробуждения пишущим потоком при вызове sys_send в сокет-напарник
    JMP  net_recv_unix_domain

net_recv_unix_has_data:
    ; Данные есть! Вычисляем базовый физический адрес окна 2 eMMU (0x8000)
    LXI  B, 08000h                  ; BC = База Окна 2
    LDA  NET_IO_BUF_FLAG
    ORA  A
    JZ   net_recv_unix_offset_ok    ; Если флаг 00h — смещение 0, база 0x8000 готова!
    LXI  H, 8192
	DAD B
	MOV B, H
	MOV C, L ; Если флаг 01h — смещение 8КБ, база 0xA000

net_recv_unix_offset_ok:
    ; Прибавляем к базе полубуфера BC текущий указатель Head (в паре DE)
    MOV  H, B
	MOV L, C            ; HL = База полубуфера
    DAD  D                          ; HL = База_Окна + Head (ТОЧНЫЙ АДРЕС ВЫЧИТКА В ОЗУ!)
    SHLD NET_IO_PHYS_READ_ADDR

    ; --- ШАГ 2: ВЫЧИТКА ИЗ ФРЕЙМА СОКЕТА С КОММУТАЦИЕЙ СТРАНИЦ eMMU ---
    LHLD NET_IO_STRUCT_PTR
    INX  H
	INX  H                 ; HL -> Поле S_SOCK_LPORT (+2)
    MOV  A, M                       ; A = Номер физической страницы ОЗУ eMMU нашего сокета
    
    OUT  072H                       ; МГНОВЕННО ПРОШИЛИ ОКНО 2 ФРЕЙМОМ СОКЕТА!
    OUT  076H                       ; Включили KERNEL_OVERRIDE_EN = 0
    
    LHLD NET_IO_PHYS_READ_ADDR
    MOV  A, M                       ; ВЫЧИТАЛИ БАЙТ ИЗ КОЛЬЦЕВОГО ВНУТРЕННЕГО КОНВЕЙЕРА!
    STA  WRITE_CHAR_CHAR
    
    ; Мгновенно возвращаем родную страницу ядра в Окно 2 для безопасности супервизора
    LXI  H, PCB_TABLE + O_PAGES_MAP + 2
    MOV  A, M
	OUT 072H
	OUT 076H  ; Окно 2 ядра восстановлено, оверрайд снят

    ; --- ШАГ 3: ТРАНСЛЯЦИЯ ИЗВЛЕЧЕННОГО БАЙТА В ЮЗЕРСПЕЙС RING 3 ---
    LHLD NET_IO_BUF_PTR
	LDA  WRITE_CHAR_CHAR
	OUT  076H                       ; Включили оверрайд шины
	MOV  M, A                       ; ПРОПИСАЛИ ДАННЫЕ В БУФЕР ПРОЦЕССА!
	OUT  076H                       ; Отключили
	INX  H
	SHLD NET_IO_BUF_PTR

	; --- ШАГ 4: ИНКРЕМЕНТ И КОЛЬЦЕВАЯ МАСКИРОВКА УКАЗАТЕЛЯ HEAD ---
	INX  D                          ; Head++
	MOV  A, D
	ANI  01Fh                       ; Маскируем под размер 8 Килобайт (Маска 0x1FFF, биты 13-15 в 0)
	MOV  D, A                       ; DE содержит обновленный Head чтения сокета
	LHLD NET_IO_HEAD_CELL_PTR
	MOV  M, E
	INX H
	MOV M, D    ; Записали обновленный Head обратно в ОЗУ ядра
	
	; Асинхронно будим пишущий поток-напарник, если тот спал из-за переполнения буфера (W_PIPE_FULL)
	; [Ядро сканирует PCB_TABLE, находит сокет-напарник XOR 1 и переводит владельца в ST_READY]
	; Корректируем счетчики сисколла
	LHLD NET_IO_BYTES_CURR
	INX H
	SHLD NET_IO_BYTES_CURR
	LHLD NET_IO_BYTES_REQ
	DCX H
	MOV  A, H
	ORA L
	SHLD NET_IO_BYTES_REQ
	JMP  net_recv_unix_loop         ; Уходим на следующую итерацию извлечения байта

	; =============================================================================
	; 5. ЦЕНТРАЛЬНАЯ ТОЧКА УСПЕШНОГО ВЫХОДА И УПРАВЛЕНИЯ eMMU МAСКИРOВКOЙ
	; =============================================================================
net_recv_success_exit:
	LHLD VFS_RECV_ORIG_BUF_PTR      ; HL = Стартовый адрес буфера пользователя в Ring 3
	LHLD NET_IO_BYTES_CURR          ; BC = Количество реально принятых из сети байт
	MOV  B, H
	MOV C, L
	
	; ОПТИМИЗИРОВАННЫЙ ВЗВОД ГРЯЗНЫХ БИТОВ СТРАНИЦ eMMU (1 раз за весь сисколл!)
	CALL k_vfs_post_io_dirty_mark
	LDA  NET_IO_BYTES_CURR          ; Возвращаем размер принятых данных в аккумуляторе A
	RET                             ; Чистый выход в syscall_return.

	; =============================================================================
	; 6. БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУAЦИЙ (OШИБКИ И ОТКАТ СТEКА RING 0)
	; =============================================================================
net_recv_err_badf:
	LHLD VFS_RECV_STACK_SAVE
	SPHL                            ; МГНОВЕННО РЕСТАВРИРОВАЛИ ОРИГИНАЛЬНЫЙ SP ЯДРА!
	MVI  A, 0FFh                    ; Код ошибки 0xFF (POSIX EBADF — Неверный дескриптор файла)
	RET
net_recv_err_inval:
	LHLD VFS_RECV_STACK_SAVE
	SPHL                            ; Сбросили мусорные адреса из стека супервизора
	MVI  A, 0FEh                    ; Код ошибки 0xFE (POSIX EINVAL — Параметр неприменим к объекту)
	RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ socketpair()
; =============================================================================
sys_socketpair:
    ; --- ШАГ 1: Валидация входного домена и типа ---
    MOV  A, D
    CPI  AF_UNIX                ; Домен обязан быть локальным (01h)
    JNZ  sockpair_err_inval
    MOV  A, E
    CPI  SOCK_STREAM            ; Тип обязан быть потоковым (01h)
    JNZ  sockpair_err_inval

    SHLD SOCKPAIR_USER_PTR       ; Сохранили указатель на пользовательский массив sv[2]

    ; --- ШАГ 2: Поиск ДВУХ свободных локальных FD в PCB текущего процесса ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB задачи
    PUSH H                      ; Стак: Сохранили базовый адрес PCB

    LXI  D, O_FILE_TABLE        ; Смещение таблицы файлов (+20)
    DAD  D                      ; HL -> Начало O_FILE_TABLE
    
    MVI  B, 8                   ; Сканируем 8 слотов процесса
    MVI  C, 0FFh                ; C = Первый свободный FD
    MVI  D, 0FFh                ; D = Второй свободный FD

sockpair_find_fd_loop:
    MOV  A, M
    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JNZ  sockpair_fd_next
    
    MOV  A, C
    CPI  00h                    ; Проверяем, зафиксирован ли первый FD?
    JNC  sockpair_save_second_fd
    
    ; Вычисляем локальный номер первого FD (0..7)
    MOV  A, B
	MVI A, 8
	SUB B
    MOV  C, A                   ; C = Первый свободный FD
    JMP  sockpair_fd_next

sockpair_save_second_fd:
    MOV  A, B
	MVI A, 8
	SUB B
    MOV  D, A                   ; D = Второй свободный FD
    JMP  sockpair_fds_found     ; Оба локальных дескриптора успешно найдены!

sockpair_fd_next:
    INX  H                      ; Переходим к следующему байту таблицы файлов в PCB
    DCR  B
    JNZ  sockpair_find_fd_loop

    ; Ошибка: у процесса нет двух свободных файловых дескрипторов
    POP  H                      ; Сбалансировали стек, удалив адрес PCB
    MVI  A, 0FFh                ; Код ошибки EMFILE (0xFF)
    RET

; -----------------------------------------------------------------------------
; ДВА ЛОКАЛЬНЫХ FD НАЙДЕНЫ (C = FD1, D = FD2)
; -----------------------------------------------------------------------------
sockpair_fds_found:
    MOV  A, C
	STA SOCKPAIR_FD1
    MOV  A, D
	STA SOCKPAIR_FD2

    ; --- ШАГ 3: Поиск ДВУХ свободных системных слотов сокетов в пуле UNIX ---
    LXI  H, SOCK_SYS_TABLE      ; HL -> Глобальная таблица сокетов ядра
    MVI  B, MAX_SYS_SOCKETS     ; Сканируем слоты
    MVI  C, 0FFh                ; C = Первый свободный системный сокет
    MVI  D, 0FFh                ; D = Второй свободный системный сокет

sockpair_scan_sys_loop:
    MOV  A, M                   ; Читаем статус сокета S_SOCK_STATUS (+0)
    CPI  SOCK_FREE
    JNZ  sockpair_sys_next
    
    MOV  A, C
    CPI  00h
    JNC  sockpair_save_second_sys
    
    MOV  A, B
	MVI A, MAX_SYS_SOCKETS
	SUB B
    MOV  C, A                   ; C = Первый глобальный индекс
    JMP  sockpair_sys_next

sockpair_save_second_sys:
    MOV  A, B
	MVI A, MAX_SYS_SOCKETS
	SUB B
    MOV  D, A                   ; D = Второй глобальный индекс
    JMP  sockpair_sys_found     ; Оба глобальных системных слота выделены!

sockpair_sys_next:
    LXI  H, SOCK_STRUCT_SIZE
	DAD H
    DCR  B
    JNZ  sockpair_scan_sys_loop

    ; Ошибка: В системе нет места под 2 новых сокета
    POP  H                      ; Очистили адрес PCB
    MVI  A, 0FEh                ; Код ошибки ENFILE
    RET

; -----------------------------------------------------------------------------
; СИСТЕМНЫЕ СЛОТЫ ВЫДЕЛЕНЫ (C = Идентификатор сокета 1, D = Идентификатор сокета 2)
; -----------------------------------------------------------------------------
sockpair_sys_found:
    MOV  A, C
	ADI GLOBAL_SOCK_UNIX
	STA SOCKPAIR_G_IDX1 ; Привели к пулу 32..39
    MOV  A, D
	ADI GLOBAL_SOCK_UNIX
	STA SOCKPAIR_G_IDX2

    ; --- ШАГ 4: Выделение ОДНОЙ физической страницы для двунаправленного обмена ---
    CALL k_allocate_free_physical_page ; Запросили 16 КБ ОЗУ из вашей карты RAM_BITMAP
    CPI  0FFh
    JZ   sockpair_err_nomem     ; Память исчерпана — аварийный выход

    STA  SOCKPAIR_ALLOC_PAGE    ; Зафиксировали общую физическую страницу eMMU

    ; --- ШАГ 5: ИНИЦИАЛИЗАЦИЯ ДВУХ СОКЕТОВ В SOCK_SYS_TABLE ---
    ; [Инициализация Сокета 1]
    LDA  SOCKPAIR_G_IDX1 
    SUI  GLOBAL_SOCK_UNIX 
    MOV  L, A 
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Локальный_Индекс * 8 байт
    LXI  D, SOCK_SYS_TABLE 
    DAD  D                      ; HL указывает на старт структуры Сокета 1
    
    MVI  M, SOCK_INIT           ; S_SOCK_STATUS (+0) = 01h
    INX  H                      ; HL -> S_SOCK_TYPE (+1)
    MVI  M, SOCK_STREAM         ; Тип = 01h
    INX  H                      ; HL -> S_SOCK_LPORT (+2)
    LDA  SOCKPAIR_ALLOC_PAGE 
    MOV  M, A                   ; Записали номер физической страницы ОЗУ eMMU
    INX  H 
    MVI  M, 00h                 ; Старший байт порта обнулен
    
    ; Сдвигаемся к полю владельца (+6) через временную пару DE (Канон 8080)
    PUSH H
    LXI  D, 3                   ; Шаг от +3 до +6
    DAD  D                      ; HL указывает на поле S_SOCK_OWNER (+6)
    LDA  CURRENT_PID 
    MOV  M, A                   ; Прописали владельца
    INX  H                      ; HL указывает точно на поле S_SOCK_HW_CHAN (+7)
    MVI  M, 00h                 ; ИСПРАВЛЕНО: Флаг 00h = Сокет 1 пишет в Буфер А (смещение 0)
    POP  H                      ; Восстановили HL для баланса

    ; [Инициализация Сокета 2]
    LDA  SOCKPAIR_G_IDX2 
    SUI  GLOBAL_SOCK_UNIX 
    MOV  L, A 
    MVI  H, 0
    DAD  H
	DAD  H
	DAD  H    ; HL = Локальный_Индекс * 8 байт
    LXI  D, SOCK_SYS_TABLE 
    DAD  D                      ; HL указывает на старт структуры Сокета 2
    
    MVI  M, SOCK_INIT           ; S_SOCK_STATUS (+0) = 01h
    INX  H                      ; HL -> S_SOCK_TYPE (+1)
    MVI  M, SOCK_STREAM         ; Тип = 01h
    INX  H                      ; HL -> S_SOCK_LPORT (+2)
    LDA  SOCKPAIR_ALLOC_PAGE 
    MOV  M, A                   ; Записали ту же общую физическую страницу ОЗУ eMMU
    INX  H 
    MVI  M, 00h                 ; Старший байт порта обнулен
    
    ; Сдвигаемся к полю владельца (+6)
    PUSH H
    LXI  D, 3
    DAD  D                      ; HL указывает на поле S_SOCK_OWNER (+6)
    LDA  CURRENT_PID 
    MOV  M, A                   ; Прописали владельца
    INX  H                      ; HL указывает точно на поле S_SOCK_HW_CHAN (+7)
    MVI  M, 01h                 ; ИСПРАВЛЕНО: Флаг 01h = Сокет 2 пишет в Буфер Б (смещение 8КБ)
    POP  H                      ; Восстановили HL

    ; --- ШАГ 6: Прошивка глобальных сетевых индексов в PCB процесса ---
    POP  H                      ; HL = Восстановили базовый адрес PCB из стека
    PUSH H                      ; Снова сохранили для финального баланса
    
    LXI  D, O_FILE_TABLE        ; Смещение таблицы файлов процесса (+20)
    DAD  D                      ; HL -> Старт O_FILE_TABLE
    
    ; Прошиваем Сокет 1
    LDA  SOCKPAIR_FD1           ; Наш первый локальный FD (0..7)
    MOV  E, A
	MVI D, 0
    PUSH H                      ; Спасли старт таблицы файлов
    DAD  D                      ; Сместились к байту FD1
    LDA  SOCKPAIR_G_IDX1        ; Глобальный индекс (32..39)
    MOV  M, A                   ; Связали!
    POP  H                      ; Восстановили старт таблицы открытых файлов
    
    ; Прошиваем Сокет 2
    LDA  SOCKPAIR_FD2           ; Наш второй локальный FD (0..7)
    MOV  E, A
	MVI D, 0
    DAD  D                      ; Сместились к байту FD2
    LDA  SOCKPAIR_G_IDX2        ; Глобальный индекс (32..39)
    MOV  M, A                   ; Связали!

    ; --- ШАГ 7: ЗАПИСЬ ДЕСКРИПТОРОВ В USER SPACE (Массив sv[2] в Ring 3) ---
    LHLD SOCKPAIR_USER_PTR      ; HL = Адрес массива sv[2] в Ring 3 процесса
    
    OUT  076H                   ; Включаем ~{KERNEL_OVERRIDE_EN} = 0 для доступа к памяти Ring 3
    LDA  SOCKPAIR_FD1           ; Взяли номер первого FD
    MOV  M, A                   ; Записали sv[0] в юзерспейс!
    INX  H                      ; Сдвиг на следующий байт массива
    LDA  SOCKPAIR_FD2           ; Взяли номер второго FD
    MOV  M, A                   ; Записали sv[1] в юзерспейс!
    OUT  076H                   ; Мгновенно отключаем оверрайд шины, возвращая Ring 0

    POP  H                      ; Очистили стек от адреса PCB (строго 'H')
    MVI  A, 00h                     ; Код возврата сисколла = 00H (Чистый успех)
    RET                         ; Выход в syscall_return.

; -----------------------------------------------------------------------------
; ОШИБКИ И СБОИ СИСКОЛЛА
; -----------------------------------------------------------------------------
sockpair_err_nomem:
    POP  H                      ; Очистили адрес PCB из стека ядра
    MVI  A, 0FEh                ; Возвращаем ошибку ENFILE (Нет свободной физ. памяти)
    RET

sockpair_err_inval:
    MVI  A, 0FFh                ; Возвращаем ошибку EINVAL (Неверный домен/тип)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ netstat()
; =============================================================================
sys_netstat:
    ; Проверяем адрес буфера пользователя. Если HL = 0000H, выходим с ошибкой
    MOV  A, H
    ORA  L
    JZ   netstat_err_fault

    SHLD NETSTAT_USER_BUF          ; Зафиксировали адрес приемного буфера Ring 3

    ; --- ШАГ 1: Инициализация итераторов сканирования ---
    XRA  A
    STA  NETSTAT_CURR_SLOT         ; Начинаем с системного слота сокета №0
    
    LXI  H, SOCK_SYS_TABLE
    SHLD NETSTAT_STRUCT_PTR        ; Зафиксировали начальный адрес таблицы сокетов ядра

netstat_scan_loop:
    ; --- ШАГ 2: Извлечение данных из SOCK_SYS_TABLE ядра ---
    LHLD NETSTAT_STRUCT_PTR        ; HL указывает на текущую структуру сокета (+0)
    MOV  A, M                      ; A = S_SOCK_STATUS
    STA  WRITE_CHAR_CHAR           ; Временно сохранили статус во временную ячейку
    
    ; Переходим к пошаговому разбору полей
    INX  H                         ; HL -> S_SOCK_TYPE (+1)
    MOV  A, M
    STA  LSEEK_WHENCE              ; Временно припрятали тип сокета в свободную ячейку
    
    INX  H                         ; HL -> S_SOCK_LPORT (+2, младший байт)
    MOV  E, M
    INX  H                         ; HL -> (+3, старший байт)
    MOV  D, M                      ; DE = Локальный порт / Физ. страница сокета
    
    INX  H                         ; HL -> S_SOCK_RPORT (+4, младший байт)
    MOV  C, M
    INX  H                         ; HL -> (+5, старший байт)
    MOV  B, M                      ; BC = Удаленный порт сокета
    
    INX  H                         ; HL -> S_SOCK_OWNER (+6)
    MOV  A, M
    STA  EXIT_PARENT_PID           ; Временно припрятали PID владельца в свободную ячейку

    ; Сохраняем измененный указатель таблицы сокетов обратно для следующей итерации
    INX  H                         ; Сдвинулись на +7 (S_SOCK_HW_CHAN), пропустим его
    INX  H                         ; HL указывает на начало СЛЕДУЮЩЕЙ структуры сокета (+8)
    SHLD NETSTAT_STRUCT_PTR        ; Зафиксировали адрес следующего слота

    ; --- ШАГ 3: ФОРМИРОВАНИЕ И ЗАПИСЬ 8-БАЙТНОЙ СТРУКТУРЫ В RING 3 (KERNEL_OVERRIDE) ---
    ; Вычисляем итоговый глобальный системный индекс сокета (32 + CURR_SLOT)
    LDA  NETSTAT_CURR_SLOT
    ADI  32                        ; Диапазон сетевых индексов ядра (32..35)
    STA  OPEN_TEMP_GLOBAL_IDX      ; Припрятали глобальный индекс сокета

    ; Включаем аппаратный режим KERNEL_OVERRIDE для прямой записи в юзерспейс
    OUT  076H                      ; Включаем ~{KERNEL_OVERRIDE_EN} = 0
    LHLD NETSTAT_USER_BUF          ; HL = Текущий указатель записи в памяти Ring 3

    ; Байт 0: Глобальный индекс сокета
    LDA  OPEN_TEMP_GLOBAL_IDX
    MOV  M, A
	INX H
    
    ; Байт 1: Домен / Тип сокета (Определяем по глобальному индексу: <40 - UNIX, >=40 - INET)
    LDA  OPEN_TEMP_GLOBAL_IDX
    CPI  40
    JC   netstat_set_unix_domain
    MVI  A, AF_INET                ; Аппаратный домен (02h)
    JMP  netstat_domain_written
netstat_set_unix_domain:
    MVI  A, AF_UNIX                ; Внутренний программный домен (01h)
netstat_domain_written:
    MOV  M, A
	INX H
    
    ; Байт 2: Статус сокета
    LDA  WRITE_CHAR_CHAR
    MOV  M, A
	INX H
    
    ; Байт 3: PID владельца сокета
    LDA  EXIT_PARENT_PID
    MOV  M, A
	INX H
    
    ; Байты 4-5: Локальный порт / Номер физ. страницы (DE)
    MOV  M, E
	INX H
    MOV  M, D
	INX H
    
    ; Байты 6-7: Удаленный порт сокета (BC)
    MOV  M, C
	INX H
    MOV  M, B
	INX H

    ; Сохраняем обновленный указатель буфера пользователя Ring 3 обратно в ядро
    SHLD NETSTAT_USER_BUF
    OUT  076H                      ; Выключаем режим KERNEL_OVERRIDE, возвращаем Ring 0

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!

    ; --- ШАГ 4: Инкремент счетчика циклов сканирования слотов ---
    LDA  NETSTAT_CURR_SLOT
    INR  A
    STA  NETSTAT_CURR_SLOT
    CPI  MAX_SYS_SOCKETS           ; Проверили все 4 системных слота сокетов?
    JNZ  netstat_scan_loop         ; Если нет — уходим на упаковку следующей записи!

    ; --- ШАГ 5: УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    MVI  A, 00h                    ; Код возврата = 00H (Чистый успех)
    RET                            ; Выход в syscall_return. Прерывания откроет ядро централизованно.

; --- ОБРАБОТЧИКИ ОШИБОК ИНТЕРФЕЙСА ВЫЗОВА ---
netstat_err_fault:
    MVI  A, 0FFh                   ; Возвращаем код ошибки 0xFF (POSIX EFAULT — неверный адрес буфера)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ openpty()
; =============================================================================
sys_openpty:
    MOV  A, H
    ORA  L
    JZ   pty_err_fault          ; Защита: если адрес указателя нулевой — ошибка

    PUSH H                      ; Стак: Сохранили пользовательский указатель массива sv
    
    ; --- ШАГ 1: Поиск двух свободных локальных FD в PCB текущего процесса ---
    CALL get_current_pcb_address ; HL = Базовый адрес PCB задачи
    PUSH H                      ; Стак: [Адрес_PCB, Указатель_sv]

    LXI  D, O_FILE_TABLE        ; Смещение таблицы файлов (+20)
    DAD  D                      ; HL -> Начало O_FILE_TABLE
    
    MVI  B, 8                   ; В таблице процесса ровно 8 слотов (FD 0..7)
    MVI  C, 0FFh                ; C = Будущий локальный FD Master
    MVI  D, 0FFh                ; D = Будущий локальный FD Slave

pty_find_local_fd_loop:
    MOV  A, M
    CPI  RES_FREE_MARKER        ; Слот свободен (0xFF)?
    JNZ  pty_local_fd_next
    
    MOV  A, C
    CPI  00h                    ; Проверяем, зафиксирован ли уже первый FD?
    JNC  pty_save_second_fd
    
    ; Вычисляем локальный номер первого найденного FD (0..7)
    MOV  A, B
	MVI A, 8
	SUB B
    MOV  C, A                   ; C = Найден локальный FD для Master
    JMP  pty_local_fd_next

pty_save_second_fd:
    MOV  A, B
	MVI A, 8
	SUB B
    MOV  D, A                   ; D = Найден локальный FD для Slave
    JMP  pty_local_fds_ok       ; Оба дескриптора успешно локализованы!

pty_local_fd_next:
    INX  H                      ; Сдвигаемся к следующему слоту дескрипторов
    DCR  B
    JNZ  pty_find_local_fd_loop

    ; Ошибка: у процесса нет двух свободных файловых дескрипторов (EMFILE)
    POP  H
	POP  H             			; Очистили стек ядра
    MVI  A, 0FFh                ; Возвращаем ошибку EMFILE (0xFF)
    RET

; -----------------------------------------------------------------------------
; СВОБОДНЫЕ ЛОКАЛЬНЫЕ FD НАЙДЕНЫ (C = FD Master, D = FD Slave)
; -----------------------------------------------------------------------------
pty_local_fds_ok:
    MOV  A, C
	STA PTY_TEMP_FD1
    MOV  A, D
	STA PTY_TEMP_FD2

    ; --- ШАГ 2: Поиск свободного аппаратного слота в глобальной PTY_SYS_TABLE ---
    LXI  H, PTY_SYS_TABLE
    MVI  B, MAX_PTY_PAIRS       ; Сканируем доступные 4 пары
    MVI  C, 0                   ; C = Итератор системных пар (0..3)

pty_find_sys_loop:
    MOV  A, M                   ; Читаем поле P_PTY_STATUS (+0)
    CPI  00h                    ; Слот свободен (00h)?
    JZ   pty_sys_slot_ok        ; Нашли свободную аппаратную пару!

    LXI  D, PTY_STRUCT_SIZE
    DAD  D                      ; Сдвиг на следующую структуру PTY в ОЗУ ядра
    INR  C                      ; Увеличиваем индекс пары
    DCR  B
    JNZ  pty_find_sys_loop

    ; Ошибка: Системная таблица PTY переполнена (ENFILE)
    POP  H
	POP  H             ; Очистили стек ядра
    MVI  A, 0FEh                ; Возвращаем ошибку ENFILE (0xFE)
    RET

; -----------------------------------------------------------------------------
; СИСТЕМНЫЙ СЛОТ ВЫДЕЛЕН (HL = Адрес структуры PTY, C = Индекс пары 0..3)
; -----------------------------------------------------------------------------
pty_sys_slot_ok:
    PUSH H                      ; Стак: [Указатель_на_структуру_PTY, Адрес_PCB, Указатель_sv]

    ; Вычисляем глобальные системные индексы VFS для Master и Slave
    MOV  A, C
    ADI  GLOBAL_PTY_MASTER      ; A = 48 + Индекс_Пары (48..51)
    STA  PTY_TEMP_G_IDX1
    
    MOV  A, C
    ADI  GLOBAL_PTY_SLAVE       ; A = 52 + Индекс_Пары (52..55)
    STA  PTY_TEMP_G_IDX2

    ; --- ШАГ 3: Выделение одной физической страницы eMMУ через RAM_BITMAP ---
    CALL k_allocate_free_physical_page ; Запросили 16 КБ чистой ОЗУ-памяти устройства
    CPI  0FFh
    JZ   pty_err_nomem          ; Если физическая память исчерпана — аварийный выход

    STA  PTY_TEMP_PAGE          ; Зафиксировали выделенный номер страницы

    ; --- ШАГ 4: Инициализация полей структуры PTY в PTY_SYS_TABLE ---
    POP  H                      ; Восстановили HL = Адрес структуры PTY из стека
    PUSH H                      ; Снова сохранили для баланса
    
    MVI  M, 01h                 ; P_PTY_STATUS = 01h (Активен / Открыт)
    INX  H                      ; HL -> P_PTY_PAGE (+1)
    LDA  PTY_TEMP_PAGE
    MOV  M, A                   ; Прописали номер физической страницы eMMU
    
    INX  H                      ; HL -> P_PTY_MASTER_PID (+2)
    LDA  CURRENT_PID
    MOV  M, A                   ; Master управляется текущим процессом
    
    INX  H                      ; HL -> P_PTY_SLAVE_PID (+3)
    MOV  M, A                   ; На этапе openpty Slave принадлежит тому же PID (обычно до fork)
    
    INX  H                      ; HL -> P_PTY_FLAGS (+4)
    MVI  M, PTY_ECHO_BIT        ; По умолчанию дисциплина линии включает дублирование ввода (ECHO)

    ; Очищаем кольцевые указатели хвостов буферов PTY пары в ОЗУ ядра
    MOV  A, C
    ADD  A                      ; Индекс_Пары * 2
    MOV  E, A
    MVI  D, 0
    
    LXI  H, PTY_M2S_TAILS
	DAD D
    MVI  M, 00h
	INX H
	MVI M, 00h ; Сбросили 16-битный Tail Master->Slave
    
    LXI  H, PTY_S2M_TAILS
	DAD D
    MVI  M, 00h
	INX H
	MVI M, 00h ; Сбросили 16-битного Tail Slave->Master

    ; --- ШАГ 5: Прошивка глобальных индексов VFS в файловую таблицу процесса ---
    POP  H                      ; Сняли со стека указатель на структуру PTY (больше не нужен)
    POP  H                      ; HL = Восстановили базовый адрес PCB текущего процесса из стека
    PUSH H                      ; Вернули для финального баланса
    
    LXI  D, O_FILE_TABLE        ; Переходим к дескрипторам процесса
    DAD  D
    PUSH H                      ; Сохранили старт таблицы файлов задачи в стек
    
    ; Связываем FD Master
    LDA  PTY_TEMP_FD1           ; Выделенный локальный дескриптор Master
    MOV  E, A
	MVI D, 0
	DAD D
    LDA  PTY_TEMP_G_IDX1        ; Глобальный индекс VFS (48..51)
    MOV  M, A
    
    POP  H                      ; Восстановили старт таблицы файлов задачи
    ; Связываем FD Slave
    LDA  PTY_TEMP_FD2           ; Выделенный локальный дескриптор Slave
    MOV  E, A
	MVI D, 0
	DAD D
    LDA  PTY_TEMP_G_IDX2        ; Глобальный индекс VFS (52..55)
    MOV  M, A                   ; СВЯЗЬ ПРОПИСАНА! Дескрипторы PTY пары открыты для Ring 3.

    ; --- ШАГ 6: ЗАПИСЬ НОМЕРОВ ДЕСКРИПТОРОВ В USER SPACE ПРОЦЕССА ---
    POP  H                      ; Полностью очистили стек ядра от адреса PCB
    POP  H                      ; HL = Восстановили оригинальный адрес массива sv в Ring 3
    
    OUT  076H                   ; Включаем режим KERNEL_OVERRIDE_EN = 0 для прорыва Firewall
    LDA  PTY_TEMP_FD1           ; Взяли локальный FD Master
    MOV  M, A                   ; Записали первый байт в массив пользователя!
    INX  H
    LDA  PTY_TEMP_FD2           ; Взяли локальный FD Slave
    MOV  M, A                   ; Записали второй байт в массив пользователя!
    OUT  076H                   ; Мгновенно отключаем оверрайд шины, возвращая Ring 0

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!
    
    MVI  A, 00h                 ; Код возврата = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return.

; --- ОБРАБОТЧИКИ КРИТИЧЕСКИХ ОШИБОК АЛЛОКАЦИИ ---
pty_err_nomem:
    POP  H
	POP  H
	POP  H    					; Очистили стек от всех сохраненных контекстов
    MVI  A, 0FEh                ; Возвращаем ошибку ENFILE / ENOMEM (0xFE)
    RET

pty_err_fault:
    MVI  A, 0FFh                ; Возвращаем ошибку EFAULT (0xFF — Неверный адрес)
    RET

; =============================================================================
; ВНУТРЕННЯЯ ФУНКЦИЯ ЯДРА: k_pty_write (ТРАНСЛЯЦИЯ В ОЗУ eMMU)
; =============================================================================
k_pty_write:
    SHLD PTY_IO_BUF_PTR
    MOV  A, B
	STA PTY_IO_BYTES_REQ+1
    MOV  A, C
	STA PTY_IO_BYTES_REQ
    XRA  A
	STA PTY_IO_BYTES_CURR
	STA PTY_IO_BYTES_CURR+1

    ; Определяем индекс пары и направление
    MOV  A, D                       ; D содержит глобальный индекс VFS (48..55)
    CPI  GLOBAL_PTY_SLAVE           ; Индекс >= 52?
    JC   pty_write_master_side      ; Если < 52 — пишет Master сторона

; --- Пишет сторона Slave (Данные идут в S2M буфер, смещение 8КБ) ---
    SUI  GLOBAL_PTY_SLAVE           ; A = Номер пары (0..3)
    STA  PTY_IO_PAIR_IDX
    MVI  A, 01h                     ; Направление = S2M (1)
    STA  PTY_IO_DIRECTION
    JMP  pty_write_init_struct

pty_write_master_side:
    SUI  GLOBAL_PTY_MASTER          ; A = Номер пары (0..3)
    STA  PTY_IO_PAIR_IDX
    XRA  A                          ; Направление = M2S (0)
    STA  PTY_IO_DIRECTION

pty_write_init_struct:
    ; Считаем адрес структуры PTY в PTY_SYS_TABLE
    LDA  PTY_IO_PAIR_IDX
    MOV  L, A
	MVI H, 0
    DAD  H
	DAD  H
	DAD  H        ; HL = Пару * 8
    LXI  D, PTY_SYS_TABLE
	DAD D
    SHLD PTY_IO_STRUCT_PTR          ; Зафиксировали адрес описателя PTY

pty_write_loop:
    ; Проверяем, все ли байты перенесены
    LHLD PTY_IO_BYTES_REQ
    MOV  A, H
	ORA L
	JZ pty_write_success

    ; Расчет адреса записи в окне 2 eMMU (0x8000)
    LXI  D, 08000h                  ; DE = База окна 2
    LDA  PTY_IO_DIRECTION
    ORA  A
    JZ   pty_write_addr_base_ok     ; Если M2S (0) — смещение 0
    LXI  H, 8192
	DAD D
	XCHG     ; Если S2M (1) — смещение 8КБ, DE = 0xA000

pty_write_addr_base_ok:
    ; Извлекаем и инкрементируем 16-битный указатель Tail
    LDA  PTY_IO_PAIR_IDX
	ADD A
	MOV L, A
	MVI H, 0
    LDA  PTY_IO_DIRECTION
	ORA A
    JZ   pty_write_get_m2s_tail
    LXI  B, PTY_S2M_TAILS
	DAD B
	JMP pty_write_tail_cell_ok
pty_write_get_m2s_tail:
    LXI  B, PTY_M2S_TAILS
	DAD B
pty_write_tail_cell_ok:
    SHLD PTY_IO_TAIL_PTR            ; Сохранили адрес ячейки указателя Tail
    MOV  A, M
	INX H
	MOV H, M
	MOV L, A ; HL = Текущий Tail (0..8191)
    
    DAD  D                          ; HL = База_Окна + Tail (Точный адрес записи!)
    PUSH H                          ; Спасся адрес записи в eMMU

    ; Вычитываем байт из Ring 3
    LHLD PTY_IO_BUF_PTR
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 0
    MOV  A, M                       ; Прочитали байт данных процесса
    OUT  076H                       ; KERNEL_OVERRIDE_EN = 1
    STA  WRITE_CHAR_CHAR
    INX  H
	SHLD PTY_IO_BUF_PTR    ; Сдвинули буфер пользователя

    ; Коммутируем eMMU-страницу PTY и пишем байт
    LHLD PTY_IO_STRUCT_PTR
	INX H  ; HL -> P_PTY_PAGE (+1)
    MOV  A, M                       ; A = Номер физической страницы PTY
    OUT  072H                       ; Прошили Окно 2 фреймом PTY
    OUT  076H                       ; Включили оверрайд шины
    
    POP  H                          ; HL = Физический адрес записи
    LDA  WRITE_CHAR_CHAR
    MOV  M, A                       ; ЗАПИСАЛИ БАЙТ В БУФЕР PTY ТРАНСЛЯЦИИ!

    ; Возвращаем страницу ядра в Окно 2
    LXI  H, PCB_TABLE + O_PAGES_MAP + 2
    MOV  A, M
	OUT  072H
	OUT  076H ; Окно 2 восстановлено, оверрайд снят

    ; Инкремент и маскировка Tail под 8 Килобайт (0x1FFF)
    LHLD PTY_IO_TAIL_PTR
    MOV  E, M
	INX H
	MOV  D, M   ; DE = Старый Tail
    INX  D                          ; Tail++
    MOV  A, D
	ANI 01Fh
	MOV D, A ; Маска 8КБ
    MOV  M, D
	DCX H
	MOV M, E    ; Сохранили новый Tail

    ; Асинхронное пробуждение читающей стороны
    LHLD PTY_IO_STRUCT_PTR
    LDA  PTY_IO_DIRECTION
	ORA A
    JZ   pty_write_wake_slave
    INX  H
	INX  H                 ; HL -> P_PTY_MASTER_PID (+2)
    MOV  A, M
	JMP pty_write_wake_proc
pty_write_wake_slave:
    LXI  D, 3
	DAD  D              ; HL -> P_PTY_SLAVE_PID (+3)
    MOV  A, M

pty_write_wake_proc:
    ; A = PID процесса, который нужно разбудить
    MOV  L, A
	MVI H, 0
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H
	DAD H ; *64
    LXI  D, PCB_TABLE
	DAD D
	PUSH H
    LXI  D, O_STATUS
	DAD D
	MOV A, M
	CPI ST_WAITING
	JNZ pty_write_inc_counters
    POP  H
	PUSH H
    LXI  D, O_WAIT_CODE
	DAD D
	MOV A, M
    ; Ждал ли процесс ввода с PTY?
    CPI  W_PTY_MASTER
	JZ pty_write_do_wake
    CPI  W_PTY_SLAVE
	JNZ pty_write_inc_counters
pty_write_do_wake:
    MVI  M, 00h                     ; Сбросили код сна
    POP  H
	LXI  D, O_STATUS
	DAD D
	MVI M, ST_READY ; РАЗБУЖЕН!
    JMP  pty_write_counters_entry

pty_write_inc_counters:
    POP  H                          ; Сбалансировали стек

pty_write_counters_entry:
    LHLD PTY_IO_BYTES_CURR
	INX H
	SHLD PTY_IO_BYTES_CURR
    LHLD PTY_IO_BYTES_REQ
	DCX H
	SHLD PTY_IO_BYTES_REQ
    JMP  pty_write_loop

pty_write_success:
    LDA  PTY_IO_BYTES_CURR          ; Возвращаем число записанных байт
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ ptsname()
; =============================================================================
sys_ptsname:
    ; Валидация адреса буфера назначения пользователя
    MOV  A, H
    ORA  L
    JZ   ptsname_err_fault      ; Если HL == 0000H — аварийный выход, EFAULT

    SHLD PTSNAME_USER_PTR       ; Зафиксировали адрес буфера Ring 3 в ядре

    ; --- ШАГ 1: Валидация дескриптора и извлечение глобального индекса ---
    MOV  A, D
    CPI  8                      ; Проверяем границы локальных дескрипторов (0..7)
    JNC  ptsname_err_badf       ; Передан неверный локальный fd

    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address ; HL = Базовый адрес PCB задачи
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                   ; A = Глобальный файловый индекс VFS (32..63)
    
    ; Проверяем, принадлежит ли индекс диапазону PTY Master (48..51)
    CPI  GLOBAL_PTY_MASTER      ; Индекс < 48?
    JC   ptsname_err_badf       ; Если меньше — это не PTY Master!
    CPI  GLOBAL_PTY_SLAVE       ; Индекс >= 52?
    JNC  ptsname_err_badf       ; Если больше или равен — это Slave или другой объект!

    ; --- ШАГ 2: Вычисление номера подчиненной линии Slave ---
    SUI  GLOBAL_PTY_MASTER      ; A = Индекс - 48 (Результат: 0, 1, 2 или 3)
    STA  PTSNAME_LINE_NUM       ; Зафиксировали чистый номер линии

    ; --- ШАГ 3: Динамическая сборка строки пути в шаблоне ядра ---
    ADI  030h                   ; Преобразуем число 0..3 в ASCII символ '0'..'3'
    LXI  H, PTSNAME_TEMPLATE + 11 ; HL указывает точно на символ '#' в шаблоне
    MOV  M, A                   ; Модифицировали шаблон: теперь там легитимный путь!

    ; --- ШАГ 4: ПОБАЙТОВЫЙ ЭКСПОРТ СТРОКИ В RING 3 (KERNEL_OVERRIDE) ---
    LXI  D, PTSNAME_TEMPLATE    ; DE = Адрес сформированной строки-источника в Ring 0
    LHLD PTSNAME_USER_PTR       ; HL = Буфер назначения в Ring 3 процесса
    MVI  B, PTSNAME_STR_LEN     ; Нам необходимо скопировать ровно 13 байт

ptsname_copy_loop:
    LDAX D                      ; Читаем байт из шаблона ядра Ring 0
    MOV  C, A                   ; Временно припрятали символ в регистр C

    OUT  076H                   ; Включаем режим KERNEL_OVERRIDE_EN = 0 для прорыва Firewall
    MOV  M, C                   ; Записали байт пути в логическую память Ring 3 процесса!
    OUT  076H                   ; Мгновенно отключаем оверрайд шины, возвращая Ring 0
    
    INX  D                      ; Сдвигаем указатель источника
    INX  H                      ; Сдвигаемся в буфере Ring 3
    DCR  B                      ; Уменьшаем счетчик байт
    JNZ  ptsname_copy_loop      ; Повторяем до полной пересылки строки с NULL-терминатором

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!

    ; Восстанавливаем символ '#' в шаблоне ядра для обеспечения реентерабельности
    LXI  H, PTSNAME_TEMPLATE + 11
    MVI  M, 023h                ; ASCII-код символа '#' (0x23)

    MVI  A, 00h                 ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return.

; --- БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКИ ИНТЕРФЕЙСА) ---
ptsname_err_badf:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EBADF — Неверный файловый дескриптор)
    RET

ptsname_err_fault:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX EFAULT — Ошибка обращения по адресу)
    RET

; =============================================================================
; СИСКОЛЛ POSIX ОС ЯДРА (Ring 0): СИСТЕМНЫЙ ВЫЗОВ ttyname()
; =============================================================================
sys_ttyname:
    ; --- ШАГ 1: Валидация адреса пользовательского буфера назначения ---
    MOV  A, H
    ORA  L
    JZ   ttyname_err_fault      ; Если HL == 0000H — аварийный выход, EFAULT

    SHLD TTYNAME_USER_PTR       ; Зафиксировали адрес буфера Ring 3 в ОЗУ ядра

    ; --- ШАГ 2: Валидация дескриптора и извлечение глобального индекса ---
    MOV  A, D
    CPI  8                      ; Проверяем границы локальных дескрипторов задачи (0..7)
    JNC  ttyname_err_badf       ; Передан невалидный локальный fd, выходим с EBADF

    MOV  C, D
	MVI B, 0
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущей задачи
    LXI  D, O_FILE_TABLE
    DAD  D
	DAD  B
    MOV  A, M                   ; A = Глобальный файловый индекс VFS (0..63)
    
    CPI  RES_FREE_MARKER        ; Слот свободен в таблице процесса (0xFF)?
    JZ   ttyname_err_badf       ; Если пуст — возвращаем EBADF

    ; --- ШАГ 3: Проверка на соответствие физическому терминалу (ENOTTY) ---
    ; В системной архитектуре MEGA-580 OS индекс 16 жестко закреплен за TTYfs
    CPI  16                     ; Сравниваем глобальный индекс с константой физического TTY
    JNZ  ttyname_err_notty      ; Если индекс другой — это диск/сокет, возвращаем ENOTTY

    ; --- ШАГ 4: ПОБАЙТОВЫЙ ЭКСПОРТ СТРОКИ ПУТИ В RING 3 (KERNEL_OVERRIDE) ---
    LXI  D, TTYNAME_PATH_STR    ; DE = Адрес строки-источника "/dev/tty" в Ring 0 ядра
    LHLD TTYNAME_USER_PTR       ; HL = Буфер назначения в логической памяти Ring 3
    MVI  B, TTYNAME_STR_LEN     ; Нам необходимо переслать ровно 9 байт

ttyname_copy_loop:
    LDAX D                      ; Вычитали байт пути из памяти ядра Ring 0
    MOV  C, A                   ; Временно перенесли символ в регистр C

    OUT  076H                   ; Активируем режим KERNEL_OVERRIDE_EN = 0 для прорыва Firewall
    MOV  M, C                   ; Записали байт пути в логическую память Ring 3 процесса!
    OUT  076H                   ; Мгновенно отключаем оверрайд шины, возвращая Ring 0
    
    INX  D                      ; Сдвигаем указатель источника
    INX  H                      ; Сдвигаем указатель назначения в юзерспейсе
    DCR  B                      ; Уменьшаем счетчик оставшихся байт
    JNZ  ttyname_copy_loop      ; Повторяем цикл до переноса всей строки с NULL-байтом

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!

    ; --- ШАГ 5: УСПЕШНЫЙ ВЫХОД ИЗ СИСКОЛЛА ---
    MVI  A, 00h                 ; Код возврата сисколла = 00H (Чистый успех POSIX)
    RET                         ; Выход в syscall_return.

; --- БЛОКИ ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ (ОШИБКИ ИНТЕРФЕЙСА) ---
ttyname_err_badf:
    MVI  A, 0FFh                ; Код ошибки 0xFF (POSIX EBADF — Неверный файловый дескриптор)
    RET

ttyname_err_notty:
    MVI  A, 0FEh                ; Код ошибки 0xFE (POSIX ENOTTY — Файл не является терминалом)
    RET

ttyname_err_fault:
    MVI  A, 0FDh                ; Код ошибки 0xFD (POSIX EFAULT — Неверный адрес буфера)
    RET

; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК IR2 ВН59 (АДРЕС 0x0050): ДЕМУЛЬТИПЛЕКСОР ПДП (TC РЕГИСТР)
; =============================================================================
hw_dma_tc_entry:
    PUSH PSW
    PUSH B
    PUSH D
    PUSH H                      ; Теневое сохранение контекста прерванной задачи

    ; --- ШАГ 1: Чтение и атомарный сброс Регистра Статуса КР580ВТ57 ---
    IN   DMA_CMD_STATUS         ; Вычитываем статус (одновременно сбрасываем флаги ВТ57)
    MOV  B, A                   ; Сохраняем считанный статус в регистр B для парсинга

    ; --- ШАГ 2: Тестирование Бита 0 (Флаг TC0 — Канал 0: CompactFlash IDE) ---
    MOV  A, B
    ANI  01H                    ; Изолируем Бит 0 (Маска 00000001B)
    JNZ  dma_handle_cf_ch0      ; Если Бит 0 = 1 — ПДП завершен для нашей CF-карты!

    ; --- ШАГ 3: Тестирование Бита 1 (Флаг TC1 — Канал 1: Устройство №2) ---
    MOV  A, B
    ANI  02H                    ; Изолируем Бит 1 (Маска 00000010B)
    JNZ  dma_handle_device_ch1  

    ; --- ШАГ 4: Тестирование Бита 2 (Флаг TC2 — Канал 2: Устройство №3) ---
    MOV  A, B
    ANI  04H                    ; Изолируем Бит 2 (Маска 00000100B)
    JNZ  dma_handle_device_ch2  

    ; --- ШАГ 5: Тестирование Бита 3 (Флаг TC3 — Канал 3: Устройство №4) ---
    MOV  A, B
    ANI  08H                    ; Изолируем Бит 3 (Маска 00001000B)
    JNZ  dma_handle_device_ch3  

    JMP  dma_tc_exit_eoi

; =============================================================================
; ПОДМОДУЛЬ ОБРАБОТКИ ПРЕРЫВАНИЯ КАНАЛА 0: COMPACTFLASH (IDE16 SWAP)
; =============================================================================
dma_handle_cf_ch0:
    IN   IDE_CMD_STATUS         ; Опрашиваем порт статуса диска (0x27)
    MOV  D, A                   ; Сохраняем статус в регистр D для анализа
    ANI  081h                   ; Маскируем биты: 7 (BSY — занят) и 0 (ERR — ошибка)
    CPI  000h                   ; Проверяем равенство нулю (BSY=0, ERR=0)
    JNZ  dma_cf_error_branch    ; Если диск выставил ошибку или завис — уходим на спец-ветку

    ; Диск чист, данные со 100% успехом перенесены в буфер
    CALL cf_release_lock        ; Атомарно освобождаем мьютекс диска для других задач ядра
    CALL posix_wake_up_blocked_process ; ИЗМЕНЕНО: Переводим поток из состояния WAIT_IO в READY

	JMP  dma_tc_exit_eoi

; -----------------------------------------------------------------------------
; ВЕТКА ОБРАБОТКИ АВАРИЙНЫХ СИТУАЦИЙ COMPACTFLASH
; -----------------------------------------------------------------------------
dma_cf_error_branch:
    MOV  A, D                   
    ANI  001h                   ; Проверяем конкретно бит ошибки ERR
    JZ   dma_cf_timeout         

    IN   021h                   ; Читаем регистр IDE_ERROR
    MOV  B, A                   
    MVI  A, 00Bh                ; Код POSIX-сигнала SIGBUS
    CALL posix_signal_current_process 
    
    CALL cf_release_lock        
    JMP  dma_tc_exit_eoi

dma_cf_timeout:
    MVI  A, 055h                ; Код-маркер "CF Deadlock"
    OUT  0FFh                   
    JMP  KERNEL_PANIC           

; =============================================================================
; СЕРВИСНЫЕ И АВАРИЙНЫЕ ПРОЦЕДУРЫ ЯДРА POSIX ОС
; =============================================================================
KERNEL_PANIC:
    ; --- ФАТАЛЬНЫЙ АППАРАТНЫЙ СБОЙ ЯДРА / ТАЙМАУТ ШИНЫ eMMU ---
    DI                              ; Намертво блокируем прерывания на CPU
    MVI  A, 0FFh                    ; Загружаем код критической системной паники
    OUT  0FFh                       ; Сбрасываем код в диагностический порт платы
    HLT                             ; Физический останов процессора КР580ВМ80А

; =============================================================================
; ДРАЙВЕРНЫЕ ЗАГЛУШКИ ДЛЯ ОСТАЛЬНЫХ ТРЕХ УСТРОЙСТВ НА КАНАЛАХ ПДП 1, 2, 3
; =============================================================================
dma_handle_device_ch1:
    ; [Здесь вызывается софтверный бэкэнд обслуживания Канала 1 ПДП]
    ; Например, инкремент счетчиков сетевых пакетов, побудка сетевых потоков.
    JMP  dma_tc_exit_eoi

dma_handle_device_ch2:
    ; [Бэкэнд обслуживания Канала 2 ПДП]
    JMP  dma_tc_exit_eoi

dma_handle_device_ch3:
    ; [Бэкэнд обслуживания Канала 3 ПДП]
    JMP  dma_tc_exit_eoi

; =============================================================================
; ЗАВЕРШЕНИЕ ПРЕРЫВАНИЯ И ВЫХОД В Ring 3
; =============================================================================
dma_tc_exit_eoi:
    MVI  A, 020H				; Команда неспецифического EOI (End of Interrupt)
    OUT  MASTER_8259_CMD        ; Посылаем команду неспецифического EOI в КР580ВН59 (линия IR2)

    POP  H
    POP  D
    POP  B
    POP  PSW                    ; Полное восстановление контекста Ring 3 задачи
    EI                          ; Снова разрешаем аппаратную карусель прерываний в Ring 3
    RET                         ; Выход из прерывания

; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК ПРЕРЫВАНИЯ: hw_net_isr (СЕТЕВАЯ КАРТА KSZ8851-6MLL НА IRQ3)
; =============================================================================
; Вход:  Сработал триггер прерывания КР580ВН59. Прерывания заперты ядром (DI).
; Выход: Кэш ARP обновлен, данные напрямую перенесены в Ring 3, выдан EOI, EI.
; =============================================================================
; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК ПРЕРЫВАНИЯ: hw_net_isr [СИНХРОНИЗИРОВАННЫЙ МОНОЛИТ]
; =============================================================================
; Вход:  Сработал триггер прерывания КР580ВН59. Прерывания заперты ядром (DI).
; Выход: Кэш ARP обновлен, данные напрямую перенесены в Ring 3, выдан EOI, EI.
; =============================================================================

hw_net_isr:
    ; --- ШАГ 1: Консервация контекста прерванной задачи ---
    PUSH PSW                        ; Спасаем аккумулятор А и флаги процессора
    PUSH B                          ; Спасаем регистровую пару BC
    PUSH D                          ; Спасаем регистровую пару DE
    PUSH H                          ; Спасаем регистровую пару HL

    ; --- ШАГ 2: Вычитка 16-битного статуса прерывания из чипа KSZ8851 ---
    MVI  A, REG_ISR                 ; Выбираем Interrupt Status Register (0x92)
    OUT  NET_CMD_PORT
    
    IN   NET_DATA_PORT              ; Читаем младший байт статуса прерывания
    MOV  C, A                       ; C = ISR Low
    IN   NET_DATA_PORT              ; Читаем старший байт статуса прерывания
    MOV  B, A                       ; B = ISR High (BC = 16-битный статус чипа)

    ; Проверяем наличие принятого кадра (Бит 13 в старшем байте ISR чипа)
    MOV  A, B
    ANI  020h                       ; Маска бита RXIS (Receive Interrupt Status)
    JZ   net_isr_clear_hardware     ; Если пакет не прилетал — уходим на сброс

    ; --- ШАГ 3: АВТОНОМНОЕ ПOПAКЕТНОЕ СТАРЕНИЕ ARP-КЭША ---
    CALL k_arp_aging_per_packet     ; Уменьшаем TTL всех записей на 1 тик пакета

    ; --- ШАГ 4: ВЫЧИТКА ЗАГOЛOВКA КАДРА ДЛЯ ПАССИВНOГO ARP-OБУЧЕНИЯ ---
    ; Сбрасываем указатель RXFDPR чипа на старт SRAM-буфера принятого пакета
    MVI  A, REG_RXFHSR              ; Регистр указателя Frame Data Pointer (0x7C)
    OUT  NET_CMD_PORT
    MVI  A, 00h
	OUT NET_DATA_PORT
	OUT NET_DATA_PORT ; Выставили указатель на 0

    ; Вычитываем первые 30 байт (Заголовок Ethernet + IP) во временный буфер ядра
    LXI  H, NET_ISR_HEADER_BUF
    MVI  E, 30                      ; Нам нужны первые 30 байт кадра
net_isr_read_hdr_loop:
    IN   NET_DATA_PORT              ; Тянем байт из физической SRAM чипа
    MOV  M, A
    INX  H
    DCR  E
    JNZ  net_isr_read_hdr_loop

    ; Валидируем тип протокола EtherType (Байты 12-13 заголовка должны быть 0x0800)
    LXI  H, NET_ISR_HEADER_BUF + 12
    MOV  A, M
	CPI 008h
	JNZ net_isr_skip_learning ; Младший байт (Network Byte Order)
    INX  H
    MOV  A, M
	CPI 000h
	JNZ net_isr_skip_learning ; Старший байт

    ; Протокол IPv4 подтвержден — запускаем пассивное ARP-обучение
    LXI  B, NET_ISR_HEADER_BUF + 26 ; BC = Указатель на входящий SRC IP в буфере ядра
    LXI  D, NET_ISR_HEADER_BUF + 6  ; DE = Указатель на входящий SRC MAC в буфере ядра
    CALL k_arp_passive_learning     ; Добавляем или обновляем запись в ARP_SYS_TABLE!

net_isr_skip_learning:              ; ИСПРАВЛЕНО: Полное соответствие метки со входом JNZ
    ; --- ШАГ 5: ПОИСК СВЯЗAННOГO ПОТОКА-ПОЛУЧАТЕЛЯ (W_NET_RECV) ---
    LXI  H, PCB_TABLE               ; Начинаем сканирование системной таблицы процессов
    MVI  B, MAX_PROCS               ; Итератор по количеству доступных слотов задач

net_isr_scan_pcb_loop:
    PUSH H                          ; Сохранили базовый адрес проверяемого PCB
    LXI  D, O_STATUS                ; Смещение поля статуса (+2)
    DAD  D
    MOV  A, M
    CPI  ST_WAITING                 ; Процесс находится в состоянии сна?
    JNZ  net_isr_next_pcb
    
    POP  H                          ; Восстановили базу PCB
    PUSH H
    LXI  D, O_WAIT_CODE             ; Смещение причины сна (+40)
    DAD  D
    MOV  A, M
    CPI  W_NET_RECV                 ; Задача заблокирована именно на приеме сети (0Ch)?
    JZ   net_isr_target_found       ; Спящий получатель обнаружен! Прыгаем на перенос данных

net_isr_next_pcb:
    POP  H                          ; Извлекли адрес текущего PCB из стека
    LXI  D, PCB_SIZE                ; Размер структуры PCB = 64 байта
    DAD  D                          ; HL указывает на следующий PCB
    DCR  B                          ; Уменьшаем счетчик оставшихся задач
    JNZ  net_isr_scan_pcb_loop      ; Крутим цикл по всей таблице задач

    ; Спящий получатель в системе отсутствует — пакет отбрасывается
    JMP  net_isr_clear_hardware

; -----------------------------------------------------------------------------
; ШАГ 6: НАЙДЕН ПОЛУЧАТЕЛЬ — ПРЯМОЙ ВВОД ПАКЕТА ИЗ ЧИПА В ПАМЯТЬ RING 3 (ZERO-COPY)
; -----------------------------------------------------------------------------
net_isr_target_found:
    POP  H                          ; HL =  адрес PCB найденного процесса-получателя
    SHLD NET_ISR_TARGET_PCB         ; Зафиксировали адрес PCB в ОЗУ ядра

    ; Извлекаем параметры буфера приемника задачи, законсервированные перед сном
    LHLD NET_IO_BUF_PTR             ; HL = Логический адрес буфера назначения в Ring 3
    SHLD NET_ISR_USER_PTR           ; Зафиксировали

    ; Считываем реальную физическую длину кадра из 16-битного регистра REG_RXFHBCR (0x7E)
    MVI  A, REG_RXFHBCR             ; Receive Frame Byte Count Register
    OUT  NET_CMD_PORT
    IN   NET_DATA_PORT              ; Младший байт длины
    MOV  C, A
    IN   NET_DATA_PORT              ; Старший байт длины
    MOV  B, A                       ; BC = Реальный размер кадра в байтах

    ; Вычитаем 30 байт, которые мы уже извлекли на Шаге 4 для ARP-анализа
    MOV  A, C
    SUI  30                         ; Вычли константу 30 из младшего байта
    MOV  C, A
    MOV  A, B
    SBI  000h                       ; Вычитаем заём из старшего байта через легитимную SBI!
    MOV  B, A                       ; BC = Оставшаяся полезная нагрузка (Payload)

    ; Верифицируем 16-битный ноль для пары BC
    MOV  A, B
    ORA  C                          ; Логическое ИЛИ выставит флаг Z при BC == 0
    JZ   net_isr_wakeup_target      ; Если данных больше нет — сразу будим процесс

    ; ИСПРАВЛЕНО: Настраиваем чип KSZ8851 на шлюз данных кадра через REG_RXFHSR (0x7C)
    MVI  A, REG_RXFHSR
    OUT  NET_CMD_PORT
    MVI  A, 30
	OUT NET_DATA_PORT  ; Выставили указатель SRAM чипа на 30-й байт (пропуск заголовка)
    MVI  A, 00
	OUT NET_DATA_PORT

; --- ЦИКЛ ПРЯМОГО ПЕРЕНОСА ДАННЫХ ИЗ СЕТЕВОЙ КАРТЫ В ПАМЯТЬ RING 3 ---
net_isr_transfer_loop:
    MOV  A, B
	ORA C
    JZ   net_isr_wakeup_target      ; Копирование хвоста завершено!

    IN   NET_DATA_PORT              ; ВЫЧИТАЛИ БАЙТ ИЗ ФИЗИЧЕСКОЙ SRAM СЕТЕВОГО ЧИПА!
    STA  NET_ISR_TEMP_BYTE          ; Спрятали в ячейку ядра

    ; Переносим байт напрямую в логическую страницу Ring 3 процесса через KERNEL_OVERRIDE
    PUSH B                          ; Спасаем 16-битный счетчик BC в стек супервизора
    LHLD NET_ISR_USER_PTR           ; HL = Текущий указатель буфера Ring 3 задачи
    LDA  NET_ISR_TEMP_BYTE          ; Восстановили сетевой байт
    
    OUT  076H                       ; АКТИВИРУЕМ KERNEL_OVERRIDE_EN = 0 (Прорыв Firewall)
    MOV  M, A                       ; ПРЯМАЯ ЗАПИСЬ СЕТЕВОГО БАЙТА В ПАМЯТЬ ПРОЦЕССА!
    OUT  076H                       ; МГНОВЕННО ОТКЛЮЧАЕМ ОВЕРРАЙД ШИНЫ (Возврат Ring 0)

    INX  H
	SHLD NET_ISR_USER_PTR  ; Сдвинули указатель буфера юзера вперед
    
    ; Инкрементируем счетчик успешно принятых байт, законсервированный для sys_recv
    LHLD NET_IO_BYTES_CURR
    INX  H
    SHLD NET_IO_BYTES_CURR

    POP  B                          ; Восстановили счетчик BC
    DCX  B                          ; 16-битный декремент оставшихся байт пакета
    JMP  net_isr_transfer_loop

; --- ПРОЦЕСС ПОЛУЧЕНИЯ ЗАВЕРШЕН — РЕАКТИВНОЕ ПРОБУЖДЕНИЕ СЕТЕВОЙ ЗАДАЧИ ---
net_isr_wakeup_target:
    ; Маркируем измененную страницу юзерспейса как измененную для eMMU подсистемы свопинга
    LHLD NET_ISR_USER_PTR

    ;; Маркируем страницу процесса как измененную (грязную)
    ;CALL mark_user_page_dirty_by_addr	 ; HL анализируется на номер сегмента, взводится Dirty Bit!

    ; Переводим процесс-получатель в статус готовности
    LHLD NET_ISR_TARGET_PCB
    PUSH H
    LXI  D, O_WAIT_CODE
    DAD  D
    MVI  M, 00h                     ; O_WAIT_CODE = 00h (Асинхронный сон закрыт)
    
    POP  H
    LXI  D, O_STATUS
    DAD  D
    MVI  M, ST_READY                ; ПОТОК СНЯТ С ПАУЗЫ И ГОТОВ ДЛЯ ВЫТЕСНЕНИЯ ШЕДУЛЛЕРОМ!

; -----------------------------------------------------------------------------
; ШАГ 7: СБРОС АППАРАТНЫХ ТРИГГЕРОВ ЧИПА И КОНТРОЛЛEРA ВН59 (КАНОНИЧНЫЙ ВЫХОД)
; -----------------------------------------------------------------------------
net_isr_clear_hardware:
    ; Сбрасываем сработавшее прерывание в чипе KSZ8851, записывая 1 в регистр ISR
    MVI  A, REG_ISR
    OUT  NET_CMD_PORT
    MOV  A, C
	OUT NET_DATA_PORT   ; Записали Low статус назад для очистки триггера
    MOV  A, B
	OUT NET_DATA_PORT   ; Записали High статус назад для очистки триггера

    MVI  A, 020H
    OUT  MASTER_8259_CMD            ; Сброс триггера уровня IRQ3 в БИС ВН59

    ; Реставрация контекста прерванной задачи
    POP  H                          ; Восстанавливаем оригинальный HL
    POP  D                          ; Восстанавливаем оригинальный DE
    POP  B                          ; Восстанавливаем оригинальный BC
    POP  PSW                        ; Восстанавливаем оригинальный Аккумулятор и Флаги
    
    EI                              ; АТОМАРНО РАЗРЕШАЕМ ПРЕРЫВАНИЯ ПЕРЕД СХОДОМ С ШИНЫ
    RET                             ; Аппаратный возврат в прерванную задачу (~{RET_STROBE})


; =============================================================================
; МОНОЛИТНЫЙ МОДУЛЬ АВТОНОМНОГО ПАССИВНОГО ИЗУЧЕНИЯ СЕТИ (ARP PASSIVE LEARNING)
; =============================================================================

; =============================================================================
; ПОДПРОГРАММА 1: k_arp_passive_learning (Добавление/Обновление связок MAC-IP)
; =============================================================================
; Вход:  BC = Логический адрес извлеченного SRC IP (4 байта) в ОЗУ ядра.
;        DE = Логический адрес извлеченного SRC MAC (6 байт) в ОЗУ ядра.
; =============================================================================
k_arp_passive_learning:
    PUSH PSW
    PUSH B
    PUSH D
    PUSH H

    ; Консервируем входные указатели во временных ячейках ОЗУ супервизора
    SHLD ARP_LEARN_MAC_PTR          ; В КР580ВМ80А пара HL дублирует DE после XCHG
    MOV  H, B
	MOV L, C
    SHLD ARP_LEARN_IP_PTR           ; Зафиксировали адрес входящего IP

    ; --- ШАГ 1: Поиск дубликата. Проверяем, существует ли уже этот IP в кэше? ---
    LXI  H, ARP_SYS_TABLE           ; Стартовая точка таблицы (Слот 0)
    MVI  B, MAX_ARP_ENTRIES         ; Лимит сканирования = 32 слота
    XRA  A
    STA  ARP_ITERATOR               ; Обнулили счетчик шагов цикла

arp_learn_search_loop:
    MOV  A, M                       ; Читаем поле статуса текущей записи (+0)
    CPI  01h                        ; Статус == VALID?
    JNZ  arp_learn_search_next      ; Если слот пуст — перешагиваем его

    ; IP адрес найден в активном слоте — выполняем побайтовое сравнение 4 байт
    PUSH H                          ; Сохранили старт текущей записи в стек
    INX  H                          ; HL переходит на поле IP (+1)
    LHLD ARP_LEARN_IP_PTR           ; DE = Входящий IP из кадра
    XCHG
    MVI  C, 4                       ; Счетчик сравнения = 4 байта

arp_learn_cmp_ip_bytes:
    LDAX D
    CMP  M                          ; Сравниваем байт кадра с байтом таблицы
    JNZ  arp_learn_cmp_miss         ; Если хоть один байт не совпал — промах слота
    INX  D
	INX  H
    DCR  C
    JNZ  arp_learn_cmp_ip_bytes

    ; --- [HITКЭША]: IP СОВПАЛ! СБРАСЫВАЕМ TTL И ОБНОВЛЯЕМ MAC ---
    POP  H                          ; Восстановили HL на начало текущей структуры (+0)
    PUSH H
    LXI  D, A_ARP_MAC
    DAD  D                          ; HL теперь указывает точно на поле MAC (+5)
    LHLD ARP_LEARN_MAC_PTR          ; DE = Актуальный MAC из входящего кадра
    XCHG
    MVI  C, 6                       ; Копируем 6 байт нового физического адреса

arp_learn_update_mac_bytes:
    LDAX D
	MOV M, A
    INX  D
	INX  H
    DCR  C
    JNZ  arp_learn_update_mac_bytes
    
    ; Сбрасываем срок жизни записи на максимум
    POP  H
    LXI  D, A_ARP_TTL
    DAD  D                          ; HL -> Поле TTL (+11)
    MVI  M, ARP_INITIAL_TTL         ; Обновили TTL = 128 пакетов
    JMP  arp_learn_final_exit       ; Успешный выход из подсистемы

arp_learn_cmp_miss:
    POP  H                          ; Восстановили HL на старт записи при несовпадении IP

arp_learn_search_next:
    LXI  D, ARP_STRUCT_SIZE
    DAD  D                          ; HL плавно шагает на +12 байт (следующая запись)
    LDA  ARP_ITERATOR
	INR A
	STA ARP_ITERATOR
    DCR  B
    JNZ  arp_learn_search_loop      ; Крутим цикл по всем 32 записям

    ; --- ШАГ 2: [MISS КЭША]: IP НОВЫЙ. ИЩЕМ СВOБОДНЫЙ СЛОТ ИЛИ ЖЕРТВУ LRU ---
    LXI  H, ARP_SYS_TABLE
    MVI  B, MAX_ARP_ENTRIES
    MVI  C, 0FFh                    ; C = Минимальный зафиксированный TTL жертвы
    SHLD ARP_VICTIM_PTR             ; По умолчанию кандидат на вытеснение — слот 0

arp_find_victim_loop:
    MOV  A, M                       ; Читаем статус записи
    ORA  A                          ; Статус == FREE (00h)?
    JZ   arp_free_slot_found        ; Нашли чистый пустой слот — немедленно занимаем его!
    
    ; Слот занят, ищем запись с наименьшим TTL для вытеснения по алгоритму старения
    PUSH H
    LXI  D, A_ARP_TTL
    DAD  D                          ; HL -> Поле TTL (+11)
    MOV  A, M                       ; А = Текущий TTL записи
    CMP  C                          ; Сравниваем с историческим минимумом
    JNC  arp_victim_skip_slot       ; Если TTL больше или равен — этот слот более «живой»
    
    MOV  C, A                       ; Зафиксировали новый температурный минимум TTL
    POP  H
	PUSH H                 ; Восстановили базу записи
    SHLD ARP_VICTIM_PTR             ; Переписали адрес самой старой «жертвы»

arp_victim_skip_slot:
    POP  H
    LXI  D, ARP_STRUCT_SIZE
    DAD  D                          ; Перешагиваем к следующей записи
    DCR  B
    JNZ  arp_find_victim_loop
    JMP  arp_write_payload_to_table ; Свободных мест нет, выжигаем самую старую жертву

arp_free_slot_found:
    SHLD ARP_VICTIM_PTR             ; Нашли свободный слот — фиксируем его адрес в указателе

arp_write_payload_to_table:
    LHLD ARP_VICTIM_PTR
    MVI  M, 01h                     ; Прописали статус VALID = 01h
    INX  H                          ; HL -> Поле IP (+1)
    
    ; Записываем 4 байта нового IPv4-адреса хоста
    LHLD ARP_LEARN_IP_PTR
	XCHG
    MVI  C, 4
arp_learn_write_ip_bytes:
    LDAX D
	MOV M, A
	INX D
	INX H
	DCR C
	JNZ arp_learn_write_ip_bytes
    
    ; Записываем 6 байт нового физического MAC-адреса хоста
    LHLD ARP_LEARN_MAC_PTR
	XCHG
    MVI  C, 6
arp_learn_write_mac_bytes:
    LDAX D
	MOV M, A
	INX D
	INX H
	DCR C
	JNZ arp_learn_write_mac_bytes
    
    ; Инициализируем стартовый лимит жизни записи
    MVI  M, ARP_INITIAL_TTL         ; Записали 128 в байт TTL (+11)

arp_learn_final_exit:
    POP  H
	POP  D
	POP  B
	POP  PSW
    RET

; =============================================================================
; ПОДПРОГРАММА 2: k_arp_aging_per_packet (Сверхбыстрое линейное старение кэша)
; =============================================================================
; Вход:  Вызывается из hw_net_isr на абсолютно каждый принятый проводной кадр.
; Выход: Все TTL уменьшены на 1. Записи с TTL == 0 аппаратно аннулируются.
; =============================================================================
k_arp_aging_per_packet:
    PUSH PSW
    PUSH B
    PUSH H

    LXI  H, ARP_SYS_TABLE           ; HL указывает на статус первого слота
    MVI  B, MAX_ARP_ENTRIES         ; Длина массива = 32 записи

arp_fast_aging_loop:
    MOV  A, M                       ; Читаем поле статуса записи
    CPI  01h                        ; Запись валидна?
    JNZ  arp_fast_aging_skip        ; Если слот пуст или заморожен — просто переходим к следующему

    ; Линейное ультрабыстрое скольжение указателя к полю TTL (+11 байт вперед)
    INX H
	INX H
	INX H
	INX H   ; +1, +2, +3, +4
    INX H
	INX H
	INX H
	INX H   ; +5, +6, +7, +8
    INX H
	INX H
	INX H           ; +9, +10, +11 (HL на ячейке TTL)

    DCR  M                          ; Уменьшаем срок жизни записи: TTL = TTL - 1
    MOV  A, M                       ; Проверяем новое значение в аккумуляторе
    ORA  A                          ; TTL дошел до нуля?
    JNZ  arp_fast_aging_back        ; Если TTL > 0 — узел активен, возвращаем указатель

    ; Срок жизни записи истек — откатываем HL назад к статусу для аннулирования
    DCX H
	DCX H
	DCX H
	DCX H   ; -1, -2, -3, -4
    DCX H
	DCX H
	DCX H
	DCX H   ; -5, -6, -7, -8
    DCX H
	DCX H
	DCX H           ; -9, -10, -11 (HL снова на статусе +0)
    
    MVI  M, 00h                     ; СТАТУС = FREE (00h). ЗАПИСЬ АТОМАРНО СГОРЕЛА!
    JMP  arp_fast_aging_next        ; Переходим к следующему слоту (HL уже на старте)

arp_fast_aging_back:
    ; Откат указателя назад для живой записи
    DCX H
	DCX H
	DCX H
	DCX H
    DCX H
	DCX H
	DCX H
	DCX H
    DCX H
	DCX H
	DCX H

arp_fast_aging_skip:
    ; Сдвигаем HL на статус СЛЕДУЮЩЕЙ 12-байтовой записи таблицы
    INX H
	INX H
	INX H
	INX H   ; +1, +2, +3, +4
    INX H
	INX H
	INX H
	INX H   ; +5, +6, +7, +8
    INX H
	INX H
	INX H
	INX H   ; +9, +10, +11, +12

arp_fast_aging_next:
    DCR  B                          ; Декрементируем счетчик 32 слотов
    JNZ  arp_fast_aging_loop        ; Крутим цикл до полной обработки таблицы

    POP  H
	POP  B
	POP  PSW
    RET

; =============================================================================
; ФУНКЦИЯ ЯДРА: ВЫДЕЛЕНИЕ ОДНОЙ СВОБОДНОЙ ФИЗИЧЕСКОЙ СТРАНИЦЫ ОЗУ
; =============================================================================
alloc_physical_frame_page:
    ; Вход: Параметры отсутствуют
    ; Выход: A = Номер выделенной физ. страницы (0..31), 
    ;        Либо A = 0FFH (Код ошибки: нет свободной памяти)
    
    LXI  H, RAM_BITMAP          ; Начинаем сканирование с первого байта битовой карты
    MVI  B, 0                   ; B = Счетчик просмотренных страниц (0..31)

alloc_find_byte_loop:
    MOV  A, M                   ; Читаем текущий байт маски (8 страниц)
    CPI  0FFH                   ; Все 8 страниц в этом байте заняты?
    JZ   alloc_next_bitmap_byte ; Если да — быстро переходим к следующему байту

    ; В текущем байте есть свободный бит (равный 0). Ищем его позицию.
    MVI  C, 001H                ; C = Тестируемая битовая маска (00000001B)
alloc_bit_search_loop:
    MOV  A, M                   ; Снова берем байт маски
    ANA  C                      ; Изолируем тестируемый бит
    JZ   alloc_free_bit_found   ; Если результат 0 — мы нашли свободную страницу!
    
    ; Сдвигаем маску для проверки следующего бита в этом байте
    MOV  A, C
    ADD  A                      ; Быстрый сдвиг влево на 1 бит (8080)
    MOV  C, A
    
    INR  B                      ; Увеличиваем общий номер страницы
    MOV  A, B
    ANI  07H                    ; Проверяем, не вышли ли за границу байта (8 бит)?
    JNZ  alloc_bit_search_loop

alloc_next_bitmap_byte:
    ; Скорректируем счетчик B на начало следующего байта (выравнивание по 8 бит)
    MOV  A, B
    ANI  0F8H                   ; Сбрасываем младшие 3 бита
    ADI  8                      ; Прибавляем 8 страниц
    MOV  B, A
    
    INX  H                      ; Переходим к следующему байту RAM_BITMAP
    MOV  A, B
    CPI  TOTAL_RAM_PAGES        ; Проверили всю память (256 страниц)?
    JNZ  alloc_find_byte_loop
    
    ; Вся физическая память заполнена!
    MVI  A, 0FFH                ; Возвращаем маркер ошибки (нет свободных фреймов)
    RET

alloc_free_bit_found:
    ; Физическая страница найдена. Номер страницы находится в регистре B.
    ; Нам необходимо установить этот бит в 1, чтобы зафиксировать захват памяти.
    MOV  A, M
    ORA  C                      ; Выставляем бит занятости через логическое ИЛИ
    MOV  M, A                   ; Сохраняем обновленный байт маски обратно в RAM_BITMAP
    
    MOV  A, B                   ; Возвращаем номер выделенной страницы в Аккумуляторе
    RET

; =============================================================================
; ФУНКЦИЯ ЯДРА: ОСВОБОЖДЕНИЕ ФИЗИЧЕСКОЙ СТРАНИЦЫ ОЗУ
; =============================================================================
free_physical_frame_page:
    ; Вход: A = Номер освобождаемой физической страницы памяти (0..31)
    ; Выход: Параметры отсутствуют. Бит в RAM_BITMAP сбрасывается в 0.
    
    MOV  B, A                   ; Сохраняем номер страницы в B
    
    ; Вычисляем, в каком байте RAM_BITMAP лежит этот бит: Индекс = Номер / 8
    ANI  0F8H                   ; Изолируем старшие биты (выполняем деление на 8 со сдвигом)
    RRC
    RRC
    RRC                         ; Сдвиг на 3 бита вправо -> A = индекс байта (0..3)
    MOV  E, A
    MVI  D, 0
    LXI  H, RAM_BITMAP
    DAD  D                      ; HL указывает точно на нужный байт маски
    
    ; Вычисляем битовую маску сброса: Маска = 1 << (Номер % 8)
    MOV  A, B
    ANI  07H                    ; A = Номер бита внутри байта (0..7)
    MOV  B, A                   ; B = Счетчик сдвигов
    
    MVI  C, 001H                ; Стартовый бит
    MOV  A, B
    CPI  0
    JZ   free_mask_ready        ; Если номер бита = 0, маска 00000001B уже готова

free_mask_shift_loop:
    MOV  A, C
    ADD  A                      ; Сдвиг влево на 1 бит
    MOV  C, A
    DCR  B
    JNZ  free_mask_shift_loop

free_mask_ready:
    ; Инвертируем маску, чтобы получить 0 в целевом бите и 1 в остальных
    MOV  A, C
    CMA                         ; Инверсия аккумулятора (команда 8080)
    MOV  C, A                   ; Теперь в C маска вида 11110111B
    
    ; Сбрасываем бит в битовой карте памяти
    MOV  A, M
    ANA  C                      ; Сброс бита через логическое И
    MOV  M, A                   ; Сохраняем измененный байт маски назад в ОЗУ ядра
    
    RET

; =============================================================================
; 1. ПОДПРОГРАММА СВЕРХБЫСТРОГО РАСЧЕТА СЕКТОРОВ LBA НА COMPACTFLASH
; Формула: LBA = (PID * 8 + Page_Num) * 32
; =============================================================================
swap_calc_lba:
    ; Вход: SWAP_CALC_PID, SWAP_CALC_PAGE
    ; Выход: SWAP_LBA_RESULT (16-битное число в паре HL)
    LDA  SWAP_CALC_PID          ; A = PID процесса (1..254)
    MOV  B, A                   ; Сохраняем PID в B

    ; Вычисляем PID * 8 через 3 быстрых сдвига влево (ADD A)
    ADD  A                      ; PID * 2
    ADD  A                      ; PID * 4
    ADD  A                      ; PID * 8
    
    MOV  C, A                   ; C = PID * 8
    LDA  SWAP_CALC_PAGE         ; A = Page_Num (0..7)
    ADD  C                      ; A = PID * 8 + Page_Num
    
    ; Переносим результат в 16-битную пару HL для умножения на 32
    MOV  L, A
    MVI  H, 0                   ; HL = (PID * 8 + Page_Num)

    ; Выполняем 5 сдвигов влево 16-битной пары HL через DAD H (*32)
    DAD  H                      ; результат * 2
    DAD  H                      ; результат * 4
    DAD  H                      ; результат * 8
    DAD  H                      ; результат * 16
    DAD  H                      ; результат * 32! Стартовый LBA сектор найден!
    
    SHLD SWAP_LBA_RESULT        ; Сохраняем вычисленный сектор LBA в ОЗУ ядра
    RET

; =============================================================================
; ПОДПРОГРАММА ЯДРА: ВЫГРУЗКА СТРАНИЦЫ С ПРИМЕНЕНИЕМ ВНЕШНЕГО ПДП
; =============================================================================
swap_out_physical_page:
    ; Вход: A  = Номер физической страницы ОЗУ (0..31), которую нужно выгрузить
    ;       BC = Вычисленный 16-битный базовый номер LBA-сектора на CompactFlash
    ; Выход: Аппаратный фрейм сброшен на CF через ПДП
    
    STA  SWAP_PAGE_NUM_BUF      ; Спрятали номер фрейма ОЗУ
    MOV  A, B
    STA  SWAP_LBA_TARGET_H      ; Сохранили старший байт LBA
    MOV  A, C
    STA  SWAP_LBA_TARGET_L      ; Сохранили младший байт LBA

    ; --- ЭТАП 1: Программный перенос из ОЗУ Ring 3 в буфер ядра ---
    OUT  076H                   ; Включаем ~{KERNEL_OVERRIDE_EN} = 0
    LDA  SWAP_PAGE_NUM_BUF
    OUT  072H                   ; Проецируем вытесняемую страницу Ring 3 в Сегмент 2 (0x8000)

    LXI  H, 08000H              ; Источник: Окно Сегмента 2 пользовательской задачи
    LXI  D, SWAP_SYS_BUFFER     ; Назначение: Системный буфер ОЗУ Ядра (16 КБ)
    LXI  B, 4096                ; Счетчик на 4096 итераций (перенос по 4 байта)
swap_out_copy_loop:
    MOV  A, M
	STAX D
	INX H
	INX D
    MOV  A, M
	STAX D
	INX H
	INX D
    MOV  A, M 
	STAX D 
	INX H
	INX D
    MOV  A, M
	STAX D
	INX H
	INX D
    DCX  B
	MOV A, B
	ORA C
	JNZ swap_out_copy_loop

    ; Восстанавливаем оригинальную страницу Ядра в окне Сегмента 2 eMMU
    CALL get_current_pcb_address
    LXI  D, O_PAGES_MAP + 2
	DAD D
	MOV A, M
	OUT 072H 
    OUT  076H                   ; Выключили режим KERNEL_OVERRIDE

    ; --- ЭТАП 2: Аппаратный ПДП-вывод из буфера ядра на диск ---
    ; ПЕРВЫЙ КРИТИЧЕСКИЙ ВЫЗОВ: Взводим КР580ВТ57 на Канал 0!
    ; Параметры: HL = адрес буфера ядра, BC = 16384 байт, D = 01H (Режим Записи/Вывода из ОЗУ)
    LXI  H, SWAP_SYS_BUFFER     ; Источник для ПДП: буфер ядра
    LXI  B, 16384               ; Полный объем 16 КБ страницы
    MVI  D, 01H                 ; Направление: Запись (Данные из ОЗУ улетают в периферию)
    CALL dma_start_ch0_transfer ; ВЫЗОВ ФУНКЦИИ: Контроллер ПДП настроен и ждет DRQ0!

    ; Настраиваем контроллер диска на запись 32 секторов
    MVI  A, 0E0H
	OUT IDE_DRV_HEAD
    MVI  A, 32
	OUT IDE_SECTOR_CNT
    LDA  SWAP_LBA_TARGET_L
	OUT IDE_LBA_LOW
    LDA  SWAP_LBA_TARGET_H
	OUT IDE_LBA_MID
    XRA  A
	OUT IDE_LBA_HIGH

    ; Аппаратный старт: выдаем чипу команду записи
    MVI  A, IDE_CMD_WRITE
    OUT  IDE_CMD_STATUS         ; В этот микротакт CF выставит DRQ0, ВТ57 заберет шину!
                                ; CPU засыпает в HOLD на 26.2 мс, ПДП качает данные.
    RET

; =============================================================================
; 3. УМНАЯ ПОДПРОГРАММА МНОГОКРАТНОГО ПОВТОРНОГО ВЫТЕСНЕНИЯ СТРАНИЦЫ С DIRTY BIT
; =============================================================================
swap_out_page_smart:
    ; Вход: A = Номер физической страницы ОЗУ (0..31), которую ядро хочет забрать
    ;       B = Номер логической страницы процесса (0..7), которую вытесняем
    ;       C = PID процесса-жертвы
    ; Выход: Фрейм ОЗУ гарантированно синхронизирован с диском и освобожден.
    STA  SWAP_PAGE_NUM_BUF      ; Сохраняем физический номер фрейма ОЗУ
    MOV  A, B
    STA  SWAP_CALC_PAGE         ; Занесли логический номер страницы (0..7)
    MOV  A, C
    STA  SWAP_CALC_PID          ; Занесли PID жертвы

    ; Вычисляем адрес PCB жертвы: PCB_TABLE + (PID * 64)
    LDA  SWAP_CALC_PID
    MOV  L, A
    MVI  H, 0
    DAD  H
    DAD  H
    DAD  H
    DAD  H
    DAD  H
    DAD  H                      ; HL = PID * 64
    LXI  D, PCB_TABLE
    DAD  D                      ; HL = База PCB жертвы
    SHLD SWAP_VICTIM_PCB_PTR    ; Сохранили базовый адрес PCB жертвы

    LXI  D, O_PAGE_DIRTY_MASK
    DAD  D                      ; HL указывает на O_PAGE_DIRTY_MASK (+41)
    MOV  A, M
    STA  SWAP_DIRTY_MASK_BUF    ; Сохранили текущую маску загрязнения процесса

    ; Формируем битовую маску для нашей логической страницы: Маска = 1 << Page_Num
    LDA  SWAP_CALC_PAGE
    MOV  B, A                   ; B = Количество сдвигов (0..7)
    MVI  C, 001H                ; Стартовый тестирующий бит
    MOV  A, B
    CPI  0
    JZ   swap_smart_mask_ready
swap_smart_mask_shift:
    MOV  A, C
    ADD  A                      ; Сдвиг влево (ADD A)
    MOV  C, A
    DCR  B
    JNZ  swap_smart_mask_shift
swap_smart_mask_ready:
    MOV  A, C
    STA  SWAP_TARGET_BIT_MASK   ; Сохранили маску тестируемого бита

    ; Проверяем бит загрязнения: (O_PAGE_DIRTY_MASK & Target_Bit)
    LDA  SWAP_DIRTY_MASK_BUF
    MOV  B, A
    LDA  SWAP_TARGET_BIT_MASK
    ANA  B                      ; Изолируем целевой бит
    JZ   swap_out_page_is_clean ; Если бит = 0 — страница ЧИСТАЯ! Запись на CF не нужна!

    ; СТРАНИЦА ГРЯЗНАЯ — ВЫПОЛНЯЕМ ФИЗИЧЕСКУЮ ПЕРЕЗАПИСЬ НА ДИСК
    CALL swap_calc_lba          ; Расчет LBA сектора подкачки -> результат в HL
    MOV  B, H
    MOV  C, L                   ; BC = Стартовый LBA на CompactFlash
    
    LDA  SWAP_PAGE_NUM_BUF      ; Восстанавливаем номер физической страницы ОЗУ
    CALL swap_out_physical_page ; Физически пишем 16 КБ (32 сектора) на CF через порты IDE

swap_out_page_is_clean:
    ; Обновление системных масок в PCB процесса-жертвы
    LHLD SWAP_VICTIM_PCB_PTR
    LXI  D, O_CF_SWAP_MASK
    DAD  D                      ; HL указывает на O_CF_SWAP_MASK (+16)
    
    ; Выставляем флаг: страница теперь находится на диске
    MOV  A, M
    PUSH H
    LDA  SWAP_TARGET_BIT_MASK
    MOV  B, A
    POP  H
    MOV  A, M
    ORA  B                      ; Взвели бит присутствия в Swap
    MOV  M, A

    ; Сброс флага грязной страницы (так как ОЗУ и Диск теперь идентичны)
    LHLD SWAP_VICTIM_PCB_PTR
    LXI  D, O_PAGE_DIRTY_MASK
    DAD  D                      ; HL указывает на O_PAGE_DIRTY_MASK (+41)
    
    LDA  SWAP_TARGET_BIT_MASK
    CMA                         ; Инвертируем маску бита (например, 11111011B)
    MOV  B, A
    MOV  A, M
    ANA  B                      ; Сбросили бит Dirty в 0 — страница синхронизирована
    MOV  M, A

    RET

; =============================================================================
; ФУНКЦИЯ ЯДРА: ДВУХЭТАПНЫЙ ПОДЪЕМ СТРАНИЦЫ С УЧЕТОМ БЛОКИРОВОК (SWAP IN AUTO)
; =============================================================================
swap_in_auto:
    ; Вход: A = Номер свободной физической страницы ОЗУ Ring 3, куда принять данные
    ;       B = Номер логической страницы процесса (0..7), которую возвращаем
    ;       C = PID процесса-владельца
    ; Выход: Данные подняты через ПДП в буфер ядра, а затем программно скопированы 
    ;        в целевой фрейм Ring 3. Блокировки сняты, маски PCB обновлены.

    STA  SWAP_IN_FRAME_BUF      ; Сохраняем номер физического фрейма назначения
    MOV  A, B
    STA  SWAP_CALC_PAGE         ; Занесли логический номер страницы для расчета LBA
    MOV  A, C
    STA  SWAP_CALC_PID          ; Занесли PID владельца для расчета LBA

    ; --- ШАГ 1: Монопольный захват CompactFlash ---
    ; Если диск занят — cf_acquire_lock усыпит текущий процесс, сбросит стек 
    ; и планировщик переключит CPU. Безопасность портов 0x30-0x37 гарантирована.
    CALL cf_acquire_lock        

    ; Вычисляем базовый LBA-сектор на CompactFlash подкачки -> результат в HL
    CALL swap_calc_lba          

    ; --- ШАГ 2: ЭТАП 1 — АППАРАТНАЯ ПДП ПЕРЕКАЧКА (CF -> SWAP_SYS_BUFFER) ---
    ; Настраиваем внешний ПДП-контроллер КР580ВТ57 на Канал 0
    ; Передаем параметры: HL = адрес буфера ядра, BC = 16384 байт, D = 00H (Чтение)
    PUSH H                      ; Спасаем вычисленный LBA-адрес накопителя
    LXI  H, SWAP_SYS_BUFFER     ; Целевой физический буфер в ОЗУ Ядра
    LXI  B, 16384               ; Объем пересылки — полная страница 16 КБ
    MVI  D, 00H                 ; Направление: Чтение (CF -> ОЗУ)
    CALL dma_start_ch0_transfer ; Внешний модуль ПДП взведен и ждет DRQ0
    POP  H                      ; Восстановили LBA-адрес диска в HL

    ; Инициализируем контроллер CompactFlash на чтение 32 секторов подкачки
    MVI  A, 0E0H
    OUT  IDE_DRV_HEAD           ; Режим LBA (Master Drive)
    MVI  A, 32
    OUT  IDE_SECTOR_CNT         ; Настраиваем чтение 32 секторов (16 КБ за раз)
    
    MOV  A, L
    OUT  IDE_LBA_LOW            ; Прописали младший адрес LBA
    MOV  A, H
    OUT  IDE_LBA_MID            ; Прописали средний адрес LBA
    XRA  A
    OUT  IDE_LBA_HIGH           ; Старшие биты LBA для swap-зоны зануляем

    MVI  A, IDE_CMD_READ        ; Выстреливаем команду чтения секторов в чип диска
    OUT  IDE_CMD_STATUS

    ; !!! ВНИМАНИЕ: АППАРАТНЫЙ ПРОСТОЙ ПРОЦЕССОРА НА 26.2 МС !!!
    ; CompactFlash выставит сигнал готовности сектора, дернет DRQ0, и КР580ВТ57
    ; заблокирует процессор линией HOLD. Вся страница аппаратно перельется в SWAP_SYS_BUFFER.
    ; Как только ПДП закончит пересылку, снимется HOLD и сработает прерывание TC.
    ; Наша RTOS проснется в обработчике hw_dma_tc_entry, который вернет управление сюда.

    ; --- ШАГ 3: ЭТАП 2 — ПРОГРАММНЫЙ ПЕРЕНОС (Буфер Ядра -> ОЗУ Ring 3) ---
    ; Включаем режим программного оверрайд-окна пользовательской памяти
    OUT  076H                   ; Сигнал ~{KERNEL_OVERRIDE_EN} = 0
    
    ; Подключаем выделенный свободный фрейм Ring 3 в логическое окно Сегмента 2 (0x8000)
    LDA  SWAP_IN_FRAME_BUF
    OUT  072H                   ; Перепрошиваем К555ИР26 для Сегмента 2

    ; Переносим 16 КБ из буфера ядра в спроецированное окно ОЗУ Ring 3
    LXI  H, SWAP_SYS_BUFFER     ; HL = Наш буфер ядра (Источник данных)
    LXI  D, 08000H              ; DE = Адрес окна Сегмента 2 eMMU (Назначение)
    LXI  B, 4096                ; Переносим по 4 байта за шаг -> 4096 итераций

swap_in_copy_loop:
    ; СЛОВО 1 (2 байта)
    MOV  A, M
    STAX D
    INX  H
	INX  D
    MOV  A, M
    STAX D
    INX  H
	INX  D
    ; СЛОВО 2 (2 байта)
    MOV  A, M
    STAX D
    INX  H
	INX  D
    MOV  A, M
    STAX D
    INX  H
	INX  D
    
    DCX  B                      ; Декремент 16-битного счетчика итераций
    MOV  A, B
    ORA  C
    JNZ  swap_in_copy_loop      ; Цикл идет, пока все 16 КБ не сядут в ОЗУ Ring 3

    ; Восстанавливаем оригинальную страницу Ядра в окне Сегмента 2 eMMU
    CALL get_current_pcb_address
    LXI  D, O_PAGES_MAP + 2     
    DAD  D
    MOV  A, M
    OUT  072H                   ; Вернули системную страницу памяти на место в окно 0x8000
    
    ; Отключаем режим KERNEL_OVERRIDE, захлопывая окно доступа в Ring 3
    OUT  076H                   ; Сигнал ~{KERNEL_OVERRIDE_EN} = 1

    ; --- ШАГ 4: Сброс масок подкачки и загрязнения в PCB процесса ---
    ; Вычисляем маску бита логической страницы: Маска = 1 << Page_Num
    LDA  SWAP_CALC_PAGE
    MOV  B, A                   ; B = Количество сдвигов (0..7)
    MVI  C, 001H                ; Стартовый бит
    MOV  A, B
    CPI  0
    JZ   swap_in_mask_done
swap_in_mask_shift_loop:
    MOV  A, C
    ADD  A
    MOV  C, A
    DCR  B
    JNZ  swap_in_mask_shift_loop
swap_in_mask_done:
    MOV  A, C
    CMA                         ; Инвертируем маску (например, 11111011B)
    STA  SWAP_IN_MASK_BIT       ; Cохранили маску инверсии сброса бита

    ; Сбрасываем бит присутствия в Swap маске текущего PCB
    CALL get_current_pcb_address
    PUSH H                      ; Сохранили базовый адрес PCB
    LXI  D, O_CF_SWAP_MASK
    DAD  D                      ; HL указывает на O_CF_SWAP_MASK (+16)
    LDA  SWAP_IN_MASK_BIT
    MOV  B, A
    MOV  A, M
    ANA  B                      ; Сбросили бит присутствия в Swap в 0
    MOV  M, A

    ; Сбрасываем Dirty-бит этой страницы в 0 (так как ОЗУ и диск сейчас идентичны)
    POP  H                      ; Восстановили базовый адрес PCB
    LXI  D, O_PAGE_DIRTY_MASK
    DAD  D                      ; HL указывает на O_PAGE_DIRTY_MASK (+41)
    MOV  A, M
    ANA  B                      ; Сбросили бит грязной страницы в 0 (страница чистая)
    MOV  M, A

    ; --- ШАГ 5: Освобождение CompactFlash и побудка очереди ---
    CALL cf_release_lock        ; Диск свободен, будим все процессы из ожидания W_CF_BUSY

    RET

; =============================================================================
; ПОДПРОГРАММА ФИКСАЦИИ МОДИФИКАЦИИ ДАННЫХ (SWAP MARK DIRTY)
; =============================================================================
swap_mark_dirty:
    ; Вход: A = Номер измененной логической страницы процесса (0..7)
    ; Выход: Бит в O_PAGE_DIRTY_MASK взведен в 1.
    
    MOV  B, A                   ; Сохраняем номер логической страницы в B
    
    ; Формируем битовую маску: Маска = 1 << Номер страницы
    MVI  C, 001H                ; Стартовый бит
    MOV  A, B
    CPI  0
    JZ   swap_mark_mask_ready
swap_mark_mask_shift:
    MOV  A, C
    ADD  A                      ; Сдвиг влево (ADD A)
    MOV  C, A
    DCR  B
    JNZ  swap_mark_mask_shift
swap_mark_mask_ready:
    ; Маска сформирована в регистре C
    
    CALL get_current_pcb_address ; HL = Базовый адрес PCB текущей активной задачи
    LXI  D, O_PAGE_DIRTY_MASK
    DAD  D                      ; HL указывает на O_PAGE_DIRTY_MASK (+41)
    
    MOV  A, M
    ORA  C                      ; Взводим целевой бит Dirty-маски в 1
    MOV  M, A                   ; Сохраняем обновленный байт маски грязи в PCB
    
    RET

; ; =============================================================================
; ; ВСПРМОГАТЕЛЬНАЯ ФУНКЦИЯ ЯДРА: ОПРЕДЕЛЕНИЕ И МАРКИРОВКА ГРЯЗНОЙ СТРАНИЦЫ
; ; =============================================================================
; mark_user_page_dirty_by_addr:
    ; ; Вход: HL = Логический адрес в пространстве Ring 3 (куда выполнялась запись)
    ; ; Выход: Вычисляется номер страницы (0..3) на основе старших бит адреса,
    ; ;        вызывается swap_mark_dirty. Регистры HL сохраняются.
    ; PUSH H
    ; PUSH B
    ; MOV  A, H                   ; Берём старший байт логического адреса (биты 15-8)
    ; RLC
    ; RLC                         ; Сдвигаем биты 15-14 в позиции 1-0 аккумулятора
    ; ANI  03H                    ; Изолируем номер логического сегмента (0..3)
    ; CALL swap_mark_dirty        ; Выставляем Dirty Bit для измененной страницы
    ; POP  B
    ; POP  H
    ; RET

; =============================================================================
; ОПТИМИЗИРОВАННЫЙ МОДУЛЬ МАСКИРОВАНИЯ СТРАНИЦ ПОСЛЕ ОПЕРАЦИЙ ВВОДА-ВЫВОДА
; =============================================================================
; Вход:  HL = Оригинальный стартовый адрес буфера Ring 3 пользователя.
;        BC = Количество реально перенесенных в память байт (результат ввода).
; Выход: В PCB процесса атомарно взведены биты грязных страниц в O_PAGE_DIRTY_MASK.
; =============================================================================
k_vfs_post_io_dirty_mark:
    ; Защита: если перенесено 0 байт, маркировать ничего не нужно
    MOV  A, B
    ORA  C
    RZ                              ; Мгновенный выход, если BC == 0

    PUSH PSW
    PUSH B
    PUSH D
    PUSH H

    ; Сохраняем начальный старший байт адреса (в нем закодирован номер сегмента)
    MOV  A, H
    STA  VFS_DIRTY_TMP_ADDR_H

    ; Вычисляем конечный адрес буфера (Старт_HL + Длина_BC)
    DAD  B                          ; HL указывает на последний измененный байт памяти
    MOV  A, H
    STA  VFS_DIRTY_TMP_END_H        ; Сохранили старший байт конечного адреса

    ; --- РАСЧЕТ И МАСКИРОВАНИЕ СТАРТОВОЙ СТРАНИЦЫ ---
    LDA  VFS_DIRTY_TMP_ADDR_H
    CALL k_calc_emmu_bit_mask       ; Вычисляем битовую маску сегмента (01h, 02h, 04h или 08h)
    MOV  E, A                       ; E = Битовая маска стартовой страницы

    ; --- РАСЧЕТ И МАСКИРОВАНИЕ КОНЕЧНОЙ СТРАНИЦЫ (Если буфер пересек границу сегментов) ---
    LDA  VFS_DIRTY_TMP_END_H
    CALL k_calc_emmu_bit_mask
    ORA  E                          ; Объединяем маски стартовой и конечной страниц через ИЛИ
    MOV  E, A                       ; В регистре E теперь итоговая маска «грязных» сегментов

    ; --- АТОМАРНЫЙ ВЗВОД МАСКИ В PCB ТЕКУЩЕГО ПРОЦЕССА ---
    CALL get_current_pcb_address    ; HL = Базовый адрес PCB активной задачи
    LXI  D, O_PAGE_DIRTY_MASK       ; Смещение поля маски измененных страниц (+41)
    DAD  D                          ; HL указывает точно на ячейку O_PAGE_DIRTY_MASK
    
    MOV  A, M
    ORA  E                          ; Наложили маску измененных страниц на старые флаги
    MOV  M, A                       ; ЖЕСТКО ЗАФИКСИРОВАЛИ ИЗМЕНЕНИЯ В PCB!

    POP  H
    POP  D
    POP  B
    POP  PSW
    RET

; =============================================================================
; ВНУТРЕННЯЯ ПОДПРОГРАММА: РАСЧЕТ БИТОВОЙ МАСКИ СЕГМЕНТА ПО СТАРШЕМУ БАЙТУ АДРЕСА
; =============================================================================
; Вход:  А = Старший байт 16-битного логического адреса (H).
; Выход: А = 8-битная маска для eMMU-коммутатора (01h, 02h, 04h или 08h).
; =============================================================================
k_calc_emmu_bit_mask:
    ; Логический адрес Ring 3 разделен на 4 сегмента по 16 Килобайт:
    ; 0x0000..0x3FFF (Сегмент 0), 0x4000..0x7FFF (Сегмент 1)
    ; 0x8000..0xBFFF (Сегмент 2), 0xC000..0xFFFF (Сегмент 3)
    ; Сдвигаем старший байт адреса вправо на 6 бит, чтобы получить чистый номер 0..3
    RLC
    RLC                             ; В процессоре 8080 RLC — это циклический сдвиг влево.
    ANI  003h                       ; Выделяем чистый номер сегмента (0, 1, 2 или 3)
    
    ; Преобразуем номер сегмента 0..3 в позиционную битовую маску
    MOV  C, A                       ; C = Счетчик сдвигов
    MVI  A, 001h                    ; Стартовая маска = Бит 0 взведен (01h)
    ORA  C                          ; Если счетчик 0 — маска 01h готова
    RZ

k_calc_emmu_shift:
    ADD  A                          ; Сдвигаем маску влево (01h -> 02h -> 04h -> 08h)
    DCR  C
    JNZ  k_calc_emmu_shift
    RET

; =============================================================================
; АППАРАТНЫЙ ОБРАБОТЧИК ПРЕРЫВАНИЯ: hw_uart_entry [ПОЛНЫЙ ИСПРАВЛЕННЫЙ МОНОЛИТ]
; =============================================================================
; Вход:  Сработал триггер прерывания КР580ВН59. Прерывания заперты ядром (DI).
; Выход: Символ уложен в RX-кольцо, Либо сгенерирован сигнал. Выдан EOI, EI.
; =============================================================================
hw_uart_entry:
    ; --- ШАГ 1: Консервация контекста прерванной задачи ---
    PUSH PSW                        ; Спасаем аккумулятор А и флаги процессора
    PUSH B                          ; Спасаем регистровую пару BC
    PUSH D                          ; Спасаем регистровую пару DE
    PUSH H                          ; Спасаем регистровую пару HL

    ; --- ШАГ 2: Контроль аппаратного состояния БИС КР580ВВ51А ---
    IN   UART_CMD                   ; Читаем регистр состояния/команд (UART_CMD)
    ANI  038h                       ; Маска ошибок: PE (Паритет), OE (Оверран), FE (Кадр)
    JNZ  uart_hardware_err_route    ; Если хоть один бит взведен — уходим на сброс

    ; --- ШАГ 3: Вычитка символа и низкоуровневая POSIX-фильтрация хоткеев ---
    IN   UART_DATA                  ; Извлекаем принятый байт из порта данных (UART_DATA)
    MOV  C, A                       ; C = Текущий принятый символ для укладки/анализа

    ; Перехват Ctrl+C (03H) -> Генерация сигнала SIGINT (Прерывание выполнения)
    CPI  003h                       ; Код ASCII для Ctrl+C
    MVI  E, SIGINT                  ; Подготовили номер сигнала = 2
    JZ   uart_isr_generate_signal

    ; Перехват Ctrl+X (18H) -> Генерация сигнала SIGTERM (Мягкое завершение)
    MOV  A, C
    CPI  018h                       ; Код ASCII для Ctrl+X
    MVI  E, SIGTERM                 ; Подготовили номер сигнала = 15
    JZ   uart_isr_generate_signal

    ; Перехват Ctrl+Z (1AH) -> Генерация сигнала SIGTSTP (Терминальный останов)
    MOV  A, C
    CPI  01Ah                       ; Код ASCII для Ctrl+Z
    MVI  E, SIGTSTP                 ; Подготовили номер сигнала = 20
    JZ   uart_isr_generate_signal

    JMP  uart_store_character       ; Хоткеи не обнаружены, идем укладывать байт в кольцо

; -----------------------------------------------------------------------------
; АСИНХРОННАЯ ГЕНЕРАЦИЯ СИГНАЛОВ ИЗ ПРЕРЫВАНИЯ ПО ХОТКЕЯМ КЛАВИАТУРЫ
; -----------------------------------------------------------------------------
uart_isr_generate_signal:
    ; Регистр E содержит номер целевого сигнала (SIGINT, SIGTERM, SIGTSTP)
    ; Сигнал доставляется активному процессу из переменной CURRENT_PID
    LDA  CURRENT_PID
    ORA  A                          ; Проверяем, не является ли текущий процесс ядром (PID 0)?
    JZ   uart_exit_close            ; Ядро аппаратно защищено от прерываний хоткеями, игнорируем!

    MOV  D, A                       ; D = PID активного процесса-получателя
    ; Вызываем наш исправленный, атомарный внутренний сисколл доставки сигналов
    CALL sys_kill                   
    JMP  uart_exit_close            ; Сигнал маскирован/доставлен, символ в буфер не кладем

; -----------------------------------------------------------------------------
; ШАГ 4: АТОМАРНАЯ УКЛАДКА СИМВОЛА В КОЛЬЦЕВОЙ БУФЕР TTY_RX_BUF
; -----------------------------------------------------------------------------
uart_store_character:
    LHLD TTY_RX_HEAD                ; HL = Текущий 16-битный индекс головы RX (0..255) (ИСПРАВЛЕНО!)
    MOV  A, L                       ; Переносим младший байт индекса в аккумулятор
    INR  A                          ; Рассчитываем Head + 1
    
    ; Сохраняем вычисленный инкремент во временную ячейку ОЗУ ядра.
    ; Операция INR A гарантированно выставит флаг нуля Z при переходе 255 -> 0,
    ; что защищает логику от мусорных флагов прерванного юзерспейс-процесса.
    STA  UART_NEXT_HEAD_CELL

    ; Проверяем буфер на переполнение (сравниваем с текущим TTY_RX_TAIL)
    LXI  D, TTY_RX_TAIL             ; (ИСПРАВЛЕНО!)
    LDAX D                          ; A = Текущий указатель хвоста RX (Tail)
    MOV  E, A                       ; E = Значение Tail
    LDA  UART_NEXT_HEAD_CELL        ; A = Новый потенциальный Head
    CMP  E                          ; Сравниваем (Head + 1) с Tail
    JZ   uart_buffer_overflow_route ; Если они сравнялись — буфер полон, отбрасываем байт

    ; --- ШАГ 5: Расчет адреса и запись в память (DAD больше не портит флаги) ---
    LHLD TTY_RX_HEAD                ; HL = Оригинальный валидный текущий Head RX (ИСПРАВЛЕНО!)
    LXI  D, TTY_RX_BUF              ; DE = Базовый физический адрес приемного буфера в Ring 0
    DAD  D                          ; HL = База_Буфера + Head (Точный адрес ячейки)
    MOV  M, C                       ; ФИЗИЧЕСКИ ЗАПИСАЛИ СИМВОЛ В ПРИЕМНЫЙ КОЛЬЦЕВОЙ БУФЕР ТЕРMИНAЛA!

    ; Фиксируем обновленный указатель головы в памяти ядра
    LDA  UART_NEXT_HEAD_CELL
    STA  TTY_RX_HEAD                ; (ИСПРАВЛЕНО!)

    ; --- ШАГ 6: Сигнальное асинхронное пробуждение процессов ---
    LXI  H, PCB_TABLE               ; Начинаем сканирование системной таблицы процессов
    MVI  B, MAX_PROCS               ; Итератор по количеству доступных слотов задач

uart_wakeup_scan_loop:
    PUSH H                          ; Сохранили базовый адрес проверяемого PCB задач
    LXI  D, O_STATUS                ; Смещение поля статуса задачи (+2)
    DAD  D
    MOV  A, M
    CPI  ST_WAITING                 ; Процесс находится в состоянии сна?
    JNZ  uart_wakeup_skip_pcb       ; Если не спит — прыгаем к следующему

    POP  H                          ; Восстановили базу PCB для проверки wait-channel
    PUSH H
    LXI  D, O_WAIT_CODE             ; Смещение причины сна (+40)
    DAD  D
    MOV  A, M
    CPI  W_TTY_IN                   ; Задача заблокирована именно на вводе с терминала (01h)?
    JNZ  uart_wakeup_skip_pcb

    ; Поток-читатель обнаружен — реактивно переводим его в состояние готовности!
    MVI  M, 00h                     ; Канал ожидания сна очищен
    POP  H                          ; Восстановили базу PCB
    PUSH H
    LXI  D, O_STATUS                ; Переходим к модификации флагов планировщика
    DAD  D
    MVI  M, ST_READY                ; ПОТОК РАЗБУЖЕН И ГОТОВ ДЛЯ ВЫТЕСНЯЮЩЕГО ШЕДУЛЛЕРА!

uart_wakeup_skip_pcb:
    POP  H                          ; Извлекли адрес текущего PCB из стека супервизора
    LXI  D, PCB_SIZE                ; Сдвиг равен размеру структуры PCB (64 байта)
    DAD  D                          ; HL указывает на следующий PCB в таблице
    DCR  B                          ; Уменьшаем счетчик оставшихся задач
    JNZ  uart_wakeup_scan_loop      ; Крутим цикл, пока не проверим всю таблицу процессов

; -----------------------------------------------------------------------------
; ШАГ 7: СБРОС АППАРАТНЫХ ТРИГГЕРОВ ПРЕРЫВАНИЙ И КАНOНИЧНЫЙ ВЫХОД
; -----------------------------------------------------------------------------
uart_exit_close:
    ; Выдаем команду неспецифического EOI (End of Interrupt) в контроллер КР580ВН59
    MVI  A, 020H
    OUT  MASTER_8259_CMD            ; Сброс текущего уровня прерывания в БИС ВН59

    ; Реставрация полного контекста прерванного по току задачи
    POP  H                          ; Восстанавливаем оригинальный HL
    POP  D                          ; Восстанавливаем оригинальный DE
    POP  B                          ; Восстанавливаем оригинальный BC
    POP  PSW                        ; Восстанавливаем оригинальный Аккумулятор и Флаги
    
    EI                          	; Снова разрешаем аппаратную карусель прерываний в Ring 3
    RET                             ; Аппаратный возврат в прерванную задачу (~{RET_STROBE})

; --- КАНАЛЫ АВАРИЙНЫХ СБРОСОВ И ОШИБОК ИНТЕРФЕЙСА ---
uart_hardware_err_route:
    MVI  A, 037h                    ; Команда сброса триггеров ошибок КР580ВВ51А (Reset Error)
    OUT  UART_CMD                   ; Посылаем управляющее слово в порт команд UART (UART_CMD)
    JMP  uart_exit_close            ; Уходим на стандартный выход и реставрацию регистров

uart_buffer_overflow_route:
    ; Кольцо RX переполнено — принятый символ отбрасывается во избежание разрушения ОЗУ,
    ; но кадр прерывания обязан корректно закрыться через сброс ВН59.
    JMP  uart_exit_close


; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ЦЕНТРАЛЬНЫЙ ИНИЦИАЛИЗАТОР VFS (vfs_init)
; =============================================================================
; Вход:  Прерывания заблокированы (DI). Стек KERNEL_STACK_TOP взведен.
; Выход: Все глобальные VFS таблицы очищены, диски, сокеты и PTY смонтированы.
; SYS_FILE_TABLE (1024 байта) очищена 16-битным циклом, модули смонтированы.
; Регистры BC, DE, HL полностью сохраняются.
; =============================================================================
vfs_init:
    PUSH PSW
    PUSH B
    PUSH D
    PUSH H                          ; Полная консервация контекста загрузки

    ; =============================================================================
    ; ШАГ 1: 16-БИТНАЯ ОЧИСТКА РАСШИРЕННОЙ SYS_FILE_TABLE (1024 БАЙТА)
    ; =============================================================================
    LXI  H, SYS_FILE_TABLE          ; HL -> Начало 1-килобайтной таблицы файлов ядра
    LXI  B, SYS_TABLE_TOTAL_SZ      ; BC = ИСПРАВЛЕНО: 16-битный счетчик итераций (1024)

vfs_init_clear_sft_16bit:
    MVI  M, RES_FREE_MARKER         ; Записываем маркер свободы 0xFF в текущую ячейку
    INX  H                          ; Шаг вперед по физической памяти Ring 0
    
    DCX  B                          ; 16-битный декремент счетчика байт
    MOV  A, B
    ORA  C                          ; ИСПРАВЛЕНО: Честная проверка 16-битного нуля для КР580ВМ80А
    JNZ  vfs_init_clear_sft_16bit   ; Крутим цикл ровно 1024 раза, пока пара BC не обнулится

    ; =============================================================================
    ; ШАГ 2: ПОЛНАЯ ОЧИСТКА ТАБЛИЦЫ МОНТИРОВАНИЯ VFS_MOUNT_TABLE
    ; =============================================================================
    LXI  H, VFS_MOUNT_TABLE         
    MVI  B, MAX_VFS_MOUNTS * VFS_MOUNT_SIZE ; 4 слота * 8 байт = 32 байта (влезает в 8 бит)
vfs_init_clear_mnt_loop:
    MVI  M, MNT_FREE                 
    INX  H
    DCR  B
    JNZ  vfs_init_clear_mnt_loop    

    ; =============================================================================
    ; ШАГ 3: ИНИЦИАЛИЗАЦИЯ И МОНТИРОВАНИЕ ДИСКОВОГО ИНТЕРФЕЙСА COMPACTFLASH
    ; =============================================================================
    CALL vfs_init_mbr               ; Извлекает LBA разделов и монтирует "/" в слот №0
    CPI  0FFh
    JZ   vfs_init_panic_disk        ; Если MBR поврежден или диска нет — Kernel Panic!

    ; =============================================================================
    ; ШАГ 4: ИНИЦИАЛИЗАЦИЯ И МОНТИРОВАНИЕ СЕТЕВОЙ SOCKETFS (ВНУТРЕННИЕ СОКЕТЫ)
    ; =============================================================================
    CALL sys_socketfs_init          ; Очищает SOCK_SYS_TABLE и монтирует "/dev/sock" в слот №1

    ; =============================================================================
    ; ШАГ 5: ИНИЦИАЛИЗАЦИЯ И МОНТИРОВАНИЕ ПОДСИСТЕМЫ ПСЕВДОТЕРМИНАЛОВ (PTY)
    ; =============================================================================
    LXI  H, VFS_MOUNT_TABLE + (2 * VFS_MOUNT_SIZE) ; HL -> Слот №2
    MVI  M, MNT_MOUNTED             
    
    INX  H
	INX  H                 
    LXI  D, PTYFS_PATH_STR          ; DE = 16-битный адрес каноничной строки "/dev/pts"
    MOV  M, E
	INX H
	MOV M, D    
    
    INX  H                          
    LXI  D, pty_vfs_gate            ; DE = 16-битный адрес центрального шлюза драйвера PTY
    MOV  M, E
	INX H
	MOV M, D    

    ; --- ШТАТНОЕ УСПЕШНОЕ ЗАВЕРШЕНИЕ ПОЛНОГО ПУСКА VFS ---
    POP  H
    POP  D
    POP  B
    POP  PSW                        
    RET                             

vfs_init_panic_disk:
    DI                              
    HLT                             

; =============================================================================
; ПОДПРОГРАММА ЯДРА: ПАРСИНГ MBR И ВЕРИФИКАЦИЯ РАЗДЕЛОВ ДИСКА ЧЕРЕЗ ПДП
; =============================================================================
; Вход:  Сектор 0 (MBR) уже загружен контроллером/ПДП в буфер ядра IDE_SYS_BUFFER
; Выход: Системные переменные заполнены геометрией из таблицы разделов.
;        При отсутствии раздела подкачки или неверной структуре — KERNEL_PANIC.
; =============================================================================
vfs_init_mbr:
    ; --- Шаг 3.1: Обнуление новых 32-битных системных переменных ядра ---
    LXI  H, SWAP_LBA
    MVI  B, 16                      ; 4 переменные (LBA/SIZE для двух ФС) * 4 байта = 16 байт
mbr_clear_loop:
    MVI  M, 0
    INX  H
    DCR  B
    JNZ  mbr_clear_loop

    ; --- Шаг 3.2: Инициализация цикла сканирования таблицы MBR ---
    LXI  H, IDE_SYS_BUFFER          ; Базовый адрес секторного буфера ОЗУ
    LXI  D, MBR_TABLE_OFFSET        ; Смещение до Partition 1
    DAD  D                          ; HL теперь указывает на старт Записи №1 (обычно 0x41BE)
    MVI  C, 4                       ; В классической структуре MBR ровно 4 первичные записи

mbr_scan_loop:
    PUSH H                          ; Спасаем в стек ядра указатель на начало текущей записи (16 байт)
    
    ; Смещение до байта File System ID (Partition Type) составляет ровно +4 байта от начала записи
    LXI  D, 4
    DAD  D
    MOV  A, M                       ; Считываем идентификатор файловой системы (ID) из таблицы

    CPI  SWAP_ID                    ; Проверяем совпадение с идентификатором "Memory Dump / SWAP" (0xDE)
    JZ   mbr_extract_swap

    CPI  EXPES_ID                   ; Проверяем совпадение с "Experimental FS" (0xE0)
    JZ   mbr_extract_expes

mbr_skip_partition:
    ; Если раздел не совпал с искомыми типами, переходим к следующему слоту таблицы
    POP  H                          ; Восстановили указатель на начало текущей записи из стека
    LXI  D, 16                      ; Размер одной записи таблицы MBR составляет строго 16 байт
    DAD  D                          ; HL указывает на следующую запись таблицы разделов
    DCR  C                          ; Уменьшаем счетчик оставшихся для анализа записей (4 -> 0)
    JNZ  mbr_scan_loop              ; Если проверили не все 4 записи — продолжаем цикл
    JMP  mbr_validate_results       ; Таблица полностью просканирована, переходим к валидации

; --- ПОДПРОГРАММЫ ПОБАЙТОВОГО ИЗВЛЕЧЕНИЯ 32-БИТНЫХ ПАРАМЕТРОВ ---

mbr_extract_swap:
    ; HL сейчас указывает на байт ID (+4 от старта записи). 
    ; Смещение до поля стартового LBA-адреса (+8) составляет еще +4 байта.
    LXI  D, 4
    DAD  D                          ; HL указывает на младший байт (Byte 0) поля 32-битного LBA
    LXI  D, SWAP_LBA                ; Целевой адрес назначения в защищенной памяти ядра
    CALL mbr_copy_32_bytes          ; Копируем 4 байта LBA-адреса. HL автоматически сдвинется на +4
    ; Теперь HL указывает точно на поле Partition Size (+12 от начала записи)
    LXI  D, SWAP_SIZE               ; Назначение: переменная размера раздела подкачки
    CALL mbr_copy_32_bytes
    JMP  mbr_skip_partition

mbr_extract_expes:
    LXI  D, 4
    DAD  D                          ; HL указывает на младший байт (Byte 0) 32-битного LBA Experimental FS
    LXI  D, EXPES_LBA               ; Целевой адрес переменной в ядре
    CALL mbr_copy_32_bytes          ; Извлекаем LBA-начало файловой системы
    LXI  D, EXPES_SIZE              ; Назначение: размер экспериментального раздела
    CALL mbr_copy_32_bytes          ; Извлекаем полный размер ФС в секторах
    JMP  mbr_skip_partition

; --- ВСПОМОГАТЕЛЬНАЯ ПРОЦЕДУРА ПОБАЙТОВОГО КОПИРОВАНИЯ (4 БАЙТА / 32 БИТА) ---
; Вход: HL - Источник (Буфер MBR), DE - Назначение (Переменная ОЗУ ядра)
; Выход: 4 байта скопированы, HL увеличился на 4, DE увеличился на 4.
mbr_copy_32_bytes:
    MVI  B, 4                       ; Счетчик циклов на 4 байта
mbr_copy_loop:
    MOV  A, M
    STAX D                          ; Сохраняем байт по адресу Little-Endian структуры
    INX  H
    INX  D
    DCR  B
    JNZ  mbr_copy_loop
    RET

; --- Шаг 3.3: Итоговая валидация структуры накопителя ---
mbr_validate_results:
    ; POSIX ОС MEGA-580 требует обязательного наличия раздела подкачки для Ring 3.
    ; Проверяем, заполнился ли хоть один байт переменной SWAP_SIZE (не равен ли размер нулю).
    LDA  SWAP_SIZE
    MOV  B, A
    LDA  SWAP_SIZE+1
    ORA  B
    MOV  B, A
    LDA  SWAP_SIZE+2
    ORA  B
    MOV  B, A
    LDA  SWAP_SIZE+3
    ORA  B                          ; Сложили по ИЛИ все 4 байта размера своп-зоны
    RNZ                             ; Если результат не равен 0 — swap найден, успешный выход в ОС!

    ; Если раздел подкачки (ID 0xDE) отсутствует, запуск многозадачности невозможен.
    ; Уходим в аварийный останов ядра (KERNEL_PANIC).
    JMP  mbr_panic

mbr_panic:
    DI                              ; Аппаратно отключаем прерывания в Ring 0
    MVI  A, 0DEh                    ; Выставляем отладочный код ошибки SWAP_NOT_FOUND
    ;OUT  0FFh                       ; Сбрасываем код в диагностический порт отладки платы
    HLT                             ; Полный физический останов процессора КР580ВМ80А

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): АЛЛОКАТОР ФИЗИЧЕСКИХ СТРАНИЦ ПАМЯТИ eMMU
; =============================================================================
; Вход:  Нет. Функция выполняется внутри защищенного сисколла (прерывания заперты).
; Выход: A = Номер выделенной физической страницы ОЗУ (0..255).
;        A = 0FFH — Ошибка: Физическая память полностью исчерпана (ENOMEM).
;        Регистры BC, DE, HL полностью сохраняются, используется ваша RAM_BITMAP.
; =============================================================================
k_allocate_free_physical_page:
    PUSH H                          ; Стак: Сохраняем регистры текущей задачи
    PUSH D
    PUSH B

    LXI  H, RAM_BITMAP              ; УТВЕРЖДЕНО: HL указывает на вашу системную RAM_BITMAP
    MVI  E, 0                       ; E = Индекс текущего байта битовой карты (0..31)

page_scan_byte_loop:
    MOV  A, M                       ; Читаем байт маски страниц из ОЗУ ядра
    CPI  0FFh                       ; Если байт равен 0xFF — все 8 страниц в нем заняты
    JNZ  page_free_bit_found        ; Если не равен 0xFF — внутри байта есть свободная страница!

    INX  H                          ; Переходим к следующему байту битовой карты
    INR  E                          ; Инкрементируем индекс байта
    MOV  A, E
    CPI  32                         ; Проверили все 32 байта (256 страниц)?
    JNZ  page_scan_byte_loop        ; Если нет — продолжаем сканирование карты

    ; --- АВАРИЙНЫЙ ВЫХОД: ФИЗИЧЕСКАЯ ПАМЯТЬ ПОЛНОСТЬЮ ИСЧЕРПАНА ---
    POP  B                          ; Полный баланс стека перед выходом
    POP  D
    POP  H
    MVI  A, 0FFh                    ; Возвращаем код ошибки 0xFF (ENOMEM)
    RET

; -----------------------------------------------------------------------------
; СВОБОДНЫЙ БИТ НАЙДЕН (HL указывает на байт маски, E = индекс байта 0..31)
; -----------------------------------------------------------------------------
page_free_bit_found:
    ; Находим точный номер бита (0..7) внутри считанного байта
    MOV  A, M                       ; Перечитали байт маски в аккумулятор
    MVI  C, 0                       ; C = Номер бита внутри байта (0..7)

page_find_bit_loop:
    RAR                             ; Сдвигаем аккумулятор вправо через флаг переноса (Carry)
    JNC  page_bit_is_free           ; Если Carry = 0 — мы нашли свободную страницу!
    
    INR  C                          ; Инкрементируем номер бита
    JMP  page_find_bit_loop         ; Идем проверять следующий бит

page_bit_is_free:
    ; --- ШАГ 1: Математический расчет абсолютного номера страницы eMMU ---
    ; Absolute_Page = (Byte_Index_E * 8) + Bit_Number_C
    MOV  A, E                       ; A = Индекс байта (0..31)
    ADD  A                          ; * 2
    ADD  A                          ; * 4
    ADD  A                          ; * 8 -> Сдвинули индекс байта влево на 3 бита
    ADD  C                          ; Прибавили номер бита -> A = Уникальный номер страницы (0..255)
    MOV  E, A                       ; Временно сохранили вычисленный номер страницы в регистр E

    ; --- ШАГ 2: Атомарное взведение бита в 1 (Помечаем как занятую) ---
    ; Генерируем маску для взведения бита. Нам нужно поставить 1 в позицию C.
    MOV  A, C                       ; A = Номер бита (0..7)
    MOV  C, A                       ; C = Счетчик сдвигов
    MVI  A, 001h                    ; Стартовая маска: взведен 0-й бит

    MOV  B, A                       ; Проверяем, нужен ли сдвиг маски бита
    MOV  A, C
    ORA  A
    MOV  A, B
    JZ   page_write_bitmap_mask     ; Если номер бита = 0, маска 01h готова

page_shift_mask_loop:
    ADD  A                          ; Сдвиг маски влево (умножение на 2)
    DCR  C
    JNZ  page_shift_mask_loop

page_write_bitmap_mask:
    ; Теперь в аккумуляторе лежит маска нужного бита (например, 04h для бита 2)
    ORA  M                          ; Накладываем маску по ИЛИ на текущий байт битовой карты
    MOV  M, A                       ; Записали обновленный байт обратно в RAM_BITMAP ОЗУ ядра

    ; --- ШАГ 3: УСПЕШНЫЙ ВЫХОД ---
    MOV  A, E                       ; Возвращаем вычисленный номер страницы (0..255) в аккумуляторе A
    
    POP  B                          ; Полное восстановление оригинального контекста регистров задачи
    POP  D
    POP  H
    RET                             ; Возврат в вызывающий сисколл (sys_fork или sys_pipe)

; =============================================================================
; ПОДПРОГРАММА ЯДРА (Ring 0): ДЕАЛЛОКАТОР ФИЗИЧЕСКИХ СТРАНИЦ ПАМЯТИ eMMU
; =============================================================================
; Вход:  A = Номер освобождаемой физической страницы ОЗУ eMMU (0..255).
; Выход: Соответствующий бит в системной RAM_BITMAP сброшен в 0.
;        Регистры BC, DE, HL полностью сохраняются, прерывания не трогаются.
; =============================================================================
k_free_physical_page:
    PUSH H                          ; Стак: Сохраняем регистровые пары задачи (8080)
    PUSH D
    PUSH B

    MOV  C, A                       ; C = Номер страницы (0..255)

    ; --- ШАГ 1: Расчет индекса байта в RAM_BITMAP и номера бита ---
    ; Номер байта = Номер_Страниы / 8 (сдвиг вправо на 3 бита)
    ; Номер бита  = Номер_Страницы % 8 (остаток, нижние 3 бита)
    
    ; Выделяем номер бита (нижние 3 бита аккумулятора)
    ANI  07h                        
    MOV  B, A                       ; B = Номер бита внутри байта (0..7)

    ; Выделяем индекс байта (сдвигаем исходный номер страницы вправо на 3 бита)
    MOV  A, C
    RRC
    RRC
    RRC                             ; Сдвинули 3 раза вправо через циклическое вращение
    ANI  01Fh                       ; Маскируем верхние биты, оставляя диапазон 0..31 байт
    MOV  E, A                       ; E = Вычисленный индекс байта в RAM_BITMAP
    MVI  D, 0                       ; DE = 16-битное смещение байта

    ; Вычисляем физический адрес целевого байта в ОЗУ ядра
    LXI  H, RAM_BITMAP              ; HL указывает на начало вашей системной битовой карты
    DAD  D                          ; HL указывает точно на байт, содержащий нужную страницу

    ; --- ШАГ 2: Генерация инверсной маски для сброса бита в 0 ---
    ; Нам нужно создать маску, где целевой бит равен 0, а все остальные биты — 1.
    ; Сначала создаем стандартную маску, где целевой бит равен 1.
    MOV  A, B                       ; A = Номер бита (0..7)
    MOV  C, A                       ; C = Счетчик сдвигов для цикла
    MVI  A, 001h                    ; Стартовая маска: взведен самый младший 0-й бит

    MOV  B, A                       ; Проверяем, требуется ли сдвиг маски бита
    MOV  A, C
    ORA  A
    MOV  A, B
    JZ   page_free_mask_ready       ; Если номер бита = 0, маска 01h готова

page_free_shift_loop:
    ADD  A                          ; Сдвиг маски влево (умножение на 2)
    DCR  C
    JNZ  page_free_shift_loop

page_free_mask_ready:
    ; Теперь в аккумуляторе лежит прямая маска бита (например, 04h для бита 2).
    ; Инвертируем её, чтобы получить маску сброса (04h превратится в 0FBh).
    CMA                             ; АППАРАТНАЯ ИНСТРУКЦИЯ 8080: Инвертировать аккумулятор A

    ; --- ШАГ 3: Атомарный сброс бита в битовой карте ОЗУ ---
    ANA  M                          ; Логическое И: сбрасываем целевой бит в 0, не трогая соседей
    MOV  M, A                       ; Записали обновленный байт обратно в RAM_BITMAP

    ; --- ШАГ 4: Восстановление контекста и выход ---
    POP  B                          ; Полное восстановление оригинального контекста регистров
    POP  D
    POP  H
    RET                             ; Возврат в вызывающий модуль ядра

; =============================================================================
; --- СИСТЕМНЫЙ ФЛАГ КОНТРОЛЯ СИНХРОНИЗАЦИИ GITHUB ---
; =============================================================================
GITHUB_SYNC_STATUS: db 01h          ; Инициализировано и синхронизировано
GITHUB_COMMIT_HASH: db "R2AKT_MAIN", 00h

; =============================================================================
; --- АППАРАТНЫЕ ЛИНИИ ПРЕРЫВАНИЙ КОНТРОЛЛЕРА КР580ВН59 ---
; =============================================================================
IRQ_SYS_TICK       EQU 00h          ; IR0 — Системный таймер ВИ53 №1 (20 мс)
IRQ_TTY		       EQU 01h          ; IR1 — Терминал ВВ51А (VT100 tty0)
IRQ_CF_DISK        EQU 02h          ; IR2 - Контроллер ПДП (с падлением на CF IDE16 и другие устройства по статусу контроллера ПДП)
IRQ_NET_ETHERNET   EQU 03h          ; IR3 — Ethernet-контроллер KSZ8851

; =============================================================================
; ВРЕМЕННЫЕ ЯЧЕЙКИ ПАМЯТИ ЯДРА ДЛЯ ОПЕРАЦИИ ЗАПИСИ (ОЗУ RING 0)
; =============================================================================
WRITE_BYTES_COUNT: DB 00H
WRITE_TEMP_HL:     DW 0000H

; =============================================================================
; ВРЕМЕННЫЕ ЯЧЕЙКИ ПАМЯТИ АЛЛОКАТОРА ЧТЕНИЯ В ОЗУ ЯДРА (RING 0)
; =============================================================================
READ_BYTES_COUNT: DB 00H
READ_TEMP_HL:     DW 0000H

; =============================================================================
; ВРЕМЕННЫЕ ЯЧЕЙКИ ОЗУ ЯДРА ДЛЯ ОПЕРАЦИИ ЗАВЕРШЕНИЯ ПРОЦЕССА (RING 0)
; =============================================================================
EXIT_TEMP_CODE:  DB 00H
EXIT_PARENT_PID: DB 00H

; =============================================================================
; ВРЕМЕННЫЕ СИСТЕМНЫЕ ПЕРЕМЕННЫЕ ВЫЗОВА WAITPID В ОЗУ ЯДРА (RING 0)
; =============================================================================
WAIT_USER_STATUS_PTR: DW 0000H
WAIT_TEMP_EXIT_CODE:  DB 00H
WAIT_TARGET_PID:    ds 1            ; Фильтр PID, переданный пользователем (-1 или конкретный)
WAIT_HAS_CHILDREN:  ds 1            ; Флаг наличия хотя бы одного живого потомка
WAIT_STACK_SAVE:    ds 2            ; Инвариант SP супервизора для sys_waitpid


; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ И БУФЕРЫ ЯДРА (ОЗУ RING 0)
; =============================================================================
EXEC_ARGV_PTR:       DW 0000H
EXEC_START_SECTOR:   DB 00H
IDE_SYS_BUFFER:      DS 512     ; Системный секторный буфер ввода-вывода ядра

; =============================================================================
; ВРЕМЕННЫЕ СИСТЕМНЫЕ СТРУКТУРЫ ДАННЫХ ДЛЯ ОТКРЫТИЯ ФАЙЛОВ (ОЗУ RING 0)
; =============================================================================
OPEN_FLAGS_BUF:        DB 00H
OPEN_ALLOCATED_FD:     DB 00H
OPEN_SYS_FILE_IDX:     DB 00H
OPEN_CURRENT_PCB_PTR:  DW 0000H

; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ ЯДРА ДЛЯ ЗАКРЫТИЯ ФАЙЛОВ (ОЗУ RING 0)
; =============================================================================
CLOSE_SYS_FILE_IDX: DB 00H

; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ ЯДРА ДЛЯ СМЕНЫ ДИРЕКТОРИИ (ОЗУ RING 0)
; =============================================================================
CHDIR_TEMP_INODE: ds 2              ; Временная ячейка для хранения 16-битного Inode каталога

; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ ЯДРА ДЛЯ POSIX БУДИЛЬНИКОВ (ОЗУ RING 0)
; =============================================================================
ALARM_OLD_SEC_RES: DB 00H
ALARM_TEMP_X2:   ds 2
ALARM_TEMP_X16:  ds 2
ALARM_NEW_TICKS: ds 2

; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ ЯДРА ДЛЯ РЕГИСТРАЦИИ СИГНАЛОВ (ОЗУ RING 0)
; =============================================================================
SIG_NUM_BUF:          DB 00H
SIG_NEW_HANDLER_L:    DB 00H
SIG_NEW_HANDLER_H:    DB 00H
SIG_OLD_STRUCT_PTR:   DW 0000H
SIG_PROC_TABLE_PTR:   DW 0000H

; =============================================================================
; ДОПОЛНИТЕЛЬНЫЕ СЛУЖЕБНЫЕ ЯЧЕЙКИ ОЗУ ЯДРА (RING 0)
; =============================================================================
READ_CHAR_CHAR: DB 00H
WRITE_CHAR_CHAR: DB 00H

; =============================================================================
; ВРЕМЕННЫЕ СТРУКТУРЫ ДАННЫХ ВЫЗОВА ОВЕРЛЕЕВ (ОЗУ RING 0)
; =============================================================================
OVL_TARGET_SEG:     DB 00H
OVL_EXT_MAP_IDX:    DB 00H
OVL_PHYS_PAGE_NUM:  DB 00H
OVL_SWAP_MASK_BUF:  DB 00H
OVL_BIT_MASK:       DB 00H
OVL_NEW_FRAME_NUM:  DB 00H
OVL_CURRENT_PCB:    DW 0000H

; =============================================================================
; СТРУКТУРЫ ДАННЫХ И КОНСТАНТЫ МЕНЕДЖЕРА ФИЗИЧЕСКОЙ ПАМЯТИ (ОЗУ ЯДРА)
; =============================================================================
; Предположим, на плате установлено 4096 КБ физического ОЗУ (256 страницы по 16 КБ).
; Для контроля 255 страниц нам требуется ровно 32 байта (256 бит) в ОЗУ Ядра.
; Бит = 0: страница свободна, Бит = 1: страница занята процессом или ядром.
TOTAL_RAM_PAGES  EQU 255         ; Всего физических фреймов памяти по 16 КБ

RAM_BITMAP:      DB 00FH        ; Базовый байт 0 (Страницы 0..7). Биты 0,1,2,3 изначально заняты Ядром ОС (PID=0) при старте!
                 DB 000H        ; Базовый байт 1 (Страницы 8..15)
                 DB 000H        ; Базовый байт 2 (Страницы 16..23)
                 DB 000H        ; Базовый байт 3 (Страницы 24..31)
				 DB 000H        ; Базовый байт 3 (Страницы 32..39)
				 DB 000H        ; Базовый байт 3 (Страницы 40..47)
				 DB 000H        ; Базовый байт 3 (Страницы 48..55)
				 DB 000H        ; Базовый байт 3 (Страницы 56..63)
				 DB 000H        ; Базовый байт 3 (Страницы 64..71)
				 DB 000H        ; Базовый байт 3 (Страницы 72..79)
				 DB 000H        ; Базовый байт 3 (Страницы 80..87)
				 DB 000H        ; Базовый байт 3 (Страницы 88..95)
				 DB 000H        ; Базовый байт 3 (Страницы 96..103)
				 DB 000H        ; Базовый байт 3 (Страницы 104..111)
				 DB 000H        ; Базовый байт 3 (Страницы 112..119)
				 DB 000H        ; Базовый байт 3 (Страницы 120..127)
				 DB 000H        ; Базовый байт 3 (Страницы 128..135)
				 DB 000H        ; Базовый байт 3 (Страницы 136..143)
				 DB 000H        ; Базовый байт 3 (Страницы 144..151)
				 DB 000H        ; Базовый байт 3 (Страницы 152..159)
				 DB 000H        ; Базовый байт 3 (Страницы 160..167)
				 DB 000H        ; Базовый байт 3 (Страницы 168..175)
				 DB 000H        ; Базовый байт 3 (Страницы 176..183)
				 DB 000H        ; Базовый байт 3 (Страницы 184..191)
				 DB 000H        ; Базовый байт 3 (Страницы 192..199)
				 DB 000H        ; Базовый байт 3 (Страницы 200..207)
				 DB 000H        ; Базовый байт 3 (Страницы 208..215)
				 DB 000H        ; Базовый байт 3 (Страницы 216..223)
				 DB 000H        ; Базовый байт 3 (Страницы 224..231)
				 DB 000H        ; Базовый байт 3 (Страницы 232..239)
				 DB 000H        ; Базовый байт 3 (Страницы 240..247)
				 DB 000H        ; Базовый байт 3 (Страницы 248..255)

; =============================================================================
; ГЛОБАЛЬНЫЕ СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ПОДСИСТЕМЫ СВОПИНГА (ОЗУ ЯДРА RING 0)
; =============================================================================
SWAP_PAGE_NUM_BUF:    DB 00H    ; Физический номер фрейма ОЗУ (0..31)
SWAP_CALC_PID:        DB 00H    ; PID процесса для расчета адреса на диске
SWAP_CALC_PAGE:       DB 00H    ; Логический номер страницы (0..7) для расчета
SWAP_LBA_RESULT:      DW 0000H  ; Вычисленный 16-битный сектор LBA
SWAP_LBA_TARGET_H:    DB 00H    ; Старший байт целевого LBA для IDE
SWAP_LBA_TARGET_L:    DB 00H    ; Младший байт целевого LBA для IDE
SWAP_VICTIM_PCB_PTR:  DW 0000H  ; Базовый адрес PCB процесса-жертвы
SWAP_DIRTY_MASK_BUF:  DB 00H    ; Временный буфер маски загрязнения
SWAP_TARGET_BIT_MASK: DB 00H    ; Маска целевого бита логической страницы

; =============================================================================
; ПОДСИСТЕМА СТРУКТУР ДАННЫХ VFS: ДЕCКРИПТОРЫ ГЕОМЕТРИИ ИЗ ТАБЛИЦЫ РАЗДЕЛОВ MBR
; =============================================================================
; Заменяет устаревшие 16-битные переменные VFS_PART_START_L / VFS_PART_START_H.
; Все поля имеют 32-битную разрядность (4 байта, порядок от младшего к старшему).
; =============================================================================
; --- ГЕОМЕТРИЯ РАЗДЕЛА ПОДКАЧКИ (PARTITION ID 0xDE - "MEMORY DUMP / SWAP") ---
SWAP_LBA:   ds  4                   ; 32-битный стартовый физический LBA-сектор свопа
SWAP_SIZE:  ds  4                   ; 32-битный полный размер раздела подкачки в секторах

; --- ГЕОМЕТРИЯ ЭКСПЕРИМЕНТАЛЬНОЙ ФАЙЛОВОЙ СИСТЕМЫ (PARTITION ID 0xE0) ---
EXPES_LBA:  ds  4                   ; 32-битный стартовый физический LBA-сектор раздела ФС
EXPES_SIZE: ds  4                   ; 32-битный полный размер раздела Experimental FS в секторах

; --- СЛУЖЕБНЫЕ РЕГИСТРЫ АБСОЛЮТНОЙ АДРЕСАЦИИ ДРАЙВЕРА CF ---
IDE_LBA_ABS_L: ds 2                 ; Младшее 16-битное слово вычисленного физического LBA
IDE_LBA_ABS_H: ds 2                 ; Старшее 16-битное слово вычисленного физического LBA

; =============================================================================
; АППАРАТНЫЕ ПОРТЫ ИНТЕРФЕЙСА IDE / COMPACTFLASH
; =============================================================================
IDE_DATA_PORT    EQU 030H       ; Регистр данных (16 бит)
IDE_SECTOR_CNT   EQU 032H       ; Счетчик секторов
IDE_LBA_LOW      EQU 033H       ; Младший байт адреса LBA (0..7)
IDE_LBA_MID      EQU 034H       ; Средний байт адреса LBA (8..15)
IDE_LBA_HIGH     EQU 035H       ; Старший байт адреса LBA (16..23)
IDE_DRV_HEAD     EQU 036H       ; Выбор диска / старшие биты LBA
IDE_CMD_STATUS   EQU 037H       ; Команда (OUT) / Статус (IN)
IDE_CMD_WRITE    EQU 030H       ; Команда IDE: Запись секторов
IDE_CMD_READ     EQU 020H       ; Команда IDE: Чтение секторов
IDE_STATUS_READY EQU 040H       ; Бит DSC + DRDY (Готовность диска)

; =============================================================================
; МОНОЛИТНЫЕ БУФЕРЫ И СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ПОДСИСТЕМЫ ПОДКАЧКИ
; =============================================================================
; Выделяем 16 КБ непрерывного физического пространства в ОЗУ Ядра под DMA-буфер
SWAP_SYS_BUFFER:     DS 16384   ; 16 КБ буфер для асинхронного приема страниц

; Временные переменные
SWAP_IN_FRAME_BUF:   DB 00H     ; Сюда прячем целевой физический фрейм ОЗУ Ring 3
SWAP_IN_MASK_BIT:    DB 00H     ; Битовая маска текущей логической страницы

; =============================================================================
; СЛУЖЕБНЫЕ КОНСТАНТЫ И ПЕРЕМЕННЫЕ ПОДСИСТЕМЫ ИНИЦИАЛИЗАЦИИ MBR
; =============================================================================
MBR_SIGNATURE_L  EQU 055H       ; Младший байт сигнатуры (0x55)
MBR_SIGNATURE_H  EQU 0AAH       ; Старший байт сигнатуры (0xAA)
VFS_REQUIRED_LBA EQU 01000H     ; Требуемый стартовый сектор раздела ФС (4096)

; Глобальные системные переменные VFS ядра в ОЗУ Ring 0
VFS_PART_START_L: DW 0000H       ; Младшие 16 бит стартового LBA раздела VFS
VFS_PART_START_H: DW 0000H       ; Старшие 16 бит стартового LBA раздела VFS
VFS_PART_TYPE:    DB 00H         ; Тип файловой системы раздела

; Временные регистры-хранители для парсинга MBR (ОЗУ Ring 0)
MBR_ENTRY_PTR:    DW 0000H       ; Указатель на текущую 16-байтовую запись раздела
MBR_SLOT_COUNTER: DB 00H         ; Счетчик просмотренных слотов (4..0)

; =============================================================================
; СЕГМЕНТ СИСТЕМНЫХ ДАННЫХ ЯДРА (ОЗУ)
; =============================================================================
CURRENT_PID:  DB 00H            ; Идентификатор текущей задачи (на старте = 0)
NEXT_PID:     DB 00H            ; Переменная-кандидат для планировщика
PROCS_COUNT:  DB 01H            ; Счетчик активных процессов (Ядро уже активно)

; Глобальная таблица занятости портов/устройств (256 байт)
; Индекс — номер порта (00H-0FFH). Значение — PID владельца или 0FFH
GLOBAL_RESOURCE_TABLE:  DS 256

; Таблица процессов (PCB Table): 256 слотов по 64 байта = 16384 байт (16 КБ)
; Такое размещение требует ровно одну физическую страницу памяти под таблицы ОС
PCB_TABLE:              DS PCB_SIZE * 256

; =============================================================================
; АРХИТЕКТУРНЫЕ ПАРАМЕТРЫ СТЕКА СУПЕРВИЗОРА (RING 0)
; =============================================================================

; =============================================================================
; АДРЕСА ПОРТОВ ПЕРИФЕРИИ И АППАРАТНЫЕ КОНСТАНТЫ СИСТЕМЫ / POSIX
; =============================================================================
MASTER_8259_DATA EQU 0A0H    ; КР580ВН59 — Порт данных
MASTER_8259_CMD  EQU 0A1H    ; КР580ВН59 — Порт команд/статуса
SYS_PIT_CH0      EQU 0B0H    ; КР580ВИ53 №1 — Канал 0 (вход системный 1 МГц -> выход IR0)
SYS_PIT_CMD      EQU 0B3H    ; КР580ВИ53 №1 — Управляющий порт
UART_PIT_CH0     EQU 0C0H    ; КР580ВИ53 №2 — Канал 0 (тактирование RxC ВВ51А)
UART_PIT_CH1     EQU 0C1H    ; КР580ВИ53 №2 — Канал 1 (тактирование TxC ВВ51А)
UART_PIT_CMD     EQU 0C3H    ; КР580ВИ53 №2 — Управляющий порт
UART_DATA        EQU 080H    ; КР580ВВ51А — Порт данных
UART_CMD         EQU 081H    ; КР580ВВ51А — Порт команд/статуса
DMA_CH0_ADDR     EQU 020H       ; Канал 0: Регистр текущего адреса (2 байта последовательно)
DMA_CH0_COUNT    EQU 021H       ; Канал 0: Счетчик терминальных циклов (2 байта)
DMA_CMD_STATUS   EQU 028H       ; Управляющий регистр (OUT) / Регистр статуса (IN)

; =============================================================================
; АКТУАЛЬНАЯ РАЗМЕТКА СТРУКТУРЫ PCB ЯДРА eMMU POSIX (64 БАЙТА)
; =============================================================================

MAX_PROCS        EQU 254     	; Ограничение PID (0=Ядро, 1..254=Задачи)
RES_FREE_MARKER  EQU 0FFH    	; Маркер свободного устройства в глобальной таблице

; --- ОФИЦИАЛЬНЫЕ СМЕЩЕНИЯ ПОЛЕЙ ВНУТРИ СТРУКТУРЫ PCB ---
O_PID            	EQU 0       ; Идентификатор текущего процесса [1 байт]
O_PPID           	EQU 1       ; Идентификатор родительского процесса [1 байт]
O_STATUS         	EQU 2       ; POSIX статус жизненного цикла [1 байт]
O_SAVED_SP       	EQU 3       ; Сохраненный указатель стека задачи [2 байта]
O_SYSCALL_RES    	EQU 5       ; Переменная кода результата syscall / exit [1 байт]
O_UID            	EQU 6       ; User ID процесса (0 = суперпользователь) [1 байт]
O_GID            	EQU 7       ; Group ID процесса [1 байт]
O_PAGES_MAP      	EQU 8       ; Номера физ. страниц ОЗУ для Сегментов 0–3 [4 байта]
O_EXT_PAGES_MAP  	EQU 12      ; Номера расширенных страниц для Сегментов 4–7 [4 байта]
O_CF_SWAP_MASK   	EQU 16      ; Битовая маска страниц в свопе на CF-карте [1 байт]
O_CWD_INODE      	EQU 17      ; Текущая рабочая директория процесса [2 байта]
O_UMASK          	EQU 19      ; Маска прав создания новых файлов umask [1 байт]
O_FILE_TABLE     	EQU 20      ; Таблица дескрипторов файлов (8 слотов по 1B) [8 байт]
; Смещения блока приоритетов (Смещены из-за удаления таблицы устройств на 32 байта)
O_PRIO_STATIC    	EQU 28      ; Статический базовый приоритет [1 байт]
O_PRIO_NICE      	EQU 29      ; Пользовательская nice-поправка (0-40, деф 20) [1 байт]
O_PRIO_DYNAMIC   	EQU 30      ; Динамический приоритет для планировщика [1 байт]
O_CPU_TICKS      	EQU 31      ; Штрафной счетчик квантов времени процессора [1 байт]
; Смещения блока сигналов
O_SIG_PENDING    	EQU 32      ; Маска ожидающих сигналов (POSIX) [2 байта]
O_SIG_BLOCKED    	EQU 34      ; Маска заблокированных сигналов [2 байта]
O_SIG_TABLE_PTR  	EQU 36      ; Системный указатель на векторы sigaction [2 байта]
O_ALARM_TICKS    	EQU 38      ; Тики будильника для системного вызова alarm [2 байта]
O_WAIT_CODE      	EQU 40      ; wait-channel (код причины сна задачи) [1 байт]
O_PAGE_DIRTY_MASK	EQU 41   	; Битовая маска (8 бит для 8 страниц процесса).
								; 1 = страница изменена (грязная), 0 = страница чистая (совпадает с диском).
O_PGID             	EQU 42      ; Идентификатор группы процессов (Process Group ID) [1 байт]
O_SID              	EQU 43      ; Идентификатор сессии (Session ID) [1 байт]
O_TTY_INDEX       	EQU 44      ; Индекс управляющего TTY (0xFF = нет терминала) [1 байт]
O_RESERVED_PAD		EQU 45      ; Резервный хвост до границы ровно 64 байт [19 байт]

PCB_SIZE         EQU 64      ; Жесткий размер структуры процесса (PID << 6)

; --- ОФИЦИАЛЬНЫЕ POSIX-СТАТУСЫ ЖИЗНЕННОГО ЦИКЛА ПРОЦЕССА ---
; Статусы процессов для планировщика
ST_FREE          	EQU 00H     ; Слот PCB полностью свободен (доступен для fork)
ST_CREATED       	EQU 01H     ; Создаётся (ресурсы взяты, идёт загрузка образа в ОЗУ)
ST_READY         	EQU 02H     ; Готов к исполнению (находится в очереди планировщика)
ST_RUNNING       	EQU 03H     ; Выполняется на CPU в данный момент (активный контекст)
ST_WAITING       	EQU 04H     ; Ожидает ввода-вывода / заблокирован на wait-channel
ST_ZOMBIE        	EQU 05H     ; Завершен (ОЗУ свободно, ждет waitpid от родителя)

; Маски критических сигналов
SIGKILL_MASK_H   	EQU 02H     ; 9-й сигнал (1-й бит старшего байта маски)
SIGALRM_MASK_H   	EQU 40H     ; 14-й сигнал (6-й бит старшего байта маски)

; =============================================================================
; ТАБЛИЦА СИСТЕМНЫХ ВЫЗОВОВ ЯДРА (УКАЗАТЕЛИ НА ФУНКЦИИ)
; =============================================================================
SYSCALL_TABLE:
    DW	sys_fork				; Index 0: SYS_FORK
    DW	sys_exit				; Index 1: SYS_EXIT
    DW	sys_waitpid				; Index 2: SYS_WAITPID
    DW	sys_request_io			; Index 3: SYS_REQUEST_IO
    DW	sys_release_io			; Index 4: SYS_RELEASE_IO
    DW	sys_open				; Index 5: SYS_OPEN
    DW	sys_close				; Index 6: SYS_CLOSE
    DW	sys_read				; Index 7: SYS_READ
    DW	sys_write				; Index 8: SYS_WRITE
    DW	sys_chdir				; Index 9: SYS_CHDIR
    DW	sys_alarm				; Index 10: SYS_ALARM
    DW	sys_sigaction			; Index 11: SYS_SIGACTION
    DW	sys_execve				; Index 12: SYS_EXECVE
	DW  sys_overlay_map			; Index 13: SYS_OVERLAY_MAP
	DW	sys_kill				; Index 14: SYS_KILL
	DW	sys_lseek				; Index 15: SYS_LSEEK
	DW	sys_pipe				; Index 16: SYS_PIPE
	DW	sys_mutex				; Index 17: SYS_MUTEX
	DW	sys_cond				; Index 18: SYS_COND
	DW	sys_sem_wait			; Index 19: SYS_SEM_WAIT
	DW	sys_sem_post			; Index 20: SYS_SEM_POST
	DW	sys_socket				; Index 21: SYS_SOCKET
	DW	sys_send				; Index 22: SYS_SEND
	DW	sys_recv				; Index 23: SYS_RECV
	DW	sys_socketpair			; Index 24: SYS_SOCKETPAIR
	DW	sys_netstat				; Index 25: SYS_NETSTAT
	DW	sys_shutdown			; Index 26: SYS_SHUTDOWN
	DW	sys_openpty				; Index 27: SYS_OPENPTY
	DW	sys_ptsname				; Index 28: SYS_PTSNAME
	DW	sys_ttyname				; Index 29: SYS_TTYNAME
	DW	sys_ioctl				; Index 30: SYS_IOCTL
	DW	sys_setsid				; Index 31: SYS_SETSID
	DW	sys_getpid				; Index 32: SYS_GETPID
	DW	sys_getppid				; Index 33: SYS_GETPPID
	DW	sys_getuid				; Index 34: SYS_GETUID

MAX_SYSCALL_NUM			EQU 35			; Размер таблицы переходов системных вызовов

#code RAM, *

ENOSYS           		EQU 0xE5		; Возвращает ошибку (0xE5)

; =============================================================================
; ПДП ВТ57
; =============================================================================
; Флаги регистра команд ВТ57 (DMA Command Register)
; Бит 0-3: Разрешение каналов 0..3
; Бит 4: 0 = Обычный приоритет (фиксированный), 1 = Циклический приоритет
; Бит 5: 0 = Обычная запись, 1 = Расширенная запись (Extended Write)
; Бит 6: 0 = TC STOP (Остановка по терминальному счетчику), 1 = Автозагрузка
; Нам нужен режим: Обычный приоритет, расширенная запись, остановка по TC, разрешить Канал 0
DMA_MODE_CONFIG  EQU 021H       ; Битовая маска 00100001B (Расширенная запись + Канал 0)

; =============================================================================
; КОНСТАНТЫ И ПЕРЕМЕННЫЕ МЬЮТЕКСА НАКОПИТЕЛЯ
; =============================================================================
W_CF_BUSY        EQU 05H        ; Код сна в O_WAIT_CODE: ожидание освобождения CF-карты

; Переменная защелки в ОЗУ Ядра (Ring 0)
CF_LOCK_VARIABLE: DB 00H        ; 00H = свободен, 0FFH = занят обменом

; =============================================================================
; КОНСТАНТЫ И БУФЕРЫ ПОДСИСТЕМЫ ВВОДА-ВЫВОДА TTY0
; =============================================================================
TTY_BUF_SIZE     EQU 64         ; Размер кольцевых буферов ядра (степень двойки)

; Выделение памяти под кольцевые буферы ядра в системном ОЗУ
TTY_RX_BUF:      DS TTY_BUF_SIZE ; Буфер приема (клавиатура)
TTY_RX_HEAD:     DB 00H
TTY_RX_TAIL:     DB 00H

TTY_TX_BUF:      DS TTY_BUF_SIZE ; Буфер передачи (экран)
TTY_TX_HEAD:     DB 00H
TTY_TX_TAIL:     DB 00H

; --- ВРЕМЕННАЯ ИЗОЛИРОВАННАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
UART_NEXT_HEAD_CELL: ds 1

; =============================================================================
; КОНСТАНТЫ ИДЕНТИФИКАТОРОВ ФАЙЛОВЫХ СИСТЕМ
; =============================================================================
SWAP_ID          EQU 0DEh           ; Идентификатор раздела "Memory Dump / SWAP"
EXPES_ID         EQU 0E0h           ; Идентификатор раздела "Experimental FS"
MBR_TABLE_OFFSET EQU 01BEh           ; Смещение таблицы разделов в секторе MBR

; Константа команды асинхронного чтения IDE/CF через ПДП
IDE_CMD_READ_DMA EQU 0C8h

; Константа команды асинхронной записи IDE/CF через ПДП
IDE_CMD_WRITE_DMA EQU 0CAh

; --- УКАЗАТЕЛЬ НА АКТИВНЫЙ ДЕСКРИПТОР ПРОЦЕССА POSIX ЯДРА ---
CURRENT_PCB_PTR: ds 2               ; 16-битный адрес текущего PCB в ОЗУ ядра (Ring 0)

; --- КОД ПРИЧИНЫ СНА (ДЛЯ ПОЛЯ O_WAIT_CODE) ---
W_CF_BUSY_IO      EQU 05h           ; Код причины сна: поток заблокирован на I/O CompactFlash

; --- ФЛАГИ СОСТОЯНИЙ POSIX ПРОЦЕССА ---
STATE_READY        EQU 00h          ; Процесс готов к выполнению
STATE_RUNNING      EQU 01h          ; Процесс выполняется прямо сейчас
STATE_BLOCKED_IO   EQU 02h          ; Процесс заблокирован, ожидает прерывания ПДП (TC)

; --- ЛОКАЛЬНЫЕ ПЕРЕМЕННЫЕ ЗАГРУЗЧИКА (ВЫДЕЛЕНЫ В ОЗУ ЯДРА RING 0) ---
EXEC_SECTOR_COUNTER: ds 3           ; 24-битный счетчик текущего сектора файла
EXEC_SECTORS_TOTAL:  ds 2           ; 16-битное общее количество секторов бинарника

; --- КОНСТАНТЫ ПРИЧИН СНА POSIX ПЛАНИРОВЩИКА ---
W_PIPE_EMPTY       EQU 07h          ; Код сна: чтение из пустого пайпа
W_PIPE_FULL        EQU 08h          ; Код сна: запись в переполненный пайп

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ В ПАМЯТИ ЯДРА RING 0 ---
PIPE_USER_ARRAY_PTR: ds 2
PIPE_TEMP_RD_FD:     ds 1
PIPE_TEMP_WR_FD:     ds 1
PIPE_ALLOCATED_PAGE: ds 1
PIPE_GLOBAL_INDEX:   ds 1

; --- ФЛАГИ СОСТОЯНИЙ POSIX ПРОЦЕССА ---
STATE_READY        EQU 00h          ; Процесс готов к выполнению
STATE_RUNNING      EQU 01h          ; Процесс выполняется прямо сейчас
STATE_BLOCKED_IO   EQU 02h          ; Процесс заблокирован, ожидает прерывания ПДП (TC)

; --- ОФИЦИАЛЬНЫЕ КОДЫ ПРИЧИН СНА (WAIT-CHANNELS) POSIX ОС MEGA-580 ---
W_TTY_IN           EQU 01h          ; Код сна: ожидание ввода с клавиатуры (терминал пуст)
W_TTY_OUT          EQU 02h          ; Код сна: ожидание освобождения буфера вывода TTY (буфер полон)
W_CHILD            EQU 03H          ; Код сна: ожидание завершения дочернего процесса (waitpid)
W_CF_BUSY_IO       EQU 05h          ; Код сна: поток заблокирован на I/O CompactFlash
W_PIPE_EMPTY       EQU 07h          ; Код сна: чтение из пустого пайпа
W_PIPE_FULL        EQU 08h          ; Код сна: запись в переполненный пайп

ECHILD             EQU 0E9h         ; Код ошибки: Нет дочерних процессов

; --- ВРЕМЕННЫЕ СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
OPEN_TEMP_LOCAL_FD: ds 1        ; Переменная локального номера дескриптора (0..7)
OPEN_TEMP_GLOBAL_IDX: ds 1      ; Переменная глобального системного индекса объекта
OPEN_IS_PIPE_FLAG:    ds 1
OPEN_ALLOCATED_PAGE:  ds 1

; --- ВРЕМЕННЫЕ СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
CLOSE_TEMP_GLOBAL_IDX: ds 1     ; Переменная для временного хранения глобального индекса

; =============================================================================
; СМЕЩЕНИЯ ПОЛЕЙ ВНУТРИ СТРУКТУРЫ ОПИСАТЕЛЯ ПАЙПА (РАЗМЕР 8 БАЙТ)
; =============================================================================
P_PAGE_NUM         EQU 0            ; Номер выделенной физической страницы ОЗУ eMMU [1 байт]
P_HEAD_PTR         EQU 1            ; Кольцевой указатель чтения Head Pointer [2 байта]
P_TAIL_PTR         EQU 3            ; Кольцевой указатель записи Tail Pointer [2 байта]
P_BYTES_COUNT      EQU 5            ; Текущий счетчик байт в FIFO буфере пайпа [1 байт]
P_READER_PID       EQU 6            ; PID процесса-читателя (для пробуждения) [1 байт]
P_WRITER_PID       EQU 7            ; PID процесса-писателя (для пробуждения) [1 байт]

PIPE_STRUCT_SIZE   EQU 8            ; Общий размер одной структуры пайпа в ядре
MAX_SYS_PIPES      EQU 4            ; Максимальное количество пайпов в операционной системе

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА СИСТЕМНЫХ ПАЙПОВ ЯДРА (IPC КАНАЛЫ) ---
PIPE_SYS_TABLE:    ds  MAX_SYS_PIPES * PIPE_STRUCT_SIZE ; Резервируем 32 байта для 4 пайпов

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ ПОДСИСТЕМЫ ЗАПИСИ В ПАЙПЫ (RING 0 ОЗУ) ---
PIPE_WRITE_IDX_SAVED:   ds 1        ; Сохраненный глобальный индекс пайпа
PIPE_W_CURR_STRUCT_PTR: ds 2        ; 16-битный указатель на текущую структуру пайпа
PIPE_WRITE_TEMP_PAGE:   ds 1        ; Номер физической страницы ОЗУ eMMU пайпа
PIPE_WRITE_TEMP_PID:    ds 1        ; Временное хранилище PID читателя для пробуждения

; --- ДОБАВЛЕННАЯ СЛУЖЕБНАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
CLOSE_TEMP_PAGE: ds 1           ; Временная ячейка для хранения номера страницы пайпа

; --- СЛУЖЕБНАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
FILE_READ_IDX_SAVED: ds 1        ; Временное сохранение индекса открытого файла

; --- СЛУЖЕБНАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
FILE_WRITE_IDX_SAVED: ds 1       ; Временное сохранение индекса открытого файла

; --- КОНСТАНТЫ РЕЖИМОВ ПОЗИЦИОНИРОВАНИЯ ---
SEEK_SET           EQU 00h
SEEK_CUR           EQU 01h
SEEK_END           EQU 02h

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
LSEEK_WHENCE:           ds 1
LSEEK_IN_OFFSET_L:      ds 2
LSEEK_IN_OFFSET_H:      ds 2
LSEEK_FILE_STRUCT_PTR:  ds 2
LSEEK_OUT_POS_L:        ds 2
LSEEK_OUT_POS_H:        ds 2
LSEEK_FD_SAVED:         ds 1
LSEEK_WHENCE_SAVED:     ds 1
lseek_G_IDX:            ds 1
LSEEK_REQ_OFF:          ds 4        ; Буфер знакового смещения пользователя
LSEEK_NEW_OFF:          ds 4        ; Буфер вычисленного нового смещения
LSEEK_CUR_SIZE:         db 00h, 040h, 00h, 00h ; Имитируем размер файла 16 Мегабайт (0x00004000)

; =============================================================================
; СМЕЩЕНИЯ ПОЛЕЙ ВНУТРИ СТРУКТУРЫ ОТКРЫТОГО ФАЙЛА ЯДРА (РАЗМЕР 16 БАЙТ)
; =============================================================================
F_INODE_NUM        EQU 0            ; Уникальный номер Inode файла на диске [1 байт]
F_CURRENT_OFFSET   EQU 1            ; Текущий 32-битный указатель позиции File Offset [4 байта]
F_FILE_SIZE        EQU 5            ; Текущий 32-битный размер файла в байтах [4 байта]
F_REF_COUNT        EQU 9            ; Счетчик ссылок на этот дескриптор (для fork/dup) [1 байт]
F_RESERVED_PAD     EQU 10           ; Резервное выравнивание структуры до 16 байт [6 байт]

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
SIGACT_SIGNUM:              ds 1
SIGACT_NEW_PTR:             ds 2
SIGACT_OLD_PTR:             ds 2
SIGACT_TARGET_VECTOR_PTR:   ds 2

UNLINK_BLOCK_COUNTER: ds 2          ; 16-битный счетчик оставшихся для очистки блоков
UNLINK_CURR_LBA_L:    ds 2          ; Младшее слово 24-битного относительного LBA
UNLINK_CURR_LBA_H:    ds 1          ; Старший байт 24-битного относительного LBA

; =============================================================================
; КОНСТАНТЫ И СМЕЩЕНИЯ ДЛЯ ПОДСИСТЕМЫ СИНХРОНИЗАЦИИ ЯДРА (МЬЮТЕКСЫ)
; =============================================================================
O_MUTEX_STATE      EQU 0            ; Статус мьютекса (00h = FREE, 01h = LOCKED) [1 байт]
O_MUTEX_OWNER      EQU 1            ; PID процесса-владельца мьютекса [1 байт]
O_MUTEX_WAITPID    EQU 2            ; PID заблокированного процесса (очередь ожидания) [1 байт]

MUTEX_STRUCT_SIZE  EQU 4            ; Размер одной структуры мьютекса в ядре
MAX_SYS_MUTEX      EQU 8            ; Максимальное количество мьютексов в системе (8 * 4 = 32B)

W_MUTEX            EQU 09h          ; Официальный код сна: процесс заблокирован на мьютексе

; Команды системного вызова
MUTEX_CMD_LOCK     EQU 00h          ; Операция захвата мьютекса
MUTEX_CMD_UNLOCK   EQU 01h          ; Операция освобождения мьютекса

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА СИСТЕМНЫХ МЬЮТЕКСОВ ЯДРА (RING 0 ОЗУ) ---
MUTEX_SYS_TABLE:   ds  MAX_SYS_MUTEX * MUTEX_STRUCT_SIZE ; Резервируем 32 байта под 8 мьютексов

; Временные ячейки планировщика для сисколла
MUTEX_TEMP_ID:     ds 1
MUTEX_TEMP_CMD:    ds 1
MUTEX_TEMP_PCB:    ds 2

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА УСЛОВНЫХ ПЕРЕМЕННЫХ ЯДРА (RING 0 ОЗУ) ---
COND_SYS_TABLE:    ds  MAX_SYS_COND * COND_STRUCT_SIZE ; Выделяем 32 байта в памяти ядра

; =============================================================================
; СМЕЩЕНИЯ И КОНСТАНТЫ ДЛЯ УСЛОВНЫХ ПЕРЕМЕННЫХ (CONDITION VARIABLES)
; =============================================================================
O_COND_STATE       EQU 0            ; Статус переменной (00h = FREE, 01h = INIT) [1 байт]
O_COND_WAITPID     EQU 1            ; PID процесса, который уснул на этой переменной [1 байт]
O_COND_RESERVED    EQU 2            ; Резерв для расширения очереди [2 байта]

COND_STRUCT_SIZE   EQU 4            ; Размер одной структуры cond в ядре
MAX_SYS_COND       EQU 8            ; Максимальное количество cond-переменных (8 * 4 = 32B)

W_COND             EQU 0Ah          ; Официальный код сна: блокировка на условной переменной

; Команды системного вызова
COND_CMD_WAIT      EQU 00h          ; Операция ожидания (wait)
COND_CMD_SIGNAL    EQU 01h          ; Операция пробуждения одного потока (signal)

; Временные ячейки для сисколла
COND_TEMP_ID:      ds 1
COND_TEMP_CMD:     ds 1
COND_TEMP_MUTEX:   ds 1             ; ID мьютекса, передаваемого при операции WAIT
COND_TEMP_STRUCT:  ds 2             ; 16-битный указатель на текущую структуру cond

; =============================================================================
; КОНСТАНТЫ И СМЕЩЕНИЯ ДЛЯ ПОДСИСТЕМЫ POSIX СЕМАФОРОВ
; =============================================================================
O_SEM_VALUE        EQU 0            ; Текущее значение счетчика семафора [1 байт]
O_SEM_WAITPID      EQU 1            ; PID процесса в очереди ожидания ресурса [1 байт]
O_SEM_RESERVED     EQU 2            ; Резерв для выравнивания структуры [2 байта]

SEM_STRUCT_SIZE    EQU 4            ; Фиксированный размер структуры семафора
MAX_SYS_SEMS       EQU 8            ; Лимит семафоров в системе (8 * 4 = 32 байта)

W_SEM              EQU 0Bh          ; Официальный код сна: процесс заблокирован на семафоре

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА СИСТЕМНЫХ СЕМАФОРОВ ЯДРА (RING 0 ОЗУ) ---
SEM_SYS_TABLE:     ds  MAX_SYS_SEMS * SEM_STRUCT_SIZE ; Резерв 32 байт под 8 семафоров

; Временные ячейки планировщика для сисколлов семафоров
SEM_TEMP_ID:       ds 1
SEM_TEMP_STRUCT:   ds 2

; =============================================================================
; КОНСТАНТЫ СИГНАЛОВ И ОШИБОК ДЛЯ ИСПРАВЛЕННОГО sys_kill()
; =============================================================================
EINTR              EQU 0E4h         ; Код ошибки: Системный вызов прерван сигналом

; Список поддерживаемых POSIX-сигналов (номера 1..15)
SIGINT             EQU 02h          ; Ctrl+C: Прерывание процесса с клавиатуры
SIGKILL            EQU 09h          ; Безусловное завершение процесса
SIGTERM            EQU 0Fh          ; Ctrl+X: Сигнал вежливого завершения (15)
SIGTSTP            EQU 12h          ; Ctrl+Z: Сигнал остановки/паузы процесса (18)

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
KILL_TEMP_PID:     ds 1             ; Целевой PID процесса-получателя
KILL_TEMP_SIG:     ds 1             ; Номер доставляемого сигнала
KILL_TARGET_PCB:   ds 2             ; 16-битный адрес PCB целевого процесса


; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
TTY_SIGNAL_BUFFER: ds 1             ; Временный буфер для коммутации сигналов в ISR


; =============================================================================
; СМЕЩЕНИЯ И КОНСТАНТЫ ДЛЯ СЕТЕВЫХ СОКЕТОВ (POSIX SOCKETS API)
; =============================================================================
S_SOCK_STATUS      EQU 0            ; Статус сокета [1 байт]
S_SOCK_TYPE        EQU 1            ; Тип сокета: 1=TCP(STREAM), 2=UDP(DGRAM) [1 байт]
S_SOCK_LPORT       EQU 2            ; Локальный порт [2 байта]
S_SOCK_RPORT       EQU 4            ; Удаленный порт [2 байта]
S_SOCK_OWNER       EQU 6            ; PID владельца сокета [1 байт]
S_SOCK_HW_CHAN     EQU 7            ; Номер аппаратного канала чипа (0..3) [1 байт]

SOCK_STRUCT_SIZE   EQU 8            ; Фиксированный размер структуры сокета в ядре
MAX_SYS_SOCKETS    EQU 4            ; Максимальное количество сокетов во всей ОС (4 * 8 = 32B)

; Коды состояний сокета
SOCK_FREE          EQU 00h
SOCK_INIT          EQU 01h

; POSIX константы типов (передаются из Ring 3)
AF_INET            EQU 02h          ; Семейство протоколов IPv4 (Internet)
SOCK_STREAM        EQU 01h          ; Потоковый сокет (TCP)
SOCK_DGRAM         EQU 02h          ; Дейтаграммный сокет (UDP)

; Новые коды асинхронного сна планировщика
W_NET_RECV         EQU 0Ch          ; Сон: ожидание сетевых данных
W_NET_CONN         EQU 0Dh          ; Сон: ожидание TCP-соединения (handshake)

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА СЕТЕВЫХ СОКЕТОВ ЯДРА (RING 0 ОЗУ) ---
SOCK_SYS_TABLE:    ds  MAX_SYS_SOCKETS * SOCK_STRUCT_SIZE ; Выделяем 32 байта в памяти ядра

; Временные переменные сисколла
SOCK_TEMP_DOMAIN:  ds 1
SOCK_TEMP_TYPE:    ds 1
SOCK_TEMP_PROTO:   ds 1
SOCK_TEMP_LOCAL_FD:ds 1
SOCK_TEMP_GLOBAL:  ds 1
SOCK_ALLOC_PAGE:   ds 1             ; Для хранения физ. страницы внутреннего сокета

; =============================================================================
; АППАРАТНЫЕ ПОРТЫ И РЕГИСТРЫ КОНТРОЛЛЕРА ETHERNET KSZ8851-6MLL
; =============================================================================
NET_CMD_PORT       EQU 080h         ; Порт выбора регистра (Address Pointer)
NET_DATA_PORT      EQU 081h         ; Порт чтения/записи данных (8-битное окно)

; Ключевые регистры чипа KSZ8851 (выбираются через порт NET_CMD_PORT)
REG_MARL           EQU 010h         ; MAC Address Register Low (Байты 0-1)
REG_MARM           EQU 012h         ; MAC Address Register Middle (Байты 2-3)
REG_MARH           EQU 014h         ; MAC Address Register High (Байты 4-5)
REG_TXCR           EQU 070h         ; Transmit Control Register
REG_RXCR           EQU 074h         ; Receive Control Register
REG_IER            EQU 090h         ; Interrupt Enable Register (Регистр маски прерываний)
REG_ISR            EQU 092h         ; Interrupt Status Register (Статус прерывания)

; Домены сокетов (передаются пользователем в регистре D)
AF_UNIX            EQU 01h          ; Внутренний сокет (Программная IPC реализация в ОЗУ)
AF_INET            EQU 02h          ; Аппаратный сетевой сокет (Чип KSZ8851-6MLL)

; Разделение пула глобальных сетевых индексов ядра (32..47)
GLOBAL_SOCK_UNIX   EQU 32           ; Начало диапазона внутренних сокетов (32..39)
GLOBAL_SOCK_INET   EQU 40           ; Начало диапазона внешних сокетов KSZ8851 (40..47)

; =============================================================================
; ДОПОЛНИТЕЛЬНЫЕ РЕГИСТРЫ ДАННЫХ И КОМАНДЫ ДЛЯ CHIP KSZ8851-6MLL
; =============================================================================
REG_TXQCR          EQU 072h         ; Transmit Queue Command Register (Запуск передачи)
REG_RXFHSR         EQU 07Ch         ; Receive Frame Status Register
REG_RXFHBCR        EQU 07Eh         ; Receive Frame Byte Count Register (Длина кадра)

TXQCR_METFE        EQU 001h         ; Бит запуска передачи кадра (Enqueue TX Packet)

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ СЕТЕВОГО ДРАЙВЕРА RING 0 ---
NET_IO_FD:         ds 1
NET_IO_BUF_PTR:    ds 2             ; Логический адрес буфера Ring 3 пользователя
NET_IO_BYTES_REQ:  ds 2             ; Количество байт, запрошенное пользователем
NET_IO_BYTES_CURR: ds 2             ; Количество успешно обработанных байт
NET_IO_GLOBAL_IDX: ds 1
NET_IO_TARGET_MAC_PTR: ds 2
NET_IO_HEAD_CELL_PTR:   ds 2         ; Адрес 16-битной ячейки Head-указателя
NET_IO_PHYS_READ_ADDR:  ds 2         ; Рассчитанный физический адрес чтения в Окне 2

	; --- ВСЕ НЕОБХОДИМЫЕ СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ В ОЗУ ЯДРА RING 0 ---
NET_IO_STRUCT_PTR:      ds 2         ; Указатель на структуру сокета в SOCK_SYS_TABLE
NET_IO_BUF_FLAG:        ds 1         ; Флаг направления буфера UNIX-сокета (00h/01h)
NET_IO_TAIL_CELL_PTR:   ds 2
NET_IO_PHYS_WRITE_ADDR: ds 2

; --- ИСПРАВЛЕНО: СИСТЕМНЫЕ БУФЕРЫ И ИНВАРИАНТЫ СТEКА В ОЗУ ЯДРA RING 0 ---
VFS_RECV_STACK_SAVE:   ds 2         ; Хранение оригинального указателя SP для sys_recv
VFS_RECV_ORIG_BUF_PTR: ds 2         ; Дубликат стартового адреса буфера Ring 3 задачи
VFS_RECV_G_IDX:        ds 1         ; Глобальный индекс VFS текущего дескриптора сокета


; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ ВЫЗОВА SOCKETPAIR (RING 0 ОЗУ) ---
SOCKPAIR_USER_PTR:   ds 2           ; Указатель на массив int sv[2] в Ring 3
SOCKPAIR_FD1:        ds 1           ; Первый локальный FD процесса
SOCKPAIR_FD2:        ds 1           ; Второй локальный FD процесса
SOCKPAIR_G_IDX1:     ds 1           ; Первый глобальный сетевой индекс (32..39)
SOCKPAIR_G_IDX2:     ds 1           ; Второй глобальный сетевой индекс (32..39)
SOCKPAIR_ALLOC_PAGE: ds 1           ; Физическая страница eMMU для двух буферов

; =============================================================================
; КОНСТАНТЫ ДЛЯ СИСТЕМНОГО ВЫЗОВА netstat()
; =============================================================================
NETSTAT_RECORD_SIZE EQU 8           ; Размер одной записи в пользовательском буфере

W_NETSTAT_DONE     EQU 00h          ; Код чистого успеха

; --- СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ВЫЗОВА NETSTAT (RING 0 ОЗУ) ---
NETSTAT_USER_BUF:   ds 2            ; Логический адрес буфера назначения в Ring 3
NETSTAT_CURR_SLOT:  ds 1            ; Текущий итератор системных сокетов (0..3)
NETSTAT_STRUCT_PTR: ds 2            ; Указатель на текущую запись в SOCK_SYS_TABLE

; =============================================================================
; КОНСТАНТЫ ДЛЯ СИСТЕМНОГО ВЫЗОВА socketfs_init()
; =============================================================================
VFS_MOUNT_SIZE     EQU 8            ; Фиксированный размер одной структуры монтирования
VFS_SLOT_SOCKETFS  EQU 2            ; Выделенный индекс слота для сетевого дерева

FS_TYPE_SOCKETFS   EQU 02h          ; Системный идентификатор типа SocketFS

; --- СТРОКА ПРЕФИКСА ПУТИ ДЛЯ СЕТЕВОГО ДЕРЕВА В ПАМЯТИ ЯДРА ---
SOCKFS_PATH_STR:   db  "/dev/sock", 00h ; NULL-терминированная строка пути

; Временная ячейка для вычислений адреса VFS
SOCKFS_MNT_PTR:    ds 2

; =============================================================================
; СМЕЩЕНИЯ ПОЛЕЙ СТРУКТУРЫ МОНТИРОВАНИЯ VFS (РАЗМЕР 8 БАЙТ)
; =============================================================================
M_MNT_STATUS       EQU 0            ; Флаг активности (00h = FREE, 01h = MOUNTED) [1 байт]
M_MNT_FS_TYPE      EQU 1            ; Тип ФС: 0=ExpFS, 1=TTYfs, 2=SocketFS [1 байт]
M_MNT_PREFIX_PTR   EQU 2            ; 16-битный указатель на строку пути-префикса [2 байта]
M_MNT_GATE_PTR     EQU 4            ; 16-битный указатель на шлюз функций драйвера [2 байта]
M_MNT_RESERVED     EQU 6            ; Резервное выравнивание структуры [2 байта]

VFS_MOUNT_SIZE     EQU 8            ; Фиксированный размер одной структуры монтирования
MAX_VFS_MOUNTS     EQU 4            ; Максимальное количество точек монтирования в ОС

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА ТОЧЕК МОНТИРОВАНИЯ VFS ЯДРА (RING 0 ОЗУ) ---
VFS_MOUNT_TABLE:   ds  MAX_VFS_MOUNTS * VFS_MOUNT_SIZE ; Выделяем 32 байта в памяти ядра

; =============================================================================
; СЕТЕВЫЕ КОМАНДЫ ДЛЯ СИСТЕМНОГО ВЫЗОВА ioctl()
; =============================================================================
SIOCSIFHWADDR      EQU 089h         ; Команда установки MAC-адреса (Hardware)
SIOCSIFADDR        EQU 08Ch         ; Команда установки IP-адреса (IPv4)

; --- СЕТЕВЫЕ ПАРАМЕТРЫ ИНТЕРФЕЙСА В ОЗУ ЯДРА RING 0 ---
SYS_IP_ADDRESS:    ds  4            ; Текущий IP-адрес ОС (4 байта, например, 192.168.1.10)
SYS_MAC_ADDRESS:   ds  6            ; Текущий MAC-адрес ОС (6 байт)

; Временные ячейки сисколла ioctl
IOCTL_TEMP_FD:     ds 1
IOCTL_TEMP_REQ:    ds 1
IOCTL_USER_PTR:    ds 2

; =============================================================================
; КОНСТАНТЫ И СМЕЩЕНИЯ ДЛЯ ПОДСИСТЕМЫ ARP (ADDRESS RESOLUTION PROTOCOL)
; =============================================================================
A_ARP_STATUS       EQU 0            ; Статус слота: 00h=FREE, 01h=VALID, 02h=PENDING
A_ARP_IP           EQU 1            ; Смещение IPv4-адреса хоста [4 байта]
A_ARP_MAC          EQU 5            ; Смещение MAC-адреса хоста [6 байт]
A_ARP_TTL          EQU 11           ; Счетчик времени жизни записи [1 байт]

ARP_STRUCT_SIZE    EQU 12           ; Размер одной записи в таблице
MAX_ARP_ENTRIES    EQU 4            ; Лимит ARP-записей в системном кэше

W_NET_ARP          EQU 0Eh          ; Код сна: процесс ждет разрешения ARP-адреса

; --- ГЛОБАЛЬНАЯ СИСТЕМНАЯ ТАБЛИЦА ARP-КЭША ЯДРА (RING 0 ОЗУ) ---
ARP_SYS_TABLE:     ds  MAX_ARP_ENTRIES * ARP_STRUCT_SIZE ; Выделяем 48 байт в ОЗУ

; Временные системные переменные подсистемы
ARP_SEARCH_IP:     ds 4             ; Искомый IP-адрес (заполняется перед поиском)
ARP_TEMP_STRUCT:   ds 2             ; Указатель на текущую структуру в таблице
ARP_LOOP_COUNTER:  ds 1             ; Итератор слотов

; =============================================================================
; КОНСТАНТЫ РЕЖИМОВ СИСКОЛЛА shutdown() (POSIX STANDARDS)
; =============================================================================
SHUT_RD            EQU 00h          ; Запретить дальнейшее чтение
SHUT_WR            EQU 01h          ; Запретить дальнейшую запись
SHUT_RDWR          EQU 02h          ; Полный останов приема и передачи

; Флаги маски закрытия сокета (записываются в поле S_SOCK_STATUS)
SOCK_SHUT_RD_MASK  EQU 010h         ; Бит запрета чтения (4-й бит статуса)
SOCK_SHUT_WR_MASK  EQU 020h         ; Бит запрета записи (5-й бит статуса)

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 ---
SHUTDOWN_TEMP_FD:   ds 1
SHUTDOWN_TEMP_HOW:  ds 1
SHUTDOWN_STRUCT_PTR:ds 2            ; 16-битный адрес текущей структуры сокета

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ ПОДСИСТЕМЫ ПРИЕМА ИЗ IPC СОКЕТОВ (RING 0 ОЗУ) ---
NET_RECV_STRUCT_PTR:    ds 2        ; Указатель на структуру текущего сокета
NET_RECV_BUF_FLAG:      ds 1        ; Флаг направления буфера (00h / 01h)
NET_RECV_HEAD_CELL_PTR: ds 2        ; Адрес ячейки Head Pointer в ОЗУ ядра
NET_RECV_PHYS_READ_ADDR:ds 2        ; Точный 16-битный адрес чтения в Окне 2 eMMU
NET_RECV_TEMP_PAGE:     ds 1        ; Физическая страница ОЗУ сокета

; Таблица 16-битных кольцевых указателей Head сокетов (8 байт в ОЗУ ядра)
NET_UNIX_HEADS:         ds 8		; Массив 16-битных кольцевых указателей Head внутренних сокетов (Выделено 8 байт)
; Таблица 16-битных кольцевых указателей Tail сокетов (Выделено 8 байт в ОЗУ ядра)
NET_UNIX_TAILS:         ds 8        ; Резерв 8 байт для 16-битных хвостов 4-х сокетов

; =============================================================================
; КОНСТАНТЫ И СМЕЩЕНИЯ ДЛЯ ПОДСИСТЕМЫ ПСЕВДОТЕРМИНАЛОВ (PTY)
; =============================================================================
PTY_STRUCT_SIZE    EQU 8            ; Фиксированный размер описателя PTY в ядре
MAX_PTY_PAIRS      EQU 4            ; Максимальное количество PTY сессий (4 пары)

; Диапазоны глобальных системных индексов VFS для PTY
GLOBAL_PTY_MASTER  EQU 48           ; Стартовый индекс для PTY Master (48..51)
GLOBAL_PTY_SLAVE   EQU 52           ; Стартовый индекс для PTY Slave (52..55)

; Смещения полей в структуре PTY_SYS_TABLE
P_PTY_STATUS       EQU 0            ; Статус: 00h=FREE, 01h=OPENED
P_PTY_PAGE         EQU 1            ; Физическая страница eMMU для буферов [1 байт]
P_PTY_MASTER_PID   EQU 2            ; PID процесса на стороне Master [1 байт]
P_PTY_SLAVE_PID    EQU 3            ; PID процесса на стороне Slave [1 байт]
P_PTY_FLAGS        EQU 4            ; Флаги дисциплины линии (ECHO, ICANON) [1 байт]
P_PTY_RESERVED     EQU 5            ; Резерв для выравнивания [3 байта]

; Коды асинхронного сна планировщика для PTY
W_PTY_MASTER       EQU 0Fh          ; Сон: Master ждет вывода от Slave
W_PTY_SLAVE        EQU 10h          ; Сон: Slave ждет ввода от Master

PTY_ECHO_BIT       EQU 01h          ; Бит флага: дублировать ввод обратно (Эхо)

; --- ГЛОБАЛЬНАЯ ТАБЛИЦА ПСЕВДОТЕРМИНАЛОВ (RING 0 ОЗУ) ---
PTY_SYS_TABLE:     ds  MAX_PTY_PAIRS * PTY_STRUCT_SIZE ; Выделяем 32 байта в ОЗУ

; Указатели кольцевых буферов PTY (4 пары * 2 направления * 2 байта = 16 байт)
PTY_M2S_TAILS:     ds  MAX_PTY_PAIRS * 2
PTY_S2M_TAILS:     ds  MAX_PTY_PAIRS * 2

; Временные ячейки сисколлов PTY
PTY_TEMP_FD1:      ds 1
PTY_TEMP_FD2:      ds 1
PTY_TEMP_G_IDX1:   ds 1
PTY_TEMP_G_IDX2:   ds 1
PTY_TEMP_PAGE:     ds 1

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ ПОДСИС ТЕМЫ PTY ВВОДА-ВЫВОДА ---
PTY_IO_BUF_PTR:    ds 2             ; Логический адрес буфера Ring 3
PTY_IO_BYTES_REQ:  ds 2             ; Сколько байт запрошено
PTY_IO_BYTES_CURR: ds 2             ; Сколько байт обработано
PTY_IO_STRUCT_PTR: ds 2             ; Указатель на структуру PTY_SYS_TABLE
PTY_IO_PAIR_IDX:   ds 1             ; Индекс пары (0..3)
PTY_IO_DIRECTION:  ds 1             ; Направление: 0 = M2S, 1 = S2M
PTY_IO_TAIL_PTR:   ds 2             ; Вычисленный адрес кольцевой ячейки

; =============================================================================
; КОНСТАНТЫ СИСКОЛЛА ptsname() ДЛЯ MEGA-580 OS
; =============================================================================
PTSNAME_STR_LEN    EQU 13           ; Длина экспортируемой строки "/dev/sock/sX" + NULL

; --- ШАБЛОН ПУТИ СТРОКИ SLAVE УСТРОЙСТВА В ПАМЯТИ ЯДРА ---
PTSNAME_TEMPLATE:  db  "/dev/sock/s#", 00h ; Шаблон пути, # заменяется на ASCII 0..3

; Временные переменные сисколла
PTSNAME_USER_PTR:  ds 2             ; Адрес буфера назначения в Ring 3
PTSNAME_LINE_NUM:  ds 1             ; Номер вычисленной линии (0..3)

; =============================================================================
; КОНСТАНТЫ СИСКОЛЛА ttyname() ДЛЯ MEGA-580 OS
; =============================================================================
TTYNAME_STR_LEN    EQU 9            ; Длина экспортируемой строки "/dev/tty" + NULL

; --- ИМЯ ФИЗИЧЕСКОГО ТЕРМИНАЛА В ПАМЯТИ ЯДРА ---
TTYNAME_PATH_STR:  db  "/dev/tty", 00h ; Фиксированная строка пути для КР580ВВ51А

; Временные переменные сисколла
TTYNAME_USER_PTR:  ds 2             ; Логический адрес буфера назначения в Ring 3

S_SOCK_RIP         EQU 4            ; Смещение: Удаленный IPv4-адрес [4 байта, вместо RPORT]

; --- СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ПРЕРЫВАНИЯ В ОЗУ ЯДРА RING 0 ---
NET_ISR_TARGET_PCB: ds 2
NET_ISR_USER_PTR:   ds 2
NET_ISR_TEMP_BYTE:  ds 1

; --- ИЗОЛИРОВАННЫЕ ОТЛАДОЧНЫЕ ПЕРЕМЕННЫЕ ЯДРА (RING 0 ОЗУ, СЕГМЕНТ ORG 0060h) ---
ARP_LEARN_IP_PTR:  ds 2
ARP_LEARN_MAC_PTR: ds 2
ARP_ITERATOR:      ds 1
ARP_VICTIM_PTR:    ds 2

; Буфер для временного хранения 30 байт заголовка входящего кадра в памяти Ring 0
NET_ISR_HEADER_BUF:ds 30

REG_RXFHSR         EQU 07Ch         ; Шлюз чтения данных из приемной SRAM чипа
ARP_INITIAL_TTL    EQU 128          ; Начальное время жизни записи (128 пакетов)

; =============================================================================
; КОНСТАНТЫ И ФЛАГИ СИСКОЛЛА open() (POSIX STANDARDS)
; =============================================================================
O_RDONLY           EQU 000h         ; Открыть только на чтение
O_WRONLY           EQU 001h         ; Открыть только на запись
O_RDWR             EQU 002h         ; Открыть на чтение и запись

EFAULT             EQU 0FDh         ; Код ошибки: Неверный адрес пространства
ENAMETOOLONG       EQU 0FCh         ; Код ошибки: Имя файла слишком длинное

; --- ВРЕМЕННЫЕ ЯЧЕЙКИ СИСКОЛЛА sys_open ---
OPEN_USER_PATH_PTR: ds 2            ; Указатель на строку пути в Ring 3
OPEN_FLAGS_SAVED:   ds 1            ; Сохраненные флаги доступа
OPEN_LOCAL_FD:      ds 1            ; Выделенный локальный дескриптор (0..7)
OPEN_MNT_SLOT:      ds 1            ; Номер совпавшего слота VFS монтирования

; --- ГЛОБАЛЬНЫЕ СИСТЕМНЫЕ ПЕРЕМЕННЫЕ ХРАНЕНИЯ КОНТЕКСТА (RING 0 ОЗУ) ---
REG_HL_SAVED:        ds 2           ; Консервация пользовательской пары HL из Ring 3
VFS_OPEN_STACK_SAVE: ds 2           ; ИСПРАВЛЕНО: Атомарный инвариант SP супервизора для sys_open

; =============================================================================
; 8-БИТНЫЕ ИНДЕКСЫ РЕГИСТРОВ KSZ8851-16MLL (РЕЖИМ ШИНЫ: 8 БИТ ДАННЫХ)
; =============================================================================
; Формат индекса: [7-2: Адрес регистра] [1: BE1] [0: BE0]

; Chip ID и Ревизия (REG_CIDER = 0xC0)
IDX_CIDER_LOW      EQU 0C1h         ; Указатель на младший байт CIDER (BE0=1)
IDX_CIDER_HIGH     EQU 0C2h         ; Указатель на старший байт CIDER (BE1=1)

; Global Reset Register (REG_GRR = 0x60)
IDX_GRR_LOW        EQU 061h         ; Указатель на младший байт GRR

; Transmit Control Register (REG_TXCR = 0x70)
IDX_TXCR_LOW       EQU 071h         ; Указатель на младший байт TXCR
IDX_TXCR_HIGH      EQU 072h         ; Указатель на старший байт TXCR

; Receive Control Register (REG_RXCR = 0x74)
IDX_RXCR_LOW       EQU 075h         ; Указатель на младший байт RXCR
IDX_RXCR_HIGH      EQU 076h         ; Указатель на старший байт RXCR

; Interrupt Enable Register (REG_IER = 0x90)
IDX_IER_LOW        EQU 091h         ; Указатель на младший байт IER
IDX_IER_HIGH       EQU 092h         ; Указатель на старший байт IER

; Interrupt Status Register (REG_ISR = 0x92)
IDX_ISR_LOW        EQU 093h         ; Указатель на младший байт ISR
IDX_ISR_HIGH       EQU 094h         ; Указатель на старший байт ISR

; Базовые 8-битные управляющие маски
GRR_GSR            EQU 001h         ; Бит глобального программного сброса
TXCR_TXE           EQU 001h         ; Разрешить передачу
RXCR_RXE           EQU 001h         ; Разрешить прием
RXCR_RXBAFE        EQU 040h         ; Разрешить Broadcast (Для ARP)
IER_RXIE           EQU 020h         ; Разрешить прерывание по приему пакета

; --- СЛУЖЕБНАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
K_SIG_CURRENT_PCB_PTR: ds 2

RES_FREE_MARKER    EQU 0FFh         ; Маркер свободного дескриптора (0xFF)

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 (УКЛАДЫВАЮТСЯ В СЕГМЕНТ ORG 0060h) ---
VFS_CLOSE_TEMP_IDX:  ds 1
VFS_CLOSE_STRUCT_PTR:ds 2

; --- СЛУЖЕБНЫЕ ЯЧЕЙКИ В ОЗУ ЯДРА RING 0 (ORG 0060h) ---
VFS_DIRTY_TMP_ADDR_H: ds 1
VFS_DIRTY_TMP_END_H:  ds 1

; =============================================================================
; ДОПОЛНИТЕЛЬНЫЕ КОНСТАНТЫ ДЛЯ ПОДСИСТЕМЫ IPC ПАЙПОВ В MEGA-580 OS
; =============================================================================
W_PIPE_EMPTY       EQU 007h         ; wait-channel: Процесс ждет появления данных в пайпе
W_PIPE_FULL        EQU 008h         ; wait-channel: Процесс ждет освобождения места в пайпе
PIPE_BUFFER_SIZE   EQU 16384        ; Максимальная емкость страницы пайпа (16 КБ)

; --- КОЛЬЦЕВЫЕ УКАЗАТЕЛИ IPC ПАЙПОВ ЯДРА (16 СЛОТОВ * 2 БАЙТА = 32 БАЙТА) ---
SYS_PIPE_HEADS:    ds  16 * 2       ; 16-битные указатели голов чтения пайпов
SYS_PIPE_TAILS:    ds  16 * 2       ; 16-битные указатели хвостов записи пайпов

; Временные ячейки сисколла sys_read
VFS_READ_STACK_SAVE:   ds 2         ; Хранение оригинального указателя SP для sys_read
VFS_READ_ORIG_BUF_PTR: ds 2
VFS_READ_G_IDX:        ds 1
VFS_READ_FILE_STRUCT:  ds 2
PIPE_READ_PAGE_TMP:    ds 1
PIPE_READ_CELL_PTR:    ds 2
VFS_SCRATCH_SECTOR:    ds 512       ; Физический 512-байтовый scratchpad-буфер сектора диска CF

; =============================================================================
; ДОПОЛНИТЕЛЬНЫЕ КОНСТАНТЫ ДЛЯ ПОДСИСТЕМЫ IPC ПАЙПОВ В MEGA-580 OS
; =============================================================================
W_PIPE_EMPTY       EQU 007h         ; wait-channel: Процесс ждет появления данных в пайпе
W_PIPE_FULL        EQU 008h         ; wait-channel: Процесс ждет освобождения места в пайпе
PIPE_BUFFER_SIZE   EQU 16384        ; Максимальная емкость страницы пайпа (16 КБ)

; --- СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ШЛЮЗА В ОЗУ ЯДРА RING 0 (ORG 0060h) ---
SOCKFS_GATE_LINE:  ds 1             ; Номер извлеченной линии (0..3)
SOCKFS_GATE_G_IDX: ds 1             ; Вычисленный результирующий глобальный индекс VFS

; --- ВРЕМЕННЫЕ ПЕРЕМЕННЫЕ ДЛЯ ПЕРЕДАЧИ АРГУМЕНТОВ В EXECVE ---
EXEC_ARGC:          ds 1            ; Счетчик количества аргументов (argc)
EXEC_ARGV_USER_PTR: ds 2            ; Указатель на массив argv[] старой задачи
EXEC_STR_POOL_TOP:  ds 2            ; Текущий плавающий указатель строк на новом стеке
EXEC_ARGV_ARRAY_TOP:ds 2            ; Текущий плавающий указатель массива указателей
EXEC_TEMP_WORD:     ds 2            ; Временная ячейка для 16-битных адресов

; --- СЛУЖЕБНАЯ ЯЧЕЙКА В ОЗУ ЯДРА RING 0 ---
K_EXEC_CURRENT_PCB: ds 2

EXEC_MAX_PATH_LEN  EQU 24           ; Лимит длины пути бинарного файла
EXEC_ENTRY_POINT   EQU 04000h       ; Фиксированная точка входа для Ring 3 программ

; --- ВРЕМЕННЫЕ СТРУКТУРЫ ПОЛНОГО ЗАГРУЗЧИКА SYS_EXECVE ---
EXEC_STACK_SAVE:    ds 2           ; Инвариант SP ядра для отката при ошибках загрузки
EXEC_FILE_PATH:     ds 24          ; Буфер ядра для имени исполняемого файла
EXEC_NEW_PAGE_NUM:  ds 1           ; Временное хранилище фрейма памяти

; =============================================================================
; АРХИТЕКТУРНЫЕ ПАРАМЕТРЫ ТАБЛИЦ VFS В MEGA-580 OS
; =============================================================================

; Идентификаторы VFS-статусов точек монтирования
MNT_FREE           EQU 000h         ; Точка монтирования свободна
MNT_MOUNTED        EQU 001h         ; Точка монтирования активна

; Временные системные переменные подсистемы VFS
VFS_DECR_SLOT_PTR: DW 0000H
VFS_FLUSH_LBA_L:   DW 0000H

; =============================================================================
; СТРУКТУРЫ ДАННЫХ И КОНСТАНТЫ ГЛОБАЛЬНОЙ ТАБЛИЦЫ ФАЙЛОВ (ОЗУ RING 0)
; =============================================================================
MAX_VFS_MOUNTS     	EQU 4			; Максимальное количество точек монтирования
VFS_MOUNT_SIZE     	EQU 8			; Размер структуры монтирования (8 байт)

SYS_FILE_SLOT_SIZE 	EQU 8			; Размер записи открытого файла (8 байт)
MAX_SYS_FILES   	EQU 128			; Лимит одновременно открытых файлов во всей системе (Количество слотов в глобальной SYS_FILE_TABLE)
GFT_SLOT_SIZE    	EQU 4			; Размер одного слота таблицы — ровно 4 байта
SYS_TABLE_TOTAL_SZ 	EQU MAX_SYS_FILES * SYS_FILE_SLOT_SIZE
; --- ГЛОБАЛЬНАЯ ТАБЛИЦА ОТКРЫТЫХ ФАЙЛОВ ЯДРА POSIX ОС ---
SYS_FILE_TABLE:    ds  SYS_TABLE_TOTAL_SZ; Выделяем в памяти Ring 0

; --- СТРОКА ПРЕФИКСА ПУТИ ДЛЯ ВИРТУАЛЬНЫХ ТЕРМИНАЛОВ PTY ---
PTYFS_PATH_STR:    db  "/dev/pts", 00h ; Фиксированный POSIX-префикс псевдоконсолей

; --- КОНСТАНТЫ АДРЕСАЦИИ ТАБЛИЦЫ MBR В СИСТЕМНОМ БУФЕРЕ ---
MBR_TABLE_OFFSET EQU 01BEh           ; Смещение таблицы разделов в секторе MBR
; Так как буфер расположен по фиксированному адресу IDE_SYS_BUFFER,
; адрес первой записи формируется как: IDE_SYS_BUFFER + MBR_TABLE_OFFSET

; Константы для глобальных VFS диапазонов PTY
GLOBAL_PTY_MASTER  EQU 48           ; Начальный глобальный индекс Master PTY (48..51)
GLOBAL_PTY_SLAVE   EQU 52           ; Начальный глобальный индекс Slave PTY (52..55)
PTY_STRUCT_SIZE    EQU 8            ; Размер структуры описателя PTY пары (8 байт)

; Временные ячейки шлюза в ОЗУ Ring 0
PTY_GATE_LINE:     ds 1             ; Номер извлеченной линии консоли (0..3)
PTY_GATE_G_IDX:    ds 1             ; Результирующий вычисленный глобальный индекс VFS

; --- ИСПРАВЛЕНО: СИСТЕМНЫЕ БУФЕРЫ И ИНВАРИАНТЫ СТЕКА В ОЗУ ЯДРА RING 0 ---
VFS_WRITE_STACK_SAVE:   ds 2         ; Хранение оригинального указателя SP для sys_write
VFS_WRITE_ORIG_BUF_PTR: ds 2         ; Дубликат стартового адреса буфера Ring 3 задачи
VFS_WRITE_G_IDX:        ds 1         ; Глобальный индекс VFS текущего дескриптора
VFS_WRITE_FILE_STRUCT:  ds 2         ; Адрес открытого описателя в SYS_FILE_TABLE

; =============================================================================
; СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ ЯДРА ДЛЯ СИСТЕМНОГО ВЫЗОВА SYS_FORK (ОЗУ RING 0)
; =============================================================================
; Выделение памяти под отладочные переменные sys_fork
FORK_CHILD_PID:  DB 00H         ; Идентификатор созданного потомка
FORK_CHILD_PCB:  DW 0000H       ; Базовый адрес структуры PCB потомка
FORK_SRC_PTR:    DW 0000H       ; Текущий логический адрес чтения памяти родителя
FORK_DST_PTR:    DW 0000H       ; Текущий адрес записи в окне Сегмента 2 потомка
FORK_TEMP_PAGES: DB 0FFH, 0FFH, 0FFH, 0FFH ; Буфер под 4 выделенные физ. страницы
FORK_CURRENT_SEGMENT: ds 1          ; Индекс копируемого фрейма (0..3)
FORK_PARENT_PAGE:     ds 1          ; Номер физической страницы родителя
FORK_CHILD_PAGE:      ds 1          ; Номер физической страницы потомка
FORK_CHILD_PCB_PTR:   ds 2          ; Адрес PCB создаваемого потомка
FORK_STACK_SAVE:      ds 2          ; Инвариант SP ядра для sys_fork
FORK_STACK_DEPTH:    ds  2          ; Глубина проседания стека родителя в байтах
FORK_PARENT_SP:      ds  2          ; Текущий указатель SP родительского процесса
FORK_CHILD_SP:       ds  2          ; Вычисленный целевой указатель SP потомка

#code _STACK, *
; Системный стек Ядра (Изолирован от пользовательских стеков Ring 3)
KERNEL_STACK_SIZE		EQU 512          ; Выделяем 512 байт под вложенные прерывания
KERNEL_STACK_TOP		EQU KERNEL_STACK + KERNEL_STACK_SIZE; <-- ВЕРШИНА СТЕКА ЯДРА (Сюда указывает SP супервизора при старте)

; Параметры масштабирования системных стеков ядра
SYS_STACKS_TOTAL_SZ EQU 16 * KERNEL_STACK_SIZE    ; 16 процессов * 512 байт = 8192 байта (8 КБ)

; Выделение памяти в ОЗУ Ring 0
SYS_STACKS_POOL:     ds  SYS_STACKS_TOTAL_SZ ; Глобальный пул изолированных стеков ядра

; --- ИЗОЛИРОВАННАЯ И ЗАЩИЩЕННАЯ ОБЛАСТЬ СТЕКА ЯДРА (GROW DOWNWARD) ---
KERNEL_STACK: ds  KERNEL_STACK_SIZE ; Выделяем ровно 512 байт в ОЗУ Ring 0
